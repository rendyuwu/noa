# BIGSU scoped packages resolve from the Biznet Gio internal registry (C2, §T.47).
# The internal network is the access boundary — no token is committed and none is
# needed. Every other package uses the public default registry.
@gio:registry=https://bigsu.biznetgio.pt/registry/
