# SPEC

## §G GOAL

NOA: MCP server for hosting-infrastructure operations. Exposes 14 RBAC-gated tools (READ + approval-gated CHANGE) to LibreChat via Streamable HTTP. Operator approves/denies CHANGE through iframe-embedded approval card served from NOA origin — reason typed there, ⊥ supplied by LLM. Admin panel manages users, roles, tool permissions, servers, audit log. Single repo, three deployables: API (`/mcp` + `/admin` routes), admin web (BIGSU), embed app (approval card).

## §C CONSTRAINTS

- C1. Python `<3.13` ≥3.11 (pgpy 0.6.0 imports removed `imghdr`; crashes tool-registry import at 3.13+). `uv` package manager, hatchling build.
- C2. Node.js 20+, Next.js 16, React 19, BIGSU (`@gio/bigsu-ui` `@gio/bigsu-icons` from `bigsu.biznetgio.pt/registry/`), pnpm. Versions pinned exact in lockfile — ⊥ caret ranges on `next`/`react`/`react-dom`. Bump = deliberate + T59 re-verify. Same discipline as C1 (Python) & T59 (LibreChat commit).
- C3. Postgres 16 via SQLAlchemy async + asyncpg. Alembic migrations. One shared DB schema.
- C4. LDAP authentication (dev bypass for local dev). LDAP = source of truth for employment, not just login.
- C5. Token-based MCP auth — ⊥ OAuth. Each user has own token — minted by admin FOR them ∨ self-minted (`/me/mcp-tokens`, T53); token carries ⊥ scope of its own ⇒ self-mint ⊥ escalation (V2). Token hashed at rest (SHA-256), plaintext shown once at mint. Bearer token in `Authorization` header per MCP request.
- C6. RBAC: admin assigns roles → role has tool permissions → user has roles. `tools/list` RBAC-filtered per user. Execution gate re-checks permission. Disabled user → zero permissions regardless of roles.
- C7. Secrets (API tokens, SSH creds) encrypted at rest via Fernet (`NOA_SECRET_ENCRYPTION_KEY`). Key encrypts server credentials, ⊥ the DB — name says so.
- C8. Reason = single field, operator-typed in approval iframe. CHANGE tool schemas ⊥ carry any reason param — ⊥ `reason`, ⊥ `proposed_reason`. LLM ⊥ author, ⊥ relay, ⊥ see reason. Reason born at approve time, ⊥ at call time. Preflight runs in-process inside CHANGE call — evidence ⊥ cross tool boundaries.
- C24. LibreChat = sole MCP client. ⊥ Claude Desktop, ⊥ VS Code, ⊥ Goose. ⇒ `X-Noa-LibreChat-User` header required ∀ MCP req (bound ∧ unbound). Link-out text (V25) survives as T59 escape hatch + iframe-load-failure path, ⊥ as other-client support. Adding a second client = spec change, reopens V3 + V25.
- C9. One-workflow-one-tool: preflight, checks, execution → internal functions. Only final workflow exposed as MCP tool. Discovery READ tools stay separate.
- C10. Ambiguous identifiers → structured "ambiguous, here are candidates" result. ⊥ guess.
- C11. No secrets in git (`.env*` gitignored except `.env.example`). Env vars for lists use JSON arrays.
- C12. Single repo, three deploy artifacts: `apps/api/` (FastAPI + FastMCP), `apps/admin-web/` (Next.js BIGSU admin), `apps/web-embed/` (Next.js approval card iframe). One Alembic. One shared `core/`.
- C13. Old repo `noa-old` = reference/patterns source, ⊥ fork base. **Port source branch = `MCP`** — ⊥ `staging`, ⊥ working tree. `core/secrets/yopass.py`, `core/secrets/password.py`, `docs/integrations/yopass.md` exist ONLY on `MCP`/`main`. DECISIONS §2 LOC table measured on `staging` ⇒ ⊥ match port source (`core/secrets` 136 staging vs 290 MCP; `whm/integrations` 1,083 vs 1,179; `core/agent` 2,281 vs 2,854). Copy mature integration layers (WHM/Proxmox/PMG, `remote_exec`, `secrets`) — ⊥ rewrite.
- C14. Hygiene: `.py` ≤ 900 lines, `.ts` ≤ 300 lines, `.tsx` ≤ 450 lines. Reusable functions — ⊥ duplication. Code quality: type hints, tests for new code, conventional commits.
- C15. yopass credential delivery: password generated server-side (`_generate_password()`), stored via PGPy client-side encrypt (`_yopass_store()`). Plaintext ⊥ cross LLM boundary, ⊥ persisted, ⊥ in vault. URL returned to operator. Shared internal helper, ⊥ welded to password-reset tool.
- C16. `core/workflows/` (7,913 LOC old) → simplification target — much weight serves chat presentation being dropped.
- C17. Embed document ! sit on NOA origin — property, ⊥ mechanism. Iframe path (T59 **PASSED** 2026-08-08, R29): `text/uri-list` selects `src` render mode → `allow-same-origin` → cookie rides; `text/html` renders `srcDoc` → opaque origin → cookie ⊥ sent → decision path dies silently. BOTH halves MEASURED at pin `45cc53c4`, ⊥ inferred: the `srcDoc` half threw `SecurityError` reading `document.cookie` ∧ `TypeError: Failed to fetch` on both requests, which is what makes the `src` half's 200 a measurement ∧ ⊥ a tautology (V87's shape one axis over). MIME dependency = upstream-fragile ⇒ see C21. Link-out path (iframe-load failure ∨ T59-fail-on-a-future-bump, V25): top-level NOA origin, cookie rides natively, ⊥ MIME dependency. **BOUNDED 2026-08-09 (T43, R32)**: "top-level" ⊥ mean "unsandboxed" when the operator got there by CLICKING inside the frame — the tab INHERITS the opener's sandbox flags (`allow-popups-to-escape-sandbox` absent ∀ paths, R13) ⇒ `allow-forms` ⊥ inherited ∵ ⊥ granted ⇒ a native `<form>` submit in that tab reaches NOTHING, MEASURED. Cookie half unchanged (`allow-same-origin` IS inherited ⇒ origin ⊥ opaque). ⇒ 2 link-out doors, ⊥ 1: the CLICK (needs `allow-popups`, ∃ only at `MCPUIResource`) ∧ the ADDRESS copied into a fresh tab (⊥ opener ⇒ ⊥ flags to inherit). V94 is the rule that falls out.
- C18. Approve/deny ⊥ travel through LLM. Only path = cookie POST from NOA-origin document (iframe ∨ top-level tab) to NOA with `noa_session` cookie + CSRF token. "May this run?" read from DB, ⊥ from LLM claim or tool arg.
- C19. LibreChat's `toolApproval` ⊥ used. Authorization split: NOA RBAC = who may call; NOA approval gate = whether specific change may run; LibreChat = transport + rendering only.
- C20. Token binding TOFU: mint → `librechat_user_id` NULL; first MCP call with `X-Noa-LibreChat-User` header → bind; subsequent calls → header ! match else 401. Header absent → 401 ∀ states ∵ C24 (LibreChat-only).
- C21. UPSTREAM DRIFT — C17 chain measured 2026-08-06 at pin `45cc53c4`, ⊥ inherited from mcp-ui docs. Source-read outcome (R10–R17): `text/uri-list` ⇒ iframe `src` + `sandbox="allow-scripts allow-same-origin"`, zero proxy indirection ⇒ first-party cookie rides ⇒ path A = **PASS, measured live 2026-08-08** (R29, T59): frame document on NOA's origin, `noa_session` (`Domain=.noa.internal`, `SameSite=Lax`) rides in, in-frame `GET /auth/me` ∧ `POST` both 200 with the operator's identity. LibreChat owns ⊥ mimeType logic; whole behaviour comes from `@mcp-ui/client` `^5.7.0` (lockfile-exact `5.7.0`). Drift source = PR #13831 "MCP Apps support" — OPEN **draft**, base `dev`, ⊥ merged, ⊥ in any release tag (R15): swaps `@mcp-ui/client` → `@modelcontextprotocol/ext-apps ^1.7.4`, classifies ONLY `text/html;profile=mcp-app` as app-backed, plain `srcDoc` → `sandbox=""` inert, grants `allow-same-origin` only when sandbox on dedicated origin (`VITE_MCP_SANDBOX_URL`). MCP Apps spec = SEP-1865, MVP scope = `text/html;profile=mcp-app` only; `text/uri-list` listed "deferred from MVP" (R16) ⇒ if #13831 becomes default render path, `text/uri-list` has ⊥ render path at all — ⊥ merely "proxy breaks same-origin". CORRECTION: ext-apps "#318" = OPEN discussion **issue**, ⊥ spec text (R16). T59 = gate, **CLEARED 2026-08-08** ⇒ V24/V37/V64 branch = path A ∧ T20,T30,T32,T41–T46,T56 unblocked. Re-verify ∀ LibreChat bump = a COMMAND, ⊥ a rebuild: `spikes/librechat-embed-render-gate/` holds the rig — `verify_librechat_pin.sh` re-asserts the pin ∧ the shipped `@mcp-ui/client` bytes (incl. `allow-forms` ABSENCE, V80), `browser_probe.mjs` re-runs the live measurement with its own negative control. #13831 still ⊥ in tree at the pin (that script fails on an `@modelcontextprotocol/ext-apps` dependency) ⇒ the drift is a future bump's problem, ⊥ today's — ∧ the bump is what re-opens V24's branch choice, ⊥ a re-read of this line.
- C22. Never-implement boundary (DECISIONS §6.2 — management policy, ⊥ technical): `whm_change_contact_email`, `whm_change_primary_domain`, `proxmox_move_vms_between_pools`, `proxmox_preflight_move_vms_between_pools`, `proxmox_get_user_by_email`, `whm_check_binary_exists`, `whm_firewall_denylist_add_ttl`. ⊥ port, ⊥ expose, ⊥ re-add. Re-add = policy decision, ⊥ agent call.
- C23. MCP protocol era = **handshake, negotiated per client `initialize`** — ⊥ a fixed digit, ⊥ sessionless `2026-07-28`. DECIDED 2026-08-04 by evidence, ⊥ preference. Driver = C24: LibreChat = sole client, `packages/api/package.json` declares `@modelcontextprotocol/sdk: ^1.29.0`, lockfile resolves **exact 1.29.0** ⇒ v1.x line ⇒ `initialize` handshake + `Mcp-Session-Id`. Serving `2026-07-28` to a v1.x client buys nothing. **CORRECTED 2026-08-07 (T71, R9, R26)**: the digit this line carried was WRONG — TS SDK v1.29.0 `LATEST_PROTOCOL_VERSION = "2025-11-25"`, ⊥ `"2025-06-18"`, ∧ its `Client.connect` sends that constant with ⊥ override knob ⇒ live negotiated era = **`2025-11-25`**. NOA picks nothing: `mcp==1.29.0` echoes the requested version when ∈ SUPPORTED else its own LATEST ⇒ the operative property = NOA's servable SET covers whatever a v1.x client asks, ⊥ one digit. Client bump inside 1.x moves the era silently ⇒ ⊥ assert a digit anywhere but a test. Server dep = `fastmcp==3.4.5` (stable 2026-07-27, `requires_python >=3.10`, satisfies C1). ⊥ `fastmcp` 4.x — beta only (4.0.0b1, needs `--pre`), removes server-initiated sampling/roots, drops 3.x shims, relocates `fastmcp.server.auth.*` ⇒ breaks T11 `TokenVerifier` for zero gain. Re-open when LibreChat ships SDK v2 — even then v2 emits `2026-07-28` only on explicit opt-in. Pin-verified 2026-08-06: `"@modelcontextprotocol/sdk": "^1.29.0"` still declared at `packages/api/package.json:112` (R11). PREMISE GAP CLOSED 2026-08-07 by T71 — read at tag `v1.29.0`, ⊥ inferred (R9); gap was real ∧ the premise was false, ⊥ merely unread. `mcp==1.29.0` SUPPORTED = `2024-11-05`,`2025-03-26`,`2025-06-18`,`2025-11-25` (R8) ⇒ BOTH the era LibreChat asks (`2025-11-25`) ∧ an older client's (`2025-06-18`) servable now. `2026-07-28` ∉ either SDK's list ⇒ era bump = SDK bump, ⊥ config flip (bounds V74's future path). Unsupported ask ⊥ dead-end: server falls back to own LATEST `2025-11-25` ∈ TS client SUPPORTED ⇒ handshake completes (R26).

## §I INTERFACES

### I.mcp — MCP Server (`apps/api`)

| Item | Contract |
|---|---|
| Transport | Streamable HTTP, single endpoint — `https://noa.internal/mcp/` in development, `https://noa-api.simondayce.my.id/mcp/` deployed (V40, T75). Host is a DEPLOYMENT property like V40's cookie parent; trailing slash ! be present ∵ T13's mount answers there ∧ bare `/mcp` costs a 307 per request (T57) |
| Auth | Per-user NOA-minted bearer token. LibreChat side: `customUserVars.NOA_MCP_TOKEN` (`sensitive: true`) → `Authorization: Bearer {{NOA_MCP_TOKEN}}` in `librechat.yaml`. Plus `X-Noa-LibreChat-User: {{LIBRECHAT_USER_ID}}` for TOFU binding — required ∀ req per C24. |
| Protocol | Handshake era, negotiated per `initialize` (C23) — NOA serves a SET, ⊥ a digit: `2024-11-05`,`2025-03-26`,`2025-06-18`,`2025-11-25` (R8); server echoes client's ask when ∈ set, else own LATEST. LibreChat @ pin asks `2025-11-25` ⇒ live era (R9,R26). `Mcp-Session-Id` in play ∀ era. Server = `fastmcp==3.4.5`. ⊥ `Mcp-Method`/`Mcp-Name` headers, ⊥ `ttlMs`/`cacheScope` — `2026-07-28`-only, absent from BOTH SDKs @ 1.29.0 (R26). |
| `tools/list` | RBAC-filtered per user. Returns only tools permitted by user's role permissions. Cache hints bounded per V74. |
| `tools/call` | Authenticate → resolve user → RBAC check → **READ: write `tool_runs` STARTED → execute → terminal row, FAILED READs included (V45,V20,T73); opening audit write fails → refuse `tool_audit_unavailable`, tool ⊥ run**; CHANGE (unapproved) → INSERT `action_requests(status=pending)` → return text + approval surface; CHANGE (approved) → execute → write `tool_runs` + `action_receipts` + audit. CHANGE args ⊥ include any reason field (C8,V15). |
| Audit header | `X-Noa-Conversation-Ref` — OPTIONAL grouping label → `tool_runs.conversation_ref` (V47). Filled by `librechat.yaml` from `{{LIBRECHAT_BODY_CONVERSATIONID}}` (R28) ∵ the call itself carries ⊥ conversation id (R27). Absent ∨ blank ∨ >255 chars ∨ off `^[A-Za-z0-9._:-]+$` → NULL ∧ call proceeds — label, ⊥ scope (DECISIONS §10.4, old V165). ⊥ auth, ⊥ gate, ⊥ required (unlike `X-Noa-LibreChat-User`, C24). |
| Tool result | READ: text (+ optional table surface for large results, V64). CHANGE: `build_change_gate_response()` → 3 branches: link-out text \| UI resource \| elicitation (future). Active = **UI resource** (path A) + link-out text beside it (V24, V25, T59 cleared). Resource = `ui://noa/approval/<id>` + `mimeType: text/uri-list` + URL as body; LibreChat's parser needs the `ui://` scheme (R31c). URL carries `action_request_id` only. |
| Exposed tools | **14 tools** (DECISIONS §6 + §9 + `noa_get_action_result` restored per V76): `whm_suspend_account`, `whm_unsuspend_account`, `whm_list_accounts`, `whm_search_accounts`, `whm_preflight_firewall_entries`, `whm_firewall_release_and_allow`, `whm_firewall_allowlist_remove`, `proxmox_reset_vm_password`, `proxmox_vm_nic(action)`, `pmg_whitelist(action)`, `pmg_whitelist_list`, `pmg_whitelist_search`, `whm_list_servers`, `noa_get_action_result` |
| Never implement | C22 list — ⊥ port, ⊥ expose, ⊥ re-add (management policy boundary) |
| Internal functions | All `*_preflight_*` tools, `update_workflow_todo`, `proxmox_list_servers`, `pmg_list_servers`, `proxmox_get_vm_status_current`, `proxmox_get_vm_config`, `proxmox_get_vm_pending`, `whm_mail_log_failed_auth_suspects`, `get_current_time`/`get_current_date`, `whm_validate_server`, `proxmox_validate_server`, `pmg_validate_server` |

### I.admin-api — Admin REST (`apps/api`)

| Route | Method | Purpose |
|---|---|---|
| `/health` | GET | Health check → `{"status":"ok"}` |
| `/auth/login` | POST | LDAP auth → JWT in httpOnly cookie |
| `/auth/logout` | POST | Clear session cookie |
| `/auth/me` | GET | Current user (cookie-only) |
| `/admin/users` | GET | List users. **⊥ POST** — user born at LOGIN (`AuthService._provision` writes `is_active=False` on 1st LDAP bind, V7/C4) ⇒ admin ENABLES via PATCH, ⊥ creates. `noa-old` has ⊥ create route ∧ the ported panel has ⊥ button |
| `/admin/users/{id}` | PATCH/DELETE | Enable/disable/delete user |
| `/admin/users/{id}/roles` | PUT | Replace user roles. Internal `user:` roles refused (400 `internal_role_forbidden`) ∧ PRESERVED — delete scoped by role-name subquery, ⊥ by the input list, so an empty list keeps them (V13, V75) |
| `/admin/users/{id}/tools` | PUT | **410 `direct_tool_grants_disabled`** (V75, T65). Exists ∵ `noa-old` shipped it ∧ the ported panel knew the address; refuses ∵ permissions flow role→user ∧ NOA has ⊥ user-level grant table. 410 ⊥ 404 (reads as an unmounted route) ∧ ⊥ 403 (invites asking a colleague when ⊥ role can do it). ⊥ body model ⇒ ⊥ dodging into a 422; ⊥ existence lookup ⇒ unknown id also 410, ⊥ an oracle for `users`. `require_admin` still first ⇒ non-admin 403 |
| `/admin/users/{id}/tokens` | GET/POST | List one operator's tokens / mint (plaintext ONCE in the POST body, V2). `require_admin`. Absent user → 404 `user_not_found`, ⊥ empty list ("holds none" ≠ "no such user"). INACTIVE user mintable — deliberate: credential grants nothing till activation (V1, V11) |
| `/admin/users/{id}/tokens/{token_id}` | DELETE | Revoke. BOTH ids in the WHERE ⇒ another operator's token id answers as a fabricated one: 404 `mcp_token_not_found`, ⊥ 403 ⇒ status ⊥ an oracle for real ids |
| `/admin/roles` | GET/POST | List/create roles. `admin` VISIBLE in list (real `roles` row, V7) ∧ refused on ∀ write (403 `reserved_role`, V13); internal `user:` roles excluded in the statement (V75). POST idempotent — re-create = 200, ⊥ event, ⊥ commit |
| `/admin/roles/{name}` | DELETE | Delete role. 403 `reserved_role`, 404 `admin_role_not_found`. Grants + assignments go by `ON DELETE CASCADE` (V14). `{name}` ⊥ a `UUID` path param ⇒ 400 `invalid_role_name` runs on READS too (`GET /roles/..%2Fadmin/tools` = 400, ⊥ 404) — keeps the 400/404 split from naming what the validator accepts |
| `/admin/roles/{name}/tools` | GET/PUT | Get/set tool permissions. `name=admin` → GET answers the WHOLE catalog, ⊥ `[]` (V10 bypasses the grant table ⇒ `[]` renders permits-nothing while permitting everything); PUT 403 `reserved_role`. PUT refuses unknown names as a SET → 400 `unknown_tools`, offenders in `detail` ⊥ body (V8), existing grants untouched (V100). Answer = grants as STORED after the write (strip, de-dup, sort, re-read), ⊥ echo of the body |
| `/admin/tools` | GET | Tool catalog — ∀ name a grant may name (V10). Vocabulary of `PUT /roles/{name}/tools`, read off the SAME `AuthorizationService` that validates that write ⇒ offered set ⊥ drift from accepted set (V66). Admin-only ⇒ V83(d)'s one-code-∀-causes rule ⊥ bind here (it binds the MCP surface); C22 names ∉ catalog ⇒ ∉ here |
| `/admin/audit/tool-runs` | GET | Query tool runs (READ+CHANGE). **7 filters**, ∀ camelCase (panel's spelling, DECISIONS §8.3): `toolName`, `status`, `risk`, `conversationRef`, `requestedByEmail`, `from`, `to` — `risk` FILTERS, ⊥ scopes ⇒ default answer = whole trail. ∀ filter lands in the `WHERE` (V93): one applied after the fetch lets `LIMIT` cut rows the filter was about to remove ⇒ short page while `nextCursor` insists there is more. Keyset page over `created_at DESC, id DESC` — `created_at` alone ⊥ unique (V92(c)); `nextCursor` from the LAST ITEM, `null` on the last page (V85 family). `limit` 1–200, default 50, bound held at the `Query` ∧ in the service (V66). Refusals: 403 (V13), 422 out-of-range `limit` ∨ unknown `status`/`risk`, 400 `invalid_audit_cursor` |
| `/admin/audit/tool-runs/{id}` | GET | Tool run detail = the list item + `requestedByUserId` + `args`. Redacted AT WRITE, ⊥ here — a 2nd redactor = a quieter 2nd home for one rule (V66, V8). `args` `{}` = took none, ⊥ "⊥ recorded" (T35 server default). `requestedByUserId` `null` once the operator row is deleted (`SET NULL`) ⇒ run outlives its requester. `{id}` = plain `str`, ⊥ a `UUID` path param ⇒ unknown ∧ malformed BOTH 404 `tool_run_not_found`: a 422 describes what the validator accepts, ⊥ what exists (T63(e), admin-side) |
| `/admin/whm/servers` | GET/POST | List/create WHM servers |
| `/admin/whm/servers/{id}` | PATCH/DELETE | Update/delete WHM server |
| `/admin/whm/servers/{id}/validate` | POST | Validate WHM server (SSH connect + fingerprint) |
| `/admin/proxmox/servers` | GET/POST | List/create Proxmox servers |
| `/admin/proxmox/servers/{id}` | PATCH/DELETE | Update/delete Proxmox server |
| `/admin/proxmox/servers/{id}/validate` | POST | Validate Proxmox server |
| `/admin/pmg/servers` | GET/POST | List/create PMG servers |
| `/admin/pmg/servers/{id}` | PATCH/DELETE | Update/delete PMG server |
| `/admin/pmg/servers/{id}/validate` | POST | Validate PMG server |
| `/me/mcp-tokens` | GET/POST | Own tokens / SELF-mint (T53). `require_session_user` ⇒ user id off the resolved session — ⊥ path segment, ⊥ query, ⊥ body field to substitute; scoping STRUCTURAL, ⊥ checked. Disabled operator refused at the session (V6) before any token logic. Same service ∧ same response shapes as the 2 `/admin/users/{id}/tokens` rows (V66) |
| `/me/mcp-tokens/{id}` | DELETE | Revoke own token. `token_id` = the ONLY caller-supplied id, scoped by the session's user id ⇒ colleague's token id deletes nothing, 404 as above |

### I.embed — Embed App (`apps/web-embed`)

| Route | Purpose |
|---|---|
| `/approvals/[id]` | Approval card (GET detail + POST approve/deny). Reason input box lives here — sole origin of `reason` (C8,V15). Compact, self-scrolling, no AppShell. |
| `/action-requests/{id}` | GET card detail (T41, BUILT). Session cookie, requester-matched in the `WHERE` (V27), check-on-read expiry AFTER that read (V32). Carries provenance + gate-time `evidence` + redacted `arguments` + the run (V29,V33,V35) ∧ `receipt` — 2 halves (`ok`,`before`,`after`,`error_code`), `null` till a terminal run records one (V46, T42) — ∧ `csrf` — freshly minted when PENDING, `null` once terminal (V39, T46). ⊥ `reason` field, ∀ status (C8,V15,V43). Absent ∨ foreign ∨ NULL-requester → 404 `action_request_not_found`, one body ∀ 3. Also T42's polling surface. |
| `/action-requests/{id}/approve` | POST approve → 202 `{action_request_id, tool_run_id}`, async execute. Body `{reason, csrf}`. Blank/whitespace `reason` → 409 `change_reason_required`. Invalid/absent CSRF → 403 `csrf_token_invalid`. Session cookie. |
| `/action-requests/{id}/deny` | POST deny → 200 `{action_request_id, status}`. Body `{reason, csrf}`. **Blank `reason` → 409 too** (V15 as amended by T37). Session cookie. |
| `/tables/{token}` | GET one parked table — **API route** (`apps/api`, `noa_api/api/routes/result_tables.py`, T56, BUILT). Session cookie; requester-match **∧** deadline in the same `WHERE` (V27, V93). Body = `columns` + `rows` + `total_rows` + `stored_rows` + `truncated` + `created_at`/`expires_at` — the bound rides in the payload (V85). Unknown ∨ foreign ∨ NULL-requester ∨ expired → 404 `result_table_not_found`, one body ∀ 4. ⊥ POST, ⊥ CSRF, ⊥ reason, ⊥ write of any kind. |
| `/tables/[token]` | Large-READ table surface (V64, T56 BUILT). SERVER component — reads `/tables/{token}` with the incoming `Cookie`, `Authorization` NEVER (C5) ⇒ the HTML reaching the frame is already the operator's table ∧ ⊥ browser route is owed. Read-only, requester-matched, ⊥ decision controls: ⊥ approve, ⊥ deny, ⊥ reason box, ⊥ `<form>`, ⊥ CSRF. Bound RENDERED ∀ table, capped or ⊥ (V85). 401 → T43's way out, reused ⊥ reimplemented (V66, V94). |
| `/api/[...path]` | Same-origin proxy → `NOA_API_URL`, server-side (T44). **ALLOWLISTED, ⊥ pass-through**: `GET auth/me`, `GET action-requests/{id}`, `POST action-requests/{id}/{approve,deny}` — 4 entries, matched on METHOD ∧ shape. Else 404 `route_not_proxied` ∧ upstream ⊥ called. `Authorization` stripped outbound; `Cookie` ∧ `Set-Cookie` ride untouched (V40). |
| `/healthz` | Liveness check |

Framing headers: only embed app allowlists `frame-ancestors https://chat.noa.internal`. Admin app sends `frame-ancestors 'none'`. Embed 401 → explicit "cannot authenticate here" state, ⊥ blank card, ⊥ live Approve button. As built (T43): link-out `<a target="_blank" rel="noopener noreferrer">` from `NOA_SIGN_IN_URL` + the same address as TEXT (V94) + a retry that re-reads in place — ⊥ login form, ⊥ credential field, ⊥ redirect (framed URL unchanged).

`/action-requests/{id}/{approve,deny}` = **API routes** (`apps/api`, `noa_api/api/routes/action_requests.py`, T37), listed under I.embed ∵ the embed is their only caller. Browser reaches them via the embed's same-origin proxy (T44), ⊥ direct — ∧ that proxy carries a BOUNDED set, ⊥ the API: a surface listed here ! also be added to `src/lib/proxy/routes.ts` ∨ the browser ⊥ reach it (T44(b)). `/approvals/[id]`, `/tables/[token]`, `/healthz`, `/api/[...path]` = the embed app itself. `/action-requests/{id}` GET = **BUILT 2026-08-09 by T41**, same router as the 2 POSTs ∧ same cookie, but a strictly weaker capability wired that way ⊥ promised: its service holds a repository with ⊥ `commit` ∧ ⊥ statement that is ⊥ a `SELECT`, while the POSTs hold the one writer of a terminal status. Sharing a router ⊥ share a writer. The CARD reads it server-side (T41(c)) ⇒ the proxy's `GET action-requests/{id}` entry now serves T42's polling, ⊥ this card — the allowlist stayed at 4 ∵ T46's answer put the token in this response, so T41 needed ⊥ mint route. `/tables/{token}` GET = **the 2nd API route under this heading**, BUILT 2026-08-09 by T56 — same cookie, ∧ a `SELECT`-only repository like the card read's (⊥ `commit`, ⊥ statement that is ⊥ a `SELECT`). The table PAGE fetches it server-side ⇒ it is ⊥ in `src/lib/proxy/routes.ts` ∧ ⊥ browser-reachable, which is why T44's count is still 4 ∧ ⊥ 5.

### I.admin-web — Admin Panel (`apps/admin-web`)

PORT from old repo `apps/web-bigsu/src/app/(protected)/admin/`: users, roles, audit, whm, proxmox, pmg. BIGSU components. Own build/deploy. ⊥ frameable. **6 pages, ⊥ 7** — `audit/tool-runs` ∧ `audit/receipts/[actionRequestId]` DELETED at T55 ∵ the ported action-request tab, its detail drawer ∧ the receipt page called 3 routes §I.admin-api names ⊥ ∧ answered 404 on EVERY load; deleted, ⊥ stubbed (T65's call). `/admin/audit` = the whole audit surface, one view, ⊥ tab strip — where `nav.ts` already pointed. A ported page whose route ∉ §I.admin-api = DEAD, ⊥ a TODO.

### I.ext — External Dependencies

| System | Interface |
|---|---|
| LibreChat | `librechat.yaml` MCP server entry: `streamable-http` → `https://noa.internal/mcp/`, `customUserVars` for token + user ID headers, ∧ 3 keys C24 forces (V88, R31): `startup: false`, `requiresOAuth: false`, NOA's host ∈ `mcpSettings.allowedDomains`. LDAP auth same directory, local registration disabled (`ALLOW_REGISTRATION=false` + `LDAP_*`, V70). Operator reference = `docs/integrations/librechat.md` (T57, T58); keys asserted by `apps/api/tests/test_librechat_config_doc.py`, ⊥ by prose. |
| LDAP | Service-account bind + search for `user_exists_and_enabled(email)`. Token revalidation on staleness interval. |
| Postgres | One DB, one schema. Alembic migrations. |
| WHM servers | SSH (asyncssh) with host-key pinning + TOFU refresh. `sudo -n` for non-root. CSF + Imunify. |
| Proxmox servers | Proxmox API (HTTP). |
| PMG servers | SSH + `pmgsh` CLI (argv-only, ⊥ shell string). |
| yopass | `POST <YOPASS_BASE_URL>/secret` for encrypted secret delivery. PGPy client-side encrypt. |

## §R RESEARCH

Sourced findings. `?` prefix = unverified. Local paths = installed package source (primary), relative to `.venv/lib/python3.11/site-packages/`.
R1–R7 (fastmcp==3.4.5 auth/ASGI API surface, consumed by T11–T13) archived in `docs/AS-BUILT.md` §R.

id|topic|finding|source
R8|server protocol + session id|`mcp==1.29.0` (Python): `LATEST_PROTOCOL_VERSION="2025-11-25"`, `DEFAULT_NEGOTIATED_VERSION="2025-03-26"`, `SUPPORTED_PROTOCOL_VERSIONS=["2024-11-05","2025-03-26","2025-06-18","2025-11-25"]`. `Mcp-Session-Id` minted by default (`stateless_http=False`, `uuid4().hex`) — LIVE-VERIFIED. `stateless_http=True` ⇒ no header, no event store, routes drop GET|`mcp/types.py:27,35`; `mcp/shared/version.py:3`; `mcp/server/streamable_http_manager.py:288,205`
R9|C23 premise = FALSE|VERIFIED 2026-08-07 @ tag `v1.29.0` (T71). C23's claim "TS SDK v1.x ⇒ `LATEST_PROTOCOL_VERSION = "2025-06-18"`" is **WRONG**: `LATEST_PROTOCOL_VERSION = '2025-11-25'`, `DEFAULT_NEGOTIATED_PROTOCOL_VERSION = '2025-03-26'`, `SUPPORTED_PROTOCOL_VERSIONS = [LATEST,'2025-06-18','2025-03-26','2024-11-05','2024-10-07']`. `2026-07-28` ∉ list ⇒ C23's CONCLUSION survives (handshake era, `Mcp-Session-Id`, era bump = SDK bump), only the digit dies. npm tarball `dist/` byte-agrees with the tag ⇒ published artifact ⊥ differ from source|`modelcontextprotocol/typescript-sdk` `v1.29.0/src/types.ts:4-6`; npm `@modelcontextprotocol/sdk@1.29.0` `dist/esm/types.js:2-4`, `dist/esm/types.d.ts:3-4`
R10|LibreChat pin candidate|`main` HEAD `45cc53c40b47645b887c3bb996168e06aaa83f4c` (2026-08-06T02:25Z). Latest tag `v0.8.7` (2026-06-24) but `prerelease=true`|github.com/danny-avila/LibreChat/commit/45cc53c4
R11|renderer deps at pin|`client/package.json:41` `"@mcp-ui/client": "^5.7.0"`, lockfile exact `5.7.0`. `packages/api/package.json:112` `"@modelcontextprotocol/sdk": "^1.29.0"` (C23 cite confirmed). Code search `ext-apps` on default branch = 0 hits|github.com/danny-avila/LibreChat/blob/45cc53c4/client/package.json#L41
R12|`text/uri-list` still `src`|LibreChat owns ⊥ mimeType logic — 3 render sites delegate to `UIResourceRenderer` (`ToolCallInfo.tsx:167`, `MCPUIResource.tsx:40`, `UIResourceCarousel.tsx:111`). mcp-ui 5.7.0: `text/uri-list` ⇒ `externalUrl` ⇒ `iframeRenderMode:'src'`; `text/html` ⇒ `rawHtml` ⇒ `srcDoc`|mcp-ui `v5.7.0/sdks/typescript/client/src/components/UIResourceRenderer.tsx#L20-25`; `utils/processResource.ts:110,117,126`
R13|sandbox value at pin|src path: `mergeSandboxPermissions(sandboxPermissions ?? '', 'allow-scripts allow-same-origin')`. Effective: `ToolCallInfo` ⇒ `allow-scripts allow-same-origin`; `MCPUIResource` ⇒ `allow-popups allow-scripts allow-same-origin`. **`allow-forms` ABSENT ∀ paths** ⇒ native `<form>` submit blocked in-frame, JS `fetch` POST works. srcDoc branch omits `allow-same-origin` deliberately|mcp-ui `v5.7.0/.../HTMLResourceRenderer.tsx#L147-153,183-184`
R14|proxy / off-origin risk|NONE at pin: `proxy?: string` opt-in, LibreChat passes none, no default, no dedicated sandbox origin. mcp-ui self-guards same-host proxy ("proxy origin must not be the same as the host origin"). For `externalUrl` a proxy IS forwarded if ever set|mcp-ui `v5.7.0/.../utils/processResource.ts#L85-110`
R15|PR #13831 state|OPEN + **draft**, base `dev` (⊥ `main`), head `feat/mcp-apps-support`, 52 commits/56 files, updated 2026-08-05, ⊥ in any tag. Swaps `@mcp-ui/client ^5.7.0` → `@modelcontextprotocol/ext-apps ^1.7.4`; `isMcpAppResource` = mimeType includes `profile=mcp-app`; plain `srcDoc` ⇒ `sandbox=""`, app-backed ⇒ `sandbox="allow-scripts allow-forms"`; `mcp-sandbox.html` adds `allow-same-origin` only when `dedicatedOrigin` (`VITE_MCP_SANDBOX_URL`). Related #11799 OPEN, stale|github.com/danny-avila/LibreChat/pull/13831/files
R16|MCP Apps spec scope|`text/html;profile=mcp-app` ONLY — "MVP supports only `text/html;profile=mcp-app` (rawHtml), with other types explicitly deferred"; `text/uri-list` sits under "Content Types (deferred from MVP)". Spec = SEP-1865, extension per SEP-1724. CORRECTION: "#318" = ext-apps **issue** #318 "Thoughts about a mimeType equivalent to text/uri-list", OPEN discussion, ⊥ spec text|modelcontextprotocol/ext-apps `specification/draft/apps.mdx#L277`; ext-apps issues/318
R17|T59 evidence, source-read stage|**SUPERSEDED by R29** — PASS-candidate at pin `45cc53c4` from source alone; the owed live run ran 2026-08-08 ∧ PASSED. Full text: `docs/AS-BUILT.md` §R17|mcp-ui `v5.7.0/.../HTMLResourceRenderer.tsx#L147-153`
R18|logout ! be server-side|OWASP Session CS §Session Expiration: "must take active actions to invalidate the session on both sides, client and server. The latter is the most relevant and mandatory". ASVS V3.3.1 (L1/L2/L3, CWE-613): "logout and expiration invalidate the session token" ⇒ cookie-clear-only logout (V6) fails V3.3.1|cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html §Session Expiration; OWASP ASVS 4.0 V3.3.1
R19|account-disable ⊥ normative|Zero source found requiring termination of LIVE sessions on account disable. NIST SP 800-63B-4 covers authenticators ⊥ sessions; OWASP nearest = §Reauthentication After Risk Events (disable ⊥ listed) ⇒ per-request `is_active` re-read = common practice, ⊥ normative backing, ∧ ⊥ substitute for R18|pages.nist.gov/800-63-4/sp800-63b.html; OWASP Session CS §Reauthentication After Risk Events
R20|session TTL numbers|NIST SP 800-63B-4 §2.2.3 (AAL2, final 2025-07-31): overall reauth SHOULD ≤ 24h, inactivity SHOULD ≤ 1h; §5.2 "When either timeout expires, the session SHALL be terminated"; §5.1.1 cookie expiry "SHALL NOT be relied upon to enforce session timeouts". OWASP: idle 15–30 min low-risk / 2–5 min high-value, absolute 4–8h. Current `AUTH_JWT_ACCESS_TOKEN_TTL_SECONDS=3600` ∈ range|pages.nist.gov/800-63-4/sp800-63b.html §2.2.3,§5.1.1,§5.2; OWASP Session CS §Session Expiration
R21|CONFLICT — denylist|OWASP JWT CS §JWT revocation: denylist = "workaround for the 'stateless session' invalidation problem", key `(jti, iss)`, raw-JWT/SHA-256 key "not safe … denylist bypass through JWT malleability", + "consider whether there is a better solution" (Token Status List, session-bound nonce, short exp, DPoP). OWASP REST CS §JWT: on explicit termination `jti` "should be submitted to a denylist". BOTH logged, ⊥ averaged|OWASP JSON_Web_Token_Cheat_Sheet §JWT revocation; OWASP REST_Security_Cheat_Sheet §JWT
R22|short TTL + rotation|Only mechanism with hard normative language: RFC 9700 §2.2.2 public-client refresh tokens "MUST be sender-constrained or use refresh token rotation"; §4.14 short access lifetime "reduc[es] the potential impact of access token leakage"; §4.14.2 rotation invalidates prior refresh token — revoke-on-logout = **MAY**, refresh-only. RFC 7009 §2: refresh revocation MUST, access SHOULD. RFC 6819 §3.1: "Implementing token revocation is more difficult with assertions than with handles"|datatracker.ietf.org/doc/html/rfc9700 §2.2.2,§4.14,§4.14.2; rfc7009 §2; rfc6819 §3.1
?R23|no propagation window|Zero normative max revocation-propagation window ∧ zero access-token-lifetime digits in RFC 9700/6819/7009. RFC 8725 (JWT BCP) silent on `jti`/replay. Per-user token-version / session-epoch counter ⊥ present in any normative source ⇒ common practice only|rfc-editor.org/rfc/rfc8725.txt; pages.nist.gov/800-63-4/sp800-63b.html §5.3
R24|PyJWT 2.13.0|VERIFIED at tag 2.13.0: `leeway: float \| timedelta = 0` default; `if iat > (now + leeway): raise ImmatureSignatureError("The token is not yet valid (iat)")` (`InvalidIssuedAtError` = non-numeric case only) ⇒ V79 correct. `_validate_jti` = type check only (`InvalidJTIError` if ⊥ str), zero revocation ⇒ denylist wholly caller's. jti/sub validation landed 2.10.0 (#1005); iat check 2.6.0 (#794), touched 2.7.0 (#847)|raw.githubusercontent.com/jpadilla/pyjwt/2.13.0/jwt/api_jwt.py; pyjwt.readthedocs.io changelog
R25|fastmcp error masking|`mask_error_details` DEFAULT **`False`** ⇒ unmasked branch raises `ToolError(f"Error calling tool {name!r}: {e}")` — `str(e)` verbatim at LLM (V19 breach by default). ∧ raw-exception→`ToolError` conversion sits INSIDE `call_tool(run_middleware=False)`; middleware wraps THAT call ⇒ by middleware time the exception is already `ToolError`, original type gone ⇒ `RuntimeError`/`TimeoutError` ⊥ distinguishable there ⇒ V19 mapping ! be a decorator on the tool fn. Server sets `mask_error_details=True` as 2nd line, for what is raised OUTSIDE a decorated tool (argument validation, registration wrapper). VERIFIED live 2026-08-07 (T19)|`fastmcp/settings.py:268`; `fastmcp/server/server.py:1248-1270,1342-1358` (fastmcp==3.4.5)
R26|era chosen by CLIENT, ⊥ NOA|TS `Client.connect` sends `protocolVersion: LATEST_PROTOCOL_VERSION` — ⊥ ctor option, ⊥ knob ⇒ a client ⊥ ask for another era without patching; reply ∉ its SUPPORTED → throws `Server's protocol version is not supported`. LibreChat @ pin `45cc53c4`: `package-lock.json` resolves `@modelcontextprotocol/sdk` **exact 1.29.0** (⊥ merely `^1.29.0`), `connection.ts:1276-1284` = stock `new Client({name,version},{capabilities:{}})` ⇒ asks `2025-11-25`. Python side echoes: `protocolVersion=requested if requested ∈ SUPPORTED else LATEST` ⇒ live era `2025-11-25`; unsupported ask → `2025-11-25` ∈ TS SUPPORTED ⇒ ⊥ dead-end. `ttlMs`/`cacheScope` grep = 0 hits in BOTH SDKs @ 1.29.0 ⇒ V74's "nothing to bound today" holds|`v1.29.0/src/client/index.ts:495,508`; LibreChat `45cc53c4` `package-lock.json`, `packages/api/src/mcp/connection.ts:1276-1284`; `mcp/server/session.py:185-187` (mcp==1.29.0)
R27|LibreChat sends ⊥ conversation id on a tool call|VERIFIED 2026-08-07 @ pin `45cc53c4` by whole-tree grep, ⊥ by reading one file: **ONE** `tools/call` emitter exists — `MCPManager.callTool` sends `params:{name,arguments}` ∧ nothing else; 3rd arg = `RequestOptions` (`timeout`,`resetTimeoutOnProgress`,`...options`), ⊥ params ⇒ ⊥ `_meta` on the wire. `onprogress` ⊥ passed anywhere on that path ⇒ the TS SDK injects ⊥ `_meta.progressToken` either. Sole caller = `api/server/services/MCP.js:1084` ⇒ ⊥ 2nd path to miss. ⇒ `tool_runs.conversation_ref` (V47) ⊥ fillable from the call itself: header (R28) ∨ NULL. Bears on T57, T73(d)|`45cc53c4` `packages/api/src/mcp/MCPManager.ts:342,514-528`; `api/server/services/MCP.js:1084`; tree grep `'tools/call'` over `packages/`+`api/` (non-test) = 1 hit
R28|`{{LIBRECHAT_BODY_CONVERSATIONID}}` = supported header placeholder|`ALLOWED_BODY_FIELDS = ['conversationId','parentMessageId','messageId']`; `processMCPEnv` runs `processBodyPlaceholders` over ∀ `headers` entry of a streamable-http server, re-resolved PER TOOL CALL (`MCPManager.ts:440` builds `resolvedHeaders` → `:512` `setRequestHeaders`) ⇒ operator YAML CAN hand NOA a conversation id as a header, closing what R27 leaves open. 2 bounds NOA ! honour: (a) absent ∨ null field → **empty string**, ⊥ literal placeholder ⇒ blank ⊥ distinguishable from absent, both = "no label"; (b) header values ASCII-encoded at `:41-42,201`. ⊥ user-supplied text — `conversationId` = LibreChat's own id off the request body|`45cc53c4` `packages/api/src/utils/env.ts:126-148,219-245,311,408-422`; `packages/api/src/mcp/MCPManager.ts:440,512`
R29|T59 render gate = PASS, LIVE|MEASURED 2026-08-08 in Chromium against LibreChat built from pin `45cc53c4`, a real MCP connection ∧ a real NOA process — ⊥ a source read, ⊥ a component double (V69's rule). NOA returned `ui://noa/approval/<id>` + `mimeType: text/uri-list` + URL body. BOTH LibreChat render sites fired: `ToolCallInfo` ⇒ iframe `src` = the NOA URL, `srcdoc` absent, `sandbox="allow-scripts allow-same-origin"`; `MCPUIResource` (`\ui{}` marker) ⇒ same `src`, `sandbox="allow-popups allow-scripts allow-same-origin"`. `allow-forms` absent in BOTH ⇒ V80. INSIDE the frame: `window.location.origin` = `http://embed.noa.internal:8000` (NOA's, ⊥ opaque), `document.cookie` = empty (httpOnly holds), `GET /auth/me` = **200** as `operator@noa.internal`, `POST` = **200** authenticated ⇒ V22's cookie POST is possible from the frame. Cookie as recorded: `noa_session`, `domain=.noa.internal`, `httpOnly`, `SameSite=Lax`. NEGATIVE CONTROL (same page as a `text/html` resource ⇒ `srcdoc`, `sandbox="allow-scripts"`): origin `null`, `document.cookie` → `SecurityError`, both requests → `TypeError: Failed to fetch` ⇒ the compare separates (V87). Green twice, 2nd time on committed code + a fresh token|`docs/spikes/librechat-embed-render-gate.md`; `spikes/librechat-embed-render-gate/evidence/probe-results.json`
R30|LibreChat ⊥ honour `tools/list_changed`|MEASURED 2026-08-08 @ pin `45cc53c4`, 2 independent reads agreeing. (a) ON THE WIRE, NOA's side works: with a session's GET stream open, the emit put `{"method":"notifications/tools/list_changed","jsonrpc":"2.0"}` on that stream. (b) IN LibreChat: same emit inside a chat turn drew nothing — the turn's JSON-RPC methods read `initialize`,`notifications/initialized`,`ping`,`tools/list`,`tools/call` ∧ stop. Source agrees: `connection.ts:1852` sets a handler for `ResourceListChangedNotificationSchema` only; `ToolListChangedNotificationSchema` = 0 hits tree-wide. ⊥ error, ⊥ disconnect on the ignored notification ⇒ emitting stays safe, just inert. Bears on V74, T66|`45cc53c4` `packages/api/src/mcp/connection.ts:1852`; tree grep `ToolListChanged` = 0; `spikes/librechat-embed-render-gate/evidence/probe-results.json`
R31|3 LibreChat MCP config bounds, ⊥ predicted by the source read|MEASURED 2026-08-08 while standing the rig up. (a) BOOT INSPECT vs C24: LibreChat inspects every startup server with ⊥ user in context ⇒ `{{LIBRECHAT_USER_ID}}` resolves to nothing ⇒ NOA 401 (correct, V3) ⇒ LibreChat infers OAuth (`requiresOAuth: true`, `connectionState: needs_authorization`) ∧ ⊥ connects again; `/api/mcp/tools` then answers ZERO tools ∀ user, silently. `startup: false` + `requiresOAuth: false` fix it (V88). (b) SSRF GUARD: ⊥ `mcpSettings.allowedDomains` ⇒ `Domain "http://noa.internal:8000" is not allowed`; `allowedAddresses` refuses CIDRs ∧ loopback literals @ pin ("host:port pairs, private space") ⇒ domain list = the lever. (c) PARSER: `ui://` scheme REQUIRED for a resource to become a UI resource; LibreChat hashes the body into a `resourceId` ∧ injects `UI Resource ID: <id>` + `UI Resource Marker: \ui{<id>}` into the text the MODEL sees ⇒ what V71's `\ui{}` instruction is for, ∧ confirmation the URL + id land in LibreChat's Mongo (V26)|`45cc53c4` `packages/api/src/mcp/parsers.ts:183`, `packages/api/src/mcp/registry/MCPServersRegistry.ts` (`resolveAllowlists`, `shouldEnableSSRFProtection`), `packages/data-provider/src/mcp.ts:166-171`; `docs/spikes/librechat-embed-render-gate.md`
R32|popup from a sandboxed frame INHERITS the sandbox|MEASURED 2026-08-09 in Chromium against NOA's own card, ⊥ a source read ∧ ⊥ a component double (V69's rule). The card's `target="_blank"` clicked INSIDE the frame: under `allow-scripts allow-same-origin` (`ToolCallInfo`, R13) → **0 tabs**, refused SILENTLY — asserted on the `context.pages()` count ∵ ⊥ exception is thrown, which is the whole reason the address also ships as text; under `allow-popups allow-scripts allow-same-origin` (`MCPUIResource`) → tab OPENS at the href. INSIDE that tab: same-origin `fetch` POST **arrives** (stub counter, polled FIRST so a missing form hit is an absence ∧ ⊥ an early read), native `<form>` submit **⊥ arrives** ∧ the tab ⊥ navigate off the probe document ⇒ 2 signals agreeing ⇒ `allow-scripts` ∧ `allow-same-origin` INHERITED, `allow-forms` ⊥ inherited ∵ ⊥ granted anywhere (R13). ⇒ a click-opened link-out = top-level BUT sandboxed (bounds C17, V25, V37, V80); an address COPIED into a fresh tab = ordinary document — ⊥ measured, ∵ a fresh tab has ⊥ opener to inherit from. The permissive run = the negative control for the 0-tab claim (V87): without it "no tab opened" passes against a link that is merely broken|`apps/web-embed/e2e/sign-in.browser.e2e.ts`; sandbox strings pinned `apps/web-embed/playwright.config.ts::MEASURED_SANDBOX`,`::POPUP_SANDBOX`; annotation `opened tab: form submit reached the stub = false`

## §V INVARIANTS

### Auth & Tokens

- V1. ∀ MCP req → resolve `users.id` + re-check `is_active`. `tools/list` RBAC-filtered per user. `tools/call` re-checks permission.
- V2. MCP auth = per-user bearer token. TWO mint surfaces (T53): admin FOR an operator (`POST /admin/users/{user_id}/tokens`, V13) ∧ operator for THEMSELF (`POST /me/mcp-tokens`, id off the SESSION ⊥ off the request). Self-mint ⊥ escalation ∵ the token carries ⊥ scope of its own — it re-issues the caller's OWN identity ∧ ∀ permission resolves off `users.id` per call (V1, V11, C6) ⇒ 0 roles mints a credential that calls 0 tools. Token ↔ `users.id`. Hashed at rest (SHA-256). Plaintext shown once at mint — POST body only, ∧ ⊥ read shape carries a field that could hold one. Revoke = delete row, `(user_id, token_id)` in the WHERE ⇒ foreign id ≡ unknown id ≡ 404 `mcp_token_not_found`, ⊥ 403 (existence ⊥ leak, V27/V76). ⊥ logged.
- V3. Token TOFU binding: minted `librechat_user_id=NULL`; NULL + `X-Noa-LibreChat-User` header present → bind; non-NULL → header ! present & equal else 401. Absent header → 401 ∀ states (bound ∧ unbound) ∵ C24 LibreChat-only. 401 body names cause: `librechat_user_header_missing` \| `librechat_user_mismatch`. ⊥ unbound-token client path.
- V4. Token revalidation: `last_ldap_check_at` on row, staleness interval. LDAP down → fail closed. Cascade revoke on admin disable + LDAP path.
- V5. Resolve MCP identity in exactly one function, called ∀ MCP request. Auth mechanism swap = one file.
- V6. Login sets httpOnly `noa_session` cookie (SameSite=Lax, `Domain` = the registrable parent V40 fixes — `.noa.internal` in development, `.simondayce.my.id` deployed, ⊥ a constant in this line, Path=/). Logout clears with max-age=0. Logout idempotent without auth. Session JWT ⊥ revocable pre-`exp` — ⊥ `jti`, ⊥ denylist ⇒ logout kills the *cookie*, ⊥ the token: a held copy verifies until `exp`, window = `AUTH_JWT_ACCESS_TOKEN_TTL_SECONDS` (default 3600). V4 cascade-revoke-on-disable = `mcp_tokens` only (T11), ⊥ session equivalent exists. ⇒ T8/T9 ! re-read `users.is_active` ∀ authenticated request — that re-read is the ONLY thing bounding a disabled admin's session, ⊥ an optimization. Session-kill-on-disable (denylist \| `jti` \| shorter TTL) = OPEN owner decision, ⊥ agent call ⇒ the logout-invalidation gap is a spec-acknowledged deviation, ⊥ an oversight. Research feeding that decision: R18–R22. Rationale: `docs/AS-BUILT.md` §V6.
- V79. Session verify clock discipline: leeway = 0 (PyJWT default, ⊥ passed). `exp` 1s past → expired; `iat` 1s future → invalid (PyJWT `iat > now + leeway` ⇒ `ImmatureSignatureError`). Holds ∵ single API process — mint & verify share one clock. **Revisit trigger: >1 API replica.** Then drift breaks the MINT side first: minter clock ahead ⇒ verifier rejects a token the instant login returns it (⊥ merely logouts near expiry). Fix then = explicit `leeway`, ⊥ silent widening. Future-`iat` rejection = `PyJWT==2.13.0` behaviour, moved across 2.x releases ⇒ pinned + tested, ⊥ assumed. VERIFIED at tag 2.13.0 (R24): `leeway` default `0`; `iat > (now + leeway)` → `ImmatureSignatureError` (`InvalidIssuedAtError` = non-numeric `iat` only); check landed 2.6.0 (#794), touched 2.7.0 (#847). `_validate_jti` = type check only ⇒ ∀ revocation logic = ours (bears on V6).
- V7. New LDAP users auto-provisioned `is_active=False`. Bootstrap admin auto-activated with `admin` role.
- V8. Passwords and tokens ⊥ appear in structured logs ∧ ⊥ in error bodies. Error body = `error_code` + `message` + `request_id`, nothing else — `detail` = internal diagnostic, logs only. Two echo paths closed by name (T64): pydantic's `RequestValidationError.errors()` carries the failing value in `input` ⇒ on `POST /auth/login` that IS the submitted password ⇒ ⊥ forward `exc.errors()` to body (`noa-old` did) ∧ ⊥ to log — `loc` + `type` only. `StarletteHTTPException.detail` likewise ⊥ echoed: status → fixed `(error_code, message)` pair ⇒ a future raiser ⊥ put internal text in a body by accident.
- V9. Rate limiter blocks after configured max failures within window. Rate-limited → 429 with `Retry-After`.

### RBAC

- V10. Admin role bypasses tool permission checks for known tools but rejects unknown/unregistered tools.
- V11. Disabled users (`is_active=False`) → zero permissions regardless of roles.
- V12. Cannot disable last active admin. Admin cannot self-deactivate. Admin self-delete → 409.
- V13. Non-admin users → 403 on admin endpoints. `admin` role reserved (cannot edit tools or delete). Internal roles cannot be assigned via API.
- V14. Admin changes produce audit events. Permission updates take effect immediately.

### Tool Discipline

- V15. CHANGE tool schemas ⊥ carry reason param of any name. Reason enters exactly once: operator types in approval iframe → approve POST body. Approve without non-blank `reason` → 409 `change_reason_required`. Gate = approve endpoint, ⊥ tool call. **AMENDED 2026-08-08 by T37: DENY ! carry one too** — same 409, same code. T34(f) handed T37 that choice ∧ this is it: a reason on BOTH is what makes the DB CHECK expressible (`ck_action_requests_decided_reason`: APPROVED ∨ DENIED ⇒ non-blank), ∧ an audit trail owes an explanation for a refused change as much as an approved one. Safe exit stays open — ⊥ answering expires the request (V32) ∧ an EXPIRED row carries ⊥ reason ∵ nobody gave one. "Blank" = whitespace, on BOTH sides: endpoint `.strip()`, DB `reason ~ '[^[:space:]]'` — ⊥ `btrim()`, which strips SPACES only ⇒ a one-tab reason the endpoint refuses would satisfy it. 2 spellings of blank = 1 too many.
- V16. READ tools execute immediately. CHANGE tools go through approval gate: create `action_request` (PENDING) → operator approves/denies → execute if approved.
- V17. One-workflow-one-tool: preflight runs in-process inside CHANGE call. Evidence born in-process, lives milliseconds, same user, ⊥ in transcript. ⊥ evidence store needed.
- V18. Ambiguous identifiers → structured "ambiguous, here are candidates" result. ⊥ guess.
- V19. Tool error sanitization: raw exceptions ⊥ reach LLM. `RuntimeError` → `tool_execution_failed`. `TimeoutError` → `timeout`.
- V20. Lifecycle enums machine-stable, **3 distinct enums** — ⊥ one flat set. `ToolRisk` = `READ` \| `CHANGE` (classification, ⊥ a status). `ActionRequestStatus` = `PENDING` \| `APPROVED` \| `DENIED` \| `EXPIRED`. `ToolRunStatus` = `STARTED` \| `COMPLETED` \| `FAILED`. `tool_runs` carries risk + status as separate columns ⇒ failed READ representable. `EXPIRED` ∉ `noa-old` `ActionRequestStatus` — **BUILT 2026-08-08 (T34)**: without it a request nobody answers stays PENDING ∀ time ∧ V23 has ⊥ truthful answer past the TTL. ∀ 3 value sets DISJOINT ⇒ a query written vs one column ⊥ match rows in another; asserted 3-way at T34 (only pairwise was possible at T35).
- V21. Whitespace-only required strings rejected. Duplicate firewall targets rejected. Invalid server_ref rejected.
- V83. Exposed-tool discipline — a tool ships WITH its gate, ⊥ after it. (a) ∀ registered MCP tool name ∈ `TOOL_CATALOG`; ⊥ ⇒ construction failure, ⊥ runtime surprise — grants are written against that catalog ∧ admin bypass hands out "every known tool" ⇒ an uncatalogued name = capability ⊥ role can be granted ∧ admin still reaches. (b) RBAC = ONE middleware ∀ tools (V1 both clauses), ⊥ per-tool code — a tool ⊥ forget a middleware. (c) Gate carries the REGISTERED set ∧ intersects: admin's grant set = whole catalog ⇒ a catalogued-but-unbuilt name otherwise reaches fastmcp's own `Unknown tool`. (d) ONE refusal code (`tool_not_permitted`) ∀ causes — no-grant ∧ ⊥-registered ∧ ⊥-catalogued identical ⇒ ⊥ existence oracle over C22 names ∨ over which tools are built. (e) V19 mapping ! be INSIDE the tool, ⊥ middleware (R25). (f) **CATALOG CLOSED 2026-08-15 (T29)**: registered set == `TOOL_CATALOG` == 14 (I.mcp) ∧ ∀ CHANGE name has a runner ⇒ (c)'s intersection has ⊥ production gap left to catch. It is ⊥ dead ∧ ⊥ deletable — a predicate that stops separating degrades SILENTLY (V87), so the catalogued-but-unregistered assertion MOVED off production onto a probe that builds `RbacToolMiddleware` with an EMPTY registered set (`test_mcp_tool_rbac.py`), the shape `assert_change_runners_cover`'s no-runner probe already had. MUTATION-CHECKED: deleting `granted & self._registered_tools` turns that probe RED ∧ nothing else in the suite. ∧ an EQUALITY now asserts registered == catalog — brittle ON PURPOSE in one direction, so a catalogued name added with ⊥ registrar re-opens the gap THERE ∧ ⊥ in front of an operator meeting fastmcp's `Unknown tool`.

### Approval Gate

- V22. Approve/deny ⊥ travel through LLM. Only path = POST from embed iframe → NOA with `noa_session` cookie + CSRF token.
- V23. "May this run?" read from `action_requests.status` in DB ∀ time — ⊥ from LLM claim, ⊥ from tool arg.
- V24. `build_change_gate_response()` shapes every CHANGE tool result; 3 branches: link-out text \| UI resource \| elicitation (future). Active branch = **UI resource** (path A), selected by T59's live run 2026-08-08 (R29), ⊥ assumed. 2 branches ship TOGETHER, ⊥ either/or: the iframe is the surface ∧ the plain URL rides in the text block ∀ result (V25) — the frame can fail to load, ∧ then the URL is the only door. Elicitation stays future; upgrade path = swap branch, backend unchanged. 3rd branch REFUSES, ⊥ falls through: `ChangeGateBranchUnavailableError` (a `ChangeGateError` ⇒ survives V19's boundary), mapped **500 EXPLICITLY**, ⊥ the 503 fallback. Branch = module constant `ACTIVE_CHANGE_GATE_BRANCH`, ⊥ a tool arg ⇒ ⊥ caller can select it. ⊥ `structured_content` on a CHANGE result. "2 branches ship TOGETHER" asserted on the COUNT — 2 blocks, text FIRST — ⊥ on "a resource exists". BUILT 2026-08-09 (T32). Rationale: `docs/AS-BUILT.md` §V24.
- V25. Text content ! always carry approval URL plain ⇒ link-out always live. Serves 2 paths: T59-fail (C21 → link-out becomes primary) + iframe-load-failure. ⊥ a non-LibreChat-client feature (C24). **BOUNDED 2026-08-09 (T43, R32)**: "always live" = the URL is always THERE, ⊥ that ∀ way of opening it works. A link CLICKED in-frame needs `allow-popups` (absent at `ToolCallInfo`, R13) ∧ the tab it opens inherits the sandbox ⇒ ⊥ `allow-forms` in it. The door that survives BOTH = the address as TEXT, copied into a fresh tab ⇒ ∀ surface handing an operator out of the frame ships the plain address BESIDE the link (V94). **2nd binding 2026-08-09 (T32)**: ONE wording ∀ branch that has a text block ⇒ "the address is always there" = 1 claim about 1 string, ⊥ a per-branch copy that drifts apart later. ∧ it ships as an ADDRESS, ⊥ markup — `href` ∧ `<a ` asserted ABSENT in the gate's text (`apps/api/tests/test_mcp_change_gate_response.py`), ∵ the door V94 keeps open is the one upstream's sandbox ⊥ withhold, ∧ a link NOA writes into the text would be that door spelled the withholdable way.
- V26. Embed URL carries `action_request_id` only — ⊥ token, ⊥ ops data. Tool-result artifacts persist in LibreChat MongoDB ⇒ assume LibreChat admin can read them. **BOUND 2026-08-09 (T32) at the SHAPE, ⊥ by filtering**: `build_change_gate_response` takes `PendingChangeRequest` (id, status, deadline) ∧ has ⊥ field for arguments, evidence ∨ requester — the same structural argument V76 makes one reader over, ∧ the reason a future field ⊥ leak by omission of a filter nobody added. Asserted 2 ways ∵ 1 is a `startswith`: the URL taken APART (`path` exact, `query == ""`, `fragment == ""`) so a query param added later goes RED instead of hiding on the end of a prefix match, ∧ sentinels driven through the REAL gate then hunted in the serialized `ToolResult`. The URL = a NAME, ⊥ a key — authorisation to read the card behind it = the cookie + V27's requester-match, evaluated at FETCH; a URL that were a key would be one an operator could paste into a chat.
- V27. Embed access control: requester-match primary (`requested_by_user_id == caller`, caller = cookie identity). Mismatch → 404, not 403 (existence ⊥ leak). **AMENDED 2026-08-08 by T37: "⊥ leak" bound at the BODY, ⊥ the status** — foreign id ∧ unknown id answer byte-identical (`error_code` + `message`; only `request_id` differs, V73). A differing code = a 403 spelled differently, ∧ a status-only test ⊥ catch it (B1's shape). NULL requester (FK `SET NULL`, T34) matches ⊥ caller ⇒ lands here: fail-closed.
- V28. Exactly one `pending → decided` transition per request, under row lock. Concurrent double-click → one wins, loser gets 409 `action_request_already_decided`. **AMENDED 2026-08-08 by T37: ∀ guard — requester-match (V27), status, deadline (V32) — evaluated AFTER `SELECT … FOR UPDATE`, in one transaction.** `noa-old` read the row, checked `status`, THEN locked (`MCP:apps/api/src/noa_api/storage/postgres/action_tool_runs.py:256-270`) ⇒ the check sat OUTSIDE the thing that makes it hold. **Test ! prove the 2 parties OVERLAPPED — V89, B5.**
- V29. Approve → async execution, 202 + `tool_run_id`, embed polls to terminal. State in DB, ⊥ in connection. **AMENDED 2026-08-08 by T37**: the `tool_runs` STARTED row ∧ `action_requests.tool_run_id` land in the SAME transaction as the decision ⇒ APPROVED-with-⊥-run ∧ run-with-⊥-approval both unrepresentable. Same-session is a fact of CONSTRUCTION (`start_change_run` on the decision repository), ⊥ an obligation a caller can get wrong. Executor handed the run AFTER commit — a handoff first could start a change whose authorization then rolled back; a death in that gap = exactly T38's reaper pair (APPROVED + STARTED). Handoff failure = logged ∧ SWALLOWED, ⊥ a 500: the approval is durable, ∧ a 500 sends the operator to click again into V28's 409, which tells them nothing.
- V30. Async host = in-process asyncio task with own session + reaper for runs stuck in STARTED. **LANDED 2026-08-09 (T38)**: `core/approvals/execution_host.py` = one task per approved change, its own session opened INSIDE the task, tracked so the loop is ⊥ the only reference (a garbage-collected task stops mid-change), cancelled ∧ awaited by the lifespan. Runs it interrupts stay STARTED on purpose — a terminal status at shutdown = an outcome nobody observed. Reaper = `core/approvals/reaper.py` on T39's loop (V66), ∧ it covers READ runs too (T73's swallowed closing write). Its 2nd population — `APPROVED` with ⊥ run — is DETECTED ∧ ⊥ repaired: T37 makes that pair unrepresentable from the decision path (V29), a DELETE of the run row reaches it via `SET NULL`, ∧ then the change may well have COMPLETED (T38(c)).
- V31. Explicit per-user cap on in-flight CHANGE executions (default 1). Over limit → 409, ⊥ silent queue. **AMENDED 2026-08-09 by T38: counting alone ⊥ hold this.** 2 approvals of DIFFERENT requests by one operator ⊥ touch each other's rows ⇒ V28's `FOR UPDATE` serializes nothing between them, ∧ both read 0 under READ COMMITTED. Count taken under a per-user `pg_advisory_xact_lock` in the decision's own transaction, ∧ the repository takes the lock ∧ returns the count in ONE method ⇒ construction, ⊥ an obligation. In flight = `tool_runs` `risk=CHANGE ∧ status=STARTED` ⇒ a run stranded by a dead process counts until the reaper resolves it, which is one of the 2 reasons that deadline exists. Code = `change_execution_limit_reached`, 409 ⊥ 429 (nothing rate-limits the operator, ∧ `Retry-After` = a number NOA ⊥ honestly produce). Guard runs LAST of the decision's guards ∧ before the run it would count (T38(d)). **Test ! prove the 2 parties OVERLAPPED — V89, ∧ the negative control shows both approvals win without the lock.**
- V32. Pending requests expire on TTL → terminal `expired`; approve/deny on expired → 409 `action_request_expired`. **Check-on-read at the DECISION door**: `expires_at <= now` under the lock already held → WRITE EXPIRED + `decided_at` → commit → THEN raise; it WRITES ∵ refusing without the write leaves a row still reading PENDING. `reason` stays NULL — V15's CHECK exempts EXPIRED ∵ an expiry is the ABSENCE of an answer, ⊥ one. **Sweep**: `core/approvals/expiry.py`, set-based `UPDATE … WHERE status = PENDING AND expires_at <= now RETURNING id`, hosted by an in-process asyncio task the lifespan starts ∧ stops. Deadline compare = `<=` at BOTH doors (T39(a)). ⊥ `FOR UPDATE` in the sweep ∧ that ⊥ weaken V28 — a bare `UPDATE` re-checks its WHERE vs the version it waited for ⇒ it SKIPS a row an approval just won, ⊥ clobbers it (T39(c)). The sweep is a THIRD writer class: EXPIRED ∧ nothing else. **Check-on-read on the RENDER path runs AFTER the requester-matched read, at BOTH doors** (T63, T41) ∵ `expire_if_due` takes an id ∧ ⊥ a requester ⇒ expiry-first lets a prompt-injected id make NOA WRITE to a row its caller ⊥ see (V27); "the caller supplied it" ≠ "the caller may see it". Ordering lives in ONE place — `core/approvals/reads.py::apply_due_expiry` (V66) — so a 3rd reader inherits it, ⊥ chooses. LANDED 2026-08-08 (T37, T39, T63) ∧ 2026-08-09 (T41). Rationale: `docs/AS-BUILT.md` §V32.
- V33. Approval context built at gate time and persisted on row, ⊥ rebuilt from transcript at render time. **BOUNDED 2026-08-15 by T28 (V98)**: persisted ⊥ mean the runner WRITES from it. The evidence stays the source of the TARGET — which server, which object, which direction, ⊥ one of which a model may re-supply ∵ inventory is editable in the window — ∧ it is ⊥ the source of the BYTES where the target offers a compare-and-set token. V98 carries that half.
- V98. A CHANGE that writes under a COMPARE-AND-SET token takes that token — ∧ the VALUE it edits — from the RUNNER's own read, ⊥ from gate time. ∵ an approval window is MINUTES ∧ a CAS token covers the WHOLE object, ⊥ the field being changed. 2 harms, both SILENT, opposite directions: (a) a gate-time TOKEN REFUSES an approved change on an edit that has nothing to do with it — after a reason was typed ∧ Approve pressed, ∧ "ask again" is the only remedy; (b) a gate-time VALUE is a LOST UPDATE, ∧ it SUCCEEDS — writing back the line the card described REVERTS an edit made in the window, as a side effect of a change that was about one field, ∧ NOA authored it. ∴ the CAS window = the runner's own read→write (what a CAS is FOR), ∧ what the APPROVAL window is checked against = the FACT the operator approved, ⊥ the bytes they saw. 3 outcomes at run time ∧ only 1 is a failure: still in the approved before-state → run; ALREADY in the asked-for state → `no_op`, ⊥ a failure (somebody reached it first ∧ the world is as the operator wanted it); the object GONE → its own code, ⊥ a neighbour. **Test ! assert on the BYTES WRITTEN, ⊥ on the return value** — a runner writing the gate-time line back answers IDENTICALLY, so the lost update is invisible from the envelope — ∧ a NEGATIVE CONTROL ships: a stale-token write the target REFUSES, else "it re-read" passes against a runner that sent ⊥ token at all (V87). Instance: T28 (`proxmox_vm_nic`, the Proxmox config digest; `noa-old` took that digest as a TOOL ARG, which is the arrangement this line retires). **T29 = the case with ⊥ CAS TOKEN AT ALL** (2026-08-15): PMG's `mynetworks` offers none, ∧ only the TOKEN half goes vacuous — the 3-outcome clause stands on its own, checked against the FACT the operator approved (is the address an entry?) RE-MEASURED by the runner. ⇒ a target that offers ⊥ compare-and-set is ⊥ exempt from re-reading; it just has less to re-read UNDER. Sibling: V97 — V97 bounds what a CHANGE reads AFTER it writes, this bounds what it writes FROM. Rationale: `docs/AS-BUILT.md` §T28(b), §T29(d).
- V34. One URL per request; `/approvals/[id]` owns whole lifecycle through receipt. **LANDED 2026-08-09 (T42)** — "through receipt" was the unbuilt half: the card's GET carries `receipt` ∧ the page renders BOTH halves (DECISIONS §6.5) — the before-state authorised against ∧ what the change did, ⊥ collapsed into a single "done", ∧ a FAILED change keeps its before-state ∵ that is the case a done-only card drops. ⊥ receipt ⇒ ⊥ outcome section, ⊥ an empty one.
- V35. Approval card shows provenance: created-at, conversation ref, origin, requesting identity.
- V36. Post-decision notification to LLM = notification only. Ignored ⇒ change still executed and recorded.

### Embed & Security

- V37. Embed iframe document ! sit on NOA origin — property holds ∀ render paths. Mechanism **MEASURED, ⊥ inherited** (C21, R29): path A = `text/uri-list` → `src` mode → `allow-same-origin` → cookie rides — CONFIRMED live 2026-08-08 at pin `45cc53c4`, frame `src` on NOA's origin, `srcdoc` absent, in-frame `GET /auth/me` ∧ `POST` both 200 as the logged-in operator, `document.cookie` empty throughout (httpOnly holds, V6); path B = link-out new-tab, top-level NOA origin, cookie rides natively (⊥ iframe, ⊥ MIME dependency). Path B stays live ∀ iframe-load failure (V25) ∧ becomes primary again if a bump fails the re-verify — V22 unchanged either way ∵ decision still cookie POST from NOA origin. **path B BOUNDED 2026-08-09 (T43, R32)**: "top-level" measured TRUE, "unsandboxed" ⊥ — a tab opened by a CLICK inside the frame inherits the opener's flags. The cookie claim stands (`allow-same-origin` inherited ⇒ origin ⊥ opaque); "forms fine there" does ⊥ (V80 as amended). Path B reached by COPYING the address = ⊥ opener ⇒ ⊥ inheritance ⇒ the unqualified claim holds only there.
- V38. Embed 401 renders explicit "cannot authenticate here" state — ⊥ blank card, ⊥ live Approve button. **BUILT 2026-08-09 (T43)** — the state carries a way OUT, ⊥ only a name (V42). Absences asserted inside the frame LibreChat serves, ∧ by NAME ⊥ by COUNT: T43 adds 2 controls that are ⊥ decisions, so a `getByRole('button')`-count assertion would now go red for the right reason spelled wrongly. What ! be absent = Approve/Deny, the reason box, `<form>`, `input`.
- V39. CSRF token server-minted, signed, session-bound. Double-submit alone insufficient — any `*.noa.internal` sibling can plant a cookie. `core/approvals/csrf.py`: HMAC-SHA256 over `(version, user_id, action_request_id, issued_at)`, wire = `v1.<issued_at>.<sig>`. **Bound to the REQUEST as well as the session** ⇒ a token minted for one card ⊥ authorise another. Key = `HMAC(AUTH_JWT_SECRET, b"noa/approvals/csrf/v1")` — DERIVED, ⊥ a 2nd required prod secret; the label makes a session JWT unusable here (ASSERTED by test, ⊥ reasoned). TTL = `APPROVAL_PENDING_TTL_SECONDS`, ⊥ a number of its own. `hmac.compare_digest`. Clock = V79's rule: zero leeway, future `issued_at` invalid. Age checked AFTER the signature ⇒ a forged stamp ⊥ buy a longer window. Refusal = 403 `csrf_token_invalid`, ONE code ∀ cause (absent, malformed, foreign operator, foreign card, stale). **⊥ minting ROUTE, EVER** — minted inside the card's own `GET /action-requests/{id}` ∧ returned in that body ⇒ a token can ⊥ be obtained without passing V27's requester-match, ∵ the match IS that read. `csrf: null` once the request is terminal ∧ the renderer reads it to draw ⊥ buttons at all (V38's family). Token ⊥ rendered as text — it rides in the `fetch` body (V26, asserted in the browser lane). BUILT 2026-08-08 (T37), settled 2026-08-09 (T41). Rationale: `docs/AS-BUILT.md` §V39.
- V40. ∀ operator-facing origin ! sit under ONE registrable parent, ∧ `noa_session` scoped to THAT parent (`AUTH_SESSION_COOKIE_DOMAIN`), SameSite=Lax. **The parent is a DEPLOYMENT property, ⊥ a constant** — `.noa.internal` in development (the hosts line, `docker-compose.yml`, ∀ Playwright/vitest fixture) ∧ `.simondayce.my.id` in staging ∧ production (T75). ∵ an origin OUTSIDE the parent receives the cookie ⊥ at all: sign-in appears to SUCCEED ∧ then every authenticated request answers 401 with ⊥ line in any log — V95's failure class, one mechanism over. ⇒ LibreChat's own origin ! share the parent too (`https://chat.simondayce.my.id` deployed, V41) ∵ the operator ARRIVES through the chat document ∧ signs in on the admin origin, so a chat host off the parent breaks the ride before any `frame-ancestors` question is reached. Bound at the MANIFEST ⊥ by discipline: `test_k8s_ingress_hosts_share_the_cookie_domain` (T75) refuses an Ingress host off the parent, ∵ "same registrable domain" as prose is exactly what a new Ingress forgets.
- V41. Admin app sends `frame-ancestors 'none'`. Embed app sends `frame-ancestors <LibreChat origin>` on ∀ response — ONE `headers()` entry on `/(.*)` (pages, `/api/*`, `/healthz`, 404), ⊥ per-route. Origin = `NOA_LIBRECHAT_ORIGIN`, default `https://chat.noa.internal` (∧ what `.env.example` ∧ `core/config.py` carry). Absent ∨ blank → that default ⇒ header ⊥ omitted; widening value (wildcard, 2nd origin, path, bare host) → build ∨ boot FAILURE, ⊥ silent default. Baked at `next build` (`output: 'standalone'` ⊥ run the config at runtime) ⇒ runtime env ⊥ widen it, ⊥ change it. ⊥ `X-Frame-Options` — absence asserted, ⊥ assumed (T45). **BOUNDED by T75**: `https://chat.noa.internal` = the DEVELOPMENT origin, ∧ deployed LibreChat is `https://chat.simondayce.my.id` (V40's parent) ⇒ the fallback is now WRONG-BY-DEFAULT for any deployed build — a build that ⊥ pass the arg ships a header REFUSING the sole client (C24): blank frame, ⊥ exception, ⊥ log line, on the one surface the operator decides from. The default STAYS (a header never omitted is still the right shape, ∧ dev + the Playwright lane legitimately want it) ∧ the guard moves to whatever BUILDS a deployed image — V101.
- V42. Embed has no login page, no LDAP form, no credential handling. 401 → "Sign in to NOA" + retry. **BUILT 2026-08-09 (T43)**: link = `<a target="_blank" rel="noopener noreferrer">` + the SAME address as text beside it (R32 says why both, V94 makes it the rule); retry = ONE re-read through the poll's own reader (`lib/approvals/poll.ts::fetchApprovalCard`, V66), ⊥ a reload ∧ ⊥ a 2nd definition of the 4 outcomes. Address from `NOA_SIGN_IN_URL` — the WHOLE URL ∵ the login route is T50's to own, read per REQUEST server-side (⊥ baked like V41's origin ∵ `output: 'standalone'` ⊥ run the config at runtime, ⊥ `NEXT_PUBLIC_*`), `http`/`https` ONLY ∵ the value becomes an `href` ⇒ `javascript:` = script execution configured by env var; absent ∨ blank ∨ off-scheme ∨ credential-bearing → ⊥ link AT ALL, ⊥ a default — a door to a host nobody deployed reads as an action REFUSED (`canDecide`'s judgement, V38's family). ⊥ redirect bound as an ABSENCE: the framed document's URL asserted UNCHANGED across the retry, ∵ "⊥ LDAP redirect inside iframe" is otherwise prose.
- V43. Exactly ONE reason field exists, operator-typed in approval iframe (C8). ⊥ `proposed_reason`, ⊥ LLM-authored reason anywhere in schema/DB/receipt. LLM-authored text (tool args, activity label) = evidence only, ⊥ authoritative, ⊥ stored in `action_requests.reason`. **AMENDED 2026-08-15 by T22: the ONE field may LEAVE NOA — it ⊥ come BACK.** C8 bars the LLM from authoring, relaying ∨ SEEING a reason; it ⊥ bar NOA from writing the operator's own words onto the system being CHANGED, ∧ WHM `suspendacct` has a suspension-note field whose only honest content is that string (the alternative = a NOA-authored placeholder in the record cPanel shows). ⇒ `AuthorizedChange` ∧ `ChangeExecutionRequest` carry `reason`, `load_authorized` FETCHES it — deliberately, ⊥ incidentally (V93: a value is fetched ∨ it is not) — ∧ the runner sends it. 2 bounds ride with it: nothing BRANCHES on it (the row said APPROVED before it was read ⇒ a VALUE, ⊥ a permission), ∧ ∀ return path to a model is closed by V96.
- V96. A value C8 keeps from the LLM ∧ NOA WRITES out ! be unreadable on ∀ path BACK to a model. The write makes a 2nd copy NOA ⊥ own; 2 return paths exist, both FOUND ∧ CLOSED 2026-08-15 by T22. **(a) the TARGET echoes it** — WHM stores the note as `suspendreason` ∧ returns it on ∀ later `listaccts` ⇒ `whm_search_accounts` would hand the model the operator's reason with ⊥ tool schema ever naming one. Withheld at the SURFACE that answers a model (`ACCOUNT_FIELDS_WITHHELD_FROM_MODEL`, `noa_api/mcp_tools/whm_read.py`), **⊥ at the normaliser**: the parked table (V64) ∧ the approval card are the operator's own (cookie + V27) ∧ ARE where the reason may be read ⇒ a field dropped from EVERYONE is dropped from the 2 surfaces that exist to show it. **(b) NOA's own AUDIT echoes it** — a runner's payload becomes `tool_runs.result_summary` (`core/audit/summaries.py`) ∧ `noa_get_action_result` returns that string ⇒ a runner repeating the note it just wrote reaches the model through V45's row, ⊥ through a schema. **⊥ via the RECEIPT** — `core/approvals/results.py` leaves `include_receipt` at default ⇒ T63's reader ⊥ join `action_receipts` (V76, T42's flag); a doc naming that door names the wrong field ∧ the wrong fix (the T22 code comments did, corrected in the same pass). Asserted on the SERIALIZED result ∧ on the DERIVED summary, ⊥ on a key set — a field dropped in one place ∧ kept in another passes a key compare (V87's shape). REACHES T25/T26: a `csf allow` comment is the same field one system over, ∧ `whm_preflight_firewall_entries` reads csf lines straight into the transcript ⇒ a reason written there returns the same way. **T25's half CLOSED 2026-08-15 — (a) AGAIN, ∧ closed by a MARKER ∵ csf's echo has ⊥ FIELD to drop.** A `suspendreason` is a key on a row ⇒ withhold the key; a csf comment is free text at the TAIL of a line ⇒ withholding it means dropping the LINE, ∧ that line is the `csf.deny`/LFD evidence DECISIONS §6.5 requires. ∴ NOA AUTHORS the comment — `noa_firewall_comment` → `noa:<action_request_id> <reason>` — ∧ `without_noa_comment_text` cuts marker → END OF LINE. To END OF LINE, ⊥ to a closing bracket: an operator types prose ∧ a reason carrying `)` ∨ `'` walks straight through any bracket rule, which is the same leak one length shorter. COST STATED ⊥ hidden — a token csf places AFTER the comment goes with it ⇒ `format_imunify_matches` renders `[expires: N]` BEFORE `(comment)`, guarded by an ORDERING test ∵ swapping back shortens a model's evidence silently. Cut at the SURFACE ⊥ the parser: the card ∧ the receipt read the same lines through T25's evidence ∧ are the operator's own. Verdict computed from the FULL parse BEFORE the cut. Marker SURVIVES = the pointer back to the row where the reason is readable behind the cookie. (b) rides again ∧ is closed twice more: the runner's payload ⊥ repeat the reason, ∧ a backend failure message quotes the command it ⊥ run ⇒ cut too. A NEGATIVE CONTROL ! ship beside the leak assert — a line with ⊥ marker (LFD's own text) kept INTACT — ∵ a rule that cut EVERY line passes the leak test ∧ destroys the evidence (V87). **T26's half CLOSED 2026-08-15 ⇒ V96 DISCHARGED.** A removal writes ⊥ comment — `csf -tra`/`-ar` ∧ Imunify `delete --purpose white` take none ⇒ nothing new LEAVES ∧ V43's permission is unused on that side — but the entry it DELETES is T25's ∧ carries the marker ⇒ the bound applies on the way BACK all the same. 3 doors: the backend failure message, which quotes the entry it ⊥ remove (cut, `backend_change_failure`); the postflight's own `csf -g` lines, read for a verdict ∧ ⊥ put in the payload; ∧ a THIRD T25 ⊥ have — **the NO-OP answer is a transcript surface**, a plain tool result ⊥ a gate response (V26) ⇒ built from server name + address + 1 MEASURED boolean, ⊥ from the lines it was decided FROM (T23's rule, one system over). Asserted with a leaky line for a DIFFERENT address in the same read, ∵ a clean read proves nothing. Instances: T22 (`suspendreason`), T25 (`csf`/Imunify comment WRITE), T26 (that same comment READ BACK on delete). ⊥ a ban on writing the reason OUT (V43) — a bound on reading it BACK.
- V44. Embed ⊥ trigger decision from inbound `postMessage`. Any listener validates `event.origin` and is read-only.
- V80. In-iframe approve/deny ! POST via JS `fetch` — ⊥ native `<form>` submit, ⊥ `<form action>` fallback. ∵ sandbox at pin `45cc53c4` = `allow-scripts allow-same-origin` (+`allow-popups` on one render site) with **`allow-forms` ABSENT ∀ render paths** (R13, re-verified LIVE 2026-08-08 at BOTH sites, R29) ⇒ a form submit dies silently in-frame. Cookie + CSRF discipline unchanged (V22,V39). Bound by test, ⊥ by discipline (V84c): (1) `spikes/librechat-embed-render-gate/verify_librechat_pin.sh` FAILS if the shipped `@mcp-ui/client` bytes ever mention `allow-forms` ⇒ a bump that grants it turns this line red instead of leaving it stale; (2) `apps/web-embed/e2e/approvals.browser.e2e.ts` frames the card at `playwright.config.ts::MEASURED_SANDBOX` (constant pinned so a spec ⊥ quietly widen it), clicks Approve in-frame, ∧ asserts the stub's hit COUNTER — ⊥ an exception, ∵ the sandbox blocks a submit SILENTLY; + a NEGATIVE CONTROL in the SAME sandbox where a `fetch` lands ∧ a native `<form>` records ZERO hits, proven to separate by widening the sandbox to grant `allow-forms` (V87). ∧ the card carries ⊥ `<form>` at all, both buttons `type="button"`. **RETRACTS half of its own earlier sentence** (T43, R32): a link-out tab is top-level BUT INHERITS the opener's sandbox ⇒ ∀ document NOA sends an operator to from in here ⊥ assume a form submits; lane = `apps/web-embed/e2e/sign-in.browser.e2e.ts` (V94). Re-check the sandbox string ∀ T59 re-verify. Rationale: `docs/AS-BUILT.md` §V80.

### Audit

- V45. Every MCP READ writes `tool_runs` row: requester, truncated summary, redacted args, queryable in admin audit.
- V46. Every CHANGE (approved) writes `tool_runs` + `action_receipts` + audit log. **LANDED 2026-08-09 (T38)**: the run's terminal status ∧ the receipt land in ONE commit — a COMPLETED run whose receipt rolled back is this line held by nothing (V69's shape) — ∧ the audit log = structured events, the way T9 satisfies V14's "produce audit events" with no table. Receipt = 2-part (`before` = the gate's own preflight off `approval_context`, `after` = what the runner answered), written for a FAILED change too ∵ a before-state with no working after-state IS the record of a change that ⊥ complete. 1 writer (`core/audit/receipts.py`), idempotent on T36's UNIQUE ⇒ executor ∧ reaper both reaching one finished run leave ONE receipt. **AMENDED 2026-08-09 by B6**: one receipt ⊥ one status — the run's terminal status needed its own first-wins guard ∧ ⊥ inherit this one; V91 carries it.
- V91. A `tool_runs` TERMINAL write = FIRST-WINS: `status = STARTED` ! sit in the UPDATE's own predicate, ⊥ in a read before it. 2 writers can reach one run — T38's executor finishing a long change ∧ T38's reaper calling it abandoned past its deadline — ∧ an unguarded `WHERE id = :id` ⊥ a race, it is 2 writers both TAKING the row: either "outcome never observed" overwrites a COMPLETED change, ∨ COMPLETED lands beside the abandonment receipt V46's `DO NOTHING` already made permanent, ∧ either way V31's in-flight count frees a slot while the task is still live. Predicate IN the statement ∵ the UPDATE re-evaluates it vs the newest COMMITTED row version ⇒ a SELECT→UPDATE gap closes with it; a check in the caller ⊥. V46 one field over: V46 makes the RECEIPT idempotent, this makes the STATUS idempotent, ∧ a finished run needs BOTH — the receipt guard reading as if it covered the status is what let B6 ship. Test ! hold the window open ∧ assert ORDER (V89), ⊥ `asyncio.gather`.
- V92. A background pass that WRITES ! carry a LIMIT ∧ ! report what the LIMIT hid. (a) **BOUND**: a repair loop reading a population it then writes ! cap the read IN the statement, ⊥ slice after loading — unbounded, 1 pass after a long outage loads ∀ row, issues N UPDATEs + N INSERTs in ONE transaction, holds those locks ∧ its own xmin till the last write lands, ∧ a failure near the end rolls the whole pass back. (b) **REPORT**: a pass that hit its bound ! ship `truncated` + `remaining` beside `count` — V85 one writer over, ∵ `count` alone answers "how many did this pass RESOLVE" ∧ an operator reads it as "how many WERE there". Total counted in the SAME statement as the page — `count(*) OVER ()`, evaluated over ∀ the `WHERE` admitted BEFORE `ORDER BY`/`LIMIT` ⇒ page ∧ total = 1 snapshot. ONE definition of "was this pass capped" (`ReapOutcome`), ⊥ a 2nd on the page object. (c) The cut ordered by a UNIQUENESS-COMPLETING key BEFORE it: `(created_at, id)`, ⊥ `created_at` alone (⊥ unique ⇒ a tied group splits differently per call). Oldest FIRST ∵ the oldest stranded run has spent the most of its operator's V31 allowance. (d) BATCH ÷ INTERVAL = the DRAIN RATE ∧ it ! be STATED, ⊥ implied: `APPROVAL_STRANDED_RUN_REAP_BATCH_SIZE` 100 (`ge=1`, `le=1000` — a batch with ⊥ ceiling = the unbounded pass spelled in an env var) ÷ 120s = 50/min. The 2 knobs COMPOSE — a faster drain = lower the interval. (e) **⊥ `FOR UPDATE ... SKIP LOCKED` on rows a live worker also writes**: it would hold each run from the READ until the batch commits ⇒ the executor's terminal write for a change that DID complete queues behind the repair loop ∧ then finds its own `status = STARTED` false (V91) ⇒ "finished late" → "reported abandoned", DETERMINISTICALLY, in the repair loop's favour. A repair loop ⊥ outrank the worker ⇒ the 2 writers keep racing on COMMIT ORDER. KNOWN LIMIT recorded ⊥ unsaid: 2 replicas both reap the same rows — outcome right (V91 + V46's `DO NOTHING`), cost = duplicated work; remedy if it bites = 1 advisory lock around the PASS (T38(d)'s instrument), ⊥ a lock on the rows. (f) Read-only ⊥ an exemption: an unbounded detector = an unbounded log entry. Instances: T38 both populations (`core/approvals/reaper.py`). Rationale: `docs/AS-BUILT.md` §V92.
- V47. Tool run audit: `requested_by_user_id`, `tool_name`, `status`, `conversation_ref`, `result_summary` (truncated), args (redacted), timing.

### Secrets & Crypto

- V48. `SecretCipher` round-trips encrypt/decrypt. Encrypted format `enc:v1:fernet:...`. Server secrets encrypted at rest.
- V49. Password generation server-side (`_generate_password()`), ⊥ LLM arg. Plaintext lives only in `execute()` scope — ⊥ persisted, ⊥ cross LLM boundary. Tool returns only `yopass_url`.
- V50. yopass: blob = `username` + `password` → PGPy client-side encrypt → `POST /secret` → URL with key in fragment. Fragment ⊥ sent to yopass server.

### Infrastructure

- V51. `/health` → 200 `{"status":"ok"}`.
- V52. Fernet key (`NOA_SECRET_ENCRYPTION_KEY`) required in production. Auto-generated in dev. Name reflects scope: encrypts server credentials, ⊥ the DB.
- V53. JWT secret required in production (auto-generated ≥32 chars in dev).
- V95. A setting whose value REACHES an operator ∨ a browser ! refuse its development default outside development — `NOA_EMBED_BASE_URL` (the approval address, V26) ∧ `NOA_API_URL` (the proxy target). Same file ∧ same block as V52/V53 (`core/config.py::_apply_environment_rules`). ∵ a deploy that ⊥ set the var BOOTS CLEAN ∧ then hands every operator `http://localhost:3001/approvals/<id>` — an address that resolves on ONE laptop ∧ silently nowhere else. ⊥ a startup WARNING: a warning in a container log is a thing nobody reads until an operator says "the button does nothing". DISTINCT from V52/V53 ∵ those guard SECRETS ∧ a missing secret is LOUD at first use; an address is WRONG at first use ∧ looks fine. Test ! carry the negative control: development boots ON the default, else the rule is unprovable from "it raised" (V87). FOUND 2026-08-09 by T32, ⊥ shipped — T74 is the half still owed. Rationale: `docs/AS-BUILT.md` §V95.
- V101. A value baked at BUILD time that reaches a BROWSER ! be supplied EXPLICITLY by whatever builds a DEPLOYED image — absent ⇒ the BUILD fails, ⊥ a fallback. V95's rule one mechanism over, ∧ it ! be a SECOND mechanism ∵ V95 lives in `core/config.py::_apply_environment_rules` ∧ a value `next build` bakes is read by ⊥ Python process, sits in ⊥ runtime env, ∧ is ⊥ overridable once the image exists (`output: 'standalone'` ⊥ execute `next.config.ts` at runtime, V41). Instance = `NOA_LIBRECHAT_ORIGIN`, the `frame-ancestors` allowlist. ∵ the failure is SILENT ∧ TOTAL: a wrong allowlist refuses the sole client (C24) ⇒ empty iframe, ⊥ error — the process ⊥ know ∧ the browser ⊥ report, so ⊥ log ∧ ⊥ healthcheck catches it. Held at the PIPELINE (`test_pipeline_embed_build_requires_the_framing_origin`, T75), ⊥ inside `resolveFrameAncestor`: that function ⊥ know which environment it is building for, ∧ a guess there reddens the dev build ∧ the Playwright lane, both of which legitimately want the default (V41).

### Firewall

- V54. ∀ WHM firewall CHANGE tools ! reject non-IPv4 targets (CIDR, IPv6, hostname). Preflight READ may accept all types.
- V55. SSH commands prefix `sudo -n` ⟺ resolved ssh user ≠ `root`. `sudo -n` non-interactive. **AMENDED 2026-08-15 by T26: a TOLERATED exit ⊥ swallow a sudo refusal.** Where a step's non-zero exit is an ordinary answer ("entry ⊥ in that list", T25(d)), `ssh_sudo_required` ! still RAISE — sudoers grants per COMMAND ⇒ `csf -v` (all the availability probe runs) may be permitted while `csf -ar` is REFUSED, ∧ a box that looks usable then tolerates EVERY step ∧ an approved CHANGE reports "found nothing to do". Worst at T26, where ⊥ step is strict ∧ the tolerated set is the whole change. 2 causes, 2 remedies (V57's line one layer down). Held at the MECHANISM — `tolerated_csf_step`/`tolerated_imunify_step` in `noa_api/mcp_tools/whm_firewall_change_common.py` — ⊥ per tool, ∵ a per-tool copy is the copy the next tool forgets.
- V56. SSH command output ! strip login/PAM/LVE banners before tool-specific parsing. `CommandResult` ! retain raw output for audit.
- V82. Host-key pin ! be enforced INSIDE the handshake, pre-auth. `known_hosts` = list form `([], [], [])` (empty trusted / CA / revoked), **⊥ `None`** — `None` = asyncssh's documented off switch: `_trusted_host_keys = None` ⇒ the comparison block, `self._owner.validate_host_public_key(...)` included, is skipped (`connection.py:3509-3511`, `:1359-1367`) ⇒ pin inert, ∀ host accepted (B2). Empty list = nothing pre-trusted ⇒ ∀ presented key falls through to the callback during KEX ⇒ mismatch raises `HostKeyNotVerifiable` → `ssh_host_key_mismatch` before user auth ⇒ wrong host ⊥ receive the decrypted password/key. Empty *tuple* `()` ⊥ allowed — falsy ⇒ asyncssh loads `~/.ssh/known_hosts` (`:3510-3518`). Post-connect fingerprint compare ⊥ satisfy this ∵ credential already sent. **Test ! use a real `asyncssh.create_server` on loopback ∧ assert the server recorded ZERO auth attempts** — a double that calls the callback itself proves the callback, ⊥ that asyncssh calls it (that double is what hid B2).
- V57. Dual-backend CSF+Imunify: ∀ firewall operation reaches the backends through ONE door — `core/integrations/whm/firewall_gate.py::run_on_usable_backends` — which reads `available["csf"]` ∧ `available["imunify"]` ∧ acts on the usable ones in parallel via `asyncio.gather`. **Zero backends available → error `no_firewall_backend`, ⊥ success.** Silent no-op on approved CHANGE = ⊥ acceptable. The check sits at the MECHANISM, **⊥ per tool** — a per-tool guard is a guard the NEXT tool forgets. ⇒ 2 properties the door owns ∧ a tool ⊥ opt out of: it ⊥ be entered with nothing to do, ∧ a hand-rolled `asyncio.gather` anywhere on the firewall path is RED, ⊥ silent (AST guard, V84c — `test_whm_firewall_gate.py`). Binaries present ∧ `sudo -n` denied → `ssh_sudo_required`, ⊥ `no_firewall_backend`: 2 causes, 2 remedies (V55). **Bounds the ZERO case only** — one backend usable ∧ the other silent is a PARTIAL answer, ⊥ covered here, → V86 (T24). AMENDED 2026-08-07 by T68. Rationale: `docs/AS-BUILT.md` §V57.

### PMG

- V58. PMG whitelist target = PMG mynetworks (`/config/mynetworks`). PMG commands use SSH + argv-safe builder only. `TERM=dumb`, `pmgsh` (argv-only).
- V59. `pmg_whitelist_search` normalizes single-host inputs: IPv4 `1.2.3.4` ≡ `1.2.3.4/32`, IPv6 `2001:db8::1` ≡ `2001:db8::1/128`. Search checks CIDR list exact membership — ⊥ containment: `1.2.3.0/24` ∈ `mynetworks` ⊥ make `1.2.3.4` an entry, ∧ saying so sends an operator to remove a CIDR they ⊥ name. BOTH sides normalized (the target ∧ ∀ stored line) — a rule held on one side reports `not whitelisted` for an address PMG whitelists, in the spelling PMG happens to use (AMENDED 2026-08-08 by T31, as built). `ipaddress.ip_network(…, strict=False)` MASKS host bits ⇒ `1.2.3.4/24` is a question about `1.2.3.0/24` ⇒ result ! ship the normalized form BESIDE the verdict; echoing the input back reads as a verdict about the operator's ADDRESS. Same family as V85/V86 one axis over — the tool ⊥ manufacture a claim its data ⊥ carry. T29 inherits ∧ it is worse there: a CHANGE WRITES the masked CIDR, ⊥ merely answers about it. **DISCHARGED 2026-08-15 (T29)**, ∧ in 2 halves. (i) An `add` WRITES the masked form — what membership was decided on ∧ what the card showed — ∧ BOTH spellings travel: on the card, in the no-op SENTENCE ∧ in the runner's payload, ∵ a model told only `203.0.113.10/24` reports a HOST where a NETWORK changed. (ii) The half the prose above missed: a REMOVAL names the ENTRY's OWN spelling, ⊥ the normalized form. `mynetworks` holds `1.2.3.4` ∧ `1.2.3.4/32` as 2 LINES ∧ 1 entry ⇒ `noa-old`'s single `delete` of the normalized form left the duplicate STANDING ∧ aimed `pmgsh delete /config/mynetworks/<cidr>` at a path segment PMG may never have PRINTED. Asserted on the BYTES (the composed `delete` path), ⊥ on the verdict — a runner sending the normalized form answers IDENTICALLY — ∧ the negative control is a line PMG stores AS `/32`, else "strip the prefix length" passes (V87).
- V60. `pmg_whitelist_add` validates IP/CIDR via `ipaddress`; single host → `/32`. Add → `pmgsh create /config/mynetworks -cidr <cidr>` then `pmgconfig sync --restart 1`. **BUILT 2026-08-15 (T29) as `pmg_whitelist(action=add)`** — V60 ∧ V61 name 2 tools ∧ ONE ships (DECISIONS §9). `-cidr <value>` = the NORMALIZED form (V59), 2 argv tokens so a hostile value ⊥ become a 2nd flag. The 2 commands are 2 OUTCOMES ⊥ one: `create` accepted ∧ `sync` failed = the config moved ∧ Postfix ⊥ ⇒ its own code, V99.
- V61. `pmg_whitelist_remove` validates + normalizes, deletes exact CIDR via `pmgsh delete /config/mynetworks/<cidr>` then `pmgconfig sync --restart 1`. **BUILT 2026-08-15 (T29) as `pmg_whitelist(action=remove)`, ∧ AMENDED twice**: `<cidr>` = the ENTRY's own spelling ⊥ the normalized form, ∧ ONE `delete` PER matching line — 2 spellings of 1 address are 2 lines in the file (V59). A REFUSED `delete` STOPS the removal: the remaining lines stand, `sync` ⊥ run, ∧ the answer is the refusal — pressing on reports a PARTIAL removal as a whole one.

### Proxmox

- V62. `proxmox_reset_vm_password`: generate password internally → yopass store → set cipassword + regenerate cloud-init + verify (crypt-compare). yopass fail → abort before set. Set fail after yopass → URL holds unapplied password, old creds valid. **libcrypt absent (`_load_crypt_lib()` → `None`) → receipt states `verification_unavailable`, ⊥ silent pass.** Verification-unavailable ≠ verified.
- V63. `proxmox_vm_nic(action: enable|disable)` — single tool, enum action param. Preflight runs inside. **BUILT 2026-08-15 (T28)**: the enum ships as `Literal["enable","disable"]` ⇒ it is in the PUBLISHED input schema, ∧ asserted there — a free-string `action` that happened to work leaves DECISIONS §9's collapse undone at the surface a model learns from. Bounded in 3 places (T25's discipline): the schema, the tool BODY (a caller reaching the function directly bypasses pydantic), ∧ the RUNNER after the JSONB round trip. `noa-old`'s 2 names (`proxmox_enable_vm_nic`, `proxmox_disable_vm_nic`) asserted ABSENT, ⊥ merely unregistered. The digest question this tool raised is ⊥ here — V98 carries it.

### Large Output

- V64. Large READ results (`whm_list_accounts`, `pmg_whitelist_list`) → tool result carries short summary + URL to NOA-origin table page, ⊥ full table in context. Zero token cost for table body. Render mechanism = path A (iframe UI resource), chosen by T59 2026-08-08 (R29) — same mechanism as V37, ⊥ a 2nd. "⊥ mix" = ⊥ 2 render mechanisms; it is ⊥ a ban on pairing the 2 fields, which mcp-ui REQUIRES together: `ui://` = LibreChat's classifier (R31c) ∧ `text/uri-list` = the `src` render mode (R12). Shared capability, ⊥ per-tool special case. Store `tool_result_tables`, read `GET /tables/{token}` (requester-match + deadline in ONE `WHERE`, V27/V93), page `/tables/[token]` (server component, cookie forwarded, read-only), resource `ui://noa/table/<token>`. "Same mechanism as V37" is CODE ⊥ prose: the 3 iframe fields come from ONE function (`mcp_tools/ui_resource.py`) that T32's gate response ∧ this surface both call — only the `ui://` name ∧ the route differ. A parked READ carries a **counts-only `structured_content` envelope** (`ok`, `total_rows`, `stored_rows`, `truncated`) ∵ V45's audit row reads status ∧ summary off it ∧ a missing envelope = FAILED; ⊥ token ∧ ⊥ URL in it, ∧ ⊥ row ∧ ⊥ column value in the transcript (V26). BUILT 2026-08-09 (T56); 1st producer T20 2026-08-10 (`178044e`, rows sorted by `user`). T30 still `.` ⇒ 1 caller of 2, ∧ the 2nd is where "shared capability" gets tested for real. Rationale: `docs/AS-BUILT.md` §V64.
- V85. A READ that CAPS rows ! carry its own bound: total-match count ∧ a truncation flag beside the rows. ∵ the model reports what it was handed — a silent first-N ⇒ "there are twenty accounts" @ 200, a fabrication the TOOL authored. ∧ the capped set ! be ordered by a stable key BEFORE the cut — an unsorted truncation = an arbitrary subset that changes between identical calls. "Ordered by a stable key" = **reproducible across identical calls**, ⊥ necessarily a sort in NOA's code; a source whose order is undocumented ∨ server-chosen still owes the sort. Distinct from V64, ⊥ overlapping: V64 = a large listing OFFLOADED whole to a table surface (nothing dropped); V85 = rows DROPPED in-process ∵ an operator-supplied limit — ⊥ a table surface here. Instances: T21 `whm_search_accounts`; **T24** (2026-08-07) `csf -g` evidence cut at 20 inside `parse_csf_grep_output` ⇒ `CSFGrepParsed.total_matches` reports the PRE-cut count ∵ the cut is the PARSER's, ∧ the kept order = csf's own (identical state ⇒ identical prefix), ⊥ a sort NOA applies; **T56** (2026-08-09) where the capped thing is a PARKED table ⇒ cut ∧ pre-cut total computed ∧ stored at WRITE time (`cap_rows` → `tool_result_tables.total_rows`/`truncated`) ∵ a reader computing `len(rows)` reports the CAP as the total, ∧ the bound is stated TWICE by design — in the tool's text block the MODEL reads ∧ on the page the OPERATOR reads (V64 puts them on opposite sides of the offload). Ordering clause DELEGATED ⊥ dropped there: `cap_rows` keeps a PREFIX ∧ never re-sorts ⇒ the producing tool owes the order (T20, T30). EXTENDED to a WRITER by V92. Rationale: `docs/AS-BUILT.md` §V85.

### Multi-Source Reads

- V86. A verdict read from a SUBSET ! name the subset. A READ that asks N sources one question ships the sources that produced ⊥ usable answer beside the combined result, ∧ zero answers ⇒ `unknown`, ⊥ the benign value. ∵ V57 bounds only the ZERO case; the PARTIAL case is where the fabrication lives — a broken CSF beside a clean Imunify reporting "this address is ⊥ blocked" on a box whose BLOCKING backend was silent. A source's own "I ⊥ recognise this output" counts as ⊥ answered, ⊥ as clean — silence ≠ evidence of absence (`core.integrations.whm.csf` holds the same line one layer down). The verdict still ships: one dead source is ⊥ a reason to withhold the others' answer, only to bound what it means. Instances: T24 (`csf`,`imunify`) = where it was written; **T25** (2026-08-15) = where the inheritance got TIGHTER. A 2-part receipt built off a half-answering pair tells the same lie about a CHANGE, where it is worse ⇒ on a CHANGE `verified: true` requires EVERY usable backend to answer the confirming read, ⊥ merely one. One silent ⇒ `status: changed` + `verified: false` + `verification: unavailable` + the backend NAMED, ∧ **NEITHER outcome boolean PRESENT** (`released`, `allowlisted`) ∵ an absent field beats a `false` nobody measured — the READ ships a bounded verdict, a CHANGE ships ⊥ claim at all. Negative control ! ship: one backend INSTALLED ≠ one backend SILENT — a CSF-only box (most of them) verifies in FULL, ∧ without that case the unverified branch fires for every such server. T26 (2026-08-15) inherited it ∧ EXTENDED it to a GATE: its no-op — "⊥ allow entry to remove" — is a claim about ABSENCE, so 1 unanswered backend opens the CARD instead of answering, ∵ answering would leave an entry standing on the backend that stayed silent with ⊥ card, ⊥ run ∧ ⊥ record. Silence ≠ evidence of absence bites hardest where the absence IS the answer. Same negative control, same shape: a backend ⊥ INSTALLED is ⊥ a backend SILENT — a CSF-only box (most of them) still no-ops, ∧ without that case the card opens for nothing on every such server. Rationale: `docs/AS-BUILT.md` §V86.
- V97. A CHANGE's postflight ! ask the CHANGE's OWN question. Where a read resolves a PRECEDENCE, the losing fact is GONE from the verdict ∧ a tool acting on the losing fact reads its own success off the WINNER. Origin ∧ instance: T26 (2026-08-15). `csf -g` (`core/integrations/whm/csf.py`) ∧ Imunify both resolve block-over-allow ⇒ an address in `csf.deny` ∧ `csf.allow` reads `blocked`, ∧ the allow entry — the ONLY thing an allowlist removal touches — is invisible. Read off the verdict, 2 harms, opposite directions: a removal that changed NOTHING reports `removed: true` ∵ the DENY entry answered for it, ∧ T26's no-op refuses a card for the address that most plainly HAS an entry to delete. ∴ the losing fact travels BESIDE the verdict, computed from the matches BEFORE the precedence drops it — `CSFGrepParsed.allow_entry`, `ImunifyIPListResult.allow_entry`, `BackendLookup.allow_entry`, asked through `holds_allow_entry`. Bound rides with it: meaningful ONLY where `answered` (V86) — a backend that said nothing has ⊥ said "there is no allow entry" ⇒ ∀ caller checks `unanswered_backends` BEFORE trusting an absence. Negative controls ! ship on BOTH sides — a block-only address reports ⊥ allow entry (else the flag reads `True` for any line at all), ∧ a blocked address with ⊥ allow entry is a CLEAN removal (else the failure branch fires for every re-blocked address). Distinct from V86, ⊥ overlapping: V86 = a verdict from a SUBSET of SOURCES; this = a verdict from a source that answered in FULL ∧ threw the asked-for fact away. **SIBLING 2026-08-15 (T28): V98** — this bounds what a CHANGE reads AFTER it writes, V98 bounds what it writes FROM; a runner reading its own success off a task's exit status is this line's failure, ∧ writing bytes an operator saw minutes ago is V98's. **2nd INSTANCE 2026-08-15 (T29)**: the verdict is re-read off `mynetworks`, ⊥ off the `200 OK` `pmgsh create`/`delete` print — sharper than a task status ∵ `require_pmg_mutation_success` already TOLERATES a non-zero exit when stdout carries `200 OK` ⇒ the command's own answer is exactly the wrong thing to verify against, ∧ the fixture proves it by accepting the write ∧ keeping the list unchanged. The postflight's REACH is bounded one line over by V99. Rationale: `docs/AS-BUILT.md` §T26.
- V99. A CHANGE whose APPLY step is SEPARATE from its WRITE step reports the 2 SEPARATELY, ∧ NAMES what its postflight ⊥ see. Origin ∧ instance: T29 (2026-08-15). `pmgsh create`/`delete` writes PMG's config ∧ `pmgconfig sync --restart 1` is what Postfix picks up ⇒ a THIRD outcome that is neither `ok` nor a plain failure: write LANDED, apply FAILED. Both ordinary shapes are wrong there — `status: changed` is a LIE (mail flow ⊥ moved), ∧ a bare failure sends the operator back to add an entry ALREADY in the config, straight into V98's `no_op` ⇒ its own code (`pmg_sync_failed`) with `applied: false` beside it, ∧ the entry named as written-but-⊥-in-force. **∧ the BOUND gets STATED, ⊥ implied**: the postflight re-reads the CONFIG (V97's question), ∧ the apply step's OWN success is ALL NOA has on whether the live table moved — NOA reads `mynetworks` through `pmgsh` ∧ has ⊥ view of Postfix. Silence about REACH is V86's failure one layer up: a `verified: true` whose verification could ⊥ see the thing that matters. DISTINCT from T28's `task_timeout`, ⊥ overlapping: that is the UNKNOWN-outcome shape (write accepted, terminal state UNREAD), this one is KNOWN ∧ known to be HALF-DONE. Corollary asserted at T29: a write step that ⊥ complete ! ⊥ `sync` — a refused `delete` stops with the remaining lines standing, ∵ applying a partial removal is the whole one reported wrongly.

### Code Quality

- V65. `.py` files ≤ 900 lines. `.ts` files ≤ 300 lines. `.tsx` files ≤ 450 lines.
- V66. Reusable functions over duplication. DRY. Shared code in `core/`.
- V67. New code ! have type hints (Python) / TypeScript types. Tests for new functionality.
- V87. A test comparing 2 outputs ! compare what the CODE decides, ⊥ what the CLOCK stamps. Time-derived bytes (`Set-Cookie`'s `Expires`, `Date`, `iat`/`exp`, receipt timestamps) ! leave an equality compare ∧ be asserted by property instead (presence, bound, order). ∵ such a compare is a coin flip — green ∀ run inside one second, red ~1-in-N across a boundary ⇒ the suite teaches "flaky, rerun it". ∧ the drop ! ship a case proving the compare STILL separates 2 outputs that genuinely differ, ∵ a field-dropping comparator degrades into a tautology SILENTLY. B4 = the `delete_cookie` case. **EXTENDED 2026-08-09 by T56 (B9)** — an ABSENCE asserted through an ORM ATTRIBUTE is the same non-separating compare, one type-coercion layer down: a JSON/JSONB column set to Python `None` serialises to `'null'::jsonb`, a VALUE that SATISFIES `NOT NULL`. ∴ a no-default ∨ NOT-NULL claim ! be issued as a STATEMENT that leaves the column out of the SQL (Core `INSERT`), ⊥ as an instance with the attribute cleared. Rule generalises past JSONB: ∀ compare where a type COERCES the sentinel the test meant to send. Rationale: `docs/AS-BUILT.md` §V87.
- V89. A test asserting a CONCURRENCY control ! prove the 2 parties OVERLAPPED. `asyncio.gather` of 2 callers is ⊥ a race — the first commonly completes before the second's read reaches the server ⇒ the test passes with the LOCK DELETED (B5, measured twice). ⇒ 3 obligations: (a) the window is HELD OPEN on purpose — a hook between the locked read ∧ the commit, ⊥ a hope; (b) the assertion is on ORDER (an event journal — `second:read` after `first:committed`), ⊥ on the win count, which comes out right for the wrong reason; (c) a NEGATIVE CONTROL shows the forbidden order is REACHABLE (an unlocked read of the same row does ⊥ wait), else the ordering assertion is a tautology. Instances: T37 (`test_action_request_decisions_live.py`) ∧ T39 (`test_action_request_expiry_live.py` — the sweep vs an approval in flight). The rig MOVED to `support/action_decisions.py` at T39 rather than being copied (V66): 2 copies of the thing that holds the window open = 2 things that can silently stop overlapping, ∧ obligation (a) is the one nothing else would catch. Rationale: `docs/AS-BUILT.md` §V89.
- V90. A test's SETUP gate ⊥ be the thing under test. A readiness ∨ preflight wait pointed at the subject converts the subject's FAILURE into a TIMEOUT — the suite reports "startup took too long" where the truth is `Expected: 200 Received: 503`, ∧ a timeout names nothing to fix ∵ every cause produces the same stall. MEASURED at T40, ⊥ reasoned: Playwright's `webServer` waiting on `/healthz` answered the 503 mutation with a 120s stall; waiting on the open SOCKET (`port`) answered the same mutation in 7s with the assertion. ⇒ the gate ! sit one layer BELOW the subject — a socket under a route, a process under a socket. Instances: T40 (`playwright.config.ts`). T41–T46 ∧ T56 inherit ∵ each writes its own e2e — the card's readiness gate ! be something that is ⊥ the card. 1st inheritor T41 (2026-08-09) added ⊥ gate: it reuses T40's socket waits ∧ waits on the FRAME's own content (`#card` attached, then a locator inside it), never on a route the card serves ⇒ **a new readiness wait is the thing to justify, ⊥ the thing to add**. Rationale: `docs/AS-BUILT.md` §V90.
- V93. A separation held by a MISSING FIELD ! be bound at the statement that FETCHES, ⊥ at the shape that DROPS. ∵ a projection with nowhere to put a value stays GREEN when the value is loaded anyway — the row is then in the process, in front of the logger, the next edit ∧ a refactor that widens the view, ∧ ∀ existing test still passes. V17 ∧ V76 both rest on structural absence ⇒ absence-at-RENDER ≠ absence-at-READ, ∧ only the 2nd is what either line claims. MEASURED at T42(b): `include_receipt=True` added to `core/approvals/results.py` left the whole suite green, incl. the payload assertions written for V76. ∴ the SQL a reader BUILDS is the assertion (`apps/api/tests/test_requester_matched_read.py` — compiled, ⊥ executed: no DB, ∵ the claim is about the statement NOA builds, ⊥ what Postgres does with it, which is the live file's). V69's shape one layer earlier — "never read" is prose until a test reads the read.
- V94. An ESCAPE HATCH out of the frame ⊥ rest on a control the frame's sandbox can withhold. NOA hands the operator out of the iframe in 2 places — 401 → sign-in (V42) ∧ ∀ link-out (V25) — ∧ the sandbox is UPSTREAM's to set (R13) ⇒ `target="_blank"` needs `allow-popups`, absent at 1 of the 2 render sites, ∧ the tab it opens INHERITS the flags ⇒ ⊥ `allow-forms` in it either (R32). ∴ ∀ escape hatch ships the plain ADDRESS as text beside the link, ∧ the behaviour is asserted at BOTH pinned sandbox strings — the permissive one is the NEGATIVE CONTROL (V87) ∵ "no tab opened" passes just as well against a link that is merely broken. Refusal is SILENT ⇒ assert on the page COUNT ∨ the hit counter, ⊥ on an exception (V80's rule, one mechanism over). Reaches T50 (a `<form>` login is inert in a click-opened tab) ∧ T56 (same frame, same hatch).
- V100. A service that OWNS a mutation owns its COMMIT boundary, ∧ owns it for the WHOLE class of mutations — ⊥ for the subset today's caller happens to reach. A repository that only `flush()`es under a session dependency that ⊥ commit answers **200 over a rollback**: the row reads back correct inside the request ∧ is GONE after it, so the endpoint, the service ∧ the repository all look right in isolation ∧ the suite stays green. MEASURED at T51 (B10) on T9's `AuthorizationService` — V14's "permission updates take effect immediately" held by nothing. ∴ 3 obligations: (a) the commit is the LAST statement of the mutation ∧ ∀ guard raises BEFORE it ⇒ a refused write persists nothing ∧ revokes nothing; (b) ∀ mutation on the class commits, ∵ a half-committing service is a trap for the NEXT task that calls the other half; (c) the test observer is a SECOND session — inside ONE session flushed ∧ committed READ THE SAME ⇒ a same-session assertion certifies the dead boundary (B2's shape), ∧ a flush-only negative control ! prove the compare separates (V87); (d) an instance FOUND is a SWEEP owed — ∀ OTHER service on the same session dependency gets read in the SAME change, ∵ the hole belongs to the DEPENDENCY, ⊥ to the service in hand. B10 fixed `AuthorizationService` ∧ stopped; `McpTokenService` carried the identical hole ∧ rode to T53 (B11).
- V102. A CI lane whose subject needs a SERVICE ! prove the service was REACHED — a SKIPPED case is ⊥ a passed one, ∧ a suite reporting its own absence as success is V86's shape landing in the HARNESS instead of a tool result. Origin ∧ instance: `apps/api/tests/support/database.py::migrated_database` calls `pytest.skip("Postgres unavailable")` — deliberate ∧ correct for a laptop without Docker, which still runs 92 of 115 test files. The SAME line makes `uv run pytest -q` exit **0** across the 23 files that cover ∀ live auth, RBAC, approval-decision, audit, migration ∧ repository behaviour ⇒ a pipeline reads GREEN over a data layer that never executed. ∴ the skip becomes OPT-OUT: `NOA_REQUIRE_POSTGRES` set ⇒ `pytest.fail`, ⊥ skip, ∧ CI sets it BESIDE the `services:` entry. BOTH halves ∨ neither — a service that fails to start returns the suite to silently-green, which is the state this exists to leave, so the flag is what makes the service's absence loud. Test ! carry the negative control (V87, V89): unset ⇒ still skips, set ⇒ fails — without the pair the flag passes with its `if` deleted. Skip behaviour stays in the ONE function that owns it (V66) ∵ one file failing where another skips reads as a real regression.
- V68. Conventional commits. No secrets in git.
- V69. `noa-old` = reference for patterns. Mature integration layers (WHM/Proxmox/PMG, `remote_exec`, `secrets`) copied, ⊥ rewritten. SSH banner stripping ∧ `sudo -n` escalation hardened there. **Host-key pinning ∧ TOFU refresh were ⊥** — B2: `noa-old` `MCP` shipped the same inert `known_hosts=None`, the port copied it faithfully, this line asserted a property the source never had. ⇒ upstream provenance ⊥ evidence a control works. ∀ ported *security* control ! land with a test that exercises the real mechanism before any §V **∨ any doc** calls it hardened (V82 = the host-key case). Re-wording THIS line ⊥ enough — B3: the same sentence stood untouched in `README.md` ∧ `AGENTS.md` after B2's fix ⇒ V84.
- V84. Doc prose = restatement, `SPEC.md` = source ⇒ ⊥ 2 truths live. (a) ∀ §V re-word ∨ retraction ! sweep restatements OUTSIDE `SPEC.md` in the SAME change — `README.md`, `AGENTS.md`, `docs/**`. Grep the CLAIM, ⊥ the citation: B3's two files never named V69, they repeated its sentence ⇒ citation-grep finds nothing ∧ the retracted premise ships anyway. `AGENTS.md` first ∵ ∀ agent loads it as instruction. (b) ∀ doc citing a §V for a security property ! cite the §V that CARRIES that property today — B2 moved host-key pinning V69→V82 ∧ the integration docs kept citing V69, i.e. pointed at the line that RETRACTS the claim. (c) property machine-readable (dep version, servable era set) ⇒ bind by test, ⊥ by discipline (T70(d) = the pattern). ⊥ machine-readable ⇒ the same-change sweep IS the control ∵ that is the one moment the whole claim set is in hand.

### LibreChat

- V70. LibreChat auth = same LDAP directory, local registration disabled.
- V71. LibreChat agent config carries model-facing safety policy: preflight-first, approval gates, no fabrication, argument discipline, `\ui{}` instruction. ⊥ mention reason-writing ∵ C8 (LLM never authors reason). **WIDENED 2026-08-09 (T32)**: the rule is about MODEL-FACING TEXT, ⊥ about the agent config file — it reaches text NOA itself EMITS, ∧ the CHANGE gate's result block is the other surface NOA controls. That block carries ⊥ `reason`, ⊥ `justif`, bound by test ∧ ⊥ by discipline (V84c). Same argument one surface over: a model told the field exists is a model that can be argued into filling it, ∧ C8's boundary is that it never has one to relay.
- V88. LibreChat's `mcpServers.noa` entry ! carry `startup: false` ∧ `requiresOAuth: false` ∧ NOA's host ∈ `mcpSettings.allowedDomains`. ⊥ 3 preferences — each closes a TOTAL, SILENT failure measured 2026-08-08 (R31). ∵ C24: boot inspection runs with ⊥ user ⇒ `{{LIBRECHAT_USER_ID}}` resolves to nothing ⇒ NOA answers 401 exactly as V3 says it ! ⇒ LibreChat reads that 401 as "this server wants OAuth", marks it `needs_authorization` ∧ never connects again ⇒ `/api/mcp/tools` reports ZERO tools ∀ operator, ∀ valid token, with ⊥ error anywhere the operator can see: NOA looks up, LibreChat looks up, the tool list is empty. ∧ with ⊥ allowlist LibreChat's SSRF guard refuses an internal URL outright (`Domain "http://noa.internal:8000" is not allowed`); `allowedAddresses` ⊥ accept a CIDR ∨ a loopback literal @ pin ⇒ the domain list is the only lever. ⇒ the config is part of the AUTH design, ⊥ deployment garnish: C24 makes an unauthenticated inspect impossible by construction, so the entry ! tell LibreChat ⊥ to try one. Config = machine-readable ⇒ bind by test, ⊥ by discipline (V84c). NOA ⊥ own the operator's running file — ⊥ enforceable server-side (an MCP server ⊥ declare its own approval need) ⇒ the binding test lives in the C21 pin harness (`spikes/librechat-embed-render-gate/` — YAML + `verify_librechat_pin.sh`), re-run ∀ LibreChat bump. T57 = the operator DOC stating the 3 keys ∧ why; doc = reference, ⊥ control. **BUILT 2026-08-22 (T57) ∧ the location holds, the PREDICATE moved**: `verify_librechat_pin.sh` RUNS it, the assertion itself = `apps/api/tests/test_librechat_config_doc.py` — ⊥ 2 copies (V66), ∧ ∵ it is an ordinary pytest it also runs EVERY commit, ⊥ only at a bump. Scope = **2 artifacts, 1 predicate**: the rig YAML (what R29/R31 measured) ∧ the yaml block of `docs/integrations/librechat.md` (what an operator copies) ⇒ prose ⊥ drift off the measured config (V84a). Until then this line described a test that ⊥ exist — the script asserted the pin ∧ the mcp-ui bytes ∧ ⊥ one key (V69's rule, turned on this line).
- V72. LibreChat `toolApproval` ⊥ configured, ⊥ relied on. VERIFIED 2026-08-04 `packages/api/src/agents/hitl/policy.ts`: `isHITLEnabled` = `policy?.enabled === true` ⇒ absent config ∧ `enabled:false` both fully bypass. ⊥ default-ask trap. §7.3 spike CLOSED. Glob-vs-RBAC mismatch (V19 split) stands regardless.

### Error Shape & Grants

- V73. ∀ error response carries `request_id` in body + `x-request-id` header, same value. (`noa-old` V8.) "∀ error response" = **5 writers, ⊥ 1**: `NoaError` handler ∧ `StarletteHTTPException` (404/405) ∧ `RequestValidationError` (422) ∧ bare `Exception` (500) ∧ `McpAuthErrorMiddleware._send_error` (raw ASGI — the mounted MCP app has ⊥ exception-handler graph). ONE seam installs ∀ of it — `install_error_handling(app)`, called by `noa_api.main` ∧ both test harnesses ⇒ ⊥ surface carries a different envelope (V66). Header set by `error_response()` ITSELF, ⊥ left to `RequestContextMiddleware`'s `send` wrapper ∵ Starlette's `ServerErrorMiddleware` sits OUTSIDE the user middleware stack ⇒ the 500 never passes through it. Inbound `x-request-id` adopted ⊥ verbatim: ≤200 chars ∧ `^[A-Za-z0-9._:-]+$` else mint `uuid4()` — the value re-emits as a response header ∧ into structlog output ⇒ unbounded/newline value = log-forging surface. Id bound to structlog contextvars ⇒ the id in the body names the log line. **BOUND**: header rides ∀ response (superset of this line); body key covers ∀ error NOA shapes, ⊥ fastmcp's JSON-RPC `error` payloads. Rationale: `docs/AS-BUILT.md` §V73.
- V74. Stale tool catalog ⊥ become a security hole: V1 execution-time RBAC re-check = backstop ⇒ cached `tools/list` may *show* a revoked tool, calling it still 403s. Handshake era (C23) has ⊥ `ttlMs`/`cacheScope` fields ⇒ nothing to bound today. NOA ! emit `notifications/tools/list_changed` on permission change; whether LibreChat honours it = **ANSWERED 2026-08-08: ⊥ it does ⊥** (R30). Source ∧ live agree — `connection.ts:1852` registers `ResourceListChangedNotificationSchema` ONLY, `ToolListChangedNotificationSchema` = 0 hits in the tree, ∧ a notification NOA provably put on the session stream drew ⊥ `tools/list` after it. Emitting = harmless (⊥ error, ⊥ disconnect) ∧ buys nothing at this pin ⇒ T66 still emits (protocol-correct, ~free, a later client may honour it) ∧ T66's TEST ! be the V1 execution backstop, ⊥ a refetch assertion over behaviour NOA ⊥ control. V14 "immediately" = authoritative at execution, best-effort in catalog display. C23 → `2026-07-28` later ⇒ add `ttlMs` ceiling + per-user `cacheScope`.
- V75. Direct per-user tool grants disabled → 410 `direct_tool_grants_disabled`. Permissions flow role→user only. Role replacement preserves internal roles. (`noa-old` V14.)
- V76. `noa_get_action_result(id)` (READ, exposed) enforces V27 requester-match against MCP-token identity. Foreign id ∧ unknown id → identical not-found shape ⇒ ⊥ enumeration oracle. Prompt injection ⊥ pull another operator's action into transcript. (`noa-old` V162.) **BUILT 2026-08-08 (T63)**: identical shape covers a MALFORMED id too — 1 code (`action_request_not_found`), 1 message, ∀ cause, ⊥ a 2nd pydantic-shaped envelope. Requester-match sits in the `WHERE` ⇒ foreign row ⊥ fetched; NULL requester (T34 `SET NULL`) matches nobody ⇒ fail-closed. "⊥ pull into transcript" bound WIDER than this line asked: the result carries ⊥ `reason` (C8, V15, V43) ∧ ⊥ preflight `evidence` (V17) ∵ `ActionResultView` has ⊥ field for either ⇒ structural, ⊥ filtered. **Mechanism named 2026-08-09 (T42)**: the receipt's join is a FLAG on the shared statement (`include_receipt`, `core/approvals/reads.py`) — the card passes it, this reader ⊥ ⇒ what a model may be told is bounded at the FETCH. The view's missing field is now the 2nd line, ⊥ the 1st (V93).
- V81. Mounted-app path guards ! compare **mount-relative** path — `scope["path"]` minus `scope["root_path"]`. Starlette `Mount` extends `root_path`, ⊥ rewrites `path` (mirrors `starlette._utils.get_route_path`) ⇒ raw-`scope["path"]` guard matches standalone ∧ never matches mounted. Failure silent: guard falls through, SDK bare `invalid_token` answers same 401 status, only body differs ⇒ status-code test ⊥ catch it (B1). Helper = `mount_relative_path()` in `noa_api/mcp_request_auth.py`, ⊥ `starlette._utils` (private module).

### Firewall TTL

- V77. `whm_firewall_release_and_allow` ! require `duration_minutes` (integer, 1–525600). ⊥ permanent allowlist entry, ⊥ server-side default — operator states duration in chat ("5 min", "2 hours", "5 days"), LLM converts to minutes as ordinary tool arg (⊥ a reason, C8 unaffected). Receipt after-state ! show resolved expiry timestamp. Preserves `noa-old` `whm_firewall_allowlist_add_ttl` bound — merge (§6.5) ⊥ drop TTL. **BUILT 2026-08-15 (T25)**: bound held in 3 places ∵ each catches a different caller — schema `Field(ge=1, le=525600)` = what `tools/list` PUBLISHES (asserted there, ∧ so is the ABSENCE of a `default`: a default is the permanent-ish entry this line forbids ∧ it is invisible from the body ∵ an omitted arg never reaches it); the tool BODY = a caller reaching the function directly (C8's `assert_no_reason_argument` shape); the RUNNER re-checks after the JSONB round trip, `bool` excluded EXPLICITLY ∵ `True` is an `int` in Python ∧ would pass as 1 minute. Resolved `expires_at` computed ONCE ⇒ csf's TTL-in-seconds ∧ Imunify's absolute epoch AGREE — deriving them apart is how 2 lists expire by however long the first SSH hop took. Asserted by PROPERTY ⊥ by bytes (V87, B4). AS BUILT: `docs/AS-BUILT.md` §T25(h).
- V78. `whm_firewall_allowlist_remove` = undo path, ⊥ TTL param ∵ removal is immediate.

## §T TASKS

Done rows keep the intent line + cites. As-built deviation detail → `docs/AS-BUILT.md` §T\<id\>;
sub-clause addresses (`T38(g)`) resolve there.

id|status|task|cites
T1|x|git init + `.gitignore` (Python, Node, env, IDE, OS). Commit as "chore: init repo"|-
T2|x|scaffold monorepo structure: `apps/api/`, `apps/admin-web/`, `apps/web-embed/`, `core/`, `docs/`. Root `pyproject.toml` (workspace), `.env.example`, `README.md`, `AGENTS.md`|C12,C14
T3|x|`apps/api/` scaffold: FastAPI + FastMCP skeleton, `pyproject.toml` deps pinned exact: `fastmcp==3.4.5` (⊥ 4.x per C23), fastapi, uvicorn, sqlalchemy[asyncio], asyncpg, alembic, pydantic, pydantic-settings, structlog, orjson, cryptography, PyJWT, python-ldap, `pgpy>=0.6.0`, asyncssh, httpx. `requires-python = ">=3.11,<3.13"`|C1,C3,C7,C23
T4|x|Postgres schema v1: `users`, `roles`, `user_roles`, `role_tool_permissions`, `mcp_tokens`, `whm_servers`, `proxmox_servers`, `pmg_servers`. Alembic initial migration|C3,V1,V2,V3,V11
T5|x|`core/config.py`: pydantic-settings, all env vars (DB, LDAP, Fernet key, JWT secret, yopass, token TTL, LDAP revalidate interval, per-user concurrency cap, pending TTL, etc.)|C9,C7
T6|x|LDAP auth service: `LDAPService` with service-account bind + search. `user_exists_and_enabled(email)`. Dev bypass mode|C4,V7
T7|x|JWT service: mint, verify, httpOnly cookie set/clear. SameSite=Lax, `Domain=.noa.internal`|V6,V8
T8|x|Login flow: `POST /auth/login` → LDAP auth → JWT cookie. `POST /auth/logout` → clear. `GET /auth/me`. Rate limiter. Build `JWTService` ONCE at app startup (lifespan/module singleton), ⊥ request-scoped dependency ∵ algorithm-allowlist + key-length guards raise at construction — per-request build turns a config error into a 500 on first login, ⊥ a boot failure. `GET /auth/me` + ∀ session-authed route ! re-read `users.is_active` (V6: ⊥ token revocation exists)|V6,V7,V8,V9,V79
T9|x|RBAC engine: `role_tool_permissions` CRUD, `get_permitted_tools(user_id)`, admin bypass for known tools, disabled-user zero-permissions. Disable ! take effect on the live session's next request ∵ V6 (⊥ session revocation) ⇒ permission resolution reads the row, ⊥ trusts the cookie's claims|V6,V10,V11,V12,V13,V14
T10|x|Token management: `mcp_tokens` model + migration (id, user_id FK, token_hash, label, librechat_user_id nullable, last_used_at, last_ldap_check_at, created_at, expires_at). Mint: generate crypto token, hash SHA-256, return plaintext once. List: prefix + label + timestamps. Revoke: delete row|C5,V2
T11|x|Token verification: subclass `fastmcp.server.auth.TokenVerifier` (R1) — `async def verify_token(self, token: str) -> AccessToken \| None`, `None` = reject. Lookup `token_hash` in DB → return **fastmcp** `AccessToken(token=…, client_id=str(user.id), scopes=[…], expires_at=…, claims={…})` — `client_id`+`scopes` REQUIRED, SDK enforces `expires_at` itself (R2). TOFU binding runs INSIDE `verify_token` via `get_http_headers()` (R4, live-verified): NULL + header → bind; non-NULL → match else 401. LDAP staleness revalidation. Cascade revoke on admin disable|C20,V2,V3,V4,R1,R2,R4
T12|x|`resolve_mcp_identity(context, *, presented_token=None) → McpIdentity` (`noa_api/mcp_request_auth.py`): single HTTP-side function — extract `Authorization: Bearer <token>`, hash, DB lookup, TOFU binding, LDAP revalidation, `is_active` check. AS BUILT: `docs/AS-BUILT.md` §T12.|V1,V4,V5,V9,V66,V73,R4,R5
T13|x|FastMCP server: `mcp = FastMCP("NOA", auth=<TokenVerifier instance>)` — `auth=` keyword-only, instance direct, ⊥ provider wrapper (R3). Mount: `mcp_app = mcp.http_app(path="/")` (transport default `"http"`) + `app.mount("/mcp", mcp_app)` + `from fastmcp.utilities.lifespan import combine_lifespans` → `FastAPI(lifespan=combine_lifespans(app_lifespan, mcp_app.lifespan))` (R6 — exact path, ⊥ in `fastmcp.__all__`). ⊥ pass `stateless_http`/`json_response`/`host`/`port`/`log_level` to `FastMCP()` — `TypeError` in v3, they belong on `http_app()` ∨ `FASTMCP_*` env (R7). Session default ON: `Mcp-Session-Id` minted unless `stateless_http=True` (R8). Mount ! carry T12: `auth=NoaTokenVerifier(context=build_mcp_auth_context(...))` + `http_app(path=…, middleware=[Middleware(McpAuthErrorMiddleware, mcp_path=…)])` — ⊥ middleware ⇒ V3 named bodies ⊥ ship, SDK bare `invalid_token` stands. AS BUILT: `docs/AS-BUILT.md` §T13.|V1,V3,V81,I.mcp,R3,R6,R7,R8,T12
T14|x|Port from `noa-old`: `core/remote_exec/` (SSH with banner stripping, `sudo -n`, host-key pinning, TOFU refresh). AS BUILT: `docs/AS-BUILT.md` §T14.|C13,V55,V56,V69,V82
T15|x|Port from `noa-old` branch `MCP`: `core/secrets/` (Fernet `SecretCipher`, `_generate_password()`, `_yopass_store()`, `docs/integrations/yopass.md`). ⊥ branch `staging` — `yopass.py`/`password.py`/yopass doc absent there. AS BUILT: `docs/AS-BUILT.md` §T15.|C7,C13,C15,V48,V49,V50,V73
T16|x|Port from `noa-old`: WHM integration layer (`whm/integrations/ssh.py` — CSF + Imunify dual-backend, `asyncio.gather` parallel check) → `core/integrations/whm/`. Two transports to one server ∵ WHM exposes ⊥ firewall API: `/json-api/` client for accounts, SSH for CSF + Imunify. AS BUILT: `docs/AS-BUILT.md` §T16.|C7,C13,C22,V48,V54,V55,V56,V57,V65,V66,V69,V73
T17|x|Port from `noa-old`: Proxmox integration layer (`proxmox/integrations/client.py`) → `core/integrations/proxmox/`. One transport ∵ Proxmox = HTTP API only ⇒ ⊥ SSH path, ⊥ SSH creds on row (I.ext). ⊥ `errors.py` (unlike T16) ∵ dict-returning, zero raises. AS BUILT: `docs/AS-BUILT.md` §T17.|C7,C13,C22,V8,V48,V66,V69
T18|x|Port from `noa-old`: PMG integration layer (`pmg/integrations/ssh.py`, `pmg/integrations/pmgsh_cli.py` — argv-safe, `TERM=dumb`) → `core/integrations/pmg/`. One transport ∵ PMG = SSH only (I.ext) ⇒ module = sole door into node: `pmgsh` reads/writes `/config/mynetworks`, `pmgconfig sync` applies. AS BUILT: `docs/AS-BUILT.md` §T18.|C7,C13,C16,I.ext,V48,V55,V56,V58,V65,V66,V69,V73
T19|x|Tool: `whm_list_servers` (READ). Expose per DECISIONS §6.6. Return server list from DB. `resolve_whm_server_ref` for UUID/name/hostname → choices on ambiguity. AS BUILT: `docs/AS-BUILT.md` §T19.|I.mcp,C22,V1,V2,V8,V10,V11,V18,V19,V21,V66,V74,V83,R25,T35,T73
T20|x|Tool: `whm_list_accounts` (READ). Port from `noa-old`. Large result → summary + table URL per V64. Mechanism = path A (iframe UI resource) — T59 CLEARED 2026-08-08, ⊥ gated any more (R29). AS BUILT: `docs/AS-BUILT.md` §T20.|I.mcp,C9,C13,V8,V18,V19,V20,V25,V26,V45,V64,V66,V83,V85,V87,R29,T19,T21,T30,T32,T56,T59,T73
T21|x|Tool: `whm_search_accounts` (READ). Port from `noa-old`. Filter + limit 1–100. AS BUILT: `docs/AS-BUILT.md` §T21.|I.mcp,C7,C9,C13,V8,V18,V19,V21,V45,V66,V69,V71,V83,V84,V85,T15,T16,T19,T20,T54
T22|x|Tool: `whm_suspend_account` (CHANGE). Internal preflight → suspend. Single tool, preflight in-process. ⊥ reason param in schema. First CHANGE tool ⇒ first end-to-end run of the gate, ∧ the first task where a reason LEAVES NOA (V43, V96). AS BUILT: `docs/AS-BUILT.md` §T22.|I.mcp,C8,C9,C10,V8,V15,V16,V17,V18,V19,V20,V21,V23,V24,V25,V26,V33,V35,V43,V45,V46,V62,V66,V76,V83,V84,V87,V93,V96,T20,T21,T32,T33,T38,T63,T73
T23|x|Tool: `whm_unsuspend_account` (CHANGE). Internal preflight → unsuspend. Not merged with suspend (opposite risk directions, DECISIONS §9). ⊥ reason param in schema. **Shape ∧ preflight ALREADY EXIST** — T22 built `collect_account_state` ∧ `match_account` in `noa_api/mcp_tools/whm_account_change.py` for both tools (V66) ⇒ this row = the mirrored guard (⊥ suspended → `no_op`), the `is_locked`/`suspendlock` case a suspension LOCK creates (`unsuspendacct` refuses it), ∧ a 2nd runner. `unsuspendacct` takes ⊥ note ⇒ V96 ⊥ bite on the WRITE — but the preflight reads a SUSPENDED row ⇒ `suspendreason` (the operator's own words) is on the evidence, which is the case T22 ⊥ meet. AS BUILT: `docs/AS-BUILT.md` §T23.|I.mcp,C8,C9,C10,V1,V8,V15,V16,V17,V18,V19,V20,V21,V23,V24,V25,V26,V33,V35,V45,V62,V66,V76,V83,V84,V87,V93,V96,T20,T21,T22,T32,T33,T38,T73
T24|x|Tool: `whm_preflight_firewall_entries` (READ exposed). DECISIONS §6.5 — operator decision point. CSF + Imunify dual check. AS BUILT: `docs/AS-BUILT.md` §T24.|I.mcp,C9,C13,V8,V18,V19,V21,V26,V54,V55,V57,V66,V69,V83,V84,V85,V86,T21,T25,T68
T25|x|Tool: `whm_firewall_release_and_allow(server_ref, target, duration_minutes)` (CHANGE merged). `duration_minutes` REQUIRED, 1–525600, LLM converts operator phrasing ("5 min", "2 hours", "5 days") → minutes. Internal: release from denylist → add to allowlist with TTL. One approval, two-part receipt (before-state = block reason + log evidence, after-state = released & allowlisted + expiry timestamp). `sudo -n` where needed. **V96 bites here** — a `csf allow` entry takes a COMMENT ∧ T24 reads csf lines straight into the transcript ⇒ an operator reason written there returns to the model the way `suspendreason` would have (T22). AS BUILT: `docs/AS-BUILT.md` §T25.|I.mcp,C8,C9,C13,C14,V15,V16,V17,V18,V19,V21,V23,V24,V25,V26,V33,V35,V43,V46,V54,V55,V57,V62,V66,V77,V85,V86,V87,V96,T22,T23,T24,T68
T26|x|Tool: `whm_firewall_allowlist_remove` (CHANGE). Undo path. `sudo -n` where needed. V96 as at T25 if a removal writes any operator text a later READ reads back. AS BUILT: `docs/AS-BUILT.md` §T26.|I.mcp,C8,C9,C13,C14,V15,V16,V17,V18,V19,V21,V23,V24,V25,V26,V33,V35,V43,V46,V54,V55,V57,V62,V66,V85,V86,V87,V96,V97,T22,T23,T24,T25
T27|x|Tool: `proxmox_reset_vm_password` (CHANGE). Internal preflight → generate password → yopass store → set cipassword + regenerate cloud-init + verify. yopass fail → abort before set. libcrypt absent → `verification_unavailable`. ⊥ reason param in schema. 2 modules (C14, V22's cut): `proxmox_password.py` = tool, `proxmox_password_runner.py` = runner. **The link ships ⟺ NOA ⊥ RULE OUT the password reached the VM** — Proxmox REFUSED the write ∨ task exited non-`OK` ⇒ ⊥ link (V62's named case); write ACCEPTED ⇒ link ships on ∀ later failure ∵ withholding the only copy of a live credential is a lockout NOA created (`noa-old` withheld it there). Payload carries ⊥ reading of the cloud-init dump ∵ it becomes `result_summary` ∧ T63 hands that to a model (V49). V96 has ⊥ instance — cloud-init has ⊥ note field ⇒ runner never reads `reason` (T23/T26's write side). AS BUILT: `docs/AS-BUILT.md` §T27.|I.mcp,C8,C9,C13,C14,C15,V8,V15,V16,V17,V18,V19,V20,V21,V26,V33,V35,V43,V45,V46,V49,V50,V62,V65,V66,V67,V83,V84,V86,V87,V93,V96,T15,T17,T32,T33,T38,T69
T28|x|Tool: `proxmox_vm_nic(action: enable\|disable)` (CHANGE merged). Single tool, enum `action` param — bounded in 3 places (schema, tool body, runner after the JSONB round trip) ∧ asserted on what `tools/list` PUBLISHES, ⊥ on behaviour. Internal preflight. DECISIONS §9 enum collapse; RBAC coarser BY DESIGN, ⊥ re-litigated. **V98 BORN HERE** — `noa-old` took the config digest as a TOOL ARG (preflight → change call, seconds); an approval gate makes that window MINUTES ⇒ the runner RE-READS ∧ writes under ITS OWN digest, ∧ the approval window is checked on the NIC's LINK STATE, ⊥ on the config bytes. Codec = `core/integrations/proxmox/nic.py` ∵ the `netN` grammar is Proxmox's ∧ BOTH halves need it (V66); a rewrite is a WHOLE-LINE write ⇒ a dropped segment is a segment deleted from a live VM. 1 NIC inferred (`auto_selected` on the card), ≥2 → `choices` (C10). V96 has ⊥ instance — `netN` has ⊥ note field ∧ NOA writes ⊥ `description` — ∧ that is ASSERTED, ⊥ assumed. AS BUILT: `docs/AS-BUILT.md` §T28.|I.mcp,C8,C9,C10,C13,C14,V16,V17,V18,V19,V20,V21,V23,V24,V25,V26,V33,V35,V43,V46,V62,V63,V66,V67,V83,V86,V87,V96,V97,V98,T17,T25,T27,T32,T33,T38
T29|x|Tool: `pmg_whitelist(action: add\|remove)` (CHANGE merged). Single tool, enum `action` param — bounded in 3 places (schema, tool body, runner after the JSONB round trip) ∧ asserted on what `tools/list` PUBLISHES. DECISIONS §9 enum collapse #2; RBAC coarser BY DESIGN, ⊥ re-litigated. Internal preflight = `read_pmg_mynetworks` REUSED (V66) — a CHANGE gathering its own before-state = a 2nd answer to the question T31 already answers. **CLOSES the exposed surface**: registered == `TOOL_CATALOG` == 14 ∧ ∀ CHANGE name has a runner ⇒ V83(f). **V59's inheritance DISCHARGED**, ∧ 2 `noa-old` defects retired with it: a removal takes EVERY matching line by PMG's OWN spelling (2 spellings = 2 lines, 1 entry), ∧ an add writes the MASKED form. Runner RE-READS ∧ decides on the FACT (V98 with ⊥ CAS token); postflight re-reads `mynetworks`, ⊥ the `200 OK` (V97). Write-landed-∧-`pmgconfig sync`-failed = its own code ∧ the postflight's reach STATED — **V99 BORN HERE**. 1 source ⇒ V86 has ⊥ partial case: an unreadable `ls` refuses, ⊥ opens a card. V96 has ⊥ instance — a `mynetworks` entry is a CIDR, ⊥ note field — ∧ that is ASSERTED, ⊥ assumed. AS BUILT: `docs/AS-BUILT.md` §T29.|I.mcp,C8,C9,C10,C13,C14,V15,V16,V17,V18,V19,V21,V23,V24,V25,V26,V33,V35,V43,V46,V55,V58,V59,V60,V61,V66,V67,V83,V86,V87,V93,V96,V97,V98,V99,T18,T24,T30,T31,T32,T33,T38
T30|x|Tool: `pmg_whitelist_list` (READ). List all mynetworks CIDRs. Large result → summary + table URL per V64. Mechanism = path A (iframe UI resource) — T59 CLEARED 2026-08-08, ⊥ gated any more (R29)|I.mcp,V58,V64,T59
T31|x|Tool: `pmg_whitelist_search` (READ). Normalize input → search exact CIDR membership. Discovery step in front of T29 — operator holds an address ∧ needs to know whether adding it is a change ∨ a no-op. AS BUILT: `docs/AS-BUILT.md` §T31.|I.mcp,C9,C13,V8,V18,V19,V21,V26,V48,V55,V58,V59,V66,V69,V82,V83,V84,T18,T21,T24,T29,T54
T32|x|CHANGE gate: `build_change_gate_response()` — single function, 3 branches (link-out text \| UI resource \| elicitation future). ACTIVE = UI resource + link-out text beside it, ⊥ either/or (V24, V25; T59 cleared 2026-08-08). Resource shape MEASURED, ⊥ guessed: `ui://noa/approval/<id>` (LibreChat's classifier needs the scheme, R31c) + `mimeType: text/uri-list` (the `src` render mode, R12) + the URL as body. URL carries `action_request_id` only. AS BUILT: `docs/AS-BUILT.md` §T32.|V24,V25,V26,V17,V19,V66,V71,V73,V87,V94,V95,R12,R29,R31,R32,T59
T33|x|CHANGE gate: MCP `tools/call` on CHANGE → INSERT `action_requests(status=pending)` + `approval_context` (persisted) + `conversation_ref`. ⊥ `proposed_reason` column — reason arrives at approve time only (C8,V43). "May this run?" from DB. AS BUILT: `docs/AS-BUILT.md` §T33.|V8,V19,V22,V23,V32,V33,V35,V43,V66,V73,V83,V84,V85,V87,I.mcp,T21,T32,T34,T37,T38,T73
T34|x|`action_requests` table + migration: `id`, `tool_name`, `requested_by_user_id`, `status` (`ActionRequestStatus` PENDING/APPROVED/DENIED/EXPIRED), `conversation_ref`, `approval_context` JSONB, `reason` (nullable until decided, operator-typed), `tool_run_id` nullable, `expires_at`, `created_at`, `decided_at`. ⊥ `proposed_reason` column. This row IS the authorization — V23 reads `status` ∀ time, ⊥ a cache of a decision held elsewhere. AS BUILT: `docs/AS-BUILT.md` §T34.|V20,V23,V27,V28,V32,V33,V43,V46,V84,T33,T35,T36,T37,T39
T35|x|`tool_runs` table + migration: `id`, `tool_name`, `requested_by_user_id`, `risk` (`ToolRisk` READ/CHANGE), `status` (`ToolRunStatus` STARTED/COMPLETED/FAILED), `conversation_ref`, `args` JSONB (redacted), `result_summary`, `timing`, `created_at`. Risk ∧ status = separate columns ⇒ failed READ representable|V20,V45,V46,V47
T36|x|`action_receipts` table + migration: `id`, `action_request_id` FK, `tool_run_id` FK, `receipt_data` JSONB, `created_at`. AS BUILT: `docs/AS-BUILT.md` §T36.|V28,V34,V43,V46,V65,V66,V67,V84,V87,C13,C16,T34,T35,T38,T42,T63
T37|x|Approve/deny endpoints: `POST /action-requests/{id}/approve` (body `{reason, csrf}`) → validate non-blank `reason` else 409 `change_reason_required` → row lock → CAS pending→approved → async execute → 202 `{tool_run_id}`. `POST /action-requests/{id}/deny` → row lock → CAS pending→denied. Mismatch → 404, already decided → 409. AS BUILT: `docs/AS-BUILT.md` §T37.|V15,V22,V23,V27,V28,V29,V32,V39,V46,V47,V66,V73,V84,V87,V89,I.embed,T33,T34,T38,T39,T41,T46
T38|x|Async executor: in-process asyncio task with own DB session. `execute_approved_tool_run(tool_run_id)`. Reaper covers BOTH runs stuck in STARTED and requests stuck APPROVED with no run started. Per-user concurrency cap. **SEAM ALREADY WIRED (T37)**: `ApprovedChangeExecutor` Protocol + `AppRuntime.approved_change_executor`, handed `(tool_run_id, action_request_id)` AFTER the decision commits; placeholder = `DeferredApprovedChangeExecutor`. T38 owes the real task, the reaper, V31's cap, ∧ the `finish_run` that moves the STARTED row T37 already opened — ⊥ the row's creation, ⊥ the link (both are T37's, inside the decision's transaction). AS BUILT: `docs/AS-BUILT.md` §T38.|V8,V19,V20,V23,V29,V30,V31,V46,V47,V51,V66,V84,V85,V87,V89,V91,V92,T33,T34,T35,T36,T37,T39,T42,T73
T39|x|Pending expiry: background sweep AND check-on-read (both, ⊥ either/or) — status=PENDING & past deadline → EXPIRED. Sweep guarantees V32 terminality without traffic; check-on-read prevents serving a stale PENDING. Approve/deny on expired → 409. Check-on-read at the DECISION door landed at T37; the sweep ∧ the render path landed here. AS BUILT: `docs/AS-BUILT.md` §T39.|V20,V23,V28,V30,V32,V51,V66,V67,V84,V87,V89,T37,T41,T63
T40|x|Embed app scaffold: `apps/web-embed` — Next.js 16, own `package.json`, `tsconfig`, `eslint`, `vitest`, `playwright`. Hygiene: `.ts≤300`, `.tsx≤450`. AS BUILT: `docs/AS-BUILT.md` §T40.|C2,C12,C14,I.embed,V65,V67,V84,V90,T41,T44,T45,T47,T56,T60,T61
T41|x|Embed: `/approvals/[id]` page — GET detail, render approval card (provenance, before-state, evidence, reason input, Approve/Deny buttons). CSRF token. Compact, self-scrolling. Buttons ! be JS-`fetch` handlers, ⊥ `<form>` submit (V80). AS BUILT: `docs/AS-BUILT.md` §T41.|I.embed,C8,V15,V17,V22,V26,V27,V32,V33,V34,V35,V38,V39,V43,V66,V80,V84,V87,V90,T37,T42,T44,T46,T63
T42|x|Embed: approve/deny POST handler — JS `fetch` to NOA API with `noa_session` cookie + CSRF (⊥ form submit, V80/R13). Poll `/action-requests/{id}` to terminal. Render receipt on completion. **SHRUNK 2026-08-09 by T41** — the POST landed there ∵ T41's own line demands JS-`fetch` handlers ⇒ the fetch was that row's, ⊥ this one's (`src/lib/approvals/decide.ts`, refusal codes rendered, in-frame POST measured under R29's sandbox). T42 = the 2 halves left: (a) POLL `/action-requests/{id}` — the proxy entry already exists (T44) ∧ the card already reports `run.status`, what is missing is the repeat until terminal; (b) the RECEIPT, which needs T36's `action_receipts` ∧ T38's executor to have written one. ⇒ T42 is blocked on T36+T38 for (b), ⊥ for (a). **CORRECTED 2026-08-09 by T36** — the TABLE landed; its only writer = T38's executor ⇒ (b) blocked on **T38 alone**, (a) on nothing. **CORRECTED 2026-08-09 by T38** — (a) LANDED; (b) blocked on **nothing** now: T38's executor writes a 2-part receipt (`{ok, before, after}`) per approved change ∧ its reaper writes one for a change nobody finished, so rows exist. What (b) owes = carrying the receipt on the card's GET payload (`core/approvals/card.py` + `ApprovalCardResponse`, the API half T41 set the precedent for) ∧ rendering BOTH halves — the before-state an operator authorised against ∧ what the change did, ⊥ collapsed into "done" (DECISIONS §6.5). ⊥ T63's job: what a model may be told is narrower (V76) ∧ the before-state is preflight evidence V17 keeps out of the transcript. Same shape T46 shrank at T37. **BUILT 2026-08-09 — (b) LANDED.** AS BUILT: `docs/AS-BUILT.md` §T42.|I.embed,V8,V17,V27,V29,V34,V38,V45,V46,V47,V66,V69,V76,V80,V84,V87,V93,T36,T38,T41,T44
T43|x|Embed: 401 state — explicit "cannot authenticate here" + "Sign in to NOA" button (new-tab link-out). ⊥ LDAP redirect inside iframe. AS BUILT: `docs/AS-BUILT.md` §T43.|V38,V42,V25,V80,V87,V94,V65,R32
T44|x|Embed: session plumbing — proxy `app/api/[...path]` or direct API origin with `Domain=.noa.internal` cookie. AS BUILT: `docs/AS-BUILT.md` §T44.|I.embed,C5,C11,C13,V22,V27,V38,V40,V41,V42,V65,V66,V67,V69,V73,V83,V84,V90,T40,T41,T42,T43,T45,T46,T56
T45|x|Embed: framing headers — `Content-Security-Policy: frame-ancestors https://chat.noa.internal`. No `X-Frame-Options` needed. AS BUILT: `docs/AS-BUILT.md` §T45.|I.embed,C13,V37,V41,V65,V67,V83,V84,V87,V90,T40,T44,T49,T60
T46|x|Embed: CSRF token carried into the decision POST. **Mint + verify LANDED 2026-08-08 (T37)** — `core/approvals/csrf.py`, V39 as amended (session- ∧ request-bound, key derived from `AUTH_JWT_SECRET`, TTL = pending TTL). T46 shrinks to the EMBED half: the card obtains a token ∧ sends it in the `fetch` body ∨ header — ⊥ hidden form field (V80). ⊥ minting ROUTE exists yet; T41's GET detail is where the card gets one. **CONTENT LANDED 2026-08-09 by T41, status left `.` deliberately** (owner call): the card takes the token off its own GET ∧ sends it in the `fetch` BODY (`src/lib/approvals/decide.ts`; asserted unit-side ∧ measured in-frame), `null` once terminal ⇒ ⊥ live token on a card with ⊥ door. So this row owns ⊥ remaining work — it stays open ∵ only `/ck:build` flips a status cell, ⊥ ∵ anything is owed. Close it with a build pass that re-runs the 2 tests ∨ fold it into T42|V39,V80,T37,T41
T47|x|Admin web scaffold: `apps/admin-web` — Next.js 16, BIGSU dependencies (`@gio/bigsu-ui`, `@gio/bigsu-icons`), `.npmrc` for `bigsu.biznetgio.pt/registry/`. Hygiene: `.ts≤300`, `.tsx≤450`|C2,C14,I.admin-web
T48|x|Admin web: PORT from `noa-old/apps/web-bigsu/src/app/(protected)/admin/` — users, roles, audit, audit/tool-runs, whm, proxmox, pmg. Copy BIGSU components, ⊥ import from old repo. Include `AGENTS.md`, `CLAUDE.md`, `.claude/skills/bigsu/`, `docs/`|C13,I.admin-web
T49|x|Admin web: framing headers — `Content-Security-Policy: frame-ancestors 'none'`|V41
T50|x|Admin web: auth/session plumbing — proxy API calls, `noa_session` cookie. Login page redirect. Login POST ! be JS `fetch` if it is to work in a tab opened by CLICKING the 401 card's link-out — that tab inherits the frame's sandbox ∧ `allow-forms` ⊥ in it (R32, V94). A native `<form>` login still works for an operator who COPIES the address|I.admin-web,V94,R32
T51|x|Admin: user management — list, enable/disable, delete, assign roles. 4 routes, ∀ behind `require_admin`: `GET /admin/users`, `PATCH`+`DELETE /admin/users/{user_id}`, `PUT /admin/users/{user_id}/roles`. ⊥ create (§I.admin-api: users born at login, V7). AS BUILT: `docs/AS-BUILT.md` §T51 — T9's engine gained its transaction boundary (⊥ commit ⇒ ∀ write answered 200 over a rollback; `commit()` into Protocol + SQL class, last statement of 6 mutations, ∀ guard raises BEFORE it)|I.admin-api,V4,V6,V7,V8,V10,V11,V12,V13,V14,V73,V75,T9
T52|x|Admin: role management CRUD — list, create, delete, set tool permissions. `admin` role reserved. 6 routes, ∀ behind `require_admin`: `GET`+`POST /admin/roles`, `DELETE /admin/roles/{name}`, `GET`+`PUT /admin/roles/{name}/tools`, `GET /admin/tools`. AS BUILT: `docs/AS-BUILT.md` §T52 — route layer only, ⊥ policy moved (T9's engine already held ∀ guard, ∀ audit event ∧ — after T51 — `commit()`); `GET /admin/tools` shipped ∉ §I.admin-api ∧ this amend adds the row|I.admin-api,V8,V10,V13,V14,V66,V73,V100,T9,T51
T53|x|Admin: token management — list tokens per user, mint (show-once plaintext), revoke. 6 routes: `GET`+`POST /admin/users/{user_id}/tokens` ∧ `DELETE /admin/users/{user_id}/tokens/{token_id}` behind `require_admin`; `GET`+`POST /me/mcp-tokens` ∧ `DELETE /me/mcp-tokens/{token_id}` behind `require_session_user` — the SELF-SERVICE half shipped under THIS row, ⊥ its own. AS BUILT: `docs/AS-BUILT.md` §T53 — route layer only, ⊥ policy moved (T10's service already held generator, digest, label cap, user-scoped delete; `noa_api.api.errors` already mapped its refusals); V100 landed HERE ⊥ at T10 (B11)|I.admin-api,V2,V6,V8,V11,V13,V14,V66,V67,V73,V87,V100,T10,T51,T52
T54|x|Admin: server management — WHM/Proxmox/PMG CRUD + validate (SSH connect, fingerprint capture, TOFU refresh)|I.admin-api
T55|x|Admin: audit — 2 routes behind `require_admin`: `GET /admin/audit/tool-runs` (7 filters incl. `risk`, keyset cursor) ∧ `GET /admin/audit/tool-runs/{id}` (redacted args, result summary, timing). Closes V45's LAST clause — writer since T73, reader NEVER existed ⇒ "queryable in admin audit" was prose (V69); live test writes through the real `SQLToolRunRepository` ∧ reads through the real reader. Reader split from writer by what it CAN do (⊥ `commit`, ⊥ non-`SELECT`), T56's discipline. Panel loses what it had ⊥ API for: action-request tab + detail drawer + receipt page DELETED. AS BUILT: `docs/AS-BUILT.md` §T55|I.admin-api,I.admin-web,V8,V13,V20,V45,V47,V66,V69,V73,V85,V87,V92,V93,T35,T37,T38,T48,T52,T56,T57,T63,T65,T73
T56|x|Table surface for large READ results: `apps/web-embed` — route `/tables/[token]`, ⊥ `apps/api` (§8.8: same app, same origin rules, same framing headers). Renders full table. Tool result carries summary + URL. Used by `whm_list_accounts`, `pmg_whitelist_list`. Mechanism = path A, T59 CLEARED 2026-08-08 (R29) ⇒ unblocked. AS BUILT: `docs/AS-BUILT.md` §T56.|V6,V8,V19,V20,V25,V26,V27,V32,V38,V43,V45,V64,V66,V73,V80,V84,V85,V87,V93,V94,I.embed,R12,R29,R31,R32,T20,T21,T30,T32,T33,T41,T43,T44,T59,T73
T57|x|`docs/integrations/librechat.md` — operator config reference for the sole MCP client (C24). Sections: `mcpServers.noa` entry (`streamable-http` → `https://noa.internal/mcp/` — trailing slash ! present ∵ T13 mount answers there; bare `/mcp` costs a 307 per request, works ⊥ free); `customUserVars.NOA_MCP_TOKEN` (`sensitive: true`); 3 headers — `Authorization: Bearer {{NOA_MCP_TOKEN}}` + `X-Noa-LibreChat-User: {{LIBRECHAT_USER_ID}}` + **`X-Noa-Conversation-Ref: {{LIBRECHAT_BODY_CONVERSATIONID}}`** (OPTIONAL, ∧ the ONLY way `tool_runs.conversation_ref` (V47) is ever non-NULL: T73 reads that header ∵ the tool call carries ⊥ conversation id (R27); ⊥ set ⇒ the audit filter §I.admin-api `conversationRef` has nothing to group on; blank ⊥ distinguishable from absent (R28a) ∧ NOA treats both as absent); ∧ V88's 3 keys WITH the silent total failure each closes (R31) — a key without its reason gets deleted on the next edit. Doc ! ship the reader's exits: working YAML = `spikes/librechat-embed-render-gate/librechat.yaml`, re-verify trigger = C21. NOA ⊥ own ∨ deploy the operator's file — doc = reference, ⊥ control. **BUILT 2026-08-22** — ∧ it landed with the test V88 SAID existed ∧ ⊥ (`apps/api/tests/test_librechat_config_doc.py`: 1 predicate, 2 artifacts — the doc's yaml block ∧ the rig YAML R29 measured; run by `verify_librechat_pin.sh` ∧ by the ordinary suite). AS BUILT: `docs/AS-BUILT.md` §T57.|I.mcp,I.ext,C21,C24,V47,V66,V70,V84,V87,V88,R27,R28,R31,T13,T73
T58|x|`docs/integrations/librechat.md` agent section: EXAMPLE system prompt carrying the model-facing safety policy (preflight-first, approval gates, no fabrication, argument discipline, `\ui{}` emission) + why each clause exists. ⊥ mention reason-writing ∵ C8. V71 WIDENED @ T32 ⇒ the rule binds MODEL-FACING TEXT, ⊥ the agent-config file ⇒ example, ⊥ shipped artifact. Same file as T57, ⊥ a 2nd doc — one integrated system, one page (`docs/integrations/` pattern: `whm.md`, `proxmox.md`, `pmg.md`, `yopass.md`). **BUILT 2026-08-22** — 6 clauses, ⊥ 5: "report only what you were told" is its own ∵ the approval-gate clause ⊥ say where the OUTCOME comes from (V23, `noa_get_action_result`). C8 silence bound by test on the BLOCK (`FORBIDDEN_REASON_KEYS` imported, ⊥ re-listed), ∧ the WHY prose sits outside it. AS BUILT: `docs/AS-BUILT.md` §T58.|C8,C9,C10,V17,V23,V25,V66,V71,V85,V86,V87,V90,V94,R31,T32,T57
T59|x|**GATE — blocks T32,T41–T46,T56 + selects V24/V37/V64 branch. Run before embed work starts, ⊥ after.** Pin candidate = `45cc53c40b47645b887c3bb996168e06aaa83f4c` (R10; tag `v0.8.7` = `prerelease`). Source-read items (a)–(c) + (e) ANSWERED at that pin by R11–R16: (a) `text/uri-list` ⇒ `src` YES, (b) `@mcp-ui/client@5.7.0` still renderer ∧ #13831 unmerged draft on `dev`, (c) `profile=mcp-app` classification absent at HEAD, (e) sandbox = `allow-scripts allow-same-origin`, **⊥ `allow-forms`** ⇒ V80. STILL OWED: (d) live cookie-into-frame run against a NOA-served `text/uri-list` resource at that pin, (f) does LibreChat honour `notifications/tools/list_changed` (V74/T66). PASS → path A (iframe). FAIL → path B (link-out primary, V25). Re-verify ∀ version bump. **VERDICT 2026-08-08 = PASS (R29, R30, R31)** — items (d) ∧ (f) closed by a live run, ⊥ a 3rd source read (V69: provenance ⊥ evidence). (d): both LibreChat render sites put the frame's `src` on NOA's origin with `allow-same-origin` ∧ ⊥ `allow-forms`, ∧ in-frame `GET /auth/me` + `POST` both answered 200 as the operator ⇒ V22's cookie POST works from the iframe; the `text/html` control landed on an opaque origin, threw `SecurityError` on `document.cookie` ∧ could ⊥ fetch ⇒ the measurement separates (V87). (f): LibreChat honours ⊥ `notifications/tools/list_changed` — 0 handlers in tree, ∧ a notification NOA provably emitted drew ⊥ refetch (R30) ⇒ V74 amended, T66 keeps the emit ∧ tests the V1 backstop instead. 3 config bounds fell out of the run ∧ went to V88/T57 (R31). AS BUILT: `docs/AS-BUILT.md` §T59.|C17,C21,V24,V25,V37,V64,V80,V87,V88,R10,R17,R29,R30,R31
T60|x|Deployment: `Dockerfile` per app. `docker-compose.yml` for local dev (Postgres + API + admin-web + embed). Domain setup: `chat.`/`embed.`/`noa.internal` + cookie `Domain=.noa.internal`|C12,V40
T61|x|CI verify lanes, on branches `master` ∧ `staging` — ⊥ `main`, see T75 for why. Invariants asserted: `apps/api/tests/test_pipeline.py`, 34 tests. ORACLE = a GREEN PIPELINE RUN, ∧ it happened on `staging` 2026-09-08 — 4 breakages ∀ found by that run ∧ by ⊥ local check, which is what the `~` was FOR. Python: `ruff check` + `ruff format --check` + `pytest`, ∧ the pytest lane ! carry a Postgres service **∧** `NOA_REQUIRE_POSTGRES=1` ∵ 23 of 115 files skip SILENTLY without one ⇒ green over the whole data layer (V102). TypeScript: eslint + tsc + vitest (`admin-web` ALSO `pnpm test:server`, its own vitest config) + playwright. `admin-web` = INTERNAL runner ∀ lane, ⊥ only the image build — `pnpm install` itself ⊥ resolve `@gio/*` off-network, so lint ∧ typecheck ∧ test are equally unrunnable on a public runner. Playwright image tag ! track the pinned `@playwright/test` 1.62.1 (C2's discipline on a CI image), ∧ that lane needs `127.0.0.1 chat.noa.internal not-chat.noa.internal` ∵ `playwright.config.ts::CHAT_ORIGIN`/`OTHER_ORIGIN` reach its framing stub by NAME — absent, the framing specs die on DNS ∧ name nothing (V90's shape). Each web lane installs from INSIDE its own package dir: the 2 apps pin different pnpm (11.21.0 ∧ 11.18.0) ∧ `packageManager` ! select the resolver that wrote that lockfile. Commands = the ones `AGENTS.md` already documents, ⊥ a 2nd definition of "clean" (V66, V84). Conventional commits enforced over the MR commit range. AS BUILT: `docs/AS-BUILT.md` §T61.|C2,C14,V66,V67,V68,V84,V90,V102,T75
T62|.|Docs: `README.md` (overview, setup, architecture), `ARCHITECTURE.md` (design decisions, topology), `AGENTS.md` (caveman communication, code standards). `ARCHITECTURE.md` ALREADY EXISTS — created at T70, scope = MCP pin rationale + re-open triggers + where-enforced ⇒ T62 = ADD topology + module boundaries + remaining design notes INTO that file, ⊥ a new one, ⊥ restate its pins section. `docs/README.md` pointer already split pins-vs-rest (T70(c))|T70
T63|x|Tool: `noa_get_action_result(action_request_id)` (READ). Requester-match vs MCP-token identity. Foreign ∧ unknown → identical not-found shape. Returns status + receipt summary, redacted args. AS BUILT: `docs/AS-BUILT.md` §T63.|I.mcp,C8,V8,V15,V17,V19,V23,V26,V27,V32,V43,V45,V66,V69,V76,V83,V84,T33,T34,T36,T37,T38,T39
T64|x|Error envelope: ∀ error response body carries `request_id`, response carries `x-request-id` header. Shared exception handler, ⊥ per-route. AS BUILT: `docs/AS-BUILT.md` §T64.|V73,V8,V3,V9,V66
T65|x|Direct per-user tool grants → 410 `direct_tool_grants_disabled`. Role replacement preserves internal roles. Internal-role preservation ALREADY shipped at T9/T51 (repository subquery, 3 layers of coverage) ⇒ T65 built the 410 ∧ stripped `apps/admin-web`'s dead direct-grant surface (migration button POSTing a route NOA has ⊥ table for; `direct_tools` field T51 stopped sending). AS BUILT: `docs/AS-BUILT.md` §T65.|V8,V13,V73,V75,I.admin-api,T9,T51
T66|x|Emit `notifications/tools/list_changed` on role/permission change. Test: revoked tool → 403 at execution even when still in client's cached catalog (V1 backstop). LibreChat-honours question ANSWERED 2026-08-08 (R30): it does **⊥** — 0 handlers in tree ∧ a notification NOA provably emitted drew ⊥ `tools/list`. ⇒ still emit (protocol-correct, ~free, a later client may honour) ∧ the ONLY assertion that may be written here is the V1 backstop; a "client refetched" test asserts behaviour NOA ⊥ control ∧ would go red on an upstream whim (V69's shape). ⇒ operator-facing consequence ! be documented, ⊥ implied: a revoked grant stays VISIBLE in LibreChat's catalog until that connection is rebuilt. BUILT: session register (`WeakSet`) written by an `on_message` middleware, read by a notifier the RBAC engine calls AFTER commit; TARGETED per affected user (`create_role` tells nobody); bounded 1s ∵ the transport write stream is unbuffered; consequence in `ARCHITECTURE.md`. AS BUILT: `docs/AS-BUILT.md` §T66.|V1,V11,V14,V66,V74,V84,C23,R30,T9,T51,T52
T67|x|Rename Fernet env var → `NOA_SECRET_ENCRYPTION_KEY` in `core/config.py`, `.env.example`, Dockerfiles, `docker-compose.yml`, README. ⊥ `NOA_DB_SECRET_KEY` (misleading — encrypts server creds, ⊥ DB). AS BUILT: `docs/AS-BUILT.md` §T67.|C7,V52,V84,T5,T60
T68|x|Firewall no-backend guard: zero available backends → error `no_firewall_backend`. Test asserts ⊥ success-with-empty-gather. Fixes `noa-old` silent no-op. AS BUILT: `docs/AS-BUILT.md` §T68.|V57,V19,V55,V65,V66,V67,V84,T24,T25,T26
T69|x|Proxmox crypt-verify guard: `_load_crypt_lib()` → `None` ⇒ receipt `verification_unavailable`, ⊥ silent pass. Test with libcrypt unavailable. BUILT inside T27 ∵ a control with ⊥ caller is one ⊥ test exercises (T15's reason for parking `redaction.py`, V69). Landed as a 3-VALUE verdict (`CryptVerdict`), ⊥ a flag on a bool: `noa-old` answered `False` for wrong-password ∧ no-hash ∧ no-libcrypt alike ⇒ **⊥ silent pass ∧ ⊥ a `MISMATCH` either** — reporting a change that SUCCEEDED as one to repeat is the harm. ∀ unavailable carries its CAUSE (3 causes = 3 remedies). Negative control ships ∵ every other test here passes against a function answering `UNAVAILABLE` unconditionally (V87). AS BUILT: `docs/AS-BUILT.md` §T69.|V62,V84,V87,C1,C13,V69,T27
T70|x|**DECIDED by research 2026-08-04, ⊥ owner input needed** — record era + `fastmcp==3.4.5` in `pyproject.toml` + README + `ARCHITECTURE.md` with C23 rationale (LibreChat on SDK v1.x, lockfile-exact 1.29.0) so pin reads as decision, ⊥ accident. Text ! say **negotiated set**, ⊥ a digit-as-decision (T71 corrected C23; `pyproject.toml`+README already rewritten there ⇒ T70 = `ARCHITECTURE.md` + re-open trigger only). Record re-open trigger: LibreChat bumps to `@modelcontextprotocol/sdk` v2 — ∧ note a 1.x bump moves the live era silently (R26). Cite R8 (server SUPPORTED list) + R9 (client LATEST, read) + R11 (pin-verified dep) + R26 (client chooses). AS BUILT: `docs/AS-BUILT.md` §T70.|C23,I.mcp,R8,R9,R11,R26,V67,V84,T62,T71
T71|x|Close R9: read `@modelcontextprotocol/sdk` v1.29.0 TS source (`node_modules` ∨ npm tarball ∨ GitHub tag) for `LATEST_PROTOCOL_VERSION` + `SUPPORTED_PROTOCOL_VERSIONS`. Confirm ∨ correct C23 premise sentence, drop `?` on R9. Cheap, read-only, ⊥ blocking — but ⊥ ship C23 rationale text as fact until done. AS BUILT: `docs/AS-BUILT.md` §T71.|C23,R8,R9,R26,V69
T72|x|Test-support imports direct: 4 WHM test files (`test_whm_client.py`, `test_whm_firewall_availability.py`, `test_whm_firewall_cli.py`, `test_whm_ssh_config.py`) import SSH doubles from `support/remote_exec.py` ∧ `build_cipher` from `support/secrets.py` direct; drop re-export shims in `support/whm.py` ⇒ one home per double, `support/whm.py` ⊥ alias hub. Mechanical, ⊥ behavior change. AS BUILT: `docs/AS-BUILT.md` §T72.|V66,V84,T16,T17,T18
T73|x|Wire `tool_runs` write into the MCP tool path: ∀ READ tool call → row (`requested_by_user_id`, `tool_name`, `risk=READ`, `status`, `conversation_ref`, redacted `args`, `result_summary`, timing). Blocked on T35 (table). Closes T19(g) — ∀ tool call unaudited until this lands. Write ! sit beside the gate (V83b: one place, ⊥ per-tool code) ∧ ! record FAILED READs too (V20: risk ∧ status = separate columns). AS BUILT: `docs/AS-BUILT.md` §T73.|V8,V20,V45,V47,V66,V83,V84,T35,T19,T57
T74|x|Environment rule for operator-facing addresses (V95): `core/config.py::_apply_environment_rules` refuses a development default on `noa_embed_base_url` ∧ `noa_api_url` outside development. Same block ∧ same shape as V52/V53's secret rules, ⊥ a new mechanism. Test = 3 cases ∵ 2 ⊥ separate: production + default → `ValueError`; production + explicit value → boots; development + default → BOOTS (the negative control — without it "it raised" is all the suite knows, V87). ⊥ a warning: the failure it catches is SILENT at the operator, ⊥ at the process. Opened by T32, which closed the code half ∧ ⊥ this one. AS BUILT: `docs/AS-BUILT.md` §T74.|V95,V26,V87,C7,T5,T32
T75|~|GitLab build + deploy + k8s manifests. **BUILT 2026-09-07, `~` ⊥ `x`**: T61's half is MET ∧ 1 of this row's own 2 is too — the `staging` pipeline BUILT ∧ PUSHED 3 images 2026-09-08 (`repo.neo.id:5443/ce-staging/noa-{api,admin-web,embed}:staging-38dd7474`) ∧ `update_manifest` rendered them into `k8s/staging/deployment-*.yaml`. What REMAINS is the half this repo ⊥ own: **⊥ manifest has been APPLIED** ∵ the ArgoCD Application is the OWNER's ∧ ⊥ configured yet ⇒ a rendered file is ⊥ evidence of a running Pod, ∧ this row ⊥ claim one on the strength of a green `deploy` job. Production images ⊥ built either: `master` ⊥ pushed. Flips to `x` on the first Argo sync reporting Healthy. ArgoCD syncs the committed `deployment-*.yaml` — ⊥ `kubectl apply`, ⊥ manual step — ⇒ the rendered files ARE the deployed state ∧ a hand-edit to one is overwritten by the next run then synced away (asserted: rendered ≡ template modulo the tag). **The ArgoCD Application = OWNER's, ⊥ this repo's**, ∧ it watches `staging` ∧ `master` only ⇒ ⊥ add Application ∨ AppProject YAML under `k8s/` — the deploy surface here ends at the committed manifest. Same 2 refs bound CI: ∀ job carries `only: [staging, master]` (asserted ∀ job, ⊥ only where it obviously matters — the build/deploy jobs had it from the pattern ∧ 5 verify lanes written fresh did ⊥) ⇒ a 3rd branch would trigger NOTHING ∧ Argo would watch nothing on it — which is why the overlay lives on these 2 refs DIRECTLY, ⊥ on an authoring branch that ⊥ reach either. Placeholders OWED from the owner before a first deploy: DB host/user/password/name, LDAP host + bind DN + base DN + bind password, bootstrap admin email, Fernet key, JWT secret. FILLED 2026-09-08 on GitLab, BOTH envs: Fernet ∧ JWT generated PER-ENV (they differ), LDAP ∧ bootstrap admin SHARED, DB server ∧ credentials shared ∧ the DATABASE NAME ⊥ — a distinct database per env, named on GitLab ∧ ⊥ here ∵ `origin` is PUBLIC (C11, V68). Production first carried staging's DSN VERBATIM, ∧ one database under 2 different Fernet keys leaves rows written by either undecryptable by the other (C7) ⇒ the per-env key was already right ∧ the shared DSN was what made it wrong. `NOA_LIBRECHAT_ORIGIN` = `https://chat.simondayce.my.id` (given 2026-09-07), set as a CI variable ∵ V101 ⇒ the build REFUSES an empty one. LDAP transport WAIVED per owner (`LDAP_ALLOW_INSECURE_TRANSPORT=true`) ⇒ `ldap://` ∧ `ldaps://` both work; C4's rule unchanged ∵ the waiver is C4's own escape hatch, ∧ it sits in the manifest where it is readable, ⊥ in code. **Artifacts live on branches `master` ∧ `staging` ∧ are ABSENT from `main` BY DESIGN** — GitHub `origin` (`rendyuwu/noa`) is PUBLIC ∧ the only remote in this clone, GitLab `master`/`staging` = `main` + this overlay (AGENTS.md). A `/check` finding "T75's files are missing" on `main` is the SPEC being read right, ⊥ drift. `.gitlab-ci.yml` stages `build` ∧ `deploy` beside T61's `verify`: 3 images (contexts already fixed by T60 — API from the repo root, each web app from its OWN dir) → `repo.neo.id:5443/ce-{staging,production}/noa-{api,admin-web,embed}`, tag `${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHORT_SHA}`, `artifacts:reports:dotenv`; deploy = `sed __IMAGE_TAG__` over `k8s/<env>/*.yaml.template` **ANCHORED to the `image:` lines** (`/^[[:space:]]*image:/s|…|…|g`), commit `[ci skip]`, push the SAME branch. The anchor is ⊥ tidiness: each template's own header names `__IMAGE_TAG__` in PROSE ⇒ an unanchored `g` rewrote that sentence too, ∧ the first real deploy 2026-09-08 produced a rendered file carrying a build tag inside a paragraph ABOUT substitution ∧ telling its reader to edit the file the next run overwrites. Caught by `test_rendered_deployment_differs_from_its_template_only_in_the_tag` — which is what it is FOR. The sibling repos this pattern came from ⊥ hit it ∵ their templates name the token ONLY on the image line (MEASURED: 1 occurrence each vs NOA's 2–3) ⇒ upstream provenance ⊥ evidence again (C13, V84). Held by 2 tests: the anchor's presence, ∧ that a template still names the token OUTSIDE its image lines so the anchor stays load-bearing. ONE tag serves 3 templates ∵ 3 images come from 1 commit ∧ each template names its own image. `k8s/{staging,production}`: 3 ConfigMap + **1 Secret, the API's ALONE** (V8, C7 — a shared `envFrom` hands the web tier the Fernet key, the JWT secret ∧ the LDAP bind password for nothing to read; T60's `test_compose_hands_no_service_a_secret_it_does_not_read` is this rule one layer down) + 3 Deployment + 3 Service + 3 Ingress (`nginx-hostport`, `simondayce-my-id-tls`). Hosts = `noa-{api,admin,embed}[-staging].simondayce.my.id`, ∀ under V40's parent. Migration = **`initContainer` on the API Deployment**, same image, `alembic upgrade head` — idempotent ∧ `replicas: 1` ⇒ ⊥ concurrent-migration question ∧ ⊥ Job lifecycle between deploys, ∧ it gates the API container the way compose's `migrate` gates `api`: a failed migration stops the rollout with the MIGRATION's error, ⊥ a web server answering 500 on every route. `replicas: 1` on the API is ⊥ a capacity choice (V79 + T38 executor/reaper + T39 sweep + T66 register are ∀ in-process; a 2nd replica's first symptom is a login rejected the instant it is issued). Liveness ONLY, on the dependency-free routes — §T.60 decided readiness ∧ k8s ⊥ reopen it (V51). `NOA_LIBRECHAT_ORIGIN` ∈ ⊥ ConfigMap (V41: build-time; a ConfigMap key would read as the lever that moves it while doing nothing) ∧ the embed build job refuses an empty one (V101). **Secret = the REPO's** (owner decision 2026-09-07) ⇒ Argo reconciles what is committed ⇒ an ORDERING precondition, ⊥ a preference: FILL `k8s/*/secret-api.yaml` BEFORE the first sync, ∵ a sync while the values are `__REPLACE_*__` OVERWRITES a live `noa-api-secret` with them. The Fernet key is the one that outlives a failed boot — it decrypts the stored server credentials (C7) ⇒ overwrite = those rows undecryptable by ANYTHING, ∧ restoring the file ⊥ help unless the prior key survives outside the cluster. MEASURED 2026-09-07: boot REFUSES the placeholder (V52's Fernet check) ⇒ the overwrite is LOUD, ⊥ silent — ∧ loud ≠ recoverable. Half-filled = the dangerous middle (1 key overwritten, rest intact ⇒ the failure names whichever subsystem touches it first) ⇒ refused BOTH directions by `test_the_secret_is_all_placeholders_or_none`, ∵ "no placeholders" would make filling the file impossible. Authored as placeholders here ∵ `origin` is PUBLIC — real values land on private GitLab only (C11, V68). Postgres = EXTERNAL host via ConfigMap, ⊥ a StatefulSet. ⊥ `.github/workflows/*`|C1,C2,C7,C11,C12,C14,V6,V8,V40,V41,V51,V52,V53,V68,V79,V95,V101,V102,T60,T61

## §B BUGS

Full cause/fix narrative → `docs/AS-BUILT.md` §B\<id\>.

id|date|cause|fix
B1|2026-08-07|`McpAuthErrorMiddleware` path guard compared raw `scope["path"]` to sub-app path; Starlette `Mount` extends `root_path`, ⊥ rewrites `path` ⇒ mounted at `/mcp` guard never matched, ∀ refusal fell to SDK bare 401 with empty body, V3 named codes (`librechat_user_header_missing` \| `librechat_user_mismatch`) vanished behind unchanged 401. Caught by T13 contract test, ⊥ shipped|V81
B2|2026-08-07|**FIXED 2026-08-07** (`known_hosts=([], [], [])`, V82). Host-key pin WAS INERT: `known_hosts=None` = asyncssh's off switch ⇒ `_PinnedHostKeySSHClient.validate_host_public_key` never called, `ssh_host_key_mismatch` unreachable from a real handshake, decrypted credential presented to whatever answers `host:port` (∀ SSH path — WHM CSF/Imunify, PMG `pmgsh`). ⊥ port drift — `noa-old` `MCP` identical ⇒ **§V69's premise FALSE**, ∧ the suite CERTIFIED the dead control (fake-handshake tests asserting `known_hosts is None` AS a safety property). FIX = 1 kwarg; empty trusted set keeps asyncssh's block live ⇒ existing fingerprint storage suffices. Verified live vs `asyncssh.create_server`: wrong pin → `ssh_host_key_mismatch` ∧ ZERO auth attempts (credential ⊥ sent). Detail: `docs/AS-BUILT.md` §B2|V82,V69
B3|2026-08-07|B2's fix re-worded §V69 ∧ ⊥ swept its restatements ⇒ `README.md:113-114` ∧ `AGENTS.md:96-97` kept teaching the premise B2 disproved — `AGENTS.md` = what ∀ agent loads as project instruction ⇒ retracted premise shipped as a standing order to trust an inert control. Neither file cited V69 ⇒ citation grep found nothing; only a CLAIM grep ("hardened") did. Separately integration docs cited §V.69 for a claim V82 now carries. Docs-only, ⊥ code defect. FIX: README ∧ AGENTS name B2/V82, integration docs re-cite §V.82, V69 widened to "§V ∨ any doc". Detail: `docs/AS-BUILT.md` §B3|V84,V69,V82
B4|2026-08-07|`test_auth_routes.py::test_logout_is_idempotent` ∧ `test_jwt_service.py::test_clear_session_cookie_is_idempotent_and_needs_no_auth` compared 2 `Set-Cookie` headers VERBATIM. Starlette `delete_cookie` → int `expires`, rendered by `Morsel.OutputString` via `_getdate(0)` = **now** ⇒ 2 clears straddling a second boundary differ 1s, byte-identical elsewhere ⇒ red ~1-in-N. Test defect, ⊥ code defect — idempotence HOLDS, `Max-Age=0` never moved. FIX: `tests/support/cookies.py::cookie_shape()` drops `expires`, both tests compare through it, + a case proving the compare still separates (V87). Detail: `docs/AS-BUILT.md` §B4|V87,V66,V67,V6,T8
B5|2026-08-08|`test_action_request_decisions_live.py::test_concurrent_approves_produce_one_decision_and_one_run` (T37, V28) **passed with `.with_for_update()` DELETED**, measured twice. `asyncio.gather` ⊥ a race: the 1st coroutine INSERTs+UPDATEs+COMMITs before the 2nd's read reaches the server ⇒ the 2nd 409s for the RIGHT reason under the WRONG mechanism ⇒ V28's row lock asserted by prose, held by nothing (B2's shape one layer up). Test defect. Caught by a MUTATION battery at build time, ⊥ by reading ∧ ⊥ by the suite. FIX: hold the 1st transaction open across its locked read, assert ORDER ⊥ win count, + a negative control proving the forbidden order is REACHABLE. Detail: `docs/AS-BUILT.md` §B5|V89,V28,V87
B6|2026-08-09|`core/audit/tool_runs.py::finish_run` (T38, `a1fb7a9`) keyed its UPDATE on `ToolRun.id` ALONE ⇒ the 2 writers reaching ONE finished run (executor finishing, reaper past `APPROVAL_STRANDED_RUN_REAP_AFTER_SECONDS`=900) ⊥ raced, both TOOK it. A: reaper FAILED + abandonment receipt → executor flips COMPLETED beside it (`ON CONFLICT DO NOTHING` keeps the STALE receipt) ⇒ status ∧ receipt contradict, minutes apart ⇒ V46's one-commit ⊥ help. B (worse): reaper's UPDATE lands ON TOP of a committed COMPLETED ⇒ a change that APPLIED on a remote host reported abandoned; same window frees a V31 slot while the task is live. Code defect — suite exercised each writer ALONE. FIX (`19be7a2`): `status == STARTED` in the WHERE ∀ 4 callers = first terminal write wins. Detail: `docs/AS-BUILT.md` §B6|V91,V46,V31,V89,T38
B7|2026-08-09|`core/approvals/execution.py` (T38(g), `a1fb7a9`) refused a change whose authorized arguments came back redacted but scanned DEPTH 1 (`for key in authorized.arguments`) while `redact_sensitive_data` RECURSES into maps ∧ sequences ⇒ `{"server": {"ssh_password": …}}` stored redacted at gate time PASSED the guard ∧ the runner would have performed the change with literal `[redacted]` where a credential belonged. A fail-closed door reading only the top level is ⊥ fail-closed. Vacuous in production today (C15/V49; T22–T29 still `.`) — defect = a door T68's rule says to ship EARLY shipped HALF-BUILT. FIX (`19be7a2`): `core/secrets/redaction.py::sensitive_key_paths` — the redactor's OWN walk answering WHERE (V66), 1 traversal rule ∧ 2 readers; refusal still by KEY NAME. Detail: `docs/AS-BUILT.md` §B7|V8,V66,V49,T38,T33
B8|2026-08-09|`core/approvals/execution.py::build_receipt` (T38(h), `a1fb7a9`) stored the runner's answer RAW while the SAME payload reached `tool_runs.result_summary` through the redacting `core/audit/summaries.py` ⇒ a runner answering `{ok, password}` kept the credential OUT of the summary ∧ PUT it in `action_receipts.receipt_data` — the row that OUTLIVES the call, in front of T42's card, T63's read ∧ the admin audit surface (V8, V45). A per-writer exemption from one redaction rule. Vacuous today (⊥ CHANGE runner exists; C15/V49 keep secrets on the yopass path). FIX (`19be7a2`): `after` goes through `redact_sensitive_data` on the way in; `before` deliberately UNCHANGED ∵ its redaction is T33's. Detail: `docs/AS-BUILT.md` §B8|V8,V45,V46,T38,T42,T63
B9|2026-08-09|`test_tool_result_tables_schema.py::test_an_insert_that_omits_a_payload_column_fails` (T56, `0f120a7`) asserted "⊥ server default on `column_labels`/`rows`" via an ORM instance with the attribute `None`. SQLAlchemy JSON/JSONB serialise Python `None` to JSON `null` (`none_as_null` defaults False) ⇒ the INSERT carried a VALUE, that value SATISFIED `NOT NULL`, ∧ the statement OMITTED nothing ⇒ green against a column carrying `server_default`, the exact state it exists to refuse. Test defect (both columns have ⊥ default). B4/B5's family — a compare that ⊥ SEPARATE (V87) — one type-coercion layer down. FIX: `sa.insert(ToolResultTable).values(**values)` with the column `del`eted ⇒ absent from the SQL. Detail: `docs/AS-BUILT.md` §B9|V87,V93,T56
B10|2026-08-18|`AuthorizationService` (T9) mutated through a repository that only `flush()`ed, under a `get_db_session` that ⊥ commit ⇒ ∀ write the engine owned answered **200 over a ROLLBACK**, ∧ V14's "permission updates take effect immediately" was held by nothing. Vacuous until T51 mounted the routes that call it (B7/B8's shape — a door shipped half-built before its caller arrives). ⊥ caught by the suite ∵ within ONE session flushed ∧ committed READ IDENTICAL ⇒ the observer ! be OUTSIDE: found live vs Postgres from a 2nd session, + a flush-only negative control reading unchanged (V87). FIX (`d7e135e`): `commit()` into the Protocol ∧ the SQL class, last statement of **6** mutations — ⊥ the 3 T51 calls, ∵ a half-committing service is T52's trap; ∀ guard raises BEFORE the commit ⇒ a refused disable revokes ⊥ token ∧ persists nothing. Detail: `docs/AS-BUILT.md` §T51|V100,V14,V87,T9,T51
B11|2026-08-18|`McpTokenService` (T10) mutated through `SQLMcpTokenRepository`, which only `flush()`ed, under the same `get_db_session` that ⊥ commit ⇒ a mint would answer **200 with a plaintext over a ROLLBACK** — operator pastes a credential that authenticates NOTHING, ⊥ error anywhere; a revoke would report the token gone while the row ∧ ∀ request it authenticates SURVIVE. B10's cause one table over, ∧ V100 ALREADY EXISTED when it was found: V100 named the class in hand at T51 ∧ nobody swept the other services on the same dependency ⇒ the hole rode to T53. ⊥ caught by the suite for B10's reason — inside ONE session flushed ∧ committed READ IDENTICAL (V87). FIX (`163f9c7`): `commit()` into the Protocol ∧ the SQL class, last statement of BOTH mutations, after the audit event ∧ after ∀ guard; witnessed from a 2nd session + flush-only negative controls, boundary removed → 6 red incl. both live-SQL cases. Sweep run same day: `auth_repository`, `authorization_repository`, `approvals/repository`, `audit/tool_runs` ∀ own a commit ⇒ 0 residual. Detail: `docs/AS-BUILT.md` §T53|V100,V2,V87,T10,T53
