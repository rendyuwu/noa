# SPEC

## §G GOAL

NOA: MCP server for hosting-infrastructure operations. Exposes 14 RBAC-gated tools (READ + approval-gated CHANGE) to LibreChat via Streamable HTTP. Operator approves/denies CHANGE through iframe-embedded approval card served from NOA origin — reason typed there, ⊥ supplied by LLM. Admin panel manages users, roles, tool permissions, servers, audit log. Single repo, three deployables: API (`/mcp` + `/admin` routes), admin web (BIGSU), embed app (approval card).

## §C CONSTRAINTS

- C1. Python `<3.13` ≥3.11 (pgpy 0.6.0 imports removed `imghdr`; crashes tool-registry import at 3.13+). `uv` package manager, hatchling build.
- C2. Node.js 20+, Next.js 16, React 19, BIGSU (`@gio/bigsu-ui` `@gio/bigsu-icons` from `bigsu.biznetgio.pt/registry/`), pnpm. Versions pinned exact in lockfile — ⊥ caret ranges on `next`/`react`/`react-dom`. Bump = deliberate + T59 re-verify. Same discipline as C1 (Python) & T59 (LibreChat commit).
- C3. Postgres 16 via SQLAlchemy async + asyncpg. Alembic migrations. One shared DB schema.
- C4. LDAP authentication (dev bypass for local dev). LDAP = source of truth for employment, not just login.
- C5. Token-based MCP auth — ⊥ OAuth. Each user has own token minted by admin. Token hashed at rest (SHA-256), plaintext shown once at mint. Bearer token in `Authorization` header per MCP request.
- C6. RBAC: admin assigns roles → role has tool permissions → user has roles. `tools/list` RBAC-filtered per user. Execution gate re-checks permission. Disabled user → zero permissions regardless of roles.
- C7. Secrets (API tokens, SSH creds) encrypted at rest via Fernet (`NOA_SECRET_ENCRYPTION_KEY`). Key encrypts server credentials, ⊥ the DB — name says so.
- C8. Reason = single field, operator-typed in approval iframe. CHANGE tool schemas ⊥ carry any reason param — ⊥ `reason`, ⊥ `proposed_reason`. LLM ⊥ author, ⊥ relay, ⊥ see reason. Reason born at approve time, ⊥ at call time. Preflight runs in-process inside CHANGE call — evidence ⊥ cross tool boundaries.
- C24. LibreChat = sole MCP client. ⊥ Claude Desktop, ⊥ VS Code, ⊥ Goose. ⇒ `X-Noa-LibreChat-User` header required ∀ MCP req (bound ∧ unbound). Link-out text (V25) survives as T59 escape hatch + iframe-load-failure path, ⊥ as other-client support. Adding a second client = spec change, reopens V3 + V25.
- C9. One-workflow-one-tool: preflight, checks, execution → internal functions. Only final workflow exposed as MCP tool. Discovery READ tools stay separate.
- C10. Ambiguous identifiers → structured "ambiguous, here are candidates" result. ⊥ guess.
- C11. No secrets in git (`.env*` gitignored except `.env.example`). Env vars for lists use JSON arrays.
- C12. Single repo, three deploy artifacts: `apps/api/` (FastAPI + FastMCP), `apps/admin-web/` (Next.js BIGSU admin), `apps/web-embed/` (Next.js approval card iframe). One Alembic. One shared `core/`.
- C13. Old repo `noa-old` = reference/patterns source, ⊥ fork base. **Port source branch = `MCP`** — ⊥ `staging`, ⊥ working tree. `core/secrets/yopass.py`, `core/secrets/password.py`, `docs/integrations/yopass.md` exist ONLY on `MCP`/`main`. DECISIONS §2 LOC table measured on `staging` ⇒ ⊥ match port source (`core/secrets` 136 staging vs 290 MCP; `whm/integrations` 1,083 vs 1,179; `core/agent` 2,281 vs 2,854). Copy mature integration layers (WHM/Proxmox/PMG, `remote_exec`, `secrets`) — ⊥ rewrite.
- C14. Hygiene: `.py` ≤ 900 lines, `.ts` ≤ 300 lines, `.tsx` ≤ 450 lines. Reusable functions — ⊥ duplication. Code quality: type hints, tests for new code, conventional commits.
- C15. yopass credential delivery: password generated server-side (`_generate_password()`), stored via PGPy client-side encrypt (`_yopass_store()`). Plaintext ⊥ cross LLM boundary, ⊥ persisted, ⊥ in vault. URL returned to operator. Shared internal helper, ⊥ welded to password-reset tool.
- C16. `core/workflows/` (7,913 LOC old) → simplification target — much weight serves chat presentation being dropped.
- C17. Embed document ! sit on NOA origin — property, ⊥ mechanism. Iframe path (T59 **PASSED** 2026-08-08, R29): `text/uri-list` selects `src` render mode → `allow-same-origin` → cookie rides; `text/html` renders `srcDoc` → opaque origin → cookie ⊥ sent → decision path dies silently. BOTH halves MEASURED at pin `45cc53c4`, ⊥ inferred: the `srcDoc` half threw `SecurityError` reading `document.cookie` ∧ `TypeError: Failed to fetch` on both requests, which is what makes the `src` half's 200 a measurement ∧ ⊥ a tautology (V87's shape one axis over). MIME dependency = upstream-fragile ⇒ see C21. Link-out path (iframe-load failure ∨ T59-fail-on-a-future-bump, V25): top-level NOA origin, cookie rides natively, ⊥ MIME dependency.
- C18. Approve/deny ⊥ travel through LLM. Only path = cookie POST from NOA-origin document (iframe ∨ top-level tab) to NOA with `noa_session` cookie + CSRF token. "May this run?" read from DB, ⊥ from LLM claim or tool arg.
- C19. LibreChat's `toolApproval` ⊥ used. Authorization split: NOA RBAC = who may call; NOA approval gate = whether specific change may run; LibreChat = transport + rendering only.
- C20. Token binding TOFU: mint → `librechat_user_id` NULL; first MCP call with `X-Noa-LibreChat-User` header → bind; subsequent calls → header ! match else 401. Header absent → 401 ∀ states ∵ C24 (LibreChat-only).
- C21. UPSTREAM DRIFT — C17 chain measured 2026-08-06 at pin `45cc53c4`, ⊥ inherited from mcp-ui docs. Source-read outcome (R10–R17): `text/uri-list` ⇒ iframe `src` + `sandbox="allow-scripts allow-same-origin"`, zero proxy indirection ⇒ first-party cookie rides ⇒ path A = **PASS, measured live 2026-08-08** (R29, T59): frame document on NOA's origin, `noa_session` (`Domain=.noa.internal`, `SameSite=Lax`) rides in, in-frame `GET /auth/me` ∧ `POST` both 200 with the operator's identity. LibreChat owns ⊥ mimeType logic; whole behaviour comes from `@mcp-ui/client` `^5.7.0` (lockfile-exact `5.7.0`). Drift source = PR #13831 "MCP Apps support" — OPEN **draft**, base `dev`, ⊥ merged, ⊥ in any release tag (R15): swaps `@mcp-ui/client` → `@modelcontextprotocol/ext-apps ^1.7.4`, classifies ONLY `text/html;profile=mcp-app` as app-backed, plain `srcDoc` → `sandbox=""` inert, grants `allow-same-origin` only when sandbox on dedicated origin (`VITE_MCP_SANDBOX_URL`). MCP Apps spec = SEP-1865, MVP scope = `text/html;profile=mcp-app` only; `text/uri-list` listed "deferred from MVP" (R16) ⇒ if #13831 becomes default render path, `text/uri-list` has ⊥ render path at all — ⊥ merely "proxy breaks same-origin". CORRECTION: ext-apps "#318" = OPEN discussion **issue**, ⊥ spec text (R16). T59 = gate, **CLEARED 2026-08-08** ⇒ V24/V37/V64 branch = path A ∧ T20,T30,T32,T41–T46,T56 unblocked. Re-verify ∀ LibreChat bump = a COMMAND, ⊥ a rebuild: `spikes/librechat-embed-render-gate/` holds the rig — `verify_librechat_pin.sh` re-asserts the pin ∧ the shipped `@mcp-ui/client` bytes (incl. `allow-forms` ABSENCE, V80), `browser_probe.mjs` re-runs the live measurement with its own negative control. #13831 still ⊥ in tree at the pin (that script fails on an `@modelcontextprotocol/ext-apps` dependency) ⇒ the drift is a future bump's problem, ⊥ today's — ∧ the bump is what re-opens V24's branch choice, ⊥ a re-read of this line.
- C22. Never-implement boundary (DECISIONS §6.2 — management policy, ⊥ technical): `whm_change_contact_email`, `whm_change_primary_domain`, `proxmox_move_vms_between_pools`, `proxmox_preflight_move_vms_between_pools`, `proxmox_get_user_by_email`, `whm_check_binary_exists`, `whm_firewall_denylist_add_ttl`. ⊥ port, ⊥ expose, ⊥ re-add. Re-add = policy decision, ⊥ agent call.
- C23. MCP protocol era = **handshake, negotiated per client `initialize`** — ⊥ a fixed digit, ⊥ sessionless `2026-07-28`. DECIDED 2026-08-04 by evidence, ⊥ preference. Driver = C24: LibreChat = sole client, `packages/api/package.json` declares `@modelcontextprotocol/sdk: ^1.29.0`, lockfile resolves **exact 1.29.0** ⇒ v1.x line ⇒ `initialize` handshake + `Mcp-Session-Id`. Serving `2026-07-28` to a v1.x client buys nothing. **CORRECTED 2026-08-07 (T71, R9, R26)**: the digit this line carried was WRONG — TS SDK v1.29.0 `LATEST_PROTOCOL_VERSION = "2025-11-25"`, ⊥ `"2025-06-18"`, ∧ its `Client.connect` sends that constant with ⊥ override knob ⇒ live negotiated era = **`2025-11-25`**. NOA picks nothing: `mcp==1.29.0` echoes the requested version when ∈ SUPPORTED else its own LATEST ⇒ the operative property = NOA's servable SET covers whatever a v1.x client asks, ⊥ one digit. Client bump inside 1.x moves the era silently ⇒ ⊥ assert a digit anywhere but a test. Server dep = `fastmcp==3.4.5` (stable 2026-07-27, `requires_python >=3.10`, satisfies C1). ⊥ `fastmcp` 4.x — beta only (4.0.0b1, needs `--pre`), removes server-initiated sampling/roots, drops 3.x shims, relocates `fastmcp.server.auth.*` ⇒ breaks T11 `TokenVerifier` for zero gain. Re-open when LibreChat ships SDK v2 — even then v2 emits `2026-07-28` only on explicit opt-in. Pin-verified 2026-08-06: `"@modelcontextprotocol/sdk": "^1.29.0"` still declared at `packages/api/package.json:112` (R11). PREMISE GAP CLOSED 2026-08-07 by T71 — read at tag `v1.29.0`, ⊥ inferred (R9); gap was real ∧ the premise was false, ⊥ merely unread. `mcp==1.29.0` SUPPORTED = `2024-11-05`,`2025-03-26`,`2025-06-18`,`2025-11-25` (R8) ⇒ BOTH the era LibreChat asks (`2025-11-25`) ∧ an older client's (`2025-06-18`) servable now. `2026-07-28` ∉ either SDK's list ⇒ era bump = SDK bump, ⊥ config flip (bounds V74's future path). Unsupported ask ⊥ dead-end: server falls back to own LATEST `2025-11-25` ∈ TS client SUPPORTED ⇒ handshake completes (R26).

## §I INTERFACES

### I.mcp — MCP Server (`apps/api`)

| Item | Contract |
|---|---|
| Transport | Streamable HTTP, single endpoint `https://noa.internal/mcp` |
| Auth | Per-user NOA-minted bearer token. LibreChat side: `customUserVars.NOA_MCP_TOKEN` (`sensitive: true`) → `Authorization: Bearer {{NOA_MCP_TOKEN}}` in `librechat.yaml`. Plus `X-Noa-LibreChat-User: {{LIBRECHAT_USER_ID}}` for TOFU binding — required ∀ req per C24. |
| Protocol | Handshake era, negotiated per `initialize` (C23) — NOA serves a SET, ⊥ a digit: `2024-11-05`,`2025-03-26`,`2025-06-18`,`2025-11-25` (R8); server echoes client's ask when ∈ set, else own LATEST. LibreChat @ pin asks `2025-11-25` ⇒ live era (R9,R26). `Mcp-Session-Id` in play ∀ era. Server = `fastmcp==3.4.5`. ⊥ `Mcp-Method`/`Mcp-Name` headers, ⊥ `ttlMs`/`cacheScope` — `2026-07-28`-only, absent from BOTH SDKs @ 1.29.0 (R26). |
| `tools/list` | RBAC-filtered per user. Returns only tools permitted by user's role permissions. Cache hints bounded per V74. |
| `tools/call` | Authenticate → resolve user → RBAC check → **READ: write `tool_runs` STARTED → execute → terminal row, FAILED READs included (V45,V20,T73); opening audit write fails → refuse `tool_audit_unavailable`, tool ⊥ run**; CHANGE (unapproved) → INSERT `action_requests(status=pending)` → return text + approval surface; CHANGE (approved) → execute → write `tool_runs` + `action_receipts` + audit. CHANGE args ⊥ include any reason field (C8,V15). |
| Audit header | `X-Noa-Conversation-Ref` — OPTIONAL grouping label → `tool_runs.conversation_ref` (V47). Filled by `librechat.yaml` from `{{LIBRECHAT_BODY_CONVERSATIONID}}` (R28) ∵ the call itself carries ⊥ conversation id (R27). Absent ∨ blank ∨ >255 chars ∨ off `^[A-Za-z0-9._:-]+$` → NULL ∧ call proceeds — label, ⊥ scope (DECISIONS §10.4, old V165). ⊥ auth, ⊥ gate, ⊥ required (unlike `X-Noa-LibreChat-User`, C24). |
| Tool result | READ: text (+ optional table surface for large results, V64). CHANGE: `build_change_gate_response()` → 3 branches: link-out text \| UI resource \| elicitation (future). Active = **UI resource** (path A) + link-out text beside it (V24, V25, T59 cleared). Resource = `ui://noa/approval/<id>` + `mimeType: text/uri-list` + URL as body; LibreChat's parser needs the `ui://` scheme (R31c). URL carries `action_request_id` only. |
| Exposed tools | **14 tools** (DECISIONS §6 + §9 + `noa_get_action_result` restored per V76): `whm_suspend_account`, `whm_unsuspend_account`, `whm_list_accounts`, `whm_search_accounts`, `whm_preflight_firewall_entries`, `whm_firewall_release_and_allow`, `whm_firewall_allowlist_remove`, `proxmox_reset_vm_password`, `proxmox_vm_nic(action)`, `pmg_whitelist(action)`, `pmg_whitelist_list`, `pmg_whitelist_search`, `whm_list_servers`, `noa_get_action_result` |
| Never implement | C22 list — ⊥ port, ⊥ expose, ⊥ re-add (management policy boundary) |
| Internal functions | All `*_preflight_*` tools, `update_workflow_todo`, `proxmox_list_servers`, `pmg_list_servers`, `proxmox_get_vm_status_current`, `proxmox_get_vm_config`, `proxmox_get_vm_pending`, `whm_mail_log_failed_auth_suspects`, `get_current_time`/`get_current_date`, `whm_validate_server`, `proxmox_validate_server`, `pmg_validate_server` |

### I.admin-api — Admin REST (`apps/api`)

| Route | Method | Purpose |
|---|---|---|
| `/health` | GET | Health check → `{"status":"ok"}` |
| `/auth/login` | POST | LDAP auth → JWT in httpOnly cookie |
| `/auth/logout` | POST | Clear session cookie |
| `/auth/me` | GET | Current user (cookie-only) |
| `/admin/users` | GET/POST | List/create users |
| `/admin/users/{id}` | PATCH/DELETE | Enable/disable/delete user |
| `/admin/users/{id}/roles` | PUT | Replace user roles |
| `/admin/users/{id}/tokens` | GET/POST | List tokens / mint new (plaintext once) |
| `/admin/users/{id}/tokens/{token_id}` | DELETE | Revoke token |
| `/admin/roles` | GET/POST | List/create roles |
| `/admin/roles/{name}` | DELETE | Delete role |
| `/admin/roles/{name}/tools` | GET/PUT | Get/set tool permissions |
| `/admin/audit/tool-runs` | GET | Query tool runs (READ+CHANGE); filters: toolName, status, conversationRef, requestedByEmail, from/to date, cursor pagination |
| `/admin/audit/tool-runs/{id}` | GET | Tool run detail: args (redacted), result summary, timing |
| `/admin/whm/servers` | GET/POST | List/create WHM servers |
| `/admin/whm/servers/{id}` | PATCH/DELETE | Update/delete WHM server |
| `/admin/whm/servers/{id}/validate` | POST | Validate WHM server (SSH connect + fingerprint) |
| `/admin/proxmox/servers` | GET/POST | List/create Proxmox servers |
| `/admin/proxmox/servers/{id}` | PATCH/DELETE | Update/delete Proxmox server |
| `/admin/proxmox/servers/{id}/validate` | POST | Validate Proxmox server |
| `/admin/pmg/servers` | GET/POST | List/create PMG servers |
| `/admin/pmg/servers/{id}` | PATCH/DELETE | Update/delete PMG server |
| `/admin/pmg/servers/{id}/validate` | POST | Validate PMG server |
| `/me/mcp-tokens` | GET/POST | List own tokens / mint new |
| `/me/mcp-tokens/{id}` | DELETE | Revoke own token |

### I.embed — Embed App (`apps/web-embed`)

| Route | Purpose |
|---|---|
| `/approvals/[id]` | Approval card (GET detail + POST approve/deny). Reason input box lives here — sole origin of `reason` (C8,V15). Compact, self-scrolling, no AppShell. |
| `/action-requests/{id}` | Thread-less confirmation detail + execution status (polling surface) |
| `/action-requests/{id}/approve` | POST approve → 202 `{action_request_id, tool_run_id}`, async execute. Body `{reason, csrf}`. Blank/whitespace `reason` → 409 `change_reason_required`. Invalid/absent CSRF → 403 `csrf_token_invalid`. Session cookie. |
| `/action-requests/{id}/deny` | POST deny → 200 `{action_request_id, status}`. Body `{reason, csrf}`. **Blank `reason` → 409 too** (V15 as amended by T37). Session cookie. |
| `/tables/[token]` | Large-READ table surface (V64). Read-only, requester-matched, ⊥ decision controls. |
| `/api/[...path]` | Same-origin proxy → `NOA_API_URL`, server-side (T44). **ALLOWLISTED, ⊥ pass-through**: `GET auth/me`, `GET action-requests/{id}`, `POST action-requests/{id}/{approve,deny}` — 4 entries, matched on METHOD ∧ shape. Else 404 `route_not_proxied` ∧ upstream ⊥ called. `Authorization` stripped outbound; `Cookie` ∧ `Set-Cookie` ride untouched (V40). |
| `/healthz` | Liveness check |

Framing headers: only embed app allowlists `frame-ancestors https://chat.noa.internal`. Admin app sends `frame-ancestors 'none'`. Embed 401 → explicit "cannot authenticate here" state, ⊥ blank card, ⊥ live Approve button.

`/action-requests/{id}/{approve,deny}` = **API routes** (`apps/api`, `noa_api/api/routes/action_requests.py`, T37), listed under I.embed ∵ the embed is their only caller. Browser reaches them via the embed's same-origin proxy (T44), ⊥ direct — ∧ that proxy carries a BOUNDED set, ⊥ the API: a surface listed here ! also be added to `src/lib/proxy/routes.ts` ∨ the browser ⊥ reach it (T44(b)). `/approvals/[id]`, `/tables/[token]`, `/healthz`, `/api/[...path]` = the embed app itself. `/action-requests/{id}` GET = T41, ⊥ built.

### I.admin-web — Admin Panel (`apps/admin-web`)

PORT from old repo `apps/web-bigsu/src/app/(protected)/admin/`: users, roles, audit, audit/tool-runs, whm, proxmox, pmg. BIGSU components. Own build/deploy. ⊥ frameable.

### I.ext — External Dependencies

| System | Interface |
|---|---|
| LibreChat | `librechat.yaml` MCP server entry: `streamable-http` → `https://noa.internal/mcp/`, `customUserVars` for token + user ID headers, ∧ 3 keys C24 forces (V88, R31): `startup: false`, `requiresOAuth: false`, NOA's host ∈ `mcpSettings.allowedDomains`. LDAP auth same directory, local registration disabled. |
| LDAP | Service-account bind + search for `user_exists_and_enabled(email)`. Token revalidation on staleness interval. |
| Postgres | One DB, one schema. Alembic migrations. |
| WHM servers | SSH (asyncssh) with host-key pinning + TOFU refresh. `sudo -n` for non-root. CSF + Imunify. |
| Proxmox servers | Proxmox API (HTTP). |
| PMG servers | SSH + `pmgsh` CLI (argv-only, ⊥ shell string). |
| yopass | `POST <YOPASS_BASE_URL>/secret` for encrypted secret delivery. PGPy client-side encrypt. |

## §R RESEARCH

Sourced findings. `?` prefix = unverified. Local paths = installed package source (primary), relative to `.venv/lib/python3.11/site-packages/`.

id|topic|finding|source
R1|fastmcp auth base|`class TokenVerifier(AuthProvider)` in `fastmcp.server.auth`; abstract `async def verify_token(self, token: str) -> AccessToken \| None`, `None` = reject. Ctor `(base_url=None, required_scopes=None, resource_base_url=None)` — `resource_server_url` ⊥ exist|`fastmcp/server/auth/auth.py:366,408,373-378` (fastmcp==3.4.5)
R2|AccessToken shape|Return fastmcp `AccessToken`, ⊥ SDK's. Required `token: str`, `client_id: str`, `scopes: list[str]`; optional `expires_at: int \| None`, `resource`, `subject`, `claims: dict` (fastmcp default `{}`). SDK middleware rejects when `expires_at < int(time.time())` ⇒ expiry enforced upstream|`fastmcp/server/auth/auth.py:54-57`; `mcp/server/auth/provider.py:39-46`; `mcp/server/auth/middleware/bearer_auth.py:70-71`
R3|verifier wiring|`FastMCP(..., auth=<TokenVerifier instance>)` — keyword-only, instance passed direct, ⊥ wrapper (TokenVerifier ⊂ AuthProvider). `RemoteAuthProvider` only if RFC 9728 metadata routes wanted|`fastmcp/server/server.py:329,424`
R4|headers inside `verify_token`|`verify_token` receives token string only, BUT `RequestContextMiddleware` inserted outermost (`middleware.insert(0, …)`) ahead of `AuthenticationMiddleware` ⇒ `get_http_request()`/`get_http_headers()` DO work inside `verify_token`. LIVE-VERIFIED 2026-08-06: `X-Noa-LibreChat-User` read there, 200 ⇒ TOFU binding may live in verifier|`fastmcp/server/http.py:404,361-383`; `mcp/server/auth/middleware/bearer_auth.py:54-65`
R5|header helper caveat|`get_http_headers()` default excludes `authorization` ∧ `mcp-session-id` — need `include={"authorization"}`. Custom `x-noa-librechat-user` returned, key lowercased. Identity in tools: `get_access_token()`, DI `CurrentAccessToken()`, `TokenClaim(name)` from `fastmcp.server.dependencies`|`fastmcp/server/dependencies.py:430-448,467,1299,1356`
R6|ASGI mount|`http_app(path=None, middleware=None, json_response=None, stateless_http=None, transport="http", event_store=None, retry_interval=None, host_origin_protection=None, allowed_hosts=None, allowed_origins=None) -> StarletteWithLifespan`. `combine_lifespans(*lifespans)` EXISTS at `fastmcp.utilities.lifespan` (⊥ in `fastmcp.__all__`); documented pattern `FastAPI(lifespan=combine_lifespans(app_lifespan, mcp_app.lifespan))` + `app.mount("/mcp", mcp_app)`|`fastmcp/server/mixins/transport.py:370-382`; `fastmcp/utilities/lifespan.py:12-14,26-37`
R7|v3 removed ctor kwargs|`FastMCP(stateless_http=…)`, `json_response`, `streamable_http_path`, `sse_path`, `host`, `port`, `debug`, `log_level`, `tool_serializer`, `include_tags`/`exclude_tags` raise `TypeError` — pass to `http_app()`/`run_http_async()` or `FASTMCP_*` env. `JWTVerifier`/`StaticTokenVerifier` behind lazy `__getattr__` ⇒ import from `fastmcp.server.auth.providers.jwt` for static analysis|`fastmcp/server/server.py:148-174`; `fastmcp/server/auth/__init__.py:34-55`
R8|server protocol + session id|`mcp==1.29.0` (Python): `LATEST_PROTOCOL_VERSION="2025-11-25"`, `DEFAULT_NEGOTIATED_VERSION="2025-03-26"`, `SUPPORTED_PROTOCOL_VERSIONS=["2024-11-05","2025-03-26","2025-06-18","2025-11-25"]`. `Mcp-Session-Id` minted by default (`stateless_http=False`, `uuid4().hex`) — LIVE-VERIFIED. `stateless_http=True` ⇒ no header, no event store, routes drop GET|`mcp/types.py:27,35`; `mcp/shared/version.py:3`; `mcp/server/streamable_http_manager.py:288,205`
R9|C23 premise = FALSE|VERIFIED 2026-08-07 @ tag `v1.29.0` (T71). C23's claim "TS SDK v1.x ⇒ `LATEST_PROTOCOL_VERSION = "2025-06-18"`" is **WRONG**: `LATEST_PROTOCOL_VERSION = '2025-11-25'`, `DEFAULT_NEGOTIATED_PROTOCOL_VERSION = '2025-03-26'`, `SUPPORTED_PROTOCOL_VERSIONS = [LATEST,'2025-06-18','2025-03-26','2024-11-05','2024-10-07']`. `2026-07-28` ∉ list ⇒ C23's CONCLUSION survives (handshake era, `Mcp-Session-Id`, era bump = SDK bump), only the digit dies. npm tarball `dist/` byte-agrees with the tag ⇒ published artifact ⊥ differ from source|`modelcontextprotocol/typescript-sdk` `v1.29.0/src/types.ts:4-6`; npm `@modelcontextprotocol/sdk@1.29.0` `dist/esm/types.js:2-4`, `dist/esm/types.d.ts:3-4`
R10|LibreChat pin candidate|`main` HEAD `45cc53c40b47645b887c3bb996168e06aaa83f4c` (2026-08-06T02:25Z). Latest tag `v0.8.7` (2026-06-24) but `prerelease=true`|github.com/danny-avila/LibreChat/commit/45cc53c4
R11|renderer deps at pin|`client/package.json:41` `"@mcp-ui/client": "^5.7.0"`, lockfile exact `5.7.0`. `packages/api/package.json:112` `"@modelcontextprotocol/sdk": "^1.29.0"` (C23 cite confirmed). Code search `ext-apps` on default branch = 0 hits|github.com/danny-avila/LibreChat/blob/45cc53c4/client/package.json#L41
R12|`text/uri-list` still `src`|LibreChat owns ⊥ mimeType logic — 3 render sites delegate to `UIResourceRenderer` (`ToolCallInfo.tsx:167`, `MCPUIResource.tsx:40`, `UIResourceCarousel.tsx:111`). mcp-ui 5.7.0: `text/uri-list` ⇒ `externalUrl` ⇒ `iframeRenderMode:'src'`; `text/html` ⇒ `rawHtml` ⇒ `srcDoc`|mcp-ui `v5.7.0/sdks/typescript/client/src/components/UIResourceRenderer.tsx#L20-25`; `utils/processResource.ts:110,117,126`
R13|sandbox value at pin|src path: `mergeSandboxPermissions(sandboxPermissions ?? '', 'allow-scripts allow-same-origin')`. Effective: `ToolCallInfo` ⇒ `allow-scripts allow-same-origin`; `MCPUIResource` ⇒ `allow-popups allow-scripts allow-same-origin`. **`allow-forms` ABSENT ∀ paths** ⇒ native `<form>` submit blocked in-frame, JS `fetch` POST works. srcDoc branch omits `allow-same-origin` deliberately|mcp-ui `v5.7.0/.../HTMLResourceRenderer.tsx#L147-153,183-184`
R14|proxy / off-origin risk|NONE at pin: `proxy?: string` opt-in, LibreChat passes none, no default, no dedicated sandbox origin. mcp-ui self-guards same-host proxy ("proxy origin must not be the same as the host origin"). For `externalUrl` a proxy IS forwarded if ever set|mcp-ui `v5.7.0/.../utils/processResource.ts#L85-110`
R15|PR #13831 state|OPEN + **draft**, base `dev` (⊥ `main`), head `feat/mcp-apps-support`, 52 commits/56 files, updated 2026-08-05, ⊥ in any tag. Swaps `@mcp-ui/client ^5.7.0` → `@modelcontextprotocol/ext-apps ^1.7.4`; `isMcpAppResource` = mimeType includes `profile=mcp-app`; plain `srcDoc` ⇒ `sandbox=""`, app-backed ⇒ `sandbox="allow-scripts allow-forms"`; `mcp-sandbox.html` adds `allow-same-origin` only when `dedicatedOrigin` (`VITE_MCP_SANDBOX_URL`). Related #11799 OPEN, stale|github.com/danny-avila/LibreChat/pull/13831/files
R16|MCP Apps spec scope|`text/html;profile=mcp-app` ONLY — "MVP supports only `text/html;profile=mcp-app` (rawHtml), with other types explicitly deferred"; `text/uri-list` sits under "Content Types (deferred from MVP)". Spec = SEP-1865, extension per SEP-1724. CORRECTION: "#318" = ext-apps **issue** #318 "Thoughts about a mimeType equivalent to text/uri-list", OPEN discussion, ⊥ spec text|modelcontextprotocol/ext-apps `specification/draft/apps.mdx#L277`; ext-apps issues/318
R17|T59 evidence verdict, source-read stage (SUPERSEDED by R29)|PASS-candidate at pin `45cc53c4` + lockfile `@mcp-ui/client@5.7.0`: `text/uri-list` ⇒ iframe `src` at NOA origin, `allow-same-origin` present, zero proxy ⇒ first-party cookie rides. ⊥ closed T59 by itself — the owed live run ran 2026-08-08 ∧ PASSED ⇒ **SUPERSEDED by R29**. Caveats = R13 (⊥ `allow-forms`) + R15 (draft kills uri-list)|mcp-ui `v5.7.0/.../HTMLResourceRenderer.tsx#L147-153`
R18|logout ! be server-side|OWASP Session CS §Session Expiration: "must take active actions to invalidate the session on both sides, client and server. The latter is the most relevant and mandatory". ASVS V3.3.1 (L1/L2/L3, CWE-613): "logout and expiration invalidate the session token" ⇒ cookie-clear-only logout (V6) fails V3.3.1|cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html §Session Expiration; OWASP ASVS 4.0 V3.3.1
R19|account-disable ⊥ normative|Zero source found requiring termination of LIVE sessions on account disable. NIST SP 800-63B-4 covers authenticators ⊥ sessions; OWASP nearest = §Reauthentication After Risk Events (disable ⊥ listed) ⇒ per-request `is_active` re-read = common practice, ⊥ normative backing, ∧ ⊥ substitute for R18|pages.nist.gov/800-63-4/sp800-63b.html; OWASP Session CS §Reauthentication After Risk Events
R20|session TTL numbers|NIST SP 800-63B-4 §2.2.3 (AAL2, final 2025-07-31): overall reauth SHOULD ≤ 24h, inactivity SHOULD ≤ 1h; §5.2 "When either timeout expires, the session SHALL be terminated"; §5.1.1 cookie expiry "SHALL NOT be relied upon to enforce session timeouts". OWASP: idle 15–30 min low-risk / 2–5 min high-value, absolute 4–8h. Current `AUTH_JWT_ACCESS_TOKEN_TTL_SECONDS=3600` ∈ range|pages.nist.gov/800-63-4/sp800-63b.html §2.2.3,§5.1.1,§5.2; OWASP Session CS §Session Expiration
R21|CONFLICT — denylist|OWASP JWT CS §JWT revocation: denylist = "workaround for the 'stateless session' invalidation problem", key `(jti, iss)`, raw-JWT/SHA-256 key "not safe … denylist bypass through JWT malleability", + "consider whether there is a better solution" (Token Status List, session-bound nonce, short exp, DPoP). OWASP REST CS §JWT: on explicit termination `jti` "should be submitted to a denylist". BOTH logged, ⊥ averaged|OWASP JSON_Web_Token_Cheat_Sheet §JWT revocation; OWASP REST_Security_Cheat_Sheet §JWT
R22|short TTL + rotation|Only mechanism with hard normative language: RFC 9700 §2.2.2 public-client refresh tokens "MUST be sender-constrained or use refresh token rotation"; §4.14 short access lifetime "reduc[es] the potential impact of access token leakage"; §4.14.2 rotation invalidates prior refresh token — revoke-on-logout = **MAY**, refresh-only. RFC 7009 §2: refresh revocation MUST, access SHOULD. RFC 6819 §3.1: "Implementing token revocation is more difficult with assertions than with handles"|datatracker.ietf.org/doc/html/rfc9700 §2.2.2,§4.14,§4.14.2; rfc7009 §2; rfc6819 §3.1
?R23|no propagation window|Zero normative max revocation-propagation window ∧ zero access-token-lifetime digits in RFC 9700/6819/7009. RFC 8725 (JWT BCP) silent on `jti`/replay. Per-user token-version / session-epoch counter ⊥ present in any normative source ⇒ common practice only|rfc-editor.org/rfc/rfc8725.txt; pages.nist.gov/800-63-4/sp800-63b.html §5.3
R24|PyJWT 2.13.0|VERIFIED at tag 2.13.0: `leeway: float \| timedelta = 0` default; `if iat > (now + leeway): raise ImmatureSignatureError("The token is not yet valid (iat)")` (`InvalidIssuedAtError` = non-numeric case only) ⇒ V79 correct. `_validate_jti` = type check only (`InvalidJTIError` if ⊥ str), zero revocation ⇒ denylist wholly caller's. jti/sub validation landed 2.10.0 (#1005); iat check 2.6.0 (#794), touched 2.7.0 (#847)|raw.githubusercontent.com/jpadilla/pyjwt/2.13.0/jwt/api_jwt.py; pyjwt.readthedocs.io changelog
R25|fastmcp error masking|`mask_error_details` DEFAULT **`False`** ⇒ unmasked branch raises `ToolError(f"Error calling tool {name!r}: {e}")` — `str(e)` verbatim at LLM (V19 breach by default). ∧ raw-exception→`ToolError` conversion sits INSIDE `call_tool(run_middleware=False)`; middleware wraps THAT call ⇒ by middleware time the exception is already `ToolError`, original type gone ⇒ `RuntimeError`/`TimeoutError` ⊥ distinguishable there ⇒ V19 mapping ! be a decorator on the tool fn. Server sets `mask_error_details=True` as 2nd line, for what is raised OUTSIDE a decorated tool (argument validation, registration wrapper). VERIFIED live 2026-08-07 (T19)|`fastmcp/settings.py:268`; `fastmcp/server/server.py:1248-1270,1342-1358` (fastmcp==3.4.5)
R26|era chosen by CLIENT, ⊥ NOA|TS `Client.connect` sends `protocolVersion: LATEST_PROTOCOL_VERSION` — ⊥ ctor option, ⊥ knob ⇒ a client ⊥ ask for another era without patching; reply ∉ its SUPPORTED → throws `Server's protocol version is not supported`. LibreChat @ pin `45cc53c4`: `package-lock.json` resolves `@modelcontextprotocol/sdk` **exact 1.29.0** (⊥ merely `^1.29.0`), `connection.ts:1276-1284` = stock `new Client({name,version},{capabilities:{}})` ⇒ asks `2025-11-25`. Python side echoes: `protocolVersion=requested if requested ∈ SUPPORTED else LATEST` ⇒ live era `2025-11-25`; unsupported ask → `2025-11-25` ∈ TS SUPPORTED ⇒ ⊥ dead-end. `ttlMs`/`cacheScope` grep = 0 hits in BOTH SDKs @ 1.29.0 ⇒ V74's "nothing to bound today" holds|`v1.29.0/src/client/index.ts:495,508`; LibreChat `45cc53c4` `package-lock.json`, `packages/api/src/mcp/connection.ts:1276-1284`; `mcp/server/session.py:185-187` (mcp==1.29.0)
R27|LibreChat sends ⊥ conversation id on a tool call|VERIFIED 2026-08-07 @ pin `45cc53c4` by whole-tree grep, ⊥ by reading one file: **ONE** `tools/call` emitter exists — `MCPManager.callTool` sends `params:{name,arguments}` ∧ nothing else; 3rd arg = `RequestOptions` (`timeout`,`resetTimeoutOnProgress`,`...options`), ⊥ params ⇒ ⊥ `_meta` on the wire. `onprogress` ⊥ passed anywhere on that path ⇒ the TS SDK injects ⊥ `_meta.progressToken` either. Sole caller = `api/server/services/MCP.js:1084` ⇒ ⊥ 2nd path to miss. ⇒ `tool_runs.conversation_ref` (V47) ⊥ fillable from the call itself: header (R28) ∨ NULL. Bears on T57, T73(d)|`45cc53c4` `packages/api/src/mcp/MCPManager.ts:342,514-528`; `api/server/services/MCP.js:1084`; tree grep `'tools/call'` over `packages/`+`api/` (non-test) = 1 hit
R28|`{{LIBRECHAT_BODY_CONVERSATIONID}}` = supported header placeholder|`ALLOWED_BODY_FIELDS = ['conversationId','parentMessageId','messageId']`; `processMCPEnv` runs `processBodyPlaceholders` over ∀ `headers` entry of a streamable-http server, re-resolved PER TOOL CALL (`MCPManager.ts:440` builds `resolvedHeaders` → `:512` `setRequestHeaders`) ⇒ operator YAML CAN hand NOA a conversation id as a header, closing what R27 leaves open. 2 bounds NOA ! honour: (a) absent ∨ null field → **empty string**, ⊥ literal placeholder ⇒ blank ⊥ distinguishable from absent, both = "no label"; (b) header values ASCII-encoded at `:41-42,201`. ⊥ user-supplied text — `conversationId` = LibreChat's own id off the request body|`45cc53c4` `packages/api/src/utils/env.ts:126-148,219-245,311,408-422`; `packages/api/src/mcp/MCPManager.ts:440,512`
R29|T59 render gate = PASS, LIVE|MEASURED 2026-08-08 in Chromium against LibreChat built from pin `45cc53c4`, a real MCP connection ∧ a real NOA process — ⊥ a source read, ⊥ a component double (V69's rule). NOA returned `ui://noa/approval/<id>` + `mimeType: text/uri-list` + URL body. BOTH LibreChat render sites fired: `ToolCallInfo` ⇒ iframe `src` = the NOA URL, `srcdoc` absent, `sandbox="allow-scripts allow-same-origin"`; `MCPUIResource` (`\ui{}` marker) ⇒ same `src`, `sandbox="allow-popups allow-scripts allow-same-origin"`. `allow-forms` absent in BOTH ⇒ V80. INSIDE the frame: `window.location.origin` = `http://embed.noa.internal:8000` (NOA's, ⊥ opaque), `document.cookie` = empty (httpOnly holds), `GET /auth/me` = **200** as `operator@noa.internal`, `POST` = **200** authenticated ⇒ V22's cookie POST is possible from the frame. Cookie as recorded: `noa_session`, `domain=.noa.internal`, `httpOnly`, `SameSite=Lax`. NEGATIVE CONTROL (same page as a `text/html` resource ⇒ `srcdoc`, `sandbox="allow-scripts"`): origin `null`, `document.cookie` → `SecurityError`, both requests → `TypeError: Failed to fetch` ⇒ the compare separates (V87). Green twice, 2nd time on committed code + a fresh token|`docs/spikes/librechat-embed-render-gate.md`; `spikes/librechat-embed-render-gate/evidence/probe-results.json`
R30|LibreChat ⊥ honour `tools/list_changed`|MEASURED 2026-08-08 @ pin `45cc53c4`, 2 independent reads agreeing. (a) ON THE WIRE, NOA's side works: with a session's GET stream open, the emit put `{"method":"notifications/tools/list_changed","jsonrpc":"2.0"}` on that stream. (b) IN LibreChat: same emit inside a chat turn drew nothing — the turn's JSON-RPC methods read `initialize`,`notifications/initialized`,`ping`,`tools/list`,`tools/call` ∧ stop. Source agrees: `connection.ts:1852` sets a handler for `ResourceListChangedNotificationSchema` only; `ToolListChangedNotificationSchema` = 0 hits tree-wide. ⊥ error, ⊥ disconnect on the ignored notification ⇒ emitting stays safe, just inert. Bears on V74, T66|`45cc53c4` `packages/api/src/mcp/connection.ts:1852`; tree grep `ToolListChanged` = 0; `spikes/librechat-embed-render-gate/evidence/probe-results.json`
R31|3 LibreChat MCP config bounds, ⊥ predicted by the source read|MEASURED 2026-08-08 while standing the rig up. (a) BOOT INSPECT vs C24: LibreChat inspects every startup server with ⊥ user in context ⇒ `{{LIBRECHAT_USER_ID}}` resolves to nothing ⇒ NOA 401 (correct, V3) ⇒ LibreChat infers OAuth (`requiresOAuth: true`, `connectionState: needs_authorization`) ∧ ⊥ connects again; `/api/mcp/tools` then answers ZERO tools ∀ user, silently. `startup: false` + `requiresOAuth: false` fix it (V88). (b) SSRF GUARD: ⊥ `mcpSettings.allowedDomains` ⇒ `Domain "http://noa.internal:8000" is not allowed`; `allowedAddresses` refuses CIDRs ∧ loopback literals @ pin ("host:port pairs, private space") ⇒ domain list = the lever. (c) PARSER: `ui://` scheme REQUIRED for a resource to become a UI resource; LibreChat hashes the body into a `resourceId` ∧ injects `UI Resource ID: <id>` + `UI Resource Marker: \ui{<id>}` into the text the MODEL sees ⇒ what V71's `\ui{}` instruction is for, ∧ confirmation the URL + id land in LibreChat's Mongo (V26)|`45cc53c4` `packages/api/src/mcp/parsers.ts:183`, `packages/api/src/mcp/registry/MCPServersRegistry.ts` (`resolveAllowlists`, `shouldEnableSSRFProtection`), `packages/data-provider/src/mcp.ts:166-171`; `docs/spikes/librechat-embed-render-gate.md`

## §V INVARIANTS

### Auth & Tokens

- V1. ∀ MCP req → resolve `users.id` + re-check `is_active`. `tools/list` RBAC-filtered per user. `tools/call` re-checks permission.
- V2. MCP auth = per-user bearer token, minted by NOA admin. Token ↔ `users.id`. Hashed at rest (SHA-256). Plaintext shown once at mint. Revoke = delete row. ⊥ logged.
- V3. Token TOFU binding: minted `librechat_user_id=NULL`; NULL + `X-Noa-LibreChat-User` header present → bind; non-NULL → header ! present & equal else 401. Absent header → 401 ∀ states (bound ∧ unbound) ∵ C24 LibreChat-only. 401 body names cause: `librechat_user_header_missing` \| `librechat_user_mismatch`. ⊥ unbound-token client path.
- V4. Token revalidation: `last_ldap_check_at` on row, staleness interval. LDAP down → fail closed. Cascade revoke on admin disable + LDAP path.
- V5. Resolve MCP identity in exactly one function, called ∀ MCP request. Auth mechanism swap = one file.
- V6. Login sets httpOnly `noa_session` cookie (SameSite=Lax, Domain=`.noa.internal`, Path=/). Logout clears with max-age=0. Logout idempotent without auth. Session JWT ⊥ revocable pre-`exp` — ⊥ `jti`, ⊥ denylist ⇒ logout kills the *cookie*, ⊥ the token: a held copy verifies until `exp`. V4 cascade-revoke-on-disable = `mcp_tokens` only (T11), ⊥ session equivalent exists. ⇒ T8/T9 ! re-read `users.is_active` ∀ authenticated request — that re-read is the ONLY thing bounding a disabled admin's session, ⊥ an optimization. Unbounded window otherwise = `AUTH_JWT_ACCESS_TOKEN_TTL_SECONDS` (default 3600). Session-kill-on-disable (denylist \| `jti` \| shorter TTL) = OPEN owner decision, ⊥ agent call. Research feeding that decision: R18 (server-side invalidation on logout = mandatory per OWASP + ASVS V3.3.1 ⇒ cookie-clear-only fails it), R19 (⊥ normative mandate to kill *live* sessions on account disable; per-request `is_active` re-read = common practice, ⊥ substitute for R18), R20 (numbers: NIST AAL2 reauth SHOULD ≤ 24h ∧ inactivity SHOULD ≤ 1h; OWASP absolute 4–8h ⇒ 3600s ∈ range), R21 (denylist = CONFLICTING OWASP guidance, key `(jti, iss)`, ⊥ raw-JWT key), R22 (short TTL + refresh rotation = only mechanism with MUST-level text). ⇒ logout-invalidation gap is a spec-acknowledged deviation, ⊥ an oversight.
- V79. Session verify clock discipline: leeway = 0 (PyJWT default, ⊥ passed). `exp` 1s past → expired; `iat` 1s future → invalid (PyJWT `iat > now + leeway` ⇒ `ImmatureSignatureError`). Holds ∵ single API process — mint & verify share one clock. **Revisit trigger: >1 API replica.** Then drift breaks the MINT side first: minter clock ahead ⇒ verifier rejects a token the instant login returns it (⊥ merely logouts near expiry). Fix then = explicit `leeway`, ⊥ silent widening. Future-`iat` rejection = `PyJWT==2.13.0` behaviour, moved across 2.x releases ⇒ pinned + tested, ⊥ assumed. VERIFIED at tag 2.13.0 (R24): `leeway` default `0`; `iat > (now + leeway)` → `ImmatureSignatureError` (`InvalidIssuedAtError` = non-numeric `iat` only); check landed 2.6.0 (#794), touched 2.7.0 (#847). `_validate_jti` = type check only ⇒ ∀ revocation logic = ours (bears on V6).
- V7. New LDAP users auto-provisioned `is_active=False`. Bootstrap admin auto-activated with `admin` role.
- V8. Passwords and tokens ⊥ appear in structured logs ∧ ⊥ in error bodies. Error body = `error_code` + `message` + `request_id`, nothing else — `detail` = internal diagnostic, logs only. Two echo paths closed by name (T64): pydantic's `RequestValidationError.errors()` carries the failing value in `input` ⇒ on `POST /auth/login` that IS the submitted password ⇒ ⊥ forward `exc.errors()` to body (`noa-old` did) ∧ ⊥ to log — `loc` + `type` only. `StarletteHTTPException.detail` likewise ⊥ echoed: status → fixed `(error_code, message)` pair ⇒ a future raiser ⊥ put internal text in a body by accident.
- V9. Rate limiter blocks after configured max failures within window. Rate-limited → 429 with `Retry-After`.

### RBAC

- V10. Admin role bypasses tool permission checks for known tools but rejects unknown/unregistered tools.
- V11. Disabled users (`is_active=False`) → zero permissions regardless of roles.
- V12. Cannot disable last active admin. Admin cannot self-deactivate. Admin self-delete → 409.
- V13. Non-admin users → 403 on admin endpoints. `admin` role reserved (cannot edit tools or delete). Internal roles cannot be assigned via API.
- V14. Admin changes produce audit events. Permission updates take effect immediately.

### Tool Discipline

- V15. CHANGE tool schemas ⊥ carry reason param of any name. Reason enters exactly once: operator types in approval iframe → approve POST body. Approve without non-blank `reason` → 409 `change_reason_required`. Gate = approve endpoint, ⊥ tool call. **AMENDED 2026-08-08 by T37: DENY ! carry one too** — same 409, same code. T34(f) handed T37 that choice ∧ this is it: a reason on BOTH is what makes the DB CHECK expressible (`ck_action_requests_decided_reason`: APPROVED ∨ DENIED ⇒ non-blank), ∧ an audit trail owes an explanation for a refused change as much as an approved one. Safe exit stays open — ⊥ answering expires the request (V32) ∧ an EXPIRED row carries ⊥ reason ∵ nobody gave one. "Blank" = whitespace, on BOTH sides: endpoint `.strip()`, DB `reason ~ '[^[:space:]]'` — ⊥ `btrim()`, which strips SPACES only ⇒ a one-tab reason the endpoint refuses would satisfy it. 2 spellings of blank = 1 too many.
- V16. READ tools execute immediately. CHANGE tools go through approval gate: create `action_request` (PENDING) → operator approves/denies → execute if approved.
- V17. One-workflow-one-tool: preflight runs in-process inside CHANGE call. Evidence born in-process, lives milliseconds, same user, ⊥ in transcript. ⊥ evidence store needed.
- V18. Ambiguous identifiers → structured "ambiguous, here are candidates" result. ⊥ guess.
- V19. Tool error sanitization: raw exceptions ⊥ reach LLM. `RuntimeError` → `tool_execution_failed`. `TimeoutError` → `timeout`.
- V20. Lifecycle enums machine-stable, **3 distinct enums** — ⊥ one flat set. `ToolRisk` = `READ` \| `CHANGE` (classification, ⊥ a status). `ActionRequestStatus` = `PENDING` \| `APPROVED` \| `DENIED` \| `EXPIRED`. `ToolRunStatus` = `STARTED` \| `COMPLETED` \| `FAILED`. `tool_runs` carries risk + status as separate columns ⇒ failed READ representable. `EXPIRED` ∉ `noa-old` `ActionRequestStatus` — **BUILT 2026-08-08 (T34)**: without it a request nobody answers stays PENDING ∀ time ∧ V23 has ⊥ truthful answer past the TTL. ∀ 3 value sets DISJOINT ⇒ a query written vs one column ⊥ match rows in another; asserted 3-way at T34 (only pairwise was possible at T35).
- V21. Whitespace-only required strings rejected. Duplicate firewall targets rejected. Invalid server_ref rejected.
- V83. Exposed-tool discipline — a tool ships WITH its gate, ⊥ after it. (a) ∀ registered MCP tool name ∈ `TOOL_CATALOG`; ⊥ ⇒ construction failure, ⊥ runtime surprise — grants are written against that catalog ∧ admin bypass hands out "every known tool" ⇒ an uncatalogued name = capability ⊥ role can be granted ∧ admin still reaches. (b) RBAC = ONE middleware ∀ tools (V1 both clauses), ⊥ per-tool code — a tool ⊥ forget a middleware. (c) Gate carries the REGISTERED set ∧ intersects: admin's grant set = whole catalog ⇒ a catalogued-but-unbuilt name otherwise reaches fastmcp's own `Unknown tool`. (d) ONE refusal code (`tool_not_permitted`) ∀ causes — no-grant ∧ ⊥-registered ∧ ⊥-catalogued identical ⇒ ⊥ existence oracle over C22 names ∨ over which tools are built. (e) V19 mapping ! be INSIDE the tool, ⊥ middleware (R25).

### Approval Gate

- V22. Approve/deny ⊥ travel through LLM. Only path = POST from embed iframe → NOA with `noa_session` cookie + CSRF token.
- V23. "May this run?" read from `action_requests.status` in DB ∀ time — ⊥ from LLM claim, ⊥ from tool arg.
- V24. `build_change_gate_response()` shapes every CHANGE tool result; 3 branches: link-out text \| UI resource \| elicitation (future). Active branch = **UI resource** (path A), selected by T59's live run 2026-08-08 (R29), ⊥ assumed. 2 branches ship TOGETHER, ⊥ either/or: the iframe is the surface ∧ the plain URL rides in the text block ∀ result (V25) — the frame can fail to load, ∧ then the URL is the only door. Elicitation stays future. Upgrade path = swap branch, backend unchanged.
- V25. Text content ! always carry approval URL plain ⇒ link-out always live. Serves 2 paths: T59-fail (C21 → link-out becomes primary) + iframe-load-failure. ⊥ a non-LibreChat-client feature (C24).
- V26. Embed URL carries `action_request_id` only — ⊥ token, ⊥ ops data. Tool-result artifacts persist in LibreChat MongoDB ⇒ assume LibreChat admin can read them.
- V27. Embed access control: requester-match primary (`requested_by_user_id == caller`, caller = cookie identity). Mismatch → 404, not 403 (existence ⊥ leak). **AMENDED 2026-08-08 by T37: "⊥ leak" bound at the BODY, ⊥ the status** — foreign id ∧ unknown id answer byte-identical (`error_code` + `message`; only `request_id` differs, V73). A differing code = a 403 spelled differently, ∧ a status-only test ⊥ catch it (B1's shape). NULL requester (FK `SET NULL`, T34) matches ⊥ caller ⇒ lands here: fail-closed.
- V28. Exactly one `pending → decided` transition per request, under row lock. Concurrent double-click → one wins, loser gets 409 `action_request_already_decided`. **AMENDED 2026-08-08 by T37: ∀ guard — requester-match (V27), status, deadline (V32) — evaluated AFTER `SELECT … FOR UPDATE`, in one transaction.** `noa-old` read the row, checked `status`, THEN locked (`MCP:apps/api/src/noa_api/storage/postgres/action_tool_runs.py:256-270`) ⇒ the check sat OUTSIDE the thing that makes it hold. **Test ! prove the 2 parties OVERLAPPED — V89, B5.**
- V29. Approve → async execution, 202 + `tool_run_id`, embed polls to terminal. State in DB, ⊥ in connection. **AMENDED 2026-08-08 by T37**: the `tool_runs` STARTED row ∧ `action_requests.tool_run_id` land in the SAME transaction as the decision ⇒ APPROVED-with-⊥-run ∧ run-with-⊥-approval both unrepresentable. Same-session is a fact of CONSTRUCTION (`start_change_run` on the decision repository), ⊥ an obligation a caller can get wrong. Executor handed the run AFTER commit — a handoff first could start a change whose authorization then rolled back; a death in that gap = exactly T38's reaper pair (APPROVED + STARTED). Handoff failure = logged ∧ SWALLOWED, ⊥ a 500: the approval is durable, ∧ a 500 sends the operator to click again into V28's 409, which tells them nothing.
- V30. Async host = in-process asyncio task with own session + reaper for runs stuck in STARTED.
- V31. Explicit per-user cap on in-flight CHANGE executions (default 1). Over limit → 409, ⊥ silent queue.
- V32. Pending requests expire on TTL → terminal `expired`; approve/deny on expired → 409 `action_request_expired`. **Check-on-read at the DECISION door LANDED 2026-08-08 (T37)**: `expires_at <= now` under the lock already held → WRITE EXPIRED + `decided_at` → commit → THEN raise. It writes ∵ refusing without the write leaves a row still reading PENDING ⇒ the next reader repeats the discovery. `reason` stays NULL — V15's CHECK exempts EXPIRED ∵ an expiry is the ABSENCE of an answer, ⊥ one. **Sweep LANDED 2026-08-08 (T39)**: `core/approvals/expiry.py`, set-based `UPDATE … WHERE status = PENDING AND expires_at <= now RETURNING id`, hosted by an in-process asyncio task the lifespan starts ∧ stops. Deadline compare = `<=` at BOTH doors (T39(a)) — a row exactly ON its deadline is one thing, ⊥ two. ⊥ `FOR UPDATE` there ∧ that ⊥ weaken V28: a bare `UPDATE` re-checks its WHERE vs the version the transaction it waited for committed ⇒ it skips a row an approval just won, ⊥ clobbers it (proven on V89's rig, T39(c)). The writer is a THIRD class that can write EXPIRED ∧ nothing else — a loop with ⊥ operator behind it ∧ a GET sit on the far side of V22 too. Check-on-read for the RENDER path landed as mechanism + request dependency; **1st live caller = T63's `noa_get_action_result` (2026-08-08)**, ∧ it runs the check AFTER its requester-matched read ∵ `expire_if_due` takes an id ∧ ⊥ requester ⇒ expiry-first lets a prompt-injected id make NOA WRITE to a row its caller ⊥ see (V27). A surface that resolves the row itself (T41's GET) ? call it first.
- V33. Approval context built at gate time and persisted on row, ⊥ rebuilt from transcript at render time.
- V34. One URL per request; `/approvals/[id]` owns whole lifecycle through receipt.
- V35. Approval card shows provenance: created-at, conversation ref, origin, requesting identity.
- V36. Post-decision notification to LLM = notification only. Ignored ⇒ change still executed and recorded.

### Embed & Security

- V37. Embed iframe document ! sit on NOA origin — property holds ∀ render paths. Mechanism **MEASURED, ⊥ inherited** (C21, R29): path A = `text/uri-list` → `src` mode → `allow-same-origin` → cookie rides — CONFIRMED live 2026-08-08 at pin `45cc53c4`, frame `src` on NOA's origin, `srcdoc` absent, in-frame `GET /auth/me` ∧ `POST` both 200 as the logged-in operator, `document.cookie` empty throughout (httpOnly holds, V6); path B = link-out new-tab, top-level NOA origin, cookie rides natively (⊥ iframe, ⊥ MIME dependency). Path B stays live ∀ iframe-load failure (V25) ∧ becomes primary again if a bump fails the re-verify — V22 unchanged either way ∵ decision still cookie POST from NOA origin.
- V38. Embed 401 renders explicit "cannot authenticate here" state — ⊥ blank card, ⊥ live Approve button.
- V39. CSRF token server-minted, signed, session-bound. Double-submit alone insufficient — any `*.noa.internal` sibling can plant cookie. **BUILT 2026-08-08 by T37, ⊥ by T46** — T46 was `.` ∧ a `.` row is ⊥ a guard; a live decision endpoint with ⊥ CSRF check IS this line's threat. `core/approvals/csrf.py`: HMAC-SHA256 over `(version, user_id, action_request_id, issued_at)`, wire = `v1.<issued_at>.<sig>`. **Bound to the REQUEST as well as the session** — more than this line asked ∧ free: a token minted for one card ⊥ authorise another, so a second open tab is ⊥ a spare key. Key = `HMAC(AUTH_JWT_SECRET, b"noa/approvals/csrf/v1")` — DERIVED, ⊥ a 2nd required prod secret ⇒ ⊥ 2nd key to rotate out of step, ∧ the label makes a session JWT unusable here (ASSERTED by test, ⊥ reasoned). TTL = `APPROVAL_PENDING_TTL_SECONDS`, ⊥ a number of its own ∵ the request's `expires_at` is checked under the lock anyway ⇒ a 2nd timeout = a 2nd thing to get wrong. `hmac.compare_digest`. Clock = V79's rule: zero leeway, future `issued_at` invalid. Age checked AFTER the signature ⇒ a forged stamp ⊥ buy a longer window. Refusal = 403 `csrf_token_invalid`, ONE code ∀ cause (absent, malformed, foreign operator, foreign card, stale) — naming which is telling a caller how close they got. ⊥ minting ROUTE here: T41's card is the surface.
- V40. Same registrable domain; `noa_session` scoped `Domain=.noa.internal`, SameSite=Lax.
- V41. Admin app sends `frame-ancestors 'none'`. Embed app sends `frame-ancestors https://chat.noa.internal`.
- V42. Embed has no login page, no LDAP form, no credential handling. 401 → "Sign in to NOA" + retry.
- V43. Exactly ONE reason field exists, operator-typed in approval iframe (C8). ⊥ `proposed_reason`, ⊥ LLM-authored reason anywhere in schema/DB/receipt. LLM-authored text (tool args, activity label) = evidence only, ⊥ authoritative, ⊥ stored in `action_requests.reason`.
- V44. Embed ⊥ trigger decision from inbound `postMessage`. Any listener validates `event.origin` and is read-only.
- V80. In-iframe approve/deny ! POST via JS `fetch` — ⊥ native `<form>` submit, ⊥ `<form action>` fallback. ∵ sandbox at pin `45cc53c4` = `allow-scripts allow-same-origin` (+`allow-popups` on one render site) with **`allow-forms` ABSENT ∀ render paths** (R13) ⇒ a form submit dies silently in-frame. Cookie + CSRF discipline unchanged (V22,V39). Link-out path (V25) = top-level document ⇒ unaffected, forms fine there. **Re-verified LIVE 2026-08-08** (R29) at BOTH render sites: `allow-scripts allow-same-origin` (`ToolCallInfo`, the tool-call panel) ∧ `allow-popups allow-scripts allow-same-origin` (`MCPUIResource`, the `\ui{}` marker in assistant text) — `allow-forms` absent in BOTH, ∧ the in-frame `fetch` POST returned 200. Bound by test, ⊥ by discipline (V84c): `spikes/librechat-embed-render-gate/verify_librechat_pin.sh` FAILS if the shipped `@mcp-ui/client` bytes ever mention `allow-forms` ⇒ a bump that grants it turns this line red instead of leaving it stale. Re-check sandbox string ∀ T59 re-verify.

### Audit

- V45. Every MCP READ writes `tool_runs` row: requester, truncated summary, redacted args, queryable in admin audit.
- V46. Every CHANGE (approved) writes `tool_runs` + `action_receipts` + audit log.
- V47. Tool run audit: `requested_by_user_id`, `tool_name`, `status`, `conversation_ref`, `result_summary` (truncated), args (redacted), timing.

### Secrets & Crypto

- V48. `SecretCipher` round-trips encrypt/decrypt. Encrypted format `enc:v1:fernet:...`. Server secrets encrypted at rest.
- V49. Password generation server-side (`_generate_password()`), ⊥ LLM arg. Plaintext lives only in `execute()` scope — ⊥ persisted, ⊥ cross LLM boundary. Tool returns only `yopass_url`.
- V50. yopass: blob = `username` + `password` → PGPy client-side encrypt → `POST /secret` → URL with key in fragment. Fragment ⊥ sent to yopass server.

### Infrastructure

- V51. `/health` → 200 `{"status":"ok"}`.
- V52. Fernet key (`NOA_SECRET_ENCRYPTION_KEY`) required in production. Auto-generated in dev. Name reflects scope: encrypts server credentials, ⊥ the DB.
- V53. JWT secret required in production (auto-generated ≥32 chars in dev).

### Firewall

- V54. ∀ WHM firewall CHANGE tools ! reject non-IPv4 targets (CIDR, IPv6, hostname). Preflight READ may accept all types.
- V55. SSH commands prefix `sudo -n` ⟺ resolved ssh user ≠ `root`. `sudo -n` non-interactive.
- V56. SSH command output ! strip login/PAM/LVE banners before tool-specific parsing. `CommandResult` ! retain raw output for audit.
- V82. Host-key pin ! be enforced INSIDE the handshake, pre-auth. `known_hosts` = list form `([], [], [])` (empty trusted / CA / revoked), **⊥ `None`** — `None` = asyncssh's documented off switch: `_trusted_host_keys = None` ⇒ the comparison block, `self._owner.validate_host_public_key(...)` included, is skipped (`connection.py:3509-3511`, `:1359-1367`) ⇒ pin inert, ∀ host accepted (B2). Empty list = nothing pre-trusted ⇒ ∀ presented key falls through to the callback during KEX ⇒ mismatch raises `HostKeyNotVerifiable` → `ssh_host_key_mismatch` before user auth ⇒ wrong host ⊥ receive the decrypted password/key. Empty *tuple* `()` ⊥ allowed — falsy ⇒ asyncssh loads `~/.ssh/known_hosts` (`:3510-3518`). Post-connect fingerprint compare ⊥ satisfy this ∵ credential already sent. **Test ! use a real `asyncssh.create_server` on loopback ∧ assert the server recorded ZERO auth attempts** — a double that calls the callback itself proves the callback, ⊥ that asyncssh calls it (that double is what hid B2).
- V57. Dual-backend CSF+Imunify: ∀ firewall operation reaches the backends through ONE door — `core/integrations/whm/firewall_gate.py::run_on_usable_backends` — which reads `available["csf"]` ∧ `available["imunify"]` ∧ acts on the usable ones in parallel via `asyncio.gather`. **Zero backends available → error `no_firewall_backend`, ⊥ success.** `noa-old` bug: empty task dict → `gather()` → `[]` → reports ok having changed nothing. Silent no-op on approved CHANGE = \⊥ acceptable. The check sits at the MECHANISM, **⊥ per tool** (AMENDED 2026-08-07 by T68 — this line read "each tool checks" ∵ T24 shipped it inside the preflight, ∧ a per-tool guard = a guard the NEXT tool forgets; the tool that forgets it is a CHANGE reporting an approved release it never made, which is the harm, ⊥ the READ's `unknown`). ⇒ 2 properties the door owns ∧ a tool ⊥ opt out of: it ⊥ be entered with nothing to do, ∧ a hand-rolled `asyncio.gather` anywhere on the firewall path is RED, ⊥ silent (AST guard, V84c — `test_whm_firewall_gate.py`). Binaries present ∧ `sudo -n` denied → `ssh_sudo_required`, ⊥ `no_firewall_backend`: 2 causes, 2 remedies, ∧ "no firewall tools here" sends an operator to install what is already installed (`noa-old` GH #82, V55). **Bounds the ZERO case only** — one backend usable ∧ the other silent is a PARTIAL answer, ⊥ covered here, → V86 (T24).

### PMG

- V58. PMG whitelist target = PMG mynetworks (`/config/mynetworks`). PMG commands use SSH + argv-safe builder only. `TERM=dumb`, `pmgsh` (argv-only).
- V59. `pmg_whitelist_search` normalizes single-host inputs: IPv4 `1.2.3.4` ≡ `1.2.3.4/32`, IPv6 `2001:db8::1` ≡ `2001:db8::1/128`. Search checks CIDR list exact membership — ⊥ containment: `1.2.3.0/24` ∈ `mynetworks` ⊥ make `1.2.3.4` an entry, ∧ saying so sends an operator to remove a CIDR they ⊥ name. BOTH sides normalized (the target ∧ ∀ stored line) — a rule held on one side reports `not whitelisted` for an address PMG whitelists, in the spelling PMG happens to use (AMENDED 2026-08-08 by T31, as built). `ipaddress.ip_network(…, strict=False)` MASKS host bits ⇒ `1.2.3.4/24` is a question about `1.2.3.0/24` ⇒ result ! ship the normalized form BESIDE the verdict; echoing the input back reads as a verdict about the operator's ADDRESS. Same family as V85/V86 one axis over — the tool ⊥ manufacture a claim its data ⊥ carry. T29 inherits ∧ it is worse there: a CHANGE WRITES the masked CIDR, ⊥ merely answers about it.
- V60. `pmg_whitelist_add` validates IP/CIDR via `ipaddress`; single host → `/32`. Add → `pmgsh create /config/mynetworks -cidr <cidr>` then `pmgconfig sync --restart 1`.
- V61. `pmg_whitelist_remove` validates + normalizes, deletes exact CIDR via `pmgsh delete /config/mynetworks/<cidr>` then `pmgconfig sync --restart 1`.

### Proxmox

- V62. `proxmox_reset_vm_password`: generate password internally → yopass store → set cipassword + regenerate cloud-init + verify (crypt-compare). yopass fail → abort before set. Set fail after yopass → URL holds unapplied password, old creds valid. **libcrypt absent (`_load_crypt_lib()` → `None`) → receipt states `verification_unavailable`, ⊥ silent pass.** Verification-unavailable ≠ verified.
- V63. `proxmox_vm_nic(action: enable|disable)` — single tool, enum action param. Preflight runs inside.

### Large Output

- V64. Large READ results (`whm_list_accounts`, `pmg_whitelist_list`) → tool result carries short summary + URL to NOA-origin table page, ⊥ full table in context. Zero token cost for table body. Render mechanism = path A (iframe UI resource), chosen by T59 2026-08-08 (R29) — same mechanism as V37, ⊥ a 2nd. "⊥ mix" = ⊥ 2 render mechanisms; it is ⊥ a ban on pairing the 2 fields, which mcp-ui REQUIRES together: `ui://` = LibreChat's classifier (`parsers.ts:183`, R31c) ∧ `text/uri-list` = the `src` render mode (R12). Measured, ∴ said plainly here. Shared capability, ⊥ per-tool special case.
- V85. A READ that CAPS rows ! carry its own bound: total-match count ∧ a truncation flag beside the rows. ∵ the model reports what it was handed — silent first-N ⇒ "there are twenty accounts" @ 200, a fabrication the TOOL authored, ⊥ the model (V71 says the agent config forbids fabrication; this says the tool ⊥ manufacture one). ∧ the capped set ! be ordered by a stable key BEFORE the cut — an unsorted truncation = arbitrary subset that changes between identical calls, ∧ upstream order (`listaccts`, `pmgsh`) = the remote system's, undocumented. Distinct from V64, ⊥ overlapping: V64 = large listing OFFLOADED whole to a table surface (nothing dropped, T59-gated); V85 = rows DROPPED in-process ∵ an operator-supplied limit. ⊥ a table surface here — a bounded search's answer fits the transcript. Instances: T21 `whm_search_accounts` (T20/T30 route through V64; T31 = exact membership, T63 = one record ⇒ neither caps). Written at 1 instance on purpose, same as V83 at T19: the rule ! exist BEFORE the 2nd capper, ∵ the 2nd is where "the first N" starts reading as "all of them". **2nd capper LANDED 2026-08-07 — T24**, `csf -g` evidence, cut at 20 inside `parse_csf_grep_output` ⇒ `CSFGrepParsed.total_matches` reports the pre-cut count ∵ the cut is the PARSER's, ⊥ the tool's, so the count ! be too. The ORDER clause deviates there, deliberately: kept order = **csf's own**, ⊥ a sort NOA applies — `csf -g` renders the current tables ∧ files ⇒ identical state gives an identical prefix, which IS the property this clause protects, while `listaccts` (where the clause was written) guarantees nothing; ∧ an alphabetical sort scrambles the deny/allow grouping the evidence is read by. ⇒ "ordered by a stable key" = **reproducible across identical calls**, ⊥ necessarily a sort in NOA's code. A source whose order is undocumented ∨ server-chosen still owes the sort.

### Multi-Source Reads

- V86. A verdict read from a SUBSET ! name the subset. A READ that asks N sources one question ships the sources that produced ⊥ usable answer beside the combined result, ∧ zero answers ⇒ `unknown`, ⊥ the benign value. ∵ V57 bounds only the ZERO case (⊥ backend usable ⇒ `no_firewall_backend`); the PARTIAL case is where the fabrication lives — `noa-old` computed the verdict from whichever backend succeeded ∧ else fell through to `not_found`, so a broken CSF beside a clean Imunify reported "this address is ⊥ blocked" on a box whose BLOCKING backend was silent. Same shape as V85 one axis over: V85 = ROWS dropped in-process, V86 = a whole SOURCE dropped; both say the tool ⊥ manufacture a claim its data ⊥ carry. A source's own "I ⊥ recognise this output" counts as ⊥ answered, ⊥ as clean — silence ≠ evidence of absence (CSF's `unknown` verdict; `core.integrations.whm.csf` holds the same line one layer down). The verdict still ships: one dead source is ⊥ a reason to withhold the others' answer, only to bound what it means. Instances: T24 (`csf`,`imunify`) = the only one today. T25/T26 inherit — a 2-part receipt (before-state, after-state) built off a half-answering pair tells the same lie about a CHANGE, where it is worse.

### Code Quality

- V65. `.py` files ≤ 900 lines. `.ts` files ≤ 300 lines. `.tsx` files ≤ 450 lines.
- V66. Reusable functions over duplication. DRY. Shared code in `core/`.
- V67. New code ! have type hints (Python) / TypeScript types. Tests for new functionality.
- V87. A test comparing 2 outputs ! compare what the CODE decides, ⊥ what the CLOCK stamps. Time-derived bytes (`Set-Cookie`'s `Expires`, `Date`, `iat`/`exp`, receipt timestamps) ! leave an equality compare ∧ be asserted by property instead (presence, bound, order). ∵ such a compare is a coin flip: green ∀ run inside one second, red ~1-in-N across a boundary ⇒ the suite teaches "flaky, rerun it", which is the habit that lets a REAL intermittent failure ride. ∧ the drop ! ship a case proving the compare STILL separates 2 outputs that genuinely differ — a field-dropping comparator degrades into a tautology silently, ∧ a tautology passes ∀ run, incl. the ones it was written to catch (V69's shape: a control asserted by prose ∧ held by nothing). B4 = the `delete_cookie` case: `expires=0` renders through `_getdate(0)` = **now**, so 2 clears differ in that byte range alone.
- V89. A test asserting a CONCURRENCY control ! prove the 2 parties OVERLAPPED. `asyncio.gather` of 2 callers is ⊥ a race — they finish in whatever order the driver returns results, ∧ the first commonly completes before the second's read reaches the server ⇒ the test passes with the LOCK DELETED (B5, measured twice). ⇒ 3 obligations: (a) the window is HELD OPEN on purpose — a hook between the locked read ∧ the commit, ⊥ a hope; (b) the assertion is on ORDER (an event journal — `second:read` after `first:committed`), ⊥ on the win count, which comes out right for the wrong reason; (c) a NEGATIVE CONTROL shows the forbidden order is REACHABLE (an unlocked read of the same row does ⊥ wait), else the ordering assertion is a tautology. Same family as V87 one axis over: V87 = a comparator that ⊥ separate, V89 = a race that ⊥ race; both = a test that ⊥ fail, which is V69's shape — a control asserted by prose ∧ held by nothing. Instances: T37 (`test_action_request_decisions_live.py`) ∧ T39 (`test_action_request_expiry_live.py` — the sweep vs an approval in flight). The rig MOVED to `support/action_decisions.py` at T39 rather than being copied (V66): 2 copies of the thing that holds the window open = 2 things that can silently stop overlapping, ∧ obligation (a) is the one nothing else would catch.
- V90. A test's SETUP gate ⊥ be the thing under test. A readiness ∨ preflight wait pointed at the subject converts the subject's FAILURE into a TIMEOUT — the suite reports "startup took too long" where the truth is `Expected: 200 Received: 503`, ∧ a timeout names nothing to fix ∵ every cause produces the same stall. MEASURED at T40, ⊥ reasoned: Playwright's `webServer` waiting on `/healthz` answered the 503 mutation with a 120s stall; waiting on the open SOCKET (`port`) answered the same mutation in 7s with the assertion. ⇒ the gate ! sit one layer BELOW the subject — a socket under a route, a process under a socket. Same family as V87 ∧ V89 one axis over: V87 = a comparator that ⊥ separate, V89 = a race that ⊥ race, V90 = a red that carries ⊥ information; ∀ 3 = V69's shape, a control asserted by prose ∧ held by nothing. V89 already records the sibling case from the OTHER side (a mutation that HANGS proves nothing — a timeout is a worse signal than a red); this is the same fact about the harness rather than about the mutation. Instances: T40 (`playwright.config.ts`). T41–T46 ∧ T56 inherit ∵ each writes its own e2e — the card's readiness gate ! be something that is ⊥ the card.
- V68. Conventional commits. No secrets in git.
- V69. `noa-old` = reference for patterns. Mature integration layers (WHM/Proxmox/PMG, `remote_exec`, `secrets`) copied, ⊥ rewritten. SSH banner stripping ∧ `sudo -n` escalation hardened there. **Host-key pinning ∧ TOFU refresh were ⊥** — B2: `noa-old` `MCP` shipped the same inert `known_hosts=None`, the port copied it faithfully, this line asserted a property the source never had. ⇒ upstream provenance ⊥ evidence a control works. ∀ ported *security* control ! land with a test that exercises the real mechanism before any §V **∨ any doc** calls it hardened (V82 = the host-key case). Re-wording THIS line ⊥ enough — B3: the same sentence stood untouched in `README.md` ∧ `AGENTS.md` after B2's fix ⇒ V84.
- V84. Doc prose = restatement, `SPEC.md` = source ⇒ ⊥ 2 truths live. (a) ∀ §V re-word ∨ retraction ! sweep restatements OUTSIDE `SPEC.md` in the SAME change — `README.md`, `AGENTS.md`, `docs/**`. Grep the CLAIM, ⊥ the citation: B3's two files never named V69, they repeated its sentence ⇒ citation-grep finds nothing ∧ the retracted premise ships anyway. `AGENTS.md` first ∵ ∀ agent loads it as instruction. (b) ∀ doc citing a §V for a security property ! cite the §V that CARRIES that property today — B2 moved host-key pinning V69→V82 ∧ the integration docs kept citing V69, i.e. pointed at the line that RETRACTS the claim. (c) property machine-readable (dep version, servable era set) ⇒ bind by test, ⊥ by discipline (T70(d) = the pattern). ⊥ machine-readable ⇒ the same-change sweep IS the control ∵ that is the one moment the whole claim set is in hand.

### LibreChat

- V70. LibreChat auth = same LDAP directory, local registration disabled.
- V71. LibreChat agent config carries model-facing safety policy: preflight-first, approval gates, no fabrication, argument discipline, `\ui{}` instruction. ⊥ mention reason-writing ∵ C8 (LLM never authors reason).
- V88. LibreChat's `mcpServers.noa` entry ! carry `startup: false` ∧ `requiresOAuth: false` ∧ NOA's host ∈ `mcpSettings.allowedDomains`. ⊥ 3 preferences — each closes a TOTAL, SILENT failure measured 2026-08-08 (R31). ∵ C24: boot inspection runs with ⊥ user ⇒ `{{LIBRECHAT_USER_ID}}` resolves to nothing ⇒ NOA answers 401 exactly as V3 says it ! ⇒ LibreChat reads that 401 as "this server wants OAuth", marks it `needs_authorization` ∧ never connects again ⇒ `/api/mcp/tools` reports ZERO tools ∀ operator, ∀ valid token, with ⊥ error anywhere the operator can see: NOA looks up, LibreChat looks up, the tool list is empty. ∧ with ⊥ allowlist LibreChat's SSRF guard refuses an internal URL outright (`Domain "http://noa.internal:8000" is not allowed`); `allowedAddresses` ⊥ accept a CIDR ∨ a loopback literal @ pin ⇒ the domain list is the only lever. ⇒ the config is part of the AUTH design, ⊥ deployment garnish: C24 makes an unauthenticated inspect impossible by construction, so the entry ! tell LibreChat ⊥ to try one. Config = machine-readable ⇒ bind by test on the YAML NOA ships (V84c), ⊥ by prose — T57 owns the file ∧ the test.
- V72. LibreChat `toolApproval` ⊥ configured, ⊥ relied on. VERIFIED 2026-08-04 `packages/api/src/agents/hitl/policy.ts`: `isHITLEnabled` = `policy?.enabled === true` ⇒ absent config ∧ `enabled:false` both fully bypass. ⊥ default-ask trap. §7.3 spike CLOSED. Glob-vs-RBAC mismatch (V19 split) stands regardless.

### Error Shape & Grants

- V73. ∀ error response carries `request_id` in body + `x-request-id` header, same value. (`noa-old` V8.) "∀ error response" = **5 writers, ⊥ 1**: `NoaError` handler ∧ `StarletteHTTPException` (404/405) ∧ `RequestValidationError` (422) ∧ bare `Exception` (500) ∧ `McpAuthErrorMiddleware._send_error` (raw ASGI — mounted MCP app has ⊥ exception-handler graph). A `NoaError`-only handler leaves 4 of them in Starlette's default shape ⇒ ⊥ `error_code`, ⊥ `request_id`. ONE seam installs ∀ of it — `install_error_handling(app)`, called by `noa_api.main` ∧ both test harnesses ⇒ ⊥ surface carries a different envelope (V66). Header set by `error_response()` ITSELF, ⊥ left to `RequestContextMiddleware`'s `send` wrapper ∵ Starlette's `ServerErrorMiddleware` sits OUTSIDE the user middleware stack ⇒ the 500 never passes through that wrapper. Both may set it; `MutableHeaders.__setitem__` replaces ⇒ ⊥ duplicate. Inbound `x-request-id` adopted ⊥ verbatim: ≤200 chars ∧ `^[A-Za-z0-9._:-]+$` else mint `uuid4()` — the value re-emits as a response header ∧ into structlog output ⇒ unbounded/newline value = log-forging surface (`noa-old` echoed verbatim). Id also bound to structlog contextvars ⇒ the id in the body names the log line; structlog default processor chain already carries `merge_contextvars` ⇒ ⊥ logging config. **BOUND**: header rides ∀ response (superset of this line); body key covers ∀ error NOA shapes, ⊥ fastmcp's JSON-RPC `error` payloads — those = a `result`/`error` object, ⊥ an HTTP error response ⇒ reaching them = patch fastmcp.
- V74. Stale tool catalog ⊥ become a security hole: V1 execution-time RBAC re-check = backstop ⇒ cached `tools/list` may *show* a revoked tool, calling it still 403s. Handshake era (C23) has ⊥ `ttlMs`/`cacheScope` fields ⇒ nothing to bound today. NOA ! emit `notifications/tools/list_changed` on permission change; whether LibreChat honours it = **ANSWERED 2026-08-08: ⊥ it does ⊥** (R30). Source ∧ live agree — `connection.ts:1852` registers `ResourceListChangedNotificationSchema` ONLY, `ToolListChangedNotificationSchema` = 0 hits in the tree, ∧ a notification NOA provably put on the session stream drew ⊥ `tools/list` after it. Emitting = harmless (⊥ error, ⊥ disconnect) ∧ buys nothing at this pin ⇒ T66 still emits (protocol-correct, ~free, a later client may honour it) ∧ T66's TEST ! be the V1 execution backstop, ⊥ a refetch assertion over behaviour NOA ⊥ control. V14 "immediately" = authoritative at execution, best-effort in catalog display. C23 → `2026-07-28` later ⇒ add `ttlMs` ceiling + per-user `cacheScope`.
- V75. Direct per-user tool grants disabled → 410 `direct_tool_grants_disabled`. Permissions flow role→user only. Role replacement preserves internal roles. (`noa-old` V14.)
- V76. `noa_get_action_result(id)` (READ, exposed) enforces V27 requester-match against MCP-token identity. Foreign id ∧ unknown id → identical not-found shape ⇒ ⊥ enumeration oracle. Prompt injection ⊥ pull another operator's action into transcript. (`noa-old` V162.) **BUILT 2026-08-08 (T63)**: identical shape covers a MALFORMED id too — 1 code (`action_request_not_found`), 1 message, ∀ cause, ⊥ a 2nd pydantic-shaped envelope. Requester-match sits in the `WHERE` ⇒ foreign row ⊥ fetched; NULL requester (T34 `SET NULL`) matches nobody ⇒ fail-closed. "⊥ pull into transcript" bound WIDER than this line asked: the result carries ⊥ `reason` (C8, V15, V43) ∧ ⊥ preflight `evidence` (V17) ∵ `ActionResultView` has ⊥ field for either ⇒ structural, ⊥ filtered.
- V81. Mounted-app path guards ! compare **mount-relative** path — `scope["path"]` minus `scope["root_path"]`. Starlette `Mount` extends `root_path`, ⊥ rewrites `path` (mirrors `starlette._utils.get_route_path`) ⇒ raw-`scope["path"]` guard matches standalone ∧ never matches mounted. Failure silent: guard falls through, SDK bare `invalid_token` answers same 401 status, only body differs ⇒ status-code test ⊥ catch it (B1). Helper = `mount_relative_path()` in `noa_api/mcp_request_auth.py`, ⊥ `starlette._utils` (private module).

### Firewall TTL

- V77. `whm_firewall_release_and_allow` ! require `duration_minutes` (integer, 1–525600). ⊥ permanent allowlist entry, ⊥ server-side default — operator states duration in chat ("5 min", "2 hours", "5 days"), LLM converts to minutes as ordinary tool arg (⊥ a reason, C8 unaffected). Receipt after-state ! show resolved expiry timestamp. Preserves `noa-old` `whm_firewall_allowlist_add_ttl` bound — merge (§6.5) ⊥ drop TTL.
- V78. `whm_firewall_allowlist_remove` = undo path, ⊥ TTL param ∵ removal is immediate.

## §T TASKS

id|status|task|cites
T1|x|git init + `.gitignore` (Python, Node, env, IDE, OS). Commit as "chore: init repo"|-
T2|x|scaffold monorepo structure: `apps/api/`, `apps/admin-web/`, `apps/web-embed/`, `core/`, `docs/`. Root `pyproject.toml` (workspace), `.env.example`, `README.md`, `AGENTS.md`|C12,C14
T3|x|`apps/api/` scaffold: FastAPI + FastMCP skeleton, `pyproject.toml` deps pinned exact: `fastmcp==3.4.5` (⊥ 4.x per C23), fastapi, uvicorn, sqlalchemy[asyncio], asyncpg, alembic, pydantic, pydantic-settings, structlog, orjson, cryptography, PyJWT, python-ldap, `pgpy>=0.6.0`, asyncssh, httpx. `requires-python = ">=3.11,<3.13"`|C1,C3,C7,C23
T4|x|Postgres schema v1: `users`, `roles`, `user_roles`, `role_tool_permissions`, `mcp_tokens`, `whm_servers`, `proxmox_servers`, `pmg_servers`. Alembic initial migration|C3,V1,V2,V3,V11
T5|x|`core/config.py`: pydantic-settings, all env vars (DB, LDAP, Fernet key, JWT secret, yopass, token TTL, LDAP revalidate interval, per-user concurrency cap, pending TTL, etc.)|C9,C7
T6|x|LDAP auth service: `LDAPService` with service-account bind + search. `user_exists_and_enabled(email)`. Dev bypass mode|C4,V7
T7|x|JWT service: mint, verify, httpOnly cookie set/clear. SameSite=Lax, `Domain=.noa.internal`|V6,V8
T8|x|Login flow: `POST /auth/login` → LDAP auth → JWT cookie. `POST /auth/logout` → clear. `GET /auth/me`. Rate limiter. Build `JWTService` ONCE at app startup (lifespan/module singleton), ⊥ request-scoped dependency ∵ algorithm-allowlist + key-length guards raise at construction — per-request build turns a config error into a 500 on first login, ⊥ a boot failure. `GET /auth/me` + ∀ session-authed route ! re-read `users.is_active` (V6: ⊥ token revocation exists)|V6,V7,V8,V9,V79
T9|x|RBAC engine: `role_tool_permissions` CRUD, `get_permitted_tools(user_id)`, admin bypass for known tools, disabled-user zero-permissions. Disable ! take effect on the live session's next request ∵ V6 (⊥ session revocation) ⇒ permission resolution reads the row, ⊥ trusts the cookie's claims|V6,V10,V11,V12,V13,V14
T10|x|Token management: `mcp_tokens` model + migration (id, user_id FK, token_hash, label, librechat_user_id nullable, last_used_at, last_ldap_check_at, created_at, expires_at). Mint: generate crypto token, hash SHA-256, return plaintext once. List: prefix + label + timestamps. Revoke: delete row|C5,V2
T11|x|Token verification: subclass `fastmcp.server.auth.TokenVerifier` (R1) — `async def verify_token(self, token: str) -> AccessToken \| None`, `None` = reject. Lookup `token_hash` in DB → return **fastmcp** `AccessToken(token=…, client_id=str(user.id), scopes=[…], expires_at=…, claims={…})` — `client_id`+`scopes` REQUIRED, SDK enforces `expires_at` itself (R2). TOFU binding runs INSIDE `verify_token` via `get_http_headers()` (R4, live-verified): NULL + header → bind; non-NULL → match else 401. LDAP staleness revalidation. Cascade revoke on admin disable|C20,V2,V3,V4,R1,R2,R4
T12|x|`resolve_mcp_identity(context, *, presented_token=None) → McpIdentity` (`noa_api/mcp_request_auth.py`): single HTTP-side function — extract `Authorization: Bearer <token>`, hash, DB lookup, TOFU binding, LDAP revalidation, `is_active` check. AS BUILT ≠ this line, 2 deviations: (a) ⊥ `request` param — `verify_token` gets no request arg, R4 contextvar serves both callers; a `Request` param ⇒ middleware ∧ verifier reach one function 2 ways. (b) → `McpIdentity` (T11 shape, ⊥ token material), ⊥ `users` row. 401 shapes named in body by `McpAuthErrorMiddleware`, ⊥ by `verify_token` (R2: ⊥ body hook) — middleware inside `AuthenticationMiddleware`, outside route, reads refusal stashed on ASGI scope ⇒ 1 resolve/req. Shapes: missing header, invalid token, expired, bound mismatch. Rate-limit failed auth keys = `x-noa-librechat-user` ∧ bearer SHA-256, ⊥ source IP (in-cluster addr ⊥ stable; C24 ⇒ IP block = fleet outage). Counted: `mcp_token_invalid`, `librechat_user_mismatch` ONLY. Store = `login_rate_limits` new scopes, ⊥ migration; math shared via `AttemptLimiter`. Header reads: `get_http_headers()` default STRIPS `authorization` ∧ `mcp-session-id` ⇒ pass `include={"authorization"}`; custom keys lowercased (`x-noa-librechat-user`) (R5). In-tool identity = `current_mcp_identity()` over `get_access_token()`, ⊥ re-parse bearer|V1,V4,V5,V9,V66,V73,R4,R5
T13|x|FastMCP server: `mcp = FastMCP("NOA", auth=<TokenVerifier instance>)` — `auth=` keyword-only, instance direct, ⊥ provider wrapper (R3). Mount: `mcp_app = mcp.http_app(path="/")` (transport default `"http"`) + `app.mount("/mcp", mcp_app)` + `from fastmcp.utilities.lifespan import combine_lifespans` → `FastAPI(lifespan=combine_lifespans(app_lifespan, mcp_app.lifespan))` (R6 — exact path, ⊥ in `fastmcp.__all__`). ⊥ pass `stateless_http`/`json_response`/`host`/`port`/`log_level` to `FastMCP()` — `TypeError` in v3, they belong on `http_app()` ∨ `FASTMCP_*` env (R7). Session default ON: `Mcp-Session-Id` minted unless `stateless_http=True` (R8). Mount ! carry T12: `auth=NoaTokenVerifier(context=build_mcp_auth_context(...))` + `http_app(path=…, middleware=[Middleware(McpAuthErrorMiddleware, mcp_path=…)])` — ⊥ middleware ⇒ V3 named bodies ⊥ ship, SDK bare `invalid_token` stands. AS BUILT ≠ this line, 3 deviations: (a) ⊥ module singleton — `build_mcp_server(*, auth)` + `build_mcp_http_app(*, auth_context)` in `noa_api/mcp_server.py`, one server per app ∵ `create_app()` runs many times in suite; `get_mcp_server()` dropped; T19+ register tools inside `build_mcp_server`. (b) `create_app()` ! own `AppRuntime` (settings, engine, session factory, `LDAPService`) ∵ `http_app()` snapshots `self.auth` at build time (`fastmcp/server/mixins/transport.py:419`) ⇒ verifier ! precede `FastAPI(...)`; lifespan keeps `JWTService` (T8 boot-failure rule) + engine dispose. (c) `MCP_APP_PATH="/"` ∵ Mount strips prefix (fastmcp default `/mcp` ⇒ `/mcp/mcp`) ⇒ endpoint answers `/mcp/`; bare `/mcp` → 307, method+body preserved — measured, ⊥ 404|V1,V3,V81,I.mcp,R3,R6,R7,R8,T12
T14|x|Port from `noa-old`: `core/remote_exec/` (SSH with banner stripping, `sudo -n`, host-key pinning, TOFU refresh). AS BUILT deviates on ONE line: `known_hosts=([], [], [])`, ⊥ the source's `known_hosts=None` — the port shipped the inert pin first (B2), fixed same day (V82). Pin tests run against a real loopback `asyncssh.create_server`, ⊥ a handshake double|C13,V55,V56,V69,V82
T15|x|Port from `noa-old` branch `MCP`: `core/secrets/` (Fernet `SecretCipher`, `_generate_password()`, `_yopass_store()`, `docs/integrations/yopass.md`). ⊥ branch `staging` — `yopass.py`/`password.py`/yopass doc absent there. AS BUILT ≠ this line, 3 deviations: (a) settings INJECTED — `SecretCipher.from_settings(settings)`, `_yopass_store(..., *, settings)`; `noa-old`'s module-global `settings` import + `@lru_cache get_secret_cipher()` + module-level `encrypt_text`/`decrypt_text` wrappers dropped ∵ ⊥ settings singleton here (T5 `get_settings()` called once, in `build_runtime`); T54 builds 1 cipher on `AppRuntime`. **SUPERSEDED 2026-08-07 by T21(e)** — T21 needed it first ⇒ `AppRuntime.secret_cipher` exists now, T54 READS it. (b) errors hoisted to `core/secrets/errors.py` ∧ subclass `NoaError` (⊥ bare `Exception` like `noa-old`) ∵ T54 decrypt raises out of an HTTP route ⇒ 1 handler shapes it (V73). Mapped: `SecretCryptoError` 500, `YopassError` 502, `YopassNotConfiguredError` 500. Codes verbatim: `yopass_not_configured`, `yopass_store_failed`. (c) `redaction.py` ⊥ ported — ⊥ named in this line, ⊥ caller until `tool_runs.args` redaction (T35,T47,T55); lands with them|C7,C13,C15,V48,V49,V50,V73
T16|x|Port from `noa-old`: WHM integration layer (`whm/integrations/ssh.py` — CSF + Imunify dual-backend, `asyncio.gather` parallel check) → `core/integrations/whm/`. Two transports to one server ∵ WHM exposes ⊥ firewall API: `/json-api/` client for accounts, SSH for CSF + Imunify. AS BUILT ≠ this line, 6 deviations: (a) `sudo -n` ⊥ a caller argument — T14 moved decision into `build_remote_command`, which reads resolved username itself ∵ V55 = biconditional ∧ boolean param lets one call site break it either way. (b) `SecretCipher` INJECTED per T15 — ⊥ settings singleton ⇒ `maybe_decrypt_text` arrives as argument, T54 builds the one instance (SUPERSEDED by T21(e) — built there, T54 reads it). (c) `CSFCLIError`/`ImunifyCLIError` collect under `WHMFirewallCLIError` base ∧ subclass `NoaError`, mapped once to 502 ∵ T54's validate route raises them out of an HTTP route ∧ V73 wants one handler; error-code strings unchanged. (d) Binary probing split out of `imunify_cli.py` → `availability.py`; on `MCP` the CSF probe ∧ `asyncio.gather` lived inside the Imunify module. (e) `addon_csf.cgi` HTTP firewall path DROPPED — uncalled on `MCP`, ∧ I.ext puts WHM firewall access on SSH. (f) `command_output_text` → `core/remote_exec/output.py` ∵ `noa-old` carried it 3× byte-identical, incl. in the PMG module T18 ports (V66). ⊥ ported, deliberately: per-reseller `token_resolver` (needs `whm_server_tokens` table absent from T4 schema ⇒ spec change, ⊥ build call — T22/T23 hit this on reseller-owned accounts), `server_ref` (named in T19's line), C22 never-implement client methods (`change_contact_email`, `change_primary_domain`) ⇒ capability ⊥ one line from exposure. `WHMClient` opens 1 httpx client PER request (⊥ pooled, unlike T17) ∵ `verify_ssl` per-server ∧ call rate human-paced|C7,C13,C22,V48,V54,V55,V56,V57,V65,V66,V69,V73
T17|x|Port from `noa-old`: Proxmox integration layer (`proxmox/integrations/client.py`) → `core/integrations/proxmox/`. One transport ∵ Proxmox = HTTP API only ⇒ ⊥ SSH path, ⊥ SSH creds on row (I.ext). ⊥ `errors.py` (unlike T16) ∵ dict-returning, zero raises. AS BUILT ≠ this line, 3 deviations: (a) C22 client methods ⊥ ported — `get_user`, `get_pool`, `get_effective_permissions`, `add_vms_to_pool`, `remove_vms_from_pool`; sole callers on `MCP` = `proxmox/tools/pool_tools.py` ∧ `core/workflows/proxmox/postflight.py` ⇒ pool-move pair + `proxmox_get_user_by_email`, ∀ on C22 list ⇒ capability ⊥ one line from exposure (mirrors T16's `change_contact_email` call). (b) `build_proxmox_client_from_creds(..., cipher: SecretCipher)` = ONE decrypt site (C7,V48); `noa-old` had 3 — admin validate service, tool `client_for_server`, workflow postflight — ∀ reaching module-global `maybe_decrypt_text` T15 deleted with the settings singleton. (c) `async with` added (`__aenter__`/`__aexit__`) ∵ `close()` exists on `MCP` ∧ called NOWHERE ⇒ ∀ tool call leaked socket + TLS context; `close()` kept, idempotent. 2 upstream oddities kept ∧ documented, ⊥ silently fixed: `_payload_error`'s trailing `status_code >= 400` branch UNREACHABLE (re-reads `message` the loop above consumed ⇒ 4xx+prose = `proxmox_api_error`, ⊥ `http_error`), ∧ `regenerate_qemu_cloudinit` returns UPID via `_request_json` ⊥ `_request_json_task` ⇒ result carries `data`, ⊥ `upid` (whether to poll = T27's call). Codes split by operator action, ⊥ cosmetics: `permission_denied` (fix Proxmox ACL) ≠ `auth_failed` (fix NOA row); `digest_mismatch` (re-read) ≠ `proxmox_api_error` (retry). `get_qemu_config` fails closed `invalid_response` on digest-free config ∵ digest = CAS token T28 writes under. `build_cipher` moved `tests/support/whm.py` → `tests/support/secrets.py`, re-exported from old name (V66)|C7,C13,C22,V8,V48,V66,V69
T18|x|Port from `noa-old`: PMG integration layer (`pmg/integrations/ssh.py`, `pmg/integrations/pmgsh_cli.py` — argv-safe, `TERM=dumb`) → `core/integrations/pmg/`. One transport ∵ PMG = SSH only (I.ext) ⇒ module = sole door into node: `pmgsh` reads/writes `/config/mynetworks`, `pmgconfig sync` applies. AS BUILT ≠ this line, 6 deviations: (a) `sudo -n` path ADDED — source had NONE ⇒ non-root `ssh_username` on `pmg_servers` row failed silently on perms; both builders take resolved `SSHConnectionConfig` ∧ route via T14 `build_remote_command` (reads username itself) ⇒ V55 biconditional holds by construction, ⊥ per call site; root ⇒ emitted command byte-identical to source. `TERM=dumb` stays AHEAD of `sudo`, ⊥ inside escalated command ∵ it is `ssh_exec` env, ⊥ a pmgsh arg. (b) denied `sudo -n` → `ssh_sudo_required`, ⊥ generic command failure — in BOTH success predicates (`require_pmgsh_success`, `require_pmg_mutation_success`); reachable only ∵ (a); collapse into "command failed" sends operator hunting an install already there (`noa-old` GH #82). (c) `PMGSHCLIError` → own module ∧ subclass `NoaError`, mapped ONCE to 502 in `noa_api/api/errors.py` ∵ T54 validate route raises it out of an HTTP route ∧ V73 wants one handler; ONE class for whole surface (matches `SSHExecutionError`; class-per-code = rewrite C13/V69 forbid), failure named in `error_code`; code strings unchanged, incl. converted `SSHExecutionError` codes. (d) `SecretCipher` INJECTED per T15/T16 ⇒ ONE decrypt site on PMG SSH path ∵ ⊥ settings singleton here. **SUPERSEDED 2026-08-08 by T31(a)** — `pmgsh_cli` ⊥ take `(row, cipher)` at all now, the caller resolves ∧ hands a `SSHConnectionConfig`. The decrypt SITE is unchanged (`resolve_pmg_ssh_config`); its CALLER moved up to the tool, which is what closes the DB session before the SSH hop. (e) `command_output_text` imported from `core/remote_exec/output.py` — source held 3rd byte-identical copy, T16 hoisted it naming this port as the reason (V66). (f) binary + path constants PUBLIC: `_PMGSH_BINARY`/`_PMGCONFIG_BINARY` → `PMGSH_BINARY`/`PMGCONFIG_BINARY` + `MYNETWORKS_PATH`, matching `CSF_BINARY` ⇒ T29–T31 tools ∧ tests assert vs them, ⊥ retype literals. `PMGCONFIG_BINARY` stays BARE name source used (≠ absolute `PMGSH_BINARY`) ∵ sudoers `secure_path` carries `/usr/bin` on default PMG node ∧ inventing a path `noa-old` never used = rewrite, ⊥ port. ⊥ ported, out of scope: `server_ref` resolution ∧ `ipaddress` normalization + mynetworks line parsing (→ T29–T31, V59–V61) — **∀ 3 LANDED 2026-08-08 by T31**, ref resolution in `core/servers/` (inventory, ⊥ integration) ∧ the parsing in `core/integrations/pmg/mynetworks.py`; `core/workflows/pmg/` (C16). Test support: SSH transport doubles WHM-named but about `core/remote_exec` ⇒ moved `tests/support/whm.py` → `tests/support/remote_exec.py`, old name re-exports ⇒ T14/T16 tests untouched (same move `support/secrets.py` made for `build_cipher`, V66; direct-import cleanup = T72)|C7,C13,C16,I.ext,V48,V55,V56,V58,V65,V66,V69,V73
T19|x|Tool: `whm_list_servers` (READ). Expose per DECISIONS §6.6. Return server list from DB. `resolve_whm_server_ref` for UUID/name/hostname → choices on ambiguity. AS BUILT ≠ this line, 7 deviations: (a) whole tool SPINE landed here, ⊥ named in this line ∧ ⊥ any §T row — `RbacToolMiddleware` (V1), `sanitize_tool_errors` (V19), registry catalog guard (V10,C22) ∵ V1 ∧ V19 bind the moment a tool exists ⇒ first tool ⊥ ship without them; discipline recorded as V83. (b) V19 mapping = decorator on tool fn, ⊥ middleware (R25); `mask_error_details=True` on server = 2nd line. (c) gate intersects REGISTERED names ∵ admin bypass grants whole CATALOG ⇒ `whm_list_accounts` (T20, catalogued ∧ unbuilt) passed the gate → fastmcp `Unknown tool` = other shape ∧ leaks which tools built; one code `tool_not_permitted` ∀ refusal (V83d). (d) `core/servers/whm_repository.py` = READS only (`list_servers`,`get_by_id`); `create`/`update`/`delete` ∧ `SQLWHMServerTokenRepository` ⊥ ported → T54 (`whm_server_tokens` ∉ T4 schema); `get_by_name` folded into resolver's in-memory match ∵ a 2nd round trip can disagree with the list the tie was judged against. (e) `WHMServerRowLike` carries `to_safe_dict()` ⇒ sole sanctioned outward render (V2,V8); T20–T26 need creds off the resolved row ⇒ widen that Protocol ∨ pass the row to `resolve_whm_ssh_config`, **⊥ re-read by id**. (f) mount harness moved `test_mcp_mount.py` → `tests/support/mcp_mount.py` + `McpSession` (handshake, `Mcp-Session-Id`, SSE unwrap) ∵ T19 RBAC tests drive the same real mount (V66). (g) **V45 UNMET at T19** — `tool_runs` = T35, unbuilt ⇒ ∀ tool call unaudited; write path = T73. **CLOSED 2026-08-07 by T73** — `ToolRunAuditMiddleware` beside this gate; ∀ READ now writes a row ∨ the call is refused|I.mcp,C22,V1,V2,V8,V10,V11,V18,V19,V21,V66,V74,V83,R25,T35,T73
T20|.|Tool: `whm_list_accounts` (READ). Port from `noa-old`. Large result → summary + table URL per V64. Mechanism = path A (iframe UI resource) — T59 CLEARED 2026-08-08, ⊥ gated any more (R29)|I.mcp,V64,T59
T21|x|Tool: `whm_search_accounts` (READ). Port from `noa-old`. Filter + limit 1–100. AS BUILT ≠ this line, 8 deviations: (a) `core/integrations/whm/accounts.py` CREATED, ⊥ named in this line — `normalize_whm_account_summary` ported from `noa-old` `whm/tools/result_shapes.py` (tool package there) into `core/` ∵ T20 ∧ T22/T23 preflights need the same field list ∧ 2 copies = one of them reports a field the others ⊥ (V66). WHITELIST, ⊥ passthrough: `listaccts` sends ip/plan/disk+bandwidth counters/theme/locale/partition ⇒ dropped, ∵ result persists in LibreChat's MongoDB (V26). Row with ⊥ `user` dropped WHOLE — `user` = the argument ∀ account CHANGE takes ⇒ a row NOA ⊥ name = a row whose follow-up call has no argument. WHM booleans normalised (`0`/`1` ∧ `"0"`/`"1"`) ∵ `"0"` truthy in Python ⇒ raw read reports a LIVE account suspended; `is_locked` falls back to `suspendlock` incl. on JSON-null (`.get` defaults only on MISSING key). (b) match PER FIELD, ⊥ joined haystack — source built `f"{user} {domain}"` ⇒ a query with a space matched ACROSS the junction (`"acme sho"` = user `acme` + domain `shop.example.com`) ⇒ operator handed a row containing nothing they typed. C13 copy-⊥-rewrite ⊥ extend to carrying a defect forward (V69's shape: upstream provenance ⊥ evidence). (c) truncation STATED — `total_matches` + `truncated` ⊥ in source, ∧ matches sorted by `user` BEFORE the cut. Generalised to **V85** in the same change ∵ the next capped READ ⊥ inherit a rule that lives only in this row. (d) `limit` bound TWICE — schema `Annotated[int, Field(ge=1, le=100)]` (what refuses over MCP ∧ what tells the model the range exists) ∧ in-tool `limit_invalid` (what holds for the in-process call, C9/V17) ⇒ bound = property of the TOOL, ⊥ of its registration. Blank `query` → `query_required` = V21's gate ∧ ⊥ expressible in schema (`min_length` counts whitespace). Both guards run before any I/O — asserted as `servers.reads == 0` ∧ zero WHM requests, ⊥ as a return value. (e) cipher wiring ⊥ named in this line ∧ **SUPERSEDES T15(a)/T16(b)** — those said "T54 builds 1 cipher on `AppRuntime`"; T21 built it (`AppRuntime.secret_cipher` = `SecretCipher.from_settings`, `McpToolContext.secret_cipher`), T54 now READS it. Construction in `build_runtime`, ⊥ lifespan ∵ tool context precedes `FastAPI(...)` (T13(b)) ∧ a bad key ! be a boot failure (T8's rule). Doc sweep same change (V84a): `core/secrets/crypto.py`, `core/integrations/whm/client.py`, `core/integrations/proxmox/client.py` each asserted the future fact as fact. (f) `WHMServerReadRepository` now GENERIC in its row (`Protocol[RowT_co]`) ∧ `resolve_whm_server_ref` preserves that type ⇒ the tool reaches credentials off the row it RESOLVED — ⊥ cast, ⊥ widened `WHMServerRowLike`. T19's `whm_ref` line named the 2 options ∧ ruled out re-read-by-id (2nd query can disagree with the list the tie was judged against); widening instead would let a resolver double be handed to something that DECRYPTS, which is what the narrow view exists to stop. (g) `whm_client_factory` on the context + `transport=` on `build_whm_client` — ⊥ named; WHM = first external HTTP boundary on the tool path ⇒ a seam ! exist, ∧ it doubles the SOCKET only. Real `WHMClient` stays in the path ∵ its whole job = normalising WHM's HTTP-**200**-with-`metadata.result: 0` refusals ⇒ a doubled client lets a tool pass against error shapes WHM never sends. Real cipher ∧ the one decrypt site stay live ⇒ `Authorization: whm root:<plaintext>` is assertable. (h) tests ⊥ named in this line: `test_mcp_tool_audit.py` gains a case driving THIS tool ∵ every other case there drives `whm_list_servers` — the tool that shipped BESIDE the middleware ⇒ the suite ⊥ distinguish "audits ∀ READ" from "audits that one" (V45,V83b); `test_mcp_tool_rbac.py` now pins the registered SET ∧ that one grant names one tool (V1 is per-tool ∧ was unobservable with a single tool registered). 8 mutations proven red: joined haystack, `truncated` off-by-one, no sort, decrypt skipped, both guards removed, schema bounds dropped, `choices` dropped, tool absent from the risk map. ⊥ built here: `whm_list_accounts` (T20 — V64 table surface, T59-gated), `fetch_whm_accounts` = the shared internal both use (C9,V17), DB session closes BEFORE the WHM round trip ∵ a pooled connection held across someone else's server turns a slow WHM host into a DB outage|I.mcp,C7,C9,C13,V8,V18,V19,V21,V45,V66,V69,V71,V83,V84,V85,T15,T16,T19,T20,T54
T22|.|Tool: `whm_suspend_account` (CHANGE). Internal preflight → suspend. Single tool, preflight in-process. ⊥ reason param in schema|I.mcp,C8,C9,V15,V16,V17
T23|.|Tool: `whm_unsuspend_account` (CHANGE). Internal preflight → unsuspend. Not merged with suspend (opposite risk directions, DECISIONS §9). ⊥ reason param in schema|I.mcp,C8,C9,V15,V16,V17
T24|x|Tool: `whm_preflight_firewall_entries` (READ exposed). DECISIONS §6.5 — operator decision point. CSF + Imunify dual check. AS BUILT ≠ this line, 8 deviations: (a) **WHM SSH layer re-signed**, ⊥ named in this line — `run_csf_command`, `run_imunify_command`, `check_csf_binary`, `check_imunify_binary`, `check_firewall_binaries` took `(row, cipher)` ∧ each resolved its OWN config ⇒ 4 resolves per call ∧ the ORM row ! outlive its session across ∀ SSH hop. Now they take a resolved `SSHConnectionConfig`; the tool resolves ONCE inside its session ∧ closes it before the first hop (T21's rule — a pooled Postgres connection held across someone else's host is how a slow WHM box becomes a DB outage). Asserted as ORDER (`session-open`,`session-close`, then ∀ `ssh`), ⊥ as a count. Commands on the wire byte-identical. (b) row refusals now raise AT the tool ⇒ an unpinned server answers `ssh_host_key_not_validated`, ⊥ `no_firewall_backend`. Those 3 (`ssh_invalid_host`,`ssh_not_configured`,`ssh_host_key_not_validated`) were never per-backend — both probes read one row ⇒ both come back "not usable" ⇒ "no firewall tools on this server", which is GH #82's misdiagnosis, the one `sudo_required` exists to prevent. `except SSHExecutionError` STAYS around `ssh_exec`: an unreachable host is still "not usable" ∵ a probe that raises ⊥ report on the other backend. (c) code = `no_firewall_backend` (V57), ⊥ source's `no_firewall_tools`. OPEN, ⊥ fixed: `BinaryCheck` carries ⊥ error code ⇒ host-down ∧ binary-absent collapse to one code; message worded "could not be run" so the SENTENCE stays true either way — the diagnosis ⊥ recovered, threading a code out of the probe = T25/T26's call. (d) `raw_output`/`raw_data` DROPPED — source shipped csf's whole `-g` dump ∧ imunify's whole JSON document beside the parsed verdict; DECISIONS §6.5 (old V75) says the `csf.deny`/`csf.allow` line ∧ ⊥ a raw iptables table, ∧ the result persists in LibreChat's MongoDB (V26). Bounded `matches` = the evidence. (e) partial answers STATED — source computed the combined verdict from whichever backend succeeded ∧ else fell through to `not_found` ⇒ a broken CSF beside a clean Imunify reported "⊥ blocked" on a box whose BLOCKING backend was silent. `unanswered_backends` names them; zero answers ⇒ `unknown`, ⊥ `not_found`; CSF's own `unknown` counts as ⊥ answered. **Generalised to V86 in the same change** ∵ T25's 2-part receipt is the 2nd instance. (f) V85's **2nd capper** ⇒ `CSFGrepParsed` gains `total_matches` (the cut is the parser's, so the count is too) ∧ the payload carries `total_matches`+`truncated`. ORDER clause deviates — see V85's amended instances line. (g) evidence ⊥ duplicated per backend — combined `matches` at top level, each backend entry = its verdict ∨ its failure code; the lines already name their source, ∧ a per-backend copy doubles the transcript to say it twice. (h) ⊥ named in this line: `tests/support/whm_firewall.py` CREATED (`FakeFirewall`, `firewall_context`, the 3-module `ssh_exec` install) ∵ T25/T26 = the next callers ∧ `support/whm.py`'s `__all__` is pinned to `FakeWHMServer` by `test_support_layout.py`; `install_fake_ssh_exec_in(monkeypatch, modules, handler)` added to `support/remote_exec.py` with the 1-module helper delegating (V66) ∵ a firewall call crosses 3 modules that each bound their own `ssh_exec`; `whm_server()` gains `ssh_password`/`ssh_private_key`/`ssh_host_key_fingerprint` overrides ∵ the defaults are ciphertext-SHAPED ∧ ⊥ decrypt. 12 mutations proven red: both guards, sequential-⊥-gather, query-an-unavailable-backend, `answered` ignoring failure ∧ CSF-`unknown`, `unknown`→`not_found` fallthrough, truncation unstated, capped-length-as-total, pin ⊥ required, sudo collapsed into no-backend, session held across SSH. ⊥ closes T68 — that row's subject = a silent no-op on an approved CHANGE (T25/T26), ⊥ this READ. Doc sweep same change (V84a): `docs/integrations/whm.md`, `core/remote_exec/sudo.py:37` (comment named `no_firewall_tools` as V57's code — false since T2), `whm/__init__.py`, `availability.py`, `csf_cli.py`, `imunify_cli.py`, `csf.py`, `mcp_tools/__init__.py`|I.mcp,C9,C13,V8,V18,V19,V21,V26,V54,V55,V57,V66,V69,V83,V84,V85,V86,T21,T25,T68
T25|.|Tool: `whm_firewall_release_and_allow(server_ref, target, duration_minutes)` (CHANGE merged). `duration_minutes` REQUIRED, 1–525600, LLM converts operator phrasing ("5 min", "2 hours", "5 days") → minutes. Internal: release from denylist → add to allowlist with TTL. One approval, two-part receipt (before-state = block reason + log evidence, after-state = released & allowlisted + expiry timestamp). `sudo -n` where needed|I.mcp,C8,C9,V54,V55,V77
T26|.|Tool: `whm_firewall_allowlist_remove` (CHANGE). Undo path. `sudo -n` where needed|I.mcp,C8,V54,V55
T27|.|Tool: `proxmox_reset_vm_password` (CHANGE). Internal preflight → generate password → yopass store → set cipassword + regenerate cloud-init + verify. yopass fail → abort before set. libcrypt absent → `verification_unavailable`. ⊥ reason param in schema|I.mcp,C8,C15,V49,V62
T28|.|Tool: `proxmox_vm_nic(action: enable\|disable)` (CHANGE merged). Single tool, enum `action` param. Internal preflight. DECISIONS §9 enum collapse|I.mcp,C8,C9,V63
T29|.|Tool: `pmg_whitelist(action: add\|remove)` (CHANGE merged). Single tool, enum `action` param. Internal preflight → `pmgsh` command. DECISIONS §9 enum collapse|I.mcp,C8,C9,V58,V60,V61
T30|.|Tool: `pmg_whitelist_list` (READ). List all mynetworks CIDRs. Large result → summary + table URL per V64. Mechanism = path A (iframe UI resource) — T59 CLEARED 2026-08-08, ⊥ gated any more (R29)|I.mcp,V58,V64,T59
T31|x|Tool: `pmg_whitelist_search` (READ). Normalize input → search exact CIDR membership. Discovery step in front of T29 — operator holds an address ∧ needs to know whether adding it is a change ∨ a no-op. AS BUILT ≠ this line, 4 deviations: (a) **PMG CLI layer re-signed**, ⊥ named in this line — `run_pmgsh_command` ∧ ∀ `run_pmg_*` took `(row, cipher)` ∧ resolved INSIDE ⇒ the tool ! hold its DB session across the SSH hop ∵ an ORM row ⊥ readable once the session closes. Now they take a resolved `SSHConnectionConfig`; the tool resolves ONCE inside its session ∧ closes before the hop (T21's rule; T24(a)'s change, one system over). Asserted as ORDER (`session-open`,`session-close`,`ssh`), ⊥ as a count. Command on the wire byte-identical. 2 consequences: row refusals (`ssh_invalid_host`,`ssh_invalid_port`,`ssh_not_configured`,`ssh_host_key_not_validated`) now raise AT the tool ⇒ they name the PMG SERVER, ⊥ arrive as `pmgsh_command_failed` (T24(b)'s shape, V82); ∧ `require_host_key_fingerprint` becomes the CALLER's — this module hard-coded `True`, which is exactly the knob T54's TOFU capture needs. **SUPERSEDES T18(d)'s row+cipher signature.** (b) `ERROR_UNKNOWN` hoisted `mcp_tools/whm_read.py` → `mcp_tools/results.py` ∵ `whm_firewall` already imported it ACROSS systems ∧ `pmg_read` = the 3rd caller ⇒ one import from a 2nd spelling (V66); ⊥ re-export left behind (T72's line). (c) result shape ≠ `noa-old`'s. DROPPED `id`/`ip`/`object_type`/`raw.line` (T24(d) — result persists in LibreChat's MongoDB, V26). ADDED `total_entries` ∵ `exists:false` @ 0 parsed entries ≠ `exists:false` @ 300, ∧ `pmgsh ls` output ⊥ separate an empty `mynetworks` from a format NOA ⊥ recognise ⇒ the count is the only thing showing WHICH one was got; ⊥ a refusal ∵ an empty whitelist is a real state ∧ refusing it would be the false alarm. `normalized_target` + the entry's RAW spelling both ship (V59 as amended) — the raw form is what an operator finds on the box ∧ what T29's removal ! name. (d) parser departs `noa-old` TWICE (T21(b): a port carries the code, ⊥ the defect) — the first parsable candidate ENDS the line (source skipped an already-`seen` candidate ∧ fell through to the ID column) ∧ duplicates KEPT, ⊥ deduped while parsing (2 spellings of one address = a fact about the whitelist, ∧ T29 ! know both lines exist). ⊥ named in this line: `core/servers/pmg_repository.py` ∧ `core/servers/pmg_ref.py` ∧ `core/integrations/pmg/mynetworks.py` — T18 deferred ∀ 3 to T29–T31; reads only, CRUD → T54. Ref resolution = a SIBLING of `whm_ref`, ⊥ a shared resolver: `core/servers/__init__` recorded at T19 that the THIRD system (Proxmox, T27/T28) is where a shared abstraction stops being a guess about what varies (V66) — policy identical (id → name → host, any tie = `choices`), row differs at both ends (WHM parses a host out of `base_url`, PMG stores it bare; `choices` fields differ). Also ⊥ named: `McpToolContext.pmg_server_repository_factory`; test-support inventory doubles for BOTH systems → `support/servers.py`, `support/pmg.py` keeping the integration-layer row + tool-path helpers (V66, the split `support/whm.py`/`support/whm_firewall.py` already has). V85 ⊥ apply (exact membership caps nothing — V85's own instances line says so), V86 ⊥ apply (1 source, ⊥ N). 10 mutations proven red: containment-for-exact, unnormalised target, a 2nd command on the read path, the SSH hop inside the session, the parser fall-through, pin ⊥ required, `total_entries` dropped, an ambiguity resolved by picking first, blank-target guard removed, raw spelling replaced by the normalised form. Doc sweep same change (V84a): `docs/integrations/pmg.md`, `core/integrations/pmg/__init__.py`, `core/servers/__init__.py`|I.mcp,C9,C13,V8,V18,V19,V21,V26,V48,V55,V58,V59,V66,V69,V82,V83,V84,T18,T21,T24,T29,T54
T32|.|CHANGE gate: `build_change_gate_response()` — single function, 3 branches (link-out text \| UI resource \| elicitation future). ACTIVE = UI resource + link-out text beside it, ⊥ either/or (V24, V25; T59 cleared 2026-08-08). Resource shape MEASURED, ⊥ guessed: `ui://noa/approval/<id>` (LibreChat's classifier needs the scheme, R31c) + `mimeType: text/uri-list` (the `src` render mode, R12) + the URL as body. URL carries `action_request_id` only|V24,V25,V26,R29,R31,T59
T33|x|CHANGE gate: MCP `tools/call` on CHANGE → INSERT `action_requests(status=pending)` + `approval_context` (persisted) + `conversation_ref`. ⊥ `proposed_reason` column — reason arrives at approve time only (C8,V43). "May this run?" from DB. AS BUILT ≠ this line, 8 deviations: (a) gate = a FUNCTION the tool calls, ⊥ a middleware — every other cross-cutting rule on the tool path is one ∵ a tool ⊥ forget a middleware (V83b), ∧ this one ⊥ be: C9/V17 put the preflight IN-PROCESS inside the CHANGE call ⇒ the evidence the card shows is the tool's own ∧ a middleware sitting outside it has none. What replaces that property: `core/approvals/repository.py` = the ONLY writer of a PENDING row ∧ it writes ⊥ other status ⇒ a CHANGE tool that skipped the gate ∧ executed has ⊥ authorization row at all, ∧ V23 reads the row. (b) `approval_context` holds ONLY what ⊥ COLUMN holds — `arguments` (redacted), `requester` (`email`+`librechat_user_id`), `evidence`. ⊥ `tool_name`, ⊥ `conversation_ref`, ⊥ a `requested_at`: ∀ 3 are columns (T34) ∧ a copy = 2 records of one moment that can disagree — T34's own argument for dropping `args`,`risk`,`decided_by_user_id`,`updated_at`. Requester EMAIL persisted ∵ the FK is `SET NULL` ⇒ a join at render time loses the identity of a decision that was made. (c) **empty preflight evidence REFUSED** (`change_evidence_required`, ⊥ named in this line, owner-decided at plan time) — T34's model docstring already asserts "the gate always holds provenance + args + in-process preflight (V17,V35)" ⇒ this makes that sentence a MECHANISM ∧ T22–T29 inherit it by construction, ⊥ by each remembering (T68's rule). (d) C8 bound AT THE DOOR, ⊥ only in the schema: `assert_no_reason_argument` refuses `reason`,`proposed_reason`,`change_reason`,`approval_reason`,`justification`. EXPLICIT set, ⊥ substring — a future `reason_code` ⊥ what C8 is about ∧ a guessing rule refuses a change for the wrong cause (case asserted). TOP LEVEL only — a reason = a first-class param ∨ ⊥ a reason; `{"payload":{"reason":…}}` = an integration's own shape. + a registry SWEEP over `await server.list_tools()`: VACUOUS today (0 CHANGE tools), written anyway per V85's rule (the rule ! exist BEFORE the 2nd instance), with a PROBE tool proving the predicate separates ⇒ ⊥ a tautology (V87). (e) `pending_ttl_seconds` on `McpToolContext`, REQUIRED ⊥ defaulted (⊥ named in this line) — `expires_at` = NOT NULL by T34 ⇒ the gate ! compute it, ∧ the TTL is settings with ONE `get_settings()` caller (T5) ⇒ a default here = a 2nd answer that drifts silently. Test-support TTL = 900 ∵ 3600 = `Settings`' own ⇒ unseparable from a hardcoded literal; the mount-wiring test builds settings at 1234 for the same reason — `mounted_app`'s harness carries the production default ∧ the first version of that assertion passed against a hardcoded `create_app` (V87's shape, caught by MUTATION ⊥ by reading). (f) repository = PENDING ∧ nothing else: `status` ⊥ a parameter, ⊥ update method, ⊥ read method (T37/T63 add theirs when they need them). V28's transition = T37's, under a row lock, off a cookie POST (V22) ⇒ a writer that could set APPROVED = a 2nd door on the authorization, on the side an LLM reaches. Asserted on BOTH the Protocol ∧ the SQL class — a Protocol-only `decide` survived the first version of that test. (g) errors = `ChangeGateError` tree, `NoaError` subclasses ∵ `sanitize_tool_errors` passes a NoaError's own code through (V19) ∧ T37 raises from the same tree into HTTP (V73). `change_gate_unavailable` 503 (fail-closed on the INSERT, T73(c)'s argument one table over); the 2 programming-error codes 500, ⊥ 400 — a reason-shaped arg ∨ a skipped preflight is NOA's bug ∧ 400 would blame the caller for it. (h) **⊥ EXERCISED: a CHANGE `tools/call` over the real mount.** T22–T29 = `.` ⇒ nothing reaches this gate through `tools/call`; tests drive `open_change_request` inside the REAL identity ∧ header contextvars (`http_request_context` + `AuthenticatedUser`), ⊥ patched names — same shape as T73's CHANGE branch, ∧ T22 = where the gate first runs over the mount. ALSO ⊥ built here: `build_change_gate_response()` (T32) ⇒ the gate returns `PendingChangeRequest` ∧ what the MODEL sees is still unwritten. 14 mutations proven red: reason guard dropped, reason guard widened to substring, evidence guard dropped, requester read from arguments, TTL hardcoded (in the gate ∧ in `create_app`), redaction skipped, commit dropped, fail-open on write failure, `tool_name` duplicated into the context, `conversation_ref` dropped, repository writes APPROVED, `create_pending` accepts a `reason`, a 2nd writer can decide. Doc sweep same change (V84a): `mcp_tools/__init__.py` (approval gate stated entirely "still to come"), `mcp_tools/context.py` (stated T33 "will" make a CHANGE tool write `action_requests` ∧ share ONE session with its audit event — false twice: the gate writes ⊥ audit row at all, T73(a))|V8,V19,V22,V23,V32,V33,V35,V43,V66,V73,V83,V84,V85,V87,I.mcp,T21,T32,T34,T37,T38,T73
T34|x|`action_requests` table + migration: `id`, `tool_name`, `requested_by_user_id`, `status` (`ActionRequestStatus` PENDING/APPROVED/DENIED/EXPIRED), `conversation_ref`, `approval_context` JSONB, `reason` (nullable until decided, operator-typed), `tool_run_id` nullable, `expires_at`, `created_at`, `decided_at`. ⊥ `proposed_reason` column. This row IS the authorization — V23 reads `status` ∀ time, ⊥ a cache of a decision held elsewhere. AS BUILT ≠ this line, 6 deviations: (a) **BOTH FKs `SET NULL` + nullable**, ⊥ named here — `noa-old` CASCADED `requested_by_user_id`. An approved CHANGE = an audit artifact (V46) ∧ T36's receipts FK into this row ⇒ CASCADE lets ONE user delete erase both what was authorised ∧ the receipt proving it ran (T35's argument for `tool_runs`, one table over). Fails CLOSED vs V27: NULL matches ⊥ caller ⇒ requester-match 404s. Asserted by a real `DELETE`, ⊥ by reading `ondelete` off metadata — that is a string until something deletes. (b) `approval_context` carries **⊥ server default**, deliberately ≠ `tool_runs.args` (`'{}'` there so "took ⊥ args" ≠ "⊥ recorded") — an empty context is ⊥ a legitimate state here ∵ the gate always holds provenance + args + in-process preflight (V17,V35) ⇒ an insert omitting it FAILS, ⊥ records a card with nothing on it (V33). (c) 4 `noa-old` columns ABSENT ∧ each bound by test, ⊥ by prose — this line names only `proposed_reason`: `args` (→ inside `approval_context`, V33 = one payload, one moment ⇒ 2 records of it can disagree), `risk` (→ `tool_runs`, which is what makes a failed READ writable; `noa-old` put it HERE instead ∧ that is precisely why its audit ⊥ say whether a run was a change), `decided_by_user_id` (V27 makes decider = requester ⇒ a 2nd identity column = 2 truths about one person), `updated_at` (V28 permits 1 mutation ∧ `decided_at` stamps it). ∧ the V43 guard scans **∀ of `Base.metadata`**, ⊥ this table ⇒ T36's receipts inherit the ban UNLISTED (`test_config.py`'s git-ls-files pattern, one axis over). (d) index set ⊥ named ∧ deliberately SMALLER than `tool_runs`': `(status, expires_at)` composite + `requested_by_user_id`. ⊥ `tool_name`, ⊥ `conversation_ref`, ⊥ `created_at` — nothing filters ∨ orders by them today, the discipline that left `risk` unindexed at T35. Composite ORDER asserted, ⊥ only membership: `status` LEADS ∵ T39's sweep = `status=PENDING AND expires_at < now()` ∧ a leading `status` serves status-only lookups too ⇒ ⊥ 2nd index; led by `expires_at` the sweep scans rows already decided. (e) `reason` = unbounded `Text`, ⊥ capped like `result_summary`(2000) ∵ ⊥ machine writes it ⇒ ⊥ result body to invite in, ∧ truncating the field that AUTHORISES a change is worse than storing a long one. T37 bounds the input at the endpoint. (f) **CLOSED 2026-08-08 by T37** (OPEN at T34): `ck_action_requests_decided_reason`, migration `0005_decided_reason_check` — `status NOT IN ('APPROVED','DENIED') OR (reason IS NOT NULL AND reason ~ '[^[:space:]]')`. T34 left the SHAPE to T37/T39 ∵ V15 named the approve ENDPOINT as the gate ∧ EXPIRED = decided-terminal with nobody to have typed one; T37 required a reason on DENY too (V15 as amended) ⇒ both decided states fit ONE predicate, ∧ PENDING/EXPIRED stay outside it so T39's sweep is still writable. 2 traps caught by TEST, ⊥ by reading: `btrim()` strips SPACES only ⇒ a one-tab reason passes it; ∧ `NULL ~ '…'` = NULL, which SATISFIES a CHECK ⇒ the `IS NOT NULL` arm is load-bearing, ⊥ redundant — without it the constraint catches every blank STRING ∧ waves through the NULL, the one case that matters most. T33 inherits 2 properties by CONSTRUCTION, ⊥ by discipline: it ! set `expires_at` ∧ ! write `approval_context` — ⊥ default on either ⇒ omitting one is an insert failure, ⊥ a silent NULL. 9 mutations proven red: `approval_context` given a default, `expires_at` made nullable, `EXPIRED` dropped, a `proposed_reason` column added, EITHER FK cascaded (2 runs), sweep index led by `expires_at`, PENDING default dropped, migration CHECK disabled. Doc sweep same change (V84a): `core/db/lifecycle.py` ∧ `core/db/models.py` both stated this table ⊥ exist yet, `test_schema_v1.py` held it in `NOT_YET_TABLES`, `test_tool_runs_schema.py::test_action_request_status_has_not_landed_early` asserted the enum ⊥ landed; `support/database.py` `MUTATED_TABLES` += it ∵ SET NULL on BOTH FKs ⇒ a leftover row outlives ∀ other row a test created|V20,V23,V27,V28,V32,V33,V43,V46,V84,T33,T35,T36,T37,T39
T35|x|`tool_runs` table + migration: `id`, `tool_name`, `requested_by_user_id`, `risk` (`ToolRisk` READ/CHANGE), `status` (`ToolRunStatus` STARTED/COMPLETED/FAILED), `conversation_ref`, `args` JSONB (redacted), `result_summary`, `timing`, `created_at`. Risk ∧ status = separate columns ⇒ failed READ representable|V20,V45,V46,V47
T36|.|`action_receipts` table + migration: `id`, `action_request_id` FK, `tool_run_id` FK, `receipt_data` JSONB, `created_at`|V46
T37|x|Approve/deny endpoints: `POST /action-requests/{id}/approve` (body `{reason, csrf}`) → validate non-blank `reason` else 409 `change_reason_required` → row lock → CAS pending→approved → async execute → 202 `{tool_run_id}`. `POST /action-requests/{id}/deny` → row lock → CAS pending→denied. Mismatch → 404, already decided → 409. AS BUILT ≠ this line, 11 deviations: (a) **executor SEAM, ⊥ the executor** (T38 `.`) — `ApprovedChangeExecutor` Protocol (`runtime_checkable` so `deps._from_state` type-guards it) on `AppRuntime`; today's only impl = `DeferredApprovedChangeExecutor`, which LOGS `approved_change_execution_deferred` ∧ starts nothing. ⊥ a silent no-op, deliberately: every call = a run that sits STARTED until T38. INERT ∵ T22–T29 `.` ⇒ ⊥ CHANGE tool exists to open a request ⇒ ⊥ operator can reach these routes in production — which is what makes the placeholder safe ∧ why correctness here is carried by TESTS, ⊥ by use. (b) **CSRF built here, ⊥ at T46** (V39 as amended) — owner decision at plan time; a `.` row is ⊥ a guard. (c) **expiry check-on-read at the decision door, ⊥ deferred to T39** (V32 as amended) — 3 lines, ∧ the alternative ships a window in which V32 is false. (d) **reason required on DENY** — T34(f)'s question, answered, + the DB CHECK that closes it (V15 as amended). (e) **decision writer = a SEPARATE class in a SEPARATE module** — `core/approvals/decisions.py`, ⊥ a method on T33's `SQLActionRequestRepository`, ∵ that one is reached from the MCP path ∧ a class that could write APPROVED there = a 2nd door on the authorization, on the side V22 closes. Nothing puts it on `McpToolContext`; asserted 2 ways — the tool context carries ⊥ decision type, ∧ T33's repository's public surface is pinned to `{create_pending, commit}`. (f) `start_change_run` lives ON the decision repository, ⊥ taken as a caller-supplied collaborator ⇒ "same session" is CONSTRUCTION, ⊥ an obligation (V29). `risk` fixed to CHANGE, ⊥ a parameter: V16 makes every row here a change, ∧ a parameter is somewhere for READ to be written into an approved change's audit row. (g) ORDER = CSRF → reason → lock. CSRF first ⇒ a forged cross-site POST never touches the DB ∧ every id answers 403 alike; blank reason before the lock ⇒ a submit that cannot succeed ⊥ queue behind someone else's transaction. Neither ordering leaks existence — a CSRF token is only obtainable from a card V27 already let the caller see. (h) errors = `ActionDecisionError`, a SIBLING tree to `ChangeGateError`, ⊥ a subclass — that tree's messages say "could not be submitted for approval", which is a sentence about a different moment, ∧ its base maps to 503 where a 404 ∧ a 409 live here. 5 members, each mapped by name (V73). (i) `lock_for_decision` → a frozen `LockedActionRequest`, ⊥ the ORM row: the service is what decides, ∧ a live ORM instance lets it write columns this design ⊥ permit it to write. (j) `reason` bounded at 2000 AT THE ENDPOINT (T34(e)'s "T37 bounds the input") → 422; blank ⊥ a pydantic constraint ∵ `min_length=1` answers 422 ∧ V15 names 409. (k) routes on the API (`noa_api/api/routes/action_requests.py`), ⊥ the embed app — §I.embed lists them ∵ the embed is their only caller. ⊥ built here: the GET detail (T41), a CSRF MINTING route (T41), the real executor + reaper + V31's cap (T38), the sweep ∧ the render-path check (T39). Gotcha, recorded ∵ it cost a full test-module cascade ∧ will recur: an alembic revision id ! be ≤ 32 chars (`alembic_version.version_num` = `VARCHAR(32)`) — a longer one fails AFTER the DDL, on the bookkeeping UPDATE. ⊥ an invariant: it fails loud at `alembic upgrade`, in-transaction, pre-merge. **20 mutations proven red**: csrf verify removed; csrf bound to session-⊥-request; csrf bound to request-⊥-session; `compare_digest` → `==`; key derivation loses its label; csrf age check dropped; `FOR UPDATE` dropped; status guard dropped; requester-match dropped; 404 → 403; blank-reason guard dropped; expiry check dropped; expiry refuses without writing; the run committed in its own transaction; executor handed the run before the commit; a denial links a run; the approved run classified READ; the CHECK disabled; the CHECK loses its `IS NOT NULL` arm; the CHECK uses bare `btrim`. **The `FOR UPDATE` one survived the first 2 versions of its test — B5, V89.** Doc sweep same change (V84a): `core/approvals/__init__.py` ("nothing here decides anything" — false now), `core/approvals/repository.py` (T37's transition now names the class ∧ the pin on this repository's surface), `core/audit/tool_runs.py` (named T38 as the 2nd `tool_runs` writer; T37 is), `core/db/models.py` (`ToolRun` ∧ `ActionRequest.tool_run_id` both said the executor opens the row), `noa_api/api/__init__.py`, `api/routes/__init__.py`, `main.py`, `mcp_tools/__init__.py` ("the endpoints (T37)" under still-to-come), `docs/spikes/librechat-embed-render-gate.md:124`|V15,V22,V23,V27,V28,V29,V32,V39,V46,V47,V66,V73,V84,V87,V89,I.embed,T33,T34,T38,T39,T41,T46
T38|.|Async executor: in-process asyncio task with own DB session. `execute_approved_tool_run(tool_run_id)`. Reaper covers BOTH runs stuck in STARTED and requests stuck APPROVED with no run started. Per-user concurrency cap. **SEAM ALREADY WIRED (T37)**: `ApprovedChangeExecutor` Protocol + `AppRuntime.approved_change_executor`, handed `(tool_run_id, action_request_id)` AFTER the decision commits; placeholder = `DeferredApprovedChangeExecutor`. T38 owes the real task, the reaper, V31's cap, ∧ the `finish_run` that moves the STARTED row T37 already opened — ⊥ the row's creation, ⊥ the link (both are T37's, inside the decision's transaction)|V29,V30,V31,T37
T39|x|Pending expiry: background sweep AND check-on-read (both, ⊥ either/or) — status=PENDING & past deadline → EXPIRED. Sweep guarantees V32 terminality without traffic; check-on-read prevents serving a stale PENDING. Approve/deny on expired → 409. Check-on-read at the DECISION door landed at T37; the sweep ∧ the render path landed here. AS BUILT ≠ this line, 11 deviations: (a) **`<=`, ⊥ the `<` this line carried** — the door compares `expires_at <= decided_at` (T37), ∧ a row exactly ON its deadline ! be ONE thing: `<` here = refused at the door ∧ left PENDING by the sweep ⇒ a row that can ⊥ be answered ∧ ⊥ become terminal. Both comparisons pinned in ONE test (`test_a_request_exactly_on_its_deadline_expires_at_both_doors`) ⇒ an edit to either door has to break it to land; 2 mutations red, one per door. (b) **A THIRD writer class**, ⊥ a method on T37's — `core/approvals/expiry.py::SQLActionRequestExpiryRepository`, whose only reachable terminal status is EXPIRED (`status` ⊥ a parameter, predicate part of the statement). Same argument that split T33 from T37, one door further: a background loop has ⊥ operator behind it ∧ a GET is ⊥ a decision ⇒ neither ! hold a writer that can set APPROVED (V22). (c) **⊥ `SELECT … FOR UPDATE` on the sweep — deliberate, ⊥ an omission.** A bare `UPDATE` takes its own row locks ∧ under READ COMMITTED re-evaluates its WHERE vs the row version the transaction it waited for committed ⇒ a sweep colliding with an in-flight approval SKIPS the row, ⊥ expires a change already handed to an executor. An explicit lock = a 2nd mechanism to keep in step with the 1st for zero gain. Proven on the V89 rig: approval HELD OPEN between its locked read ∧ its commit, sweep issued INSIDE that window, assertion on ORDER (`sweep:returned` after `approve:committed`), + the negative control (a plain SELECT returns BEFORE that commit ⇒ the handshake genuinely overlapped). The 2 moments differ on purpose — the approval judges vs a `now` before the deadline, the sweep vs one after: that IS the case, a TTL passing while a human decides. (d) ONE predicate, one definition (`status = PENDING AND expires_at <= now`) — sweep ∧ check-on-read = the SAME `UPDATE` with an id added, ⊥ 2 queries with 2 spellings of "which rows are due". (e) Sweep writes `reason = NULL` EXPLICITLY. NULL on ∀ PENDING row by construction, but T34's CHECK EXEMPTS EXPIRED ⇒ ⊥ at the DB level would catch an EXPIRED row carrying one. Held by a test that seeds one by hand — else V69's shape, a property asserted by prose ∧ held by nothing. `tool_run_id` LEFT alone: ⊥ invariant says an expiry clears a link, ∧ clearing one = a destructive write with nothing behind it. (f) **Loop SLEEPS FIRST.** A pass at boot makes ∀ app start touch Postgres ∧ V51 says `/health` answers with Postgres down; one interval (60s) against an hour-long TTL buys nothing worth that. (g) Loop SWALLOWS per pass — `except Exception`, ⊥ `BaseException` ∵ `CancelledError` ∈ BaseException @ 3.11+ ⇒ `stop()` still stops it. A guarantee that ends the first time Postgres blinks ⊥ a guarantee. `run_once` RAISES: the LOOP is what swallows, so a caller driving one pass sees the failure. (h) Host = `PendingExpirySweeper` on `AppRuntime`, started by the lifespan ∧ stopped BEFORE `engine.dispose()` — a pass in flight would otherwise run vs a disposed pool, a shutdown-time error in the one component whose job is working when things go wrong. Ordering asserted, ⊥ described. (i) **Check-on-read ships as MECHANISM + request dependency** (`get_action_request_expiry_service`), ⊥ a live route: T41 ∧ T63 = `.` ⇒ ⊥ GET exists to call it. T37's executor-seam shape — correctness carried by TESTS, ⊥ by use. (j) `APPROVAL_EXPIRY_SWEEP_INTERVAL_SECONDS` (default 60, `ge=1`) = a RESOLUTION, ⊥ a lifetime — how late a request may still READ pending — ∧ deliberately ⊥ derived from the TTL. (k) 3 V66 moves same change: `_now`/`_as_utc` out of `decisions.py` → `core/approvals/clock.py` (2 doors compare a deadline ⇒ 1 definition of now); `SessionFactory` Protocol → `core/db/session.py` (`McpSessionFactory` states the same shape app-side ∵ `core/` ⊥ import an app); the live row helpers ∧ `ObservedDecisionRepository` → `support/action_decisions.py` (2 live files race the same window, ∧ 2 copies of that instrument = 2 things that can silently stop overlapping). **9 mutations proven red**: sweep predicate loses `status = PENDING`; sweep `<=`→`<`; door `<=`→`<`; check-on-read loses its id filter; sweep stops clearing `reason`; loop ⊥ survives a failing pass; loop sweeps before it sleeps; `stop()` drops the task ⊥ cancels it; lifespan stops the sweeper AFTER disposing the engine. A 10th ⊥ red ∧ recorded ∵ it will recur: dropping `task.cancel()` while KEEPING `await task` makes `stop()` await an infinite task ⇒ the suite HANGS, ⊥ fails — a timeout is a worse signal than a red, ∧ a mutation that hangs proves nothing. Doc sweep same change (V84a): `core/approvals/__init__.py` ("Four modules" — six now, ∧ what the 3rd writer is for), `core/db/models.py` (index comment still said `expires_at < now()`), `core/db/lifecycle.py` (`ActionRequestStatus` named T39 as still to come), `.env.example`|V20,V23,V28,V30,V32,V51,V66,V67,V84,V87,V89,T37,T41,T63
T40|x|Embed app scaffold: `apps/web-embed` — Next.js 16, own `package.json`, `tsconfig`, `eslint`, `vitest`, `playwright`. Hygiene: `.ts≤300`, `.tsx≤450`. AS BUILT ≠ this line, 9 deviations: (a) **⊥ BIGSU, ⊥ Tailwind, ⊥ `.npmrc`** — owner-decided at plan time, ∧ the asymmetry was already in the spec: T47 names `@gio/bigsu-ui`, `@gio/bigsu-icons` ∧ the registry `.npmrc`; this line names ⊥ one of them. The card = a form ∧ 2 buttons in a small iframe ⇒ a design system + an internal-registry dependency on the embed's build is cost without return. Held by an eslint `no-restricted-imports` rule on `@gio/*`, ⊥ by prose (V84c) ⇒ adopting BIGSU here ! DELETE the rule on purpose. Dropping Next.js ENTIRELY was raised ∧ refused: it collides with C2 ∧ C12 ∧ T56 (§8.8 puts the table surface HERE, ⊥ `apps/api`), ∧ a static bundle ⊥ set `frame-ancestors` (T45,V41), ⊥ proxy the cookie server-side (T44 + AGENTS.md "browser ⊥ call FastAPI direct"), ⊥ hand the card a server-minted CSRF token off-URL (T41,V26,V39), ⊥ render the 401 state (V38) — 4 properties that ! have a server, ∧ the only other server is the one §8.8 rules out. (b) **`/healthz` BUILT here**, ⊥ named in this line — the one §I.embed route ⊥ other §T row owns, ∧ a scaffold with zero routes has nothing for lint/build/vitest/playwright to be green ABOUT. Touches nothing else, deliberately: a liveness probe that calls a dependency reports the DEPENDENCY's outage as this app's, ∧ a restart is then the wrong remedy. (c) **2nd eslint firewall rule** — `apps/admin-web` unimportable from here; C12's "⊥ shared source, ⊥ shared deps, ⊥ aliases" lived in AGENTS.md prose ∧ nothing held it. (d) **3 pins deliberate, ⊥ latest**: `typescript 6.0.3` ⊥ 7.0.2 ∵ `typescript-eslint@8.66.0` declares `typescript: >=4.8.4 <6.1.0` ⇒ TS7 puts lint ∧ typecheck on DIFFERENT compilers; `eslint 9.39.5` ⊥ 10.8.1 ∵ 9.x = the line this plugin set is proven against (C13); `@types/node 24.13.3` ⊥ 26.2.0 ∵ types track the RUNTIME major — a types package a major AHEAD describes APIs that are ⊥ there ∧ `tsc` then accepts code the deploy ⊥ run. Recorded ∵ it will recur: pnpm 11's `minimumReleaseAge` policy REFUSED `@types/node@26.2.0` (published <24h) ∧ auto-wrote a `pnpm-workspace.yaml` carrying an exclude for it — the exclude was DELETED ∧ the pin moved instead, ∵ a committed policy bypass for a types package is a worse artifact than a version choice. ⊥ `pnpm-workspace.yaml` in tree ⇒ ⊥ a workspace; own `pnpm-lock.yaml` committed (C12). (e) **`@testing-library/*` ⊥ installed** — nothing renders yet ∧ a dependency with ⊥ consumer is dead weight; lands with the card (T41). `vitest` still configured `environment: 'jsdom'` ∵ ∀ test this package grows renders DOM, ∧ route handlers keep working under it (Node's own `Request`/`Response` survive names jsdom ⊥ define). (f) **Playwright `webServer` waits on the SOCKET (`port: 3001`), ⊥ on `/healthz`** — MEASURED, ⊥ reasoned: the first version waited on the route, ∧ the `/healthz`-answers-503 mutation came back as a 120s startup TIMEOUT instead of `Expected: 200 Received: 503`. **Generalised to V90 in the same change** ∵ T41–T46 ∧ T56 each write their own e2e. (g) **hygiene scan = `git ls-files --cached --others --exclude-standard`**, ⊥ the bare `ls-files` `test_config.py` uses — tracked-only went red on its own first run at 0 files scanned, ∧ a brand-new oversized file stays invisible until `git add` ⇒ the cap would first speak at commit time, after the author stopped looking. The `scanned > 0` assertion is LOAD-BEARING, ⊥ decoration: an empty file list satisfies EVERY limit ⇒ a broken `ls-files` call reads as compliance (V87's shape). (h) **`AGENTS.md` ∧ `CLAUDE.md` inside `apps/web-embed` = written by `next dev` ITSELF**, committed per the note they carry — removing them from a diff only re-creates the uncommitted change. ⊥ NOA-authored guidance; T47 inherits the same pair. (i) `next.config.ts` pins `turbopack.root` ∧ `outputFileTracingRoot` to this package + `output: 'standalone'` (T60's input) ∵ ⊥ workspace ⇒ a lockfile higher up the monorepo otherwise drags unrelated files into the watch set ∧ into the standalone bundle. **4 mutations proven red**: caret on `next`; a 301-line `.ts` under `src/`; `/healthz` answering 503 (unit ∧ e2e — 2 lanes, 1 mutation); the `webServer` wiring deleted. ⊥ built here: CSP `frame-ancestors` (T45), the same-origin `/api/[...path]` proxy ∧ root-`.env` loading (T44), the card (T41), the table surface (T56), `Dockerfile` (T60), CI (T61). Doc sweep same change (V84a): `apps/web-embed/README.md` said "Not scaffolded yet" ∧ "Blocked on §T.59" — both stale since the gate cleared 2026-08-08; root `README.md`'s per-web-app check list gained the e2e lane|C2,C12,C14,I.embed,V65,V67,V84,V90,T41,T44,T45,T47,T56,T60,T61
T41|.|Embed: `/approvals/[id]` page — GET detail, render approval card (provenance, before-state, evidence, reason input, Approve/Deny buttons). CSRF token. Compact, self-scrolling. Buttons ! be JS-`fetch` handlers, ⊥ `<form>` submit (V80)|I.embed,V35,V34,V80
T42|.|Embed: approve/deny POST handler — JS `fetch` to NOA API with `noa_session` cookie + CSRF (⊥ form submit, V80/R13). Poll `/action-requests/{id}` to terminal. Render receipt on completion|I.embed,V29,V34,V80
T43|.|Embed: 401 state — explicit "cannot authenticate here" + "Sign in to NOA" button (new-tab link-out). ⊥ LDAP redirect inside iframe|V38,V42
T44|x|Embed: session plumbing — proxy `app/api/[...path]` or direct API origin with `Domain=.noa.internal` cookie. AS BUILT ≠ this line, 8 deviations: (a) **"or direct API origin" ⊥ a choice** — `AGENTS.md` settles it ("Browser ⊥ call FastAPI direct. Same-origin proxy route per web app") ∧ §I.embed's own footnote said the same ⇒ the disjunction was dead when this line was written. Recorded so it ⊥ read as still-open. (b) **ALLOWLIST, ⊥ pass-through** — owner-decided at plan time, ∧ the argument is 2 §V lines this row ⊥ cite: V42 says this app has ⊥ login page, ⊥ LDAP form, ⊥ credential handling, ∧ a generic proxy puts `POST /auth/login` ON the origin; V41 makes the embed the ONLY frameable NOA origin (admin answers `frame-ancestors 'none'`) ⇒ a pass-through also puts `/admin/*` (T51–T55) inside the LibreChat frame with the operator's cookie, which is the reachability that header exists to deny. `core` of it = `src/lib/proxy/routes.ts`, 4 entries, matched on METHOD ∧ shape TOGETHER (right path + wrong method = refused), ∧ the count PINNED by test ⇒ T41's CSRF-mint route, T42 ∧ T56 each widen it on purpose. Same shape as V83b ∧ V57-after-T68: guard at the MECHANISM, ∵ a guard the next surface has to remember is one it forgets. (c) **Refusal = 404 `route_not_proxied`, ⊥ 403, ∧ upstream ⊥ called** — the ORDER is the property: "this origin ⊥ carry the login route" is a fact about a request never SENT, ⊥ about a response that came back, ∧ a 404 an upstream produced looks identical from the browser ⇒ held by the `fetch` spy unit-side ∧ by the stub's `/__hits` in the browser lane, ⊥ by the status. Body = `{error_code, message}`, ⊥ `request_id`: V73 binds the API's 5 writers ∧ ⊥ request reached one ⇒ an id here names ⊥ log line. 404 ⊥ 403 ∵ a code that varied by cause makes this origin an existence oracle for the API's surface (V27's shape, one origin over). (d) **`authorization` STRIPPED outbound** — the 1 departure from the ported source. ∀ surface this app reaches is cookie-authenticated (V40, V22); a bearer token is LibreChat's to send (C5), ⊥ a browser's ⇒ forwarding the header makes the embed origin a relay for one. (e) **`{id}` ⊥ shape-validated** — the rule matches ONE non-empty segment ∧ hands it on. V27 owns what absent ∨ malformed ∨ foreign answers ∧ answers ∀ 3 alike; a UUID check here = a 2nd, more talkative judge in front of it. (f) **root `.env` loading landed here** (T40 parked it) ∧ as `config/root-env.ts`, ⊥ inline in `next.config.ts`: the PRECEDENCE rule is the part that can be got wrong — the real environment always wins, which is how the Playwright run points `NOA_API_URL` at its own stub — ∧ a rule inside the config ⊥ testable without importing the config's side effects. `key in env`, ⊥ truthiness: an explicitly-empty value = an operator's deliberate unset, ∧ refilling it re-points the proxy at an API they turned off. ⊥ per-app `.env.local` (C11) — 2 env files for 1 deployment = 2 places for `NOA_API_URL` to disagree, ∧ a proxy quietly talking to the wrong API is ⊥ a failure anyone sees. (g) **Methods bounded by what the module EXPORTS** — `GET` ∧ `POST` only ⇒ Next answers 405 for the rest ∧ the restriction costs ⊥ code to hold. `runtime = 'nodejs'` (a streaming body needs `duplex: 'half'`) ∧ `dynamic = 'force-dynamic'` (a cached proxy answer serves one operator's session state to the next request), both asserted on the module's own exports ∵ that is the knob Next reads — a response-header assertion tests the harness (T40(b)'s rule). (h) **e2e boots a STUB upstream ∧ ⊥ reuse the dev server** — V90's family, one step past T40(f). Stub ∵ a real API drags Postgres ∧ LDAP into a Playwright run ∧ turns either one's outage into a red PROXY test; `reuseExistingServer: false` ∵ a reused server was given a DIFFERENT `NOA_API_URL` ∧ would decide the outcome silently. Both servers still gate on the open SOCKET. `eslint.config.mjs` gained a Node-globals block SCOPED to `e2e/support/**/*.mjs` — a `process` reference inside `src/` stays an error. **Ported, ⊥ rewritten** (C13): `noa-old` `MCP:apps/web-bigsu/src/lib/proxy/http.ts` → `src/lib/proxy/http.ts`; V69 is why its tests came WITH it — traversal (raw, `%2e%2e`, backslash), off-origin `Location`, host scrub, status-0 remap ∧ the undici decompression rule each re-proven vs THIS copy, ⊥ cited from the source. **19 mutations proven red**: allowlist removed; method dropped from the match; empty path segment accepted; shape-length compare loosened; `authorization` forwarded; `cookie` dropped; `Set-Cookie` ⊥ copied back; post-normalization prefix-escape check dropped; `../` segments ⊥ stripped; off-origin `Location` returned raw (open redirect); internal host left in `Refresh`/`Link`; opaque-redirect status 0 ⊥ remapped; pass-through encoding relabelled identity; root `.env` overwrites the environment; truthiness instead of `in`; quotes left in the value; route moved off the Node runtime; answers made cacheable; refusal forwards upstream FIRST. The allowlist mutation was re-run on the BROWSER lane ∧ went red there too (login ∧ admin) ⇒ that lane SEPARATES, ⊥ merely passes. ⊥ built here: the card ∧ its CSRF-MINT route (T41), the decision POST ∧ polling (T42), the 401 state (T43), CSP `frame-ancestors` (T45), the CSRF carry (T46), the table surface (T56). Doc sweep same change (V84a): `apps/web-embed/README.md` listed this proxy under "Still to come" ∧ named ⊥ allowlist; root `README.md`'s e2e note described ONE server|I.embed,C5,C11,C13,V22,V27,V38,V40,V41,V42,V65,V66,V67,V69,V73,V83,V84,V90,T40,T41,T42,T43,T45,T46,T56
T45|x|Embed: framing headers — `Content-Security-Policy: frame-ancestors https://chat.noa.internal`. No `X-Frame-Options` needed|V37,V41
T46|.|Embed: CSRF token carried into the decision POST. **Mint + verify LANDED 2026-08-08 (T37)** — `core/approvals/csrf.py`, V39 as amended (session- ∧ request-bound, key derived from `AUTH_JWT_SECRET`, TTL = pending TTL). T46 shrinks to the EMBED half: the card obtains a token ∧ sends it in the `fetch` body ∨ header — ⊥ hidden form field (V80). ⊥ minting ROUTE exists yet; T41's GET detail is where the card gets one|V39,V80,T37,T41
T47|.|Admin web scaffold: `apps/admin-web` — Next.js 16, BIGSU dependencies (`@gio/bigsu-ui`, `@gio/bigsu-icons`), `.npmrc` for `bigsu.biznetgio.pt/registry/`. Hygiene: `.ts≤300`, `.tsx≤450`|C2,C14,I.admin-web
T48|.|Admin web: PORT from `noa-old/apps/web-bigsu/src/app/(protected)/admin/` — users, roles, audit, audit/tool-runs, whm, proxmox, pmg. Copy BIGSU components, ⊥ import from old repo. Include `AGENTS.md`, `CLAUDE.md`, `.claude/skills/bigsu/`, `docs/`|C13,I.admin-web
T49|.|Admin web: framing headers — `Content-Security-Policy: frame-ancestors 'none'`|V41
T50|.|Admin web: auth/session plumbing — proxy API calls, `noa_session` cookie. Login page redirect|I.admin-web
T51|.|Admin: user management CRUD — list, create, enable/disable, delete, assign roles|I.admin-api
T52|.|Admin: role management CRUD — list, create, delete, set tool permissions. `admin` role reserved|I.admin-api,V13
T53|.|Admin: token management — list tokens per user, mint (show-once plaintext), revoke|I.admin-api,V2
T54|.|Admin: server management — WHM/Proxmox/PMG CRUD + validate (SSH connect, fingerprint capture, TOFU refresh)|I.admin-api
T55|.|Admin: audit — tool runs list (filters: toolName, status, conversationRef, user, date range, cursor pagination), detail view (args redacted, result summary, timing)|I.admin-api,V45,V47
T56|.|Table surface for large READ results: `apps/web-embed` — route `/tables/[token]`, ⊥ `apps/api` (§8.8: same app, same origin rules, same framing headers). Renders full table. Tool result carries summary + URL. Used by `whm_list_accounts`, `pmg_whitelist_list`. Mechanism = path A, T59 CLEARED 2026-08-08 (R29) ⇒ unblocked|V64,I.embed,T59
T57|.|LibreChat `librechat.yaml` entry: `mcpServers.noa` → `streamable-http` → `https://noa.internal/mcp`, `customUserVars.NOA_MCP_TOKEN` (`sensitive: true`), headers `Authorization: Bearer {{NOA_MCP_TOKEN}}` + `X-Noa-LibreChat-User: {{LIBRECHAT_USER_ID}}` + **`X-Noa-Conversation-Ref: {{LIBRECHAT_BODY_CONVERSATIONID}}`** — OPTIONAL, ∧ the ONLY way `tool_runs.conversation_ref` (V47) is ever non-NULL: T73 reads that header ∵ the tool call carries ⊥ conversation id (R27); ⊥ written here ⇒ the audit filter §I.admin-api `conversationRef` has nothing to group on. Blank ⊥ distinguishable from absent (R28a) ∧ NOA treats both as absent. Operator-YAML registration. URL ! carry trailing slash `https://noa.internal/mcp/` ∵ T13 mount answers there; bare `/mcp` costs a 307 per request (works, ⊥ free). **+3 keys V88 forces, each closing a silent total failure measured 2026-08-08 (R31)**: `startup: false` ∧ `requiresOAuth: false` (boot inspect has ⊥ user ⇒ NOA 401 per C24/V3 ⇒ LibreChat infers OAuth, marks `needs_authorization`, ∧ answers ZERO tools ∀ operator forever) ∧ NOA's host ∈ `mcpSettings.allowedDomains` (else the SSRF guard refuses the URL; `allowedAddresses` ⊥ take CIDR ∨ loopback @ pin). Ship the YAML in-repo ∧ TEST those keys (V84c) — prose ⊥ hold a config that fails silently. Working reference: `spikes/librechat-embed-render-gate/librechat.yaml`|I.mcp,I.ext,V47,V70,V71,V88,R27,R28,R31,T13,T73
T58|.|LibreChat agent config: system prompt with safety policy (preflight-first, approval gates, no fabrication, argument discipline, `\ui{}` emission instruction)|V71
T59|x|**GATE — blocks T32,T41–T46,T56 + selects V24/V37/V64 branch. Run before embed work starts, ⊥ after.** Pin candidate = `45cc53c40b47645b887c3bb996168e06aaa83f4c` (R10; tag `v0.8.7` = `prerelease`). Source-read items (a)–(c) + (e) ANSWERED at that pin by R11–R16: (a) `text/uri-list` ⇒ `src` YES, (b) `@mcp-ui/client@5.7.0` still renderer ∧ #13831 unmerged draft on `dev`, (c) `profile=mcp-app` classification absent at HEAD, (e) sandbox = `allow-scripts allow-same-origin`, **⊥ `allow-forms`** ⇒ V80. STILL OWED: (d) live cookie-into-frame run against a NOA-served `text/uri-list` resource at that pin, (f) does LibreChat honour `notifications/tools/list_changed` (V74/T66). PASS → path A (iframe). FAIL → path B (link-out primary, V25). Re-verify ∀ version bump. **VERDICT 2026-08-08 = PASS (R29, R30, R31)** — items (d) ∧ (f) closed by a live run, ⊥ a 3rd source read (V69: provenance ⊥ evidence). (d): both LibreChat render sites put the frame's `src` on NOA's origin with `allow-same-origin` ∧ ⊥ `allow-forms`, ∧ in-frame `GET /auth/me` + `POST` both answered 200 as the operator ⇒ V22's cookie POST works from the iframe; the `text/html` control landed on an opaque origin, threw `SecurityError` on `document.cookie` ∧ could ⊥ fetch ⇒ the measurement separates (V87). (f): LibreChat honours ⊥ `notifications/tools/list_changed` — 0 handlers in tree, ∧ a notification NOA provably emitted drew ⊥ refetch (R30) ⇒ V74 amended, T66 keeps the emit ∧ tests the V1 backstop instead. 3 config bounds fell out of the run ∧ went to V88/T57 (R31). AS BUILT ≠ this line, 3 deviations in the RIG, each named ∵ each is something the run ⊥ exercise: (a) probe tools sat on a SECOND FastMCP server at `/mcp-embed-probe` behind the same `NoaTokenVerifier` ∵ `register_mcp_tools` refuses uncatalogued names (V10, V83a) ⇒ token auth ∧ TOFU (V3) exercised, `RbacToolMiddleware` ∧ `ToolRunAuditMiddleware` ⊥. (b) the in-frame POST hit a stand-in with ⊥ CSRF (V39), ⊥ row lock (V28), ⊥ reason (V15) — it answered "did the cookie arrive", nothing else; T33/T37 build the real one. (c) plain HTTP ∧ one browser (Chromium 141) ⇒ TLS cookie flags ∧ Firefox/WebKit unmeasured. Rig committed ∵ C21's re-verify: `spikes/librechat-embed-render-gate/`, verdict `docs/spikes/librechat-embed-render-gate.md`|C17,C21,V24,V25,V37,V64,V80,V87,V88,R10,R17,R29,R30,R31
T60|.|Deployment: `Dockerfile` per app. `docker-compose.yml` for local dev (Postgres + API + admin-web + embed). Domain setup: `chat.`/`embed.`/`noa.internal` + cookie `Domain=.noa.internal`|C12,V40
T61|.|CI: lint + typecheck + test per app. Python: pytest + ruff. TypeScript: eslint + tsc + vitest + playwright. Conventional commits enforced|C14,V67,V68
T62|.|Docs: `README.md` (overview, setup, architecture), `ARCHITECTURE.md` (design decisions, topology), `AGENTS.md` (caveman communication, code standards). `ARCHITECTURE.md` ALREADY EXISTS — created at T70, scope = MCP pin rationale + re-open triggers + where-enforced ⇒ T62 = ADD topology + module boundaries + remaining design notes INTO that file, ⊥ a new one, ⊥ restate its pins section. `docs/README.md` pointer already split pins-vs-rest (T70(c))|T70
T63|x|Tool: `noa_get_action_result(action_request_id)` (READ). Requester-match vs MCP-token identity. Foreign ∧ unknown → identical not-found shape. Returns status + receipt summary, redacted args. AS BUILT ≠ this line, 6 deviations: (a) **"receipt summary" = `tool_runs`, ⊥ `action_receipts`** — owner-decided at plan time: T36 = `.` ∧ its writer T38 = `.` ⇒ a join there answers `null` while LOOKING like a feature. `action_requests.tool_run_id` is written inside the decision's own transaction (T37, V29) ⇒ the run row IS today's execution record (status, redacted summary, timing — V47); T36 joins BESIDE it when it lands. (b) **a 4th class in `core/approvals/`, ∧ the only one that writes NOTHING** — `results.py`: ⊥ `commit`, ⊥ status parameter, ⊥ statement that is ⊥ a `SELECT`. Same split that separated the first 3 writers, one door further: the one write this path needs (a stale PENDING made terminal before it is served) is T39's, whose reachable status is EXPIRED ∧ nothing else. (c) **requester-match in the `WHERE`** (`id = :id AND requested_by_user_id = :caller`) ⇒ a foreign row is ⊥ fetched ∧ a NULL requester (T34 `SET NULL`) matches nobody. **MEASURED non-difference, recorded ∵ V69**: moving that compare into a Python `!=` after the read leaves EVERY test green, live ones included — `None != caller` is True ⇒ even the orphan case still refuses. Statement kept for defence in depth (the row ⊥ exist in-process to be logged ∨ half-dropped by a later edit) ∧ said in the docstring, ⊥ implied by a test name. (d) **ORDER = requester-match, THEN expiry** — reverse of `expiry.py`'s own docstring, deliberately: `expire_if_due` takes an id ∧ ⊥ requester ⇒ expiry-first lets a prompt-injected id make NOA WRITE to a request its caller ⊥ see (V27, V76). Read-first ⇒ a foreign id = pure no-op. Cost = 1 poll of freshness if a decision commits in the window — inherent to ∀ unlocked read, ∧ V23 keeps the authority in the row. Closes T39(i)'s "⊥ a live route": this is the check-on-read's 1st live caller. (e) **id = `str`, parsed IN-TOOL, ⊥ a schema UUID** ⇒ malformed answers the SAME code ∧ SAME message as unknown ∧ foreign: ONE refusal for the whole family (V83d's shape), ⊥ a 2nd pydantic-shaped envelope for "there is ⊥ such request". `ERROR_ACTION_REQUEST_NOT_FOUND = ActionRequestNotFoundError.error_code` — imported, ⊥ retyped ⇒ this tool ∧ T37's endpoint answer 1 code for 1 fact; the MESSAGE is this surface's own (⊥ "to decide" — nothing is decided here). (f) **3 exclusions STRUCTURAL, ⊥ filtered** — `ActionResultView` has ⊥ field for `reason` (C8, V15, V43: LLM ⊥ author, ⊥ relay, ⊥ SEE), ⊥ for `evidence` (V17: born in-process, ⊥ in transcript), ⊥ for the requester block (V26) ⇒ the tool ⊥ emit what it never loads. ⊥ named in this line: `core/approvals/context.py` (V66 — `"arguments"` was spelled in the gate ∧ in `decisions`, T63 = the 3rd, ∧ a misspelt JSONB key reads as an ABSENT one ⇒ 1 constant, 1 extraction rule); `noa_api/mcp_tools/noa_read.py` (own module ∵ its subject = the approval gate, ⊥ a hosting system); `authenticated_caller` → `support/mcp_identity.py` (V66 — T33's gate tests ∧ this one both need a caller whose id they then assert a row against). **7 mutations proven red**: requester filter dropped from the WHERE; `reason` carried into the view ∧ payload; whole `approval_context` used as the arguments (evidence leaks); expiry check-on-read dropped; expiry run BEFORE the matched read; malformed id given its own code; tool registered CHANGE. An 8th ran GREEN ∧ is recorded as (c) — measured, ⊥ assumed. Doc sweep same change (V84a): `core/approvals/__init__.py` ("Six modules"), `core/approvals/expiry.py` (named T63 as a FUTURE caller ∧ put the check BEFORE the read), `noa_api/api/deps.py`, `noa_api/mcp_tools/__init__.py` (still listed T39 as still-to-come), `test_mcp_tool_rbac.py` ∧ `test_action_request_expiry.py` (both said T31/T63 unbuilt)|I.mcp,C8,V8,V15,V17,V19,V23,V26,V27,V32,V43,V45,V66,V69,V76,V83,V84,T33,T34,T36,T37,T38,T39
T64|x|Error envelope: ∀ error response body carries `request_id`, response carries `x-request-id` header. Shared exception handler, ⊥ per-route. AS BUILT ≠ this line, 5 deviations: (a) "shared handler" = 4 handlers + 1 middleware, ⊥ one — `NoaError`-only left 404/405, 422 ∧ 500 in Starlette's default shape; ∀ registered in `install_error_handling(app)` (renamed from `install_error_handler`), the seam `main` ∧ both test harnesses already called (V66). (b) `noa_api/api/request_context.py` NEW, ⊥ named in this line — `RequestContextMiddleware` mints/adopts the id ∧ publishes it 3 ways ∵ 3 readers ⊥ reach each other: `scope["state"]` (raw-ASGI `McpAuthErrorMiddleware` + the fresh `Request` `ServerErrorMiddleware` builds), a `ContextVar`, ∧ structlog contextvars — without the 3rd the body id names nothing. (c) inbound `x-request-id` SANITIZED, ⊥ echoed verbatim (V73). (d) pydantic errors ⊥ forwarded to body ∨ log (V8). (e) `error_response()` sets the header itself ∵ the 500 answers from outside the middleware stack. Body key set now `{error_code, message, request_id}` ⇒ 2 existing V8 shape tests widened (`test_auth_routes.py`, `test_mcp_request_auth.py`), ⊥ new behavior. Verification: `apps/api/tests/test_error_envelope.py`, 1 group per writer + sanitizer + shape|V73,V8,V3,V9,V66
T65|.|Direct per-user tool grants → 410 `direct_tool_grants_disabled`. Role replacement preserves internal roles|V75,I.admin-api
T66|.|Emit `notifications/tools/list_changed` on role/permission change. Test: revoked tool → 403 at execution even when still in client's cached catalog (V1 backstop). LibreChat-honours question ANSWERED 2026-08-08 (R30): it does **⊥** — 0 handlers in tree ∧ a notification NOA provably emitted drew ⊥ `tools/list`. ⇒ still emit (protocol-correct, ~free, a later client may honour) ∧ the ONLY assertion that may be written here is the V1 backstop; a "client refetched" test asserts behaviour NOA ⊥ control ∧ would go red on an upstream whim (V69's shape). ⇒ operator-facing consequence ! be documented, ⊥ implied: a revoked grant stays VISIBLE in LibreChat's catalog until that connection is rebuilt|V1,V14,V74,C23,R30
T67|x|Rename Fernet env var → `NOA_SECRET_ENCRYPTION_KEY` in `core/config.py`, `.env.example`, Dockerfiles, `docker-compose.yml`, README. ⊥ `NOA_DB_SECRET_KEY` (misleading — encrypts server creds, ⊥ DB). AS BUILT ≠ this line, 4 deviations: (a) ⊥ a rename — `NOA_DB_SECRET_KEY` NEVER existed here: `git log -S` finds it in 2 places only, this row ∧ the test asserting its absence ⇒ name born correct at T5; `core/config.py` (field `noa_secret_encryption_key`, `_resolve_encryption_key` prod-required + dev-gen), `.env.example:14-17` (+ scope comment), `README.md:106` (+ scope sentence) ∀ already carried it. Recorded so "⊥ rename performed" reads as measurement, ⊥ as skipped work. (b) Dockerfiles ∧ `docker-compose.yml` named as write targets ∧ ⊥ touched ∵ ⊥ exist — T60 writes them, AFTER this row closes `x` ⇒ `NOA_DB_SECRET_KEY` there would meet nothing red (same "named ∧ ⊥ touched" shape as T70(a)). (c) ⇒ delivered work = BINDING, ⊥ renaming: `test_config.py::test_legacy_encryption_key_name_absent_from_every_tracked_file` scans ∀ git-tracked file (`git ls-files`, ⊥ FS walk ∵ it drops the dev's own `.env` ∧ `.venv` ∧ `node_modules` with ⊥ 2nd ignore list to drift) ⇒ T60's unwritten files inherit the rule unlisted; allowlist = `SPEC.md`,`DECISIONS.md`,`test_config.py` ∵ their subject IS the prohibition. + `::test_operator_docs_name_the_env_var_the_settings_field_defines` (parametrized `.env.example`,`README.md`) binds doc name → field ⇒ a code-only rename leaves README's scope sentence RED, ⊥ stale (V84a/c; pattern T70(d),T72(a)). BOTH red-first by mutation (legacy key appended to `.env.example`; field constant renamed), then reverted. (d) ⊥ named in this line: `REPO_ROOT` + the 3 name constants hoisted in `test_config.py`, existing `::test_encryption_key_name_reflects_scope` ∧ `::test_env_example_documents_every_required_var` re-read them ⇒ 1 place to edit on rename. FLAGGED, ⊥ fixed (T60's): `README.md:74` ∧ `AGENTS.md` instruct `docker compose up -d postgres` ∧ ⊥ compose file in repo|C7,V52,V84,T5,T60
T68|x|Firewall no-backend guard: zero available backends → error `no_firewall_backend`. Test asserts ⊥ success-with-empty-gather. Fixes `noa-old` silent no-op. AS BUILT ≠ this line, 5 deviations: (a) the guard moved OFF the tools onto the MECHANISM — `core/integrations/whm/firewall_gate.py` NEW (⊥ named in this line): `require_usable_backends` raises, `run_on_usable_backends` = the only place a backend set becomes concurrent work ⇒ it ⊥ be entered with nothing to do. T24's version was a per-tool `_availability_refusal`, ∧ a per-tool check = a check the NEXT tool forgets, which is precisely this row's subject (T24: "⊥ closes T68 — that row's subject = a silent no-op on an approved CHANGE"). ⇒ T25/T26 inherit by construction, ⊥ by discipline. (b) V57's literal "each tool checks `available[…]`" now reads THROUGH one door — the property (zero ⇒ error, ⊥ empty-gather success) unchanged ∧ strengthened; ⊥ a §V re-word here (that = `/ck:spec`'s call). `AGENTS.md`'s line stays true as written. (c) proof the guard is ⊥ per-tool any more = the 2 zero-backend cases in `test_whm_tools_firewall_preflight.py` pass with NOTHING about availability written in that tool ∧ go red when the gate call is dropped from the fan-out — i.e. the tool tests now certify the shared door, ⊥ a copy. (d) recurrence bound by AST, ⊥ by prose — `test_whm_firewall_gate.py::test_the_firewall_fan_out_lives_in_one_place` pins the firewall path to exactly 2 `asyncio.gather` sites (`availability.check_firewall_binaries` = computes availability ⇒ ⊥ gated by it; `firewall_gate.run_on_usable_backends` = the door) ⇒ a hand-rolled fan-out in T25/T26 is RED, ⊥ silent (V84c, pattern `test_support_layout.py`/`test_pins.py`). ⊥ new exception class: `WHMFirewallCLIError` already covers "⊥ be run, ∨ ⊥ be trusted to have run", already lists `ssh_sudo_required`, already mapped once to 502 (V73); class-per-code = the rewrite C13/V69 forbid. `BACKEND_CSF`/`BACKEND_IMUNIFY` moved to `availability.py` (the module whose `as_tools_dict` spells them) ⇒ graph one-directional, ⊥ an alias hub in the tool (T72). 4 mutations proven red: gate call dropped (gate tests **∧** both tool tests), `sudo_required` ignored, unusable backend fanned out, hand-rolled gather in the tool. (e) **⊥ exercised: a live CHANGE tool.** T25/T26 = `.`, blocked behind the approval gate (T32–T38, ∀ `.`) ⇒ the door is proven on the READ path ∧ by unit tests, ⊥ on an approved CHANGE. What T25/T26 still owe = routing their mutation fan-out through this door (the AST guard makes ⊥ doing so red) ∧ V86's 2-part receipt. Doc sweep same change (V84a): `docs/integrations/whm.md:133-135,315`, `core/integrations/whm/availability.py` docstring, `whm/__init__.py` module list, `test_whm_firewall_availability.py` (module docstring ∧ `:222` — both named the TOOL as the enforcement point)|V57,V19,V55,V65,V66,V67,V84,T24,T25,T26
T69|.|Proxmox crypt-verify guard: `_load_crypt_lib()` → `None` ⇒ receipt `verification_unavailable`, ⊥ silent pass. Test with libcrypt unavailable|V62
T70|x|**DECIDED by research 2026-08-04, ⊥ owner input needed** — record era + `fastmcp==3.4.5` in `pyproject.toml` + README + `ARCHITECTURE.md` with C23 rationale (LibreChat on SDK v1.x, lockfile-exact 1.29.0) so pin reads as decision, ⊥ accident. Text ! say **negotiated set**, ⊥ a digit-as-decision (T71 corrected C23; `pyproject.toml`+README already rewritten there ⇒ T70 = `ARCHITECTURE.md` + re-open trigger only). Record re-open trigger: LibreChat bumps to `@modelcontextprotocol/sdk` v2 — ∧ note a 1.x bump moves the live era silently (R26). Cite R8 (server SUPPORTED list) + R9 (client LATEST, read) + R11 (pin-verified dep) + R26 (client chooses). AS BUILT ≠ this line, 5 deviations: (a) `pyproject.toml` UNTOUCHED — named as a write target, zero edits landed; T71(b) already corrected the digit there ∧ ⊥ else about the pin belongs in a dependency file. Recorded so "named ∧ ⊥ touched" reads as decision, ⊥ omission. (b) README touched ANYWAY — the parenthetical's "README already rewritten" was half true: T71 fixed the DIGITS, ⊥ the POINTER, which still read "Full rationale: C23, T70" ⇒ sent the reader to a §T row instead of a document, ∧ that document ⊥ exist yet. Rewritten to point at `ARCHITECTURE.md` ∧ carry the silent-1.x-bump trigger where the reader lands first. (c) `docs/README.md` touched, ⊥ named in this line — it forward-referenced `ARCHITECTURE.md` (§T.62); file now exists at T70 carrying pins only ⇒ pointer SPLIT (pins §T.70, rest §T.62), else the docs index credits a task that ⊥ create it. (d) doc-binding TESTS ⊥ named in this line — `test_pins.py::test_pin_docs_track_the_servable_set` (parametrized over `ARCHITECTURE.md` ∧ `README.md`, BOTH directions: era added to `SUPPORTED_PROTOCOL_VERSIONS` ⇒ docs incomplete, era removed ⇒ docs wrong) + `::test_architecture_doc_records_the_pin_and_its_re_open_trigger`. ∵ "pin reads as decision" ⊥ hold on prose alone — T71 found the false digit standing in 4 files precisely ∵ nothing compared prose to source (V84c). Convention the test rests on, in those 2 files ONLY: backticked date = protocol era, bare date = verification date. (e) `ARCHITECTURE.md` CREATED here, scope = MCP pins section ONLY ⇒ T62 still owes topology + module boundaries + the rest, INTO this file|C23,I.mcp,R8,R9,R11,R26,V67,V84,T62,T71
T71|x|Close R9: read `@modelcontextprotocol/sdk` v1.29.0 TS source (`node_modules` ∨ npm tarball ∨ GitHub tag) for `LATEST_PROTOCOL_VERSION` + `SUPPORTED_PROTOCOL_VERSIONS`. Confirm ∨ correct C23 premise sentence, drop `?` on R9. Cheap, read-only, ⊥ blocking — but ⊥ ship C23 rationale text as fact until done. AS BUILT: premise **CORRECTED, ⊥ confirmed** — `LATEST_PROTOCOL_VERSION = '2025-11-25'` @ tag `v1.29.0` ⇒ every "era = `2025-06-18`" sentence shipped was false (R9). 3 deviations from "read-only": (a) the read spilled past the 2 named constants — a digit ⊥ actionable without knowing WHO picks it ⇒ `Client.connect` + LibreChat's lockfile + `connection.ts` read too, landing R26; live era = `2025-11-25`. (b) prose in 4 files asserted the false digit as fact (`README.md:44,47`, `noa_api/mcp_server.py:3`, `apps/api/pyproject.toml:12`, `tests/test_pins.py:29`) ⇒ corrected here, ⊥ deferred to T70 — this line's own bar ("⊥ ship C23 rationale text as fact until done") makes leaving them a knowing false claim. (c) a source-read alone ⊥ hold: old tests "proved" `2025-06-18` only ∵ the harness ASKED for it — a self-fulfilling assertion, same shape as B2's dead control (V69) ⇒ `tests/support/mcp_mount.py` now speaks as LibreChat does (`CLIENT_LATEST_PROTOCOL_VERSION`), + `test_the_mount_answers_the_era_the_client_asks_for` (3 cases: client-latest, older client, unsupported→fallback) ∧ `test_pins.py::test_server_serves_every_era_a_v1_client_can_ask_for` (set covers both, `2026-07-28` still out)|C23,R8,R9,R26,V69
T72|x|Test-support imports direct: 4 WHM test files (`test_whm_client.py`, `test_whm_firewall_availability.py`, `test_whm_firewall_cli.py`, `test_whm_ssh_config.py`) import SSH doubles from `support/remote_exec.py` ∧ `build_cipher` from `support/secrets.py` direct; drop re-export shims in `support/whm.py` ⇒ one home per double, `support/whm.py` ⊥ alias hub. Mechanical, ⊥ behavior change. AS BUILT ≠ this line, 3 deviations: (a) guard test `tests/test_support_layout.py` CREATED, ⊥ named in this line ∵ "mechanical" describes the EDIT, ⊥ the property — ∧ the property survives only until the next port re-adds a shim, which is invisible at the call site (`from support.whm import command_result` reads WHM-owned). Machine-readable ⇒ bind by test, ⊥ discipline (V84c, the pattern `test_pins.py` set). 2 asserts: `support.whm.__all__ == ["FakeWHMServer"]` ∧ an AST scan of ∀ test module for `support.whm` imports naming anything else ⇒ a new test file inherits the rule unlisted. BOTH red pre-change. (b) `support/whm.py` KEEPS its `PINNED_FINGERPRINT` ∧ `SSH_PASSWORD` imports — ⊥ surviving shims: `FakeWHMServer` takes both as field defaults ⇒ they stay module ATTRIBUTES ⇒ an attribute-reachability assert ⊥ available; guard binds `__all__` + import sites instead. (c) 3 docstrings ⊥ named in this line asserted the re-export as fact ⇒ fixed same change per V84a (restatement ⊥ outlive what it restates — code comments included, ⊥ only `README`/`AGENTS`/`docs`): `test_whm_firewall_cli.py:17` named `support.whm` as the patched `ssh_exec` namespace (wrong module even before this row — T18 moved it), `support/remote_exec.py:3-7` ∧ `support/secrets.py:3-5` both said `support/whm.py` re-exports them|V66,V84,T16,T17,T18
T73|x|Wire `tool_runs` write into the MCP tool path: ∀ READ tool call → row (`requested_by_user_id`, `tool_name`, `risk=READ`, `status`, `conversation_ref`, redacted `args`, `result_summary`, timing). Blocked on T35 (table). Closes T19(g) — ∀ tool call unaudited until this lands. Write ! sit beside the gate (V83b: one place, ⊥ per-tool code) ∧ ! record FAILED READs too (V20: risk ∧ status = separate columns). AS BUILT ≠ this line, 7 deviations: (a) `risk` ⊥ a constant — registrars now return `Mapping[str, ToolRisk]` (`register_whm_read_tools`, `register_mcp_tools`), RBAC middleware takes the KEYS, audit middleware takes the MAP ∵ a hardcoded `READ` makes T22's first CHANGE tool record itself as a READ **∧** write a row for a change that ⊥ happened — silently, in the audit trail. Risk declared where the tool is defined ⇒ a registrar ⊥ omit it (`test_every_registered_tool_declares_a_risk`). CHANGE = skipped here: its `tools/call` opens the gate (T33) ∧ executes nothing; V46's row = T38's. (b) **middleware ORDER load-bearing ∧ measured** — `FastMCP._run_middleware` composes over `reversed(self.middleware)` (`server.py:513`) ⇒ first added = OUTERMOST. RBAC first, audit second ⇒ audit wraps EXECUTION, ⊥ refusals. First red-first attempt used an UNREGISTERED name ∧ **survived the order swap** ∵ such a name ∉ risk map either way; the case that tells = a REGISTERED tool with grant revoked (`test_a_call_refused_by_rbac_writes_no_row`). `mcp_server.py` prose corrected to that, ⊥ the wrong claim the weak test licensed. (c) **fail-closed on the opening write** (owner-decided): STARTED row ⊥ commit ⇒ tool ⊥ run, caller gets `tool_audit_unavailable` in the tool envelope + `is_error` ∵ V45 says ∀ READ writes a row — fail-open leaves the invariant asserted by prose ∧ held by nothing = B2's shape (V69). Terminal write ⊥ symmetric: tool already ran ⇒ log `mcp_tool_audit_finish_failed`, leave row STARTED (T38 reaper's state), ⊥ misreport a call that happened. (d) `conversation_ref` source RESEARCHED, ⊥ assumed — LibreChat @ pin `45cc53c4` sends `params:{name,arguments}`, **⊥ `_meta`** (`packages/api/src/mcp/MCPManager.ts:512-518`) ⇒ ⊥ conversation id in the call at all. Its header templating DOES resolve `{{LIBRECHAT_BODY_CONVERSATIONID}}` (`packages/api/src/utils/env.ts:130`, `ALLOWED_BODY_FIELDS=['conversationId','parentMessageId','messageId']`) ⇒ NOA reads header `X-Noa-Conversation-Ref`; **T57 owes the YAML side** (named ∧ ⊥ touched, T67(b)/T70(a) shape). Absent ∨ unusable → NULL ∧ call proceeds (label, ⊥ scope — old V165 dissolved). Sanitized ∵ it lands in Postgres AND structlog = V73's log-forging surface ⇒ `sanitize_header_label(value,*,max_length)` extracted in `request_context.py`, `sanitize_request_id` delegates (V66). (e) 2 statements, 2 TRANSACTIONS — `SQLToolRunRepository` owns its session ∧ commits (like `SQLMcpIdentityRepository`, ⊥ like the flush-only `core/` repositories) ∵ one transaction spanning the call means the row appears only once the call ENDS ⇒ a process dying mid-call leaves nothing ∧ STARTED has no purpose. Asserted as commit ORDER (`["STARTED","COMPLETED"]`), ⊥ as a count. (f) `core/secrets/redaction.py` ported (T35 deferred it here) with ONE departure from `noa-old`: that repo **encrypted** sensitive audit args (`_encrypt_sensitive_args`) so admin could decrypt back; §V45/V47 say **redacted** ⇒ one-way here, ∵ a recoverable audit table turns one Fernet key into every credential ever passed to a tool. Applied to `result_summary` too, ⊥ only `args`. (g) ⊥ named in this line: `tests/support/tool_runs.py` double wired into `build_tool_context` for **∀** mount test, ⊥ only the audit ones — the middleware refuses what it ⊥ record ⇒ the T19 RBAC tests would fail for an unrelated reason, ∧ a fixture that disabled the audit path would let the suite pass with V45 unheld. 11 mutations proven red (missing middleware, order swap, fail-open, always-COMPLETED, unredacted args, unredacted summary, verbatim conv-ref, no truncation, single commit, risk=CHANGE, CHANGE audited, finish raises). Doc sweep same change (V84a): `mcp_tools/__init__.py`, `core/db/models.py`, `core/auth/tool_catalog.py`, `test_tool_runs_schema.py`, `docs/integrations/whm.md:252` all asserted "⊥ built"/"table ⊥ exist" — the whm.md line was false twice over (T35 built the table) ⇒ ⊥ citation-grep, CLAIM-grep found it|V8,V20,V45,V47,V66,V83,V84,T35,T19,T57

## §B BUGS

id|date|cause|fix
B1|2026-08-07|`McpAuthErrorMiddleware` path guard compared raw `scope["path"]` to sub-app path; Starlette `Mount` extends `root_path`, ⊥ rewrites `path` ⇒ mounted at `/mcp` guard never matched, ∀ refusal fell to SDK bare 401 with empty body, V3 named codes (`librechat_user_header_missing` \| `librechat_user_mismatch`) vanished behind unchanged 401. Caught by T13 contract test, ⊥ shipped|V81
B2|2026-08-07|**FIXED 2026-08-07** (`known_hosts=([], [], [])`, V82). Host-key pin WAS INERT — `ssh_exec` accepts any host key. `core/remote_exec/ssh.py:99` passes `known_hosts=None` ⇒ asyncssh sets `_trusted_host_keys = None` (`asyncssh/connection.py:3509-3511`) ∧ the whole comparison, incl. `self._owner.validate_host_public_key(...)`, sits inside `if self._trusted_host_keys is not None:` (`connection.py:1359-1367`) ⇒ falls through to `return key`. asyncssh's own doc (`connection.py:7576-7577`): "If this is explicitly set to `None`, server host key validation will be disabled." ⇒ `_PinnedHostKeySSHClient.validate_host_public_key` (`ssh.py:55`) never called, `ssh_host_key_mismatch` (`ssh.py:213-220`) unreachable from a real handshake, `require_host_key_fingerprint=True` = DB-column-non-empty check ∧ nothing more. Proven live vs real `asyncssh.create_server` with a wrong pin → connected, exit 0, command ran. **⊥ port drift** — `noa-old` `MCP` has the identical `known_hosts=None` + callback pairing ⇒ **§V69's premise FALSE**: "host-key pinning, TOFU refresh already hardened there" names a control that never worked upstream; C13/V69 copy-⊥-rewrite was obeyed correctly ∧ inherited a dead control. Port made the claim worse: `ssh.py:9-11` docstring (⊥ in source) states the inverse of the truth. Suite CERTIFIES the dead control: `apps/api/tests/test_remote_exec_ssh.py:125-131` has the double call the callback itself + synthesize `HostKeyNotVerifiable`, docstring `:3-5` claims "the way the real handshake does", `:268` asserts `known_hosts is None` AS a safety property. Blast radius = ∀ SSH-carried path: WHM `csf_cli.py:94`, `imunify_cli.py:141`, `availability.py:101,105`; PMG `pmgsh` writes to `/config/mynetworks`. Decrypted SSH password/key presented to whatever answers `host:port` (KEX+auth complete before any check) ⇒ MITM/DNS-hijack harvests the credential; spoofed host returns forged `csf -g` text → `parse_csf_grep_output` renders a clean verdict → T25 receipt before-state. Proxmox unaffected (HTTP-only, I.ext). False docs now: `docs/integrations/whm.md:189`. FIX OWED, ⊥ applied: `ssh_get_host_fingerprint` ! also → base64 pubkey, T54 stores beside fingerprint, pass `known_hosts=([key], [], [])` ⇒ reject in-handshake ∧ pre-auth, dead callback class deletable. Post-connect fingerprint compare = stopgap ONLY ∵ credential already sent. **AS FIXED ≠ that plan** — no pubkey column, no T54 dependency, one kwarg: `known_hosts=([], [], [])`. Empty trusted set ⇒ `_trusted_host_keys` non-`None` ⇒ asyncssh keeps the block live ∧ falls through to `validate_host_public_key` in KEX, so the existing fingerprint-only storage suffices ∧ `_PinnedHostKeySSHClient` goes from dead code to the live check. Same kwarg on the TOFU path ⇒ `ssh_get_host_fingerprint` now reports the key the handshake validated (`get_server_host_key()` fallback retained). Closes the TOFU-refresh gap too. Verified live vs `asyncssh.create_server`: wrong pin → `ssh_host_key_mismatch` ∧ server recorded ZERO auth attempts (credential ⊥ sent); right pin → command ran. Fake-handshake pin tests REPLACED by loopback-server tests; `known_hosts is None` assertion inverted to `== ([], [], [])`. `docs/integrations/whm.md:189` true as of the fix. §V69 re-worded|V82,V69
B3|2026-08-07|B2's fix re-worded §V69 ∧ ⊥ swept its restatements ⇒ `README.md:113-114` ("SSH banner stripping, `sudo -n` escalation, host-key pinning, and TOFU refresh are already hardened in them") ∧ `AGENTS.md:96-97` (same sentence, caveman) kept teaching the premise B2 disproved. `AGENTS.md` = the file ∀ agent loads as project instruction ⇒ retracted premise shipped as a standing order to trust an inert control ∧ to copy it forward on the next port. Neither file cited V69 ⇒ a citation grep found nothing; only a CLAIM grep ("hardened") found them. Separately `docs/integrations/whm.md:21` ∧ `docs/integrations/pmg.md:24` cited §V.69 for "host-key pinning with TOFU refresh": that claim is TRUE of NOA post-7cbc11c, but V69 ⊥ carry it any more — V82 does ⇒ citation pointed at the line that retracts the claim it supports. Docs-only, ⊥ code defect. FIX: README ∧ AGENTS now state pinning ∧ TOFU were ⊥ hardened upstream + name B2/V82; integration docs re-cite §V.82; V69 widened to "§V ∨ any doc"|V84,V69,V82
B4|2026-08-07|`test_auth_routes.py::test_logout_is_idempotent` ∧ `test_jwt_service.py::test_clear_session_cookie_is_idempotent_and_needs_no_auth` compared 2 `Set-Cookie` headers VERBATIM. Starlette `delete_cookie` → `set_cookie(..., max_age=0, expires=0)`, ∧ `http.cookies.Morsel.OutputString` renders an int `expires` via `_getdate(0)` = **now** as HTTP-date ⇒ 2 clears straddling a second boundary emit `expires=Fri, 07 Aug 2026 16:11:11 GMT` vs `...16:11:12 GMT`, byte-identical elsewhere ⇒ red ~1-in-N, green on rerun ∧ on 3 repeats. Test defect, ⊥ code defect — idempotence HOLDS: what differed is a stamp a browser reads as "already expired" either way, ∧ `Max-Age=0` (the attribute that does the clearing) never moved. Load ⊥ the cause; a 1s gap between the 2 asserts is the whole mechanism. Reproduced deterministically: 2 `clear_session_cookie` calls + `sleep(1.05)` ⇒ raw compare False, `cookie_shape` compare True. Pre-existing since T8. FIX: `tests/support/cookies.py::cookie_shape(header)` → `(name, value, attributes − expires)`, both tests compare through it (⊥ 2 copies of the drop rule — the 2 files already own separate `set_cookie_header` ∵ httpx `get_list` vs Starlette `getlist`, so the SHARED part is the clock rule, V66) + `test_jwt_service.py::test_cookie_shape_still_separates_cookies_that_differ` (a clear aimed at another `Domain` ! stay distinguishable) so the drop ⊥ degrade into a tautology (V87)|V87,V66,V67,V6,T8
B5|2026-08-08|`test_action_request_decisions_live.py::test_concurrent_approves_produce_one_decision_and_one_run` (T37, V28) asserted "one winner, one `tool_runs` row" over an `asyncio.gather` of 2 `service.approve` calls ∧ **passed with `.with_for_update()` DELETED** — measured twice: the plain gather, ∧ a 2nd version that added an event handshake but released the first transaction BEFORE the second's SELECT was issued. Cause: gather ⊥ a race — the first coroutine finishes INSERT+UPDATE+COMMIT before the second's read reaches the server ⇒ the second reads APPROVED ∧ 409s for the RIGHT reason under the WRONG mechanism. ⇒ V28's row lock was asserted by prose ∧ held by nothing — B2's shape ∧ V69's, one layer up. Test defect, ⊥ code defect: `FOR UPDATE` was present ∧ correct throughout. ⊥ caught by reading ∧ ⊥ by the suite — caught by a MUTATION battery run at build time, which is the only reason it is in this table ∧ ⊥ in production. FIX: `ObservedDecisionRepository` holds the first transaction open between its locked read ∧ its commit; the second ANNOUNCES before its own read; the assertion moved to ORDER (`second:read` after `first:committed`) with the win count kept as a second check; + `test_an_unlocked_read_of_the_same_row_does_not_wait` proving the forbidden order is REACHABLE, else the new assertion is itself a tautology. 20/20 mutations red after|V89,V28,V87
