# AS-BUILT (NOA)

Archive of as-built deviation notes for completed `SPEC.md` §T tasks.
`SPEC.md` keeps the task intent line + cites; the deviation detail lives here.
Sub-clause addresses cited elsewhere (`T38(g)`, `T32(b)`) resolve in this file.
Same caveman encoding as `SPEC.md` (see `FORMAT.md`).

## T12

AS BUILT ≠ this line, 2 deviations: (a) ⊥ `request` param — `verify_token` gets no request arg, R4 contextvar serves both callers; a `Request` param ⇒ middleware ∧ verifier reach one function 2 ways. (b) → `McpIdentity` (T11 shape, ⊥ token material), ⊥ `users` row. 401 shapes named in body by `McpAuthErrorMiddleware`, ⊥ by `verify_token` (R2: ⊥ body hook) — middleware inside `AuthenticationMiddleware`, outside route, reads refusal stashed on ASGI scope ⇒ 1 resolve/req. Shapes: missing header, invalid token, expired, bound mismatch. Rate-limit failed auth keys = `x-noa-librechat-user` ∧ bearer SHA-256, ⊥ source IP (in-cluster addr ⊥ stable; C24 ⇒ IP block = fleet outage). Counted: `mcp_token_invalid`, `librechat_user_mismatch` ONLY. Store = `login_rate_limits` new scopes, ⊥ migration; math shared via `AttemptLimiter`. Header reads: `get_http_headers()` default STRIPS `authorization` ∧ `mcp-session-id` ⇒ pass `include={"authorization"}`; custom keys lowercased (`x-noa-librechat-user`) (R5). In-tool identity = `current_mcp_identity()` over `get_access_token()`, ⊥ re-parse bearer

## T13

AS BUILT ≠ this line, 3 deviations: (a) ⊥ module singleton — `build_mcp_server(*, auth)` + `build_mcp_http_app(*, auth_context)` in `noa_api/mcp_server.py`, one server per app ∵ `create_app()` runs many times in suite; `get_mcp_server()` dropped; T19+ register tools inside `build_mcp_server`. (b) `create_app()` ! own `AppRuntime` (settings, engine, session factory, `LDAPService`) ∵ `http_app()` snapshots `self.auth` at build time (`fastmcp/server/mixins/transport.py:419`) ⇒ verifier ! precede `FastAPI(...)`; lifespan keeps `JWTService` (T8 boot-failure rule) + engine dispose. (c) `MCP_APP_PATH="/"` ∵ Mount strips prefix (fastmcp default `/mcp` ⇒ `/mcp/mcp`) ⇒ endpoint answers `/mcp/`; bare `/mcp` → 307, method+body preserved — measured, ⊥ 404

## T14

AS BUILT deviates on ONE line: `known_hosts=([], [], [])`, ⊥ the source's `known_hosts=None` — the port shipped the inert pin first (B2), fixed same day (V82). Pin tests run against a real loopback `asyncssh.create_server`, ⊥ a handshake double

## T15

AS BUILT ≠ this line, 3 deviations: (a) settings INJECTED — `SecretCipher.from_settings(settings)`, `_yopass_store(..., *, settings)`; `noa-old`'s module-global `settings` import + `@lru_cache get_secret_cipher()` + module-level `encrypt_text`/`decrypt_text` wrappers dropped ∵ ⊥ settings singleton here (T5 `get_settings()` called once, in `build_runtime`); T54 builds 1 cipher on `AppRuntime`. **SUPERSEDED 2026-08-07 by T21(e)** — T21 needed it first ⇒ `AppRuntime.secret_cipher` exists now, T54 READS it. (b) errors hoisted to `core/secrets/errors.py` ∧ subclass `NoaError` (⊥ bare `Exception` like `noa-old`) ∵ T54 decrypt raises out of an HTTP route ⇒ 1 handler shapes it (V73). Mapped: `SecretCryptoError` 500, `YopassError` 502, `YopassNotConfiguredError` 500. Codes verbatim: `yopass_not_configured`, `yopass_store_failed`. (c) `redaction.py` ⊥ ported — ⊥ named in this line, ⊥ caller until `tool_runs.args` redaction (T35,T47,T55); lands with them

## T16

AS BUILT ≠ this line, 6 deviations: (a) `sudo -n` ⊥ a caller argument — T14 moved decision into `build_remote_command`, which reads resolved username itself ∵ V55 = biconditional ∧ boolean param lets one call site break it either way. (b) `SecretCipher` INJECTED per T15 — ⊥ settings singleton ⇒ `maybe_decrypt_text` arrives as argument, T54 builds the one instance (SUPERSEDED by T21(e) — built there, T54 reads it). (c) `CSFCLIError`/`ImunifyCLIError` collect under `WHMFirewallCLIError` base ∧ subclass `NoaError`, mapped once to 502 ∵ T54's validate route raises them out of an HTTP route ∧ V73 wants one handler; error-code strings unchanged. (d) Binary probing split out of `imunify_cli.py` → `availability.py`; on `MCP` the CSF probe ∧ `asyncio.gather` lived inside the Imunify module. (e) `addon_csf.cgi` HTTP firewall path DROPPED — uncalled on `MCP`, ∧ I.ext puts WHM firewall access on SSH. (f) `command_output_text` → `core/remote_exec/output.py` ∵ `noa-old` carried it 3× byte-identical, incl. in the PMG module T18 ports (V66). ⊥ ported, deliberately: per-reseller `token_resolver` (needs `whm_server_tokens` table absent from T4 schema ⇒ spec change, ⊥ build call — T22/T23 hit this on reseller-owned accounts), `server_ref` (named in T19's line), C22 never-implement client methods (`change_contact_email`, `change_primary_domain`) ⇒ capability ⊥ one line from exposure. `WHMClient` opens 1 httpx client PER request (⊥ pooled, unlike T17) ∵ `verify_ssl` per-server ∧ call rate human-paced

## T17

AS BUILT ≠ this line, 3 deviations: (a) C22 client methods ⊥ ported — `get_user`, `get_pool`, `get_effective_permissions`, `add_vms_to_pool`, `remove_vms_from_pool`; sole callers on `MCP` = `proxmox/tools/pool_tools.py` ∧ `core/workflows/proxmox/postflight.py` ⇒ pool-move pair + `proxmox_get_user_by_email`, ∀ on C22 list ⇒ capability ⊥ one line from exposure (mirrors T16's `change_contact_email` call). (b) `build_proxmox_client_from_creds(..., cipher: SecretCipher)` = ONE decrypt site (C7,V48); `noa-old` had 3 — admin validate service, tool `client_for_server`, workflow postflight — ∀ reaching module-global `maybe_decrypt_text` T15 deleted with the settings singleton. (c) `async with` added (`__aenter__`/`__aexit__`) ∵ `close()` exists on `MCP` ∧ called NOWHERE ⇒ ∀ tool call leaked socket + TLS context; `close()` kept, idempotent. 2 upstream oddities kept ∧ documented, ⊥ silently fixed: `_payload_error`'s trailing `status_code >= 400` branch UNREACHABLE (re-reads `message` the loop above consumed ⇒ 4xx+prose = `proxmox_api_error`, ⊥ `http_error`), ∧ `regenerate_qemu_cloudinit` returns UPID via `_request_json` ⊥ `_request_json_task` ⇒ result carries `data`, ⊥ `upid` (whether to poll = T27's call). Codes split by operator action, ⊥ cosmetics: `permission_denied` (fix Proxmox ACL) ≠ `auth_failed` (fix NOA row); `digest_mismatch` (re-read) ≠ `proxmox_api_error` (retry). `get_qemu_config` fails closed `invalid_response` on digest-free config ∵ digest = CAS token T28 writes under. `build_cipher` moved `tests/support/whm.py` → `tests/support/secrets.py`, re-exported from old name (V66)

## T18

AS BUILT ≠ this line, 6 deviations: (a) `sudo -n` path ADDED — source had NONE ⇒ non-root `ssh_username` on `pmg_servers` row failed silently on perms; both builders take resolved `SSHConnectionConfig` ∧ route via T14 `build_remote_command` (reads username itself) ⇒ V55 biconditional holds by construction, ⊥ per call site; root ⇒ emitted command byte-identical to source. `TERM=dumb` stays AHEAD of `sudo`, ⊥ inside escalated command ∵ it is `ssh_exec` env, ⊥ a pmgsh arg. (b) denied `sudo -n` → `ssh_sudo_required`, ⊥ generic command failure — in BOTH success predicates (`require_pmgsh_success`, `require_pmg_mutation_success`); reachable only ∵ (a); collapse into "command failed" sends operator hunting an install already there (`noa-old` GH #82). (c) `PMGSHCLIError` → own module ∧ subclass `NoaError`, mapped ONCE to 502 in `noa_api/api/errors.py` ∵ T54 validate route raises it out of an HTTP route ∧ V73 wants one handler; ONE class for whole surface (matches `SSHExecutionError`; class-per-code = rewrite C13/V69 forbid), failure named in `error_code`; code strings unchanged, incl. converted `SSHExecutionError` codes. (d) `SecretCipher` INJECTED per T15/T16 ⇒ ONE decrypt site on PMG SSH path ∵ ⊥ settings singleton here. **SUPERSEDED 2026-08-08 by T31(a)** — `pmgsh_cli` ⊥ take `(row, cipher)` at all now, the caller resolves ∧ hands a `SSHConnectionConfig`. The decrypt SITE is unchanged (`resolve_pmg_ssh_config`); its CALLER moved up to the tool, which is what closes the DB session before the SSH hop. (e) `command_output_text` imported from `core/remote_exec/output.py` — source held 3rd byte-identical copy, T16 hoisted it naming this port as the reason (V66). (f) binary + path constants PUBLIC: `_PMGSH_BINARY`/`_PMGCONFIG_BINARY` → `PMGSH_BINARY`/`PMGCONFIG_BINARY` + `MYNETWORKS_PATH`, matching `CSF_BINARY` ⇒ T29–T31 tools ∧ tests assert vs them, ⊥ retype literals. `PMGCONFIG_BINARY` stays BARE name source used (≠ absolute `PMGSH_BINARY`) ∵ sudoers `secure_path` carries `/usr/bin` on default PMG node ∧ inventing a path `noa-old` never used = rewrite, ⊥ port. ⊥ ported, out of scope: `server_ref` resolution ∧ `ipaddress` normalization + mynetworks line parsing (→ T29–T31, V59–V61) — **∀ 3 LANDED 2026-08-08 by T31**, ref resolution in `core/servers/` (inventory, ⊥ integration) ∧ the parsing in `core/integrations/pmg/mynetworks.py`; `core/workflows/pmg/` (C16). Test support: SSH transport doubles WHM-named but about `core/remote_exec` ⇒ moved `tests/support/whm.py` → `tests/support/remote_exec.py`, old name re-exports ⇒ T14/T16 tests untouched (same move `support/secrets.py` made for `build_cipher`, V66; direct-import cleanup = T72)

## T19

AS BUILT ≠ this line, 7 deviations: (a) whole tool SPINE landed here, ⊥ named in this line ∧ ⊥ any §T row — `RbacToolMiddleware` (V1), `sanitize_tool_errors` (V19), registry catalog guard (V10,C22) ∵ V1 ∧ V19 bind the moment a tool exists ⇒ first tool ⊥ ship without them; discipline recorded as V83. (b) V19 mapping = decorator on tool fn, ⊥ middleware (R25); `mask_error_details=True` on server = 2nd line. (c) gate intersects REGISTERED names ∵ admin bypass grants whole CATALOG ⇒ `whm_list_accounts` (T20, catalogued ∧ unbuilt) passed the gate → fastmcp `Unknown tool` = other shape ∧ leaks which tools built; one code `tool_not_permitted` ∀ refusal (V83d). (d) `core/servers/whm_repository.py` = READS only (`list_servers`,`get_by_id`); `create`/`update`/`delete` ∧ `SQLWHMServerTokenRepository` ⊥ ported → T54 (`whm_server_tokens` ∉ T4 schema); `get_by_name` folded into resolver's in-memory match ∵ a 2nd round trip can disagree with the list the tie was judged against. (e) `WHMServerRowLike` carries `to_safe_dict()` ⇒ sole sanctioned outward render (V2,V8); T20–T26 need creds off the resolved row ⇒ widen that Protocol ∨ pass the row to `resolve_whm_ssh_config`, **⊥ re-read by id**. (f) mount harness moved `test_mcp_mount.py` → `tests/support/mcp_mount.py` + `McpSession` (handshake, `Mcp-Session-Id`, SSE unwrap) ∵ T19 RBAC tests drive the same real mount (V66). (g) **V45 UNMET at T19** — `tool_runs` = T35, unbuilt ⇒ ∀ tool call unaudited; write path = T73. **CLOSED 2026-08-07 by T73** — `ToolRunAuditMiddleware` beside this gate; ∀ READ now writes a row ∨ the call is refused

## T20

AS BUILT (`178044e`) = this line + 7 things it ⊥ name: (a) **counts-only `structured_content` envelope, ∧ T56(f)'s "⊥ `structured_content`" was WRONG here** — T32(b)'s reason has 2 clauses ∧ the load-bearing one (a CHANGE call skips the audit middleware, V46 ⇒ ⊥ reader owed an envelope) is FALSE for a READ: `ToolRunAuditMiddleware` reads the run's status ∧ summary off `structured_content` ∧ `status_for_payload(None)` = FAILED BY DESIGN (`core/audit/summaries.py` — a result that ⊥ be read as a success is ⊥ evidence of one) ⇒ content-only, ∀ successful large listing files itself in the audit trail as a FAILURE with an empty summary, silently, for the one tool whose answer is thousands of rows (V20, V45). `table_result_envelope` = `tool_ok(total_rows, stored_rows, truncated)` — `tool_ok`, ⊥ a dict literal, ∵ `ok` is the field a status is read off ∧ a surface spelling its own success = a surface the audit path records as a failure. ⊥ rows, ⊥ token, ⊥ URL ⇒ T32(b)'s SURVIVING clause (an envelope ⊥ become a 3rd place the address lives) holds, asserted by JSON-dumping the envelope ∧ grepping token + path + base out of it. Content array BYTE-IDENTICAL ⇒ R29's measured render path untouched. Both counts unconditional in SHAPE even when they agree (`_rows_sentence`'s rule one representation over: a reader inferring "⊥ truncated" from equal numbers reports a capped table as complete the 1st time a key goes missing). A measured reason copied ACROSS a risk boundary = the defect; a reason ! be re-derived per risk (V69's shape). (b) **`sanitize_tool_errors` WIDENS a return type ⊥ fixes one** — `AnswerT` TypeVar, `ToolAnswer = ToolResult \| ToolPayload` (`mcp_tools/results.py`) ⇒ success = content blocks whose ORDER is part of what the operator is told (V25), failure = the `{"ok": false}` envelope every tool shares ⇒ ONE refusal shape in front of the model however a tool succeeds (V19) ∧ `status_for_payload` still reads a failure off any tool's result (V20). Registered handler annotated with the UNION on purpose: fastmcp derives ⊥ output schema from a return type that can be a `ToolResult`. (c) **⊥ `limit` param** — V85's cap = the bound an OPERATOR asked for; nothing is dropped on their behalf here. Only bound = the table's own (`RESULT_TABLE_MAX_ROWS`), applied at the WRITE ∧ reported in the text ∧ in the envelope ⇒ V85 answered by the SURFACE, ⊥ by the tool. Rows sorted by `user` BEFORE hand-off all the same — V85's clause DELEGATED at T56(d) lands HERE: `cap_rows` = a prefix ∧ never a re-sort, `listaccts` order = WHM's own ∧ undocumented ⇒ an unsorted capped page = an arbitrary subset that changes between 2 identical calls. (d) **`fetch_whm_accounts` returns the RESOLVED server's name** (`server=`) ∧ the summary names it — a tool that echoed `server_ref` back tells an operator with several WHM boxes nothing they ⊥ type. Resolution already happened inside the shared internal ⇒ free; re-reading it in the caller = a 2nd resolution that can DISAGREE with the 1st (T19's `whm_ref` rule, T21(f)). ⊥ name → generic sentence, ⊥ `None` printed at the top of an operator's page. (e) **9 columns DECLARED (`WHM_ACCOUNT_TABLE_COLUMNS`), ⊥ derived from a row** — `listaccts` rows are SPARSE (`normalize_whm_account_summary` omits a field WHM ⊥ send) ⇒ a column list built from the 1st row drops a column every later row has. Keys pinned by test vs what the normaliser can emit ⇒ the 2 lists ⊥ drift (V66). Labels sit beside the keys; T21's whitelist unchanged (⊥ ip, ⊥ plan, ⊥ disk counters, ⊥ theme — V26). (f) **a park that fails REFUSES the READ** — `ResultTableUnavailableError` → `result_table_unavailable` (V19), ⊥ a result carrying the address of a table that was never stored: a dead link in a transcript that persists is found later, by an operator, ∧ a refusal ⊥ (V26). Cap ∧ redaction stay the SURFACE's (V8, V85) — this tool hands rows over ∧ takes ⊥ writer exemption. (g) test-support, ⊥ a behaviour change: `UNREGISTERED_TOOL` `whm_list_accounts` → `pmg_whitelist_list` (T30) in `test_mcp_tool_rbac.py` ∧ `test_mcp_tool_audit.py` — this tool ANSWERS now ⇒ V10's stand-in ! be a name the registry genuinely ⊥ know, else those files assert V10 ∧ "an unregistered name writes ⊥ row" against a tool that answers. Lane: `test_whm_tools_list_accounts.py`, 24 cases; audit lane += `test_a_large_read_is_recorded_completed_with_its_counts` + its negative control; table-surface lane += 3 envelope cases (the counts, both counts when nothing was dropped, ⊥ address ∧ ⊥ token). 6 mutations proven red: the envelope removed (reddens the AUDIT case alone ∧ its negative control stays green), the sort dropped, a column dropped, the summary's server dropped, the account whitelist turned passthrough, the park failure unnamed. Verification: 1915 pytest (+31), ruff check + format clean; web-embed 286 vitest + eslint + tsc clean (docs only there). Doc sweep same change (V84a): `mcp_tools/__init__.py` (2 places said T20 ⊥ built), `apps/web-embed/README.md` (2 places said the route has ⊥ producer), `docs/integrations/whm.md` (+ a `whm_list_accounts` section)

## T21

AS BUILT ≠ this line, 8 deviations: (a) `core/integrations/whm/accounts.py` CREATED, ⊥ named in this line — `normalize_whm_account_summary` ported from `noa-old` `whm/tools/result_shapes.py` (tool package there) into `core/` ∵ T20 ∧ T22/T23 preflights need the same field list ∧ 2 copies = one of them reports a field the others ⊥ (V66). WHITELIST, ⊥ passthrough: `listaccts` sends ip/plan/disk+bandwidth counters/theme/locale/partition ⇒ dropped, ∵ result persists in LibreChat's MongoDB (V26). Row with ⊥ `user` dropped WHOLE — `user` = the argument ∀ account CHANGE takes ⇒ a row NOA ⊥ name = a row whose follow-up call has no argument. WHM booleans normalised (`0`/`1` ∧ `"0"`/`"1"`) ∵ `"0"` truthy in Python ⇒ raw read reports a LIVE account suspended; `is_locked` falls back to `suspendlock` incl. on JSON-null (`.get` defaults only on MISSING key). (b) match PER FIELD, ⊥ joined haystack — source built `f"{user} {domain}"` ⇒ a query with a space matched ACROSS the junction (`"acme sho"` = user `acme` + domain `shop.example.com`) ⇒ operator handed a row containing nothing they typed. C13 copy-⊥-rewrite ⊥ extend to carrying a defect forward (V69's shape: upstream provenance ⊥ evidence). (c) truncation STATED — `total_matches` + `truncated` ⊥ in source, ∧ matches sorted by `user` BEFORE the cut. Generalised to **V85** in the same change ∵ the next capped READ ⊥ inherit a rule that lives only in this row. (d) `limit` bound TWICE — schema `Annotated[int, Field(ge=1, le=100)]` (what refuses over MCP ∧ what tells the model the range exists) ∧ in-tool `limit_invalid` (what holds for the in-process call, C9/V17) ⇒ bound = property of the TOOL, ⊥ of its registration. Blank `query` → `query_required` = V21's gate ∧ ⊥ expressible in schema (`min_length` counts whitespace). Both guards run before any I/O — asserted as `servers.reads == 0` ∧ zero WHM requests, ⊥ as a return value. (e) cipher wiring ⊥ named in this line ∧ **SUPERSEDES T15(a)/T16(b)** — those said "T54 builds 1 cipher on `AppRuntime`"; T21 built it (`AppRuntime.secret_cipher` = `SecretCipher.from_settings`, `McpToolContext.secret_cipher`), T54 now READS it. Construction in `build_runtime`, ⊥ lifespan ∵ tool context precedes `FastAPI(...)` (T13(b)) ∧ a bad key ! be a boot failure (T8's rule). Doc sweep same change (V84a): `core/secrets/crypto.py`, `core/integrations/whm/client.py`, `core/integrations/proxmox/client.py` each asserted the future fact as fact. (f) `WHMServerReadRepository` now GENERIC in its row (`Protocol[RowT_co]`) ∧ `resolve_whm_server_ref` preserves that type ⇒ the tool reaches credentials off the row it RESOLVED — ⊥ cast, ⊥ widened `WHMServerRowLike`. T19's `whm_ref` line named the 2 options ∧ ruled out re-read-by-id (2nd query can disagree with the list the tie was judged against); widening instead would let a resolver double be handed to something that DECRYPTS, which is what the narrow view exists to stop. (g) `whm_client_factory` on the context + `transport=` on `build_whm_client` — ⊥ named; WHM = first external HTTP boundary on the tool path ⇒ a seam ! exist, ∧ it doubles the SOCKET only. Real `WHMClient` stays in the path ∵ its whole job = normalising WHM's HTTP-**200**-with-`metadata.result: 0` refusals ⇒ a doubled client lets a tool pass against error shapes WHM never sends. Real cipher ∧ the one decrypt site stay live ⇒ `Authorization: whm root:<plaintext>` is assertable. (h) tests ⊥ named in this line: `test_mcp_tool_audit.py` gains a case driving THIS tool ∵ every other case there drives `whm_list_servers` — the tool that shipped BESIDE the middleware ⇒ the suite ⊥ distinguish "audits ∀ READ" from "audits that one" (V45,V83b); `test_mcp_tool_rbac.py` now pins the registered SET ∧ that one grant names one tool (V1 is per-tool ∧ was unobservable with a single tool registered). 8 mutations proven red: joined haystack, `truncated` off-by-one, no sort, decrypt skipped, both guards removed, schema bounds dropped, `choices` dropped, tool absent from the risk map. ⊥ built here: `whm_list_accounts` (T20 — V64 table surface, T59-gated), `fetch_whm_accounts` = the shared internal both use (C9,V17), DB session closes BEFORE the WHM round trip ∵ a pooled connection held across someone else's server turns a slow WHM host into a DB outage

## T22

AS BUILT (`5297529`) = this line + 8 things it ⊥ name: (a) **preflight = `fetch_whm_accounts`, ⊥ a 2nd resolve-and-list** — T21's internal, shared (V66); `collect_account_state` + `match_account` sit on top ∧ T23 inherits both. `fetch_whm_accounts` gained `server_id=` in the same change ∵ the runner needs the machine the CARD described (see (e)) ∧ re-reading it in the caller = a 2nd resolution that can disagree (T19's `whm_ref` rule, T20(d) one field over). ONE `listaccts` per gate call, asserted as a request COUNT — a 2nd read would mean the card describes a state the tool ⊥ gather. Match EXACT ∧ case-SENSITIVE on `user`: cPanel usernames are, ∧ a fold would suspend `acme` where an operator approved `Acme` (C10). (b) **already suspended → `no_op`, ⊥ row opened** — ⊥ named in this line; the preflight is what DISCOVERS there is nothing to do, ∧ a card for a change that would do nothing costs an operator a decision ∧ leaves a row somebody closes. Trace = a structured log line ∧ nothing else, which is the right amount for a call that changed nothing (a CHANGE `tools/call` writes ⊥ `tool_runs` row either, T73). Payload built from the USERNAME ∧ the server, ⊥ from the account summary — that summary holds `suspendreason` ∧ this answer is transcript (V26, ∧ (d)). (c) **the executor WIDENED, ∧ T38's structural absence retracted** — `AuthorizedChange`/`ChangeExecutionRequest` said "⊥ `reason`, ∧ nowhere to put one"; WHM `suspendacct` has a suspension note whose only honest content is the operator's own words ⇒ `reason: str` on both, `load_authorized` selects it, `_run` passes it. Non-OPTIONAL ∵ only APPROVED rows reach there ∧ T34's `ck_action_requests_decided_reason` refuses a decided row with a blank one; `row.reason or ""` = a type coercion, ⊥ a fallback. V43 amended, V96 written. V29 ⊥ amended — its subject is the async handoff ∧ the one-transaction rule, ∧ the absence retracted here lived only in docstrings. (d) **V96 = 2 return paths, ∧ the FIRST doc naming the door named the WRONG one** — path (a) WHM echoes the note as `suspendreason` on ∀ later `listaccts` ⇒ `whm_search_accounts` withholds the field (`ACCOUNT_FIELDS_WITHHELD_FROM_MODEL`), the parked table keeps it (cookie-gated, V27); path (b) the runner's payload → `tool_runs.result_summary` → `noa_get_action_result` → the model. The T22 code comments ∧ `docs/integrations/whm.md` said path (b) ran through `action_receipts.receipt_data` ∧ T63 — **FALSE**, VERIFIED at the spec pass ⊥ at build: `core/approvals/results.py:143` leaves `include_receipt` at default ⇒ that reader ⊥ join `action_receipts` (V76, T42's flag) ∧ the receipt is read only by the card ∧ the admin audit surface. Conclusion survived, mechanism died; 4 sites corrected ∧ the test gained an assertion on the DERIVED summary, ∵ the payload assertion alone would stay green if `result_summary` ever composed its own text (V69's shape one layer over). (e) **runner resolves by `evidence["server_id"]`, ⊥ by `server_ref`** — inventory can be edited between a request ∧ its approval, ∧ the string is the MODEL's (V33). Asserted on the HOST the mutation reached with the arguments naming a DIFFERENT server, ∵ that is the only way "it ran somewhere else" shows. A row deleted in the gap → `whm_server_unavailable` BEFORE the mutation; a `server_id` that ⊥ parse → same code, ∵ it round-tripped through JSONB. (f) **postflight has a THIRD answer** — suspended → verified; still live → `postflight_failed` (WHM accepted a call that ⊥ take); the confirming read ⊥ answering → `{ok: true, verified: false, verification: "unavailable"}`. V62's rule one system over: a failure there sends an operator to re-suspend an account that may already be suspended, ∧ a plain success claims a confirmation nobody has. `noa-old` had ⊥ third answer — it returned `postflight_failed` for both. (g) **3 placeholders retired** — `build_change_runners` was `{}` ∧ `test_no_change_tool_is_registered_yet` asserted the emptiness (replaced by the positive: the registered CHANGE set == the runner-map keys, ∵ a runner under a name nothing exposes is as invisible as a tool with no runner, ∧ `assert_change_runners_cover` catches only the 2nd); `test_the_real_registry_passes_the_guard` now asserts `ToolRisk.CHANGE ∈ registered` so the guard is ⊥ passing vacuously (V87); `test_mcp_change_gate.py`'s "T22 is where the gate first runs over the real mount" became a mount lane here. `test_mcp_tool_audit.py`'s CHANGE branch was driven by a SYNTHETIC risk map around a tool that did ⊥ exist ⇒ the mount lane re-makes that claim with a real registration. `UNREGISTERED_TOOL` unchanged (`proxmox_reset_vm_password` — still unbuilt), unlike T20(g). (h) lane = `test_whm_tools_suspend_account.py`, 24 cases in 3 groups (tool / runner / mount) + `test_a_suspension_note_never_reaches_the_model` in T21's file + `test_the_operator_reason_travels_from_the_row_to_the_runner` in the LIVE executor file. That last one is the V93-shaped hole: the runner test proves the note goes on the wire ∧ the service test proves the pass-through, both against values a TEST handed them ⇒ `load_authorized` could answer `""` ∧ everything stays green. **6 mutations proven red**: no-op guard removed, postflight dropped, a NOA-authored note in place of `request.reason`, the `suspendreason` withholding removed, the runner unregistered, `load_authorized` no longer reading the row's reason. Verification: 1975 pytest (0 skipped — live lanes ran), ruff check + format clean. Doc sweep same change (V84a): `docs/integrations/whm.md` gains the tool, the runner ∧ the withheld field, ∧ its "⊥ built yet" table drops the 2 rows T20 ∧ this task built

## T23

AS BUILT (`a395682`) = this line + 9 things it ⊥ name: (a) **2 states open ⊥ request, ⊥ 1, ∧ they are ⊥ the same answer** — ⊥ suspended → `no_op` (T22's argument one state over: the preflight DISCOVERS there is nothing to lift); `is_locked` → `account_suspension_locked`, a REFUSAL. The row folds both into "the mirrored guard" ∧ they carry distinct codes ∵ distinct remedies: a no-op means the operator already has what they asked for, a lock means they ⊥ get it until an administrator unlocks the account on the server. Neither writes an `action_requests` row; both leave a structured log line ∧ nothing else (V8). (b) **the lock guard fires on a POSITIVE lock ONLY, ∧ that bound is the decision this row ⊥ name** — `listaccts` OMITS the field on cPanel versions that ⊥ have it ⇒ fail-closed on absence takes the tool off a whole cPanel generation, which costs more than the failed run it prevents. ⇒ the guard = the cheap EARLY half ∧ WHM's own execute-time refusal stays the AUTHORITY (`whm_api_error` carrying WHM's `reason`, the sentence that names what to unlock, passed through by T22's `_passthrough_failure`). Bound by TEST (`test_an_account_whose_whm_never_reported_a_lock_is_not_refused`), ⊥ by docstring — ∧ the negative control ships beside it (suspended ∧ UNLOCKED → a request opens) ∵ "a locked account is refused" passes just as well vs a tool that refuses EVERY unsuspension (V87). (c) **BOTH lock spellings asserted at the GUARD, ⊥ only at the normaliser** — `is_locked` ∧ the older `suspendlock`. `core/integrations/whm/accounts.py` has normalised the pair since T21 *for this task*, but nothing had asserted that the guard reads the NORMALISED summary ⇒ a guard reading one raw key would leave a cPanel generation ungated ∧ every existing test green. (d) **shared, ⊥ mirrored (V66) — ∧ T22's code MOVED to the shared shape rather than sitting beside a copy.** `_verify_suspended` → `_verify_account_state(direction=_AccountChangeDirection)`, a frozen record carrying `target_suspended` + the 3 sentences; `_resolve_change_target` extracted out of T22's runner (evidence → `WHMClient` + username + server name, V33); `_open_account_change` = the gate tail both tools end in; `SERVER_REF_DESCRIPTION` = 1 string, 2 schemas. `MESSAGE_POSTFLIGHT_FAILED` → `MESSAGE_POSTFLIGHT_SUSPEND_FAILED` (⊥ importers outside the module). File 472 → 780 lines, C14 cap 900; a verbatim mirror measured ~757 ∧ would have been TWO copies of V62's 3-answer postflight, where the 3rd answer is exactly what a copy drops. (e) **V96a bites on the READ here, ⊥ on the WRITE** — `unsuspendacct` takes ⊥ note ⇒ nothing leaves NOA, ∧ the outgoing query is asserted EXACT (`{"api.version": "1", "user": …}`) ⊥ by the absence of one key name, ∵ what wants guarding is a note param added LATER under whatever spelling WHM would use. What T22 could ⊥ meet: an account being unsuspended IS suspended when the preflight reads it ⇒ its `listaccts` row carries `suspendreason` = the operator's own words from the suspension (T22 reads LIVE accounts ∧ never saw one). That summary rides onto `approval_context` as evidence, where the card ∧ V27's requester-match are its only readers — a model ⊥ reach it (`ActionResultView` has ⊥ field for evidence, V76; `include_receipt` default False, V93's fetch-side bound). What is ⊥ closed by construction = the 2 answers that land in a TRANSCRIPT (V26), so both are built from the username ∧ the server name, ∧ both are asserted for the note. (f) **DECISIONS §9's split ASSERTED, ⊥ restated** — `test_a_grant_for_one_direction_does_not_reach_the_other` reads `tools/list` under a role granted ONLY unsuspend (V1). The merged pairs (`proxmox_vm_nic`, `pmg_whitelist`) accepted RBAC coarsening ∧ RECORDED it as a cost; this pair ⊥, ∧ that is true only while the 2 names stay separately grantable. Same claim in the MCP annotation: `destructiveHint: False` on unsuspend vs `True` on suspend. (g) **5 source docstrings ∧ 4 test docstrings taught a premise T22 already disproved** — "`build_change_runners` is empty until T22-T29 land" still stood in `main.py`, `mcp_tools/__init__.py` ∧ `core/approvals/{__init__,execution,execution_host}.py` AFTER T22 shipped a runner. B3's shape exactly, ∧ invisible to a citation grep ∵ ⊥ one names a §V — only a CLAIM grep ("empty until") found them. Swept here (V84a). Worst of them: `test_mcp_change_gate.py`'s schema sweep said "vacuous today — every registered tool is a READ" while `build_mcp_server` HAD been registering a CHANGE tool since T22 ⇒ the assertion was LIVE ∧ its own docstring said it was ⊥, which is the prose telling a future reader the test may be deleted. (h) lane = `test_whm_tools_unsuspend_account.py`, 30 cases in 3 groups (tool / runner / mount), seams unchanged from T22 (real `WHMClient` over `support.whm_api`'s socket double, real `SecretCipher`, real resolver, real `sanitize_tool_errors`). **7 mutations proven red**: no-op guard removed, lock guard removed, the `suspendlock` spelling, `target_suspended` flipped, the verification-unavailable branch collapsed into a failure, the runner unregistered (fires `assert_change_runners_cover` at CONSTRUCTION, ⊥ at an operator's Approve), ∧ the runner echoing `request.reason` into its payload. Verification: 2005 pytest (0 skipped — live lanes ran; 1975 at T22 + 30), ruff check + format clean. Doc sweep same change (V84a): `docs/integrations/whm.md` gains the tool-table row, a `### whm_unsuspend_account` section ∧ the `is_locked` cite, ∧ its "Not built yet" table drops the unsuspend row. (i) KNOWN LIMIT recorded ⊥ unsaid: per-reseller API tokens ⊥ ported (⊥ `whm_server_tokens` table, ⊥ §C ∧ ⊥ §V mentions one) ⇒ a reseller-owned account refuses BOTH directions at WHM, ∧ what NOA can truthfully do is what it does — `whm_api_error` + WHM's own `reason` into the receipt, a NAMED failure ⊥ a silent one. Adding that table = a spec change, ⊥ a build decision.

## T24

AS BUILT ≠ this line, 8 deviations: (a) **WHM SSH layer re-signed**, ⊥ named in this line — `run_csf_command`, `run_imunify_command`, `check_csf_binary`, `check_imunify_binary`, `check_firewall_binaries` took `(row, cipher)` ∧ each resolved its OWN config ⇒ 4 resolves per call ∧ the ORM row ! outlive its session across ∀ SSH hop. Now they take a resolved `SSHConnectionConfig`; the tool resolves ONCE inside its session ∧ closes it before the first hop (T21's rule — a pooled Postgres connection held across someone else's host is how a slow WHM box becomes a DB outage). Asserted as ORDER (`session-open`,`session-close`, then ∀ `ssh`), ⊥ as a count. Commands on the wire byte-identical. (b) row refusals now raise AT the tool ⇒ an unpinned server answers `ssh_host_key_not_validated`, ⊥ `no_firewall_backend`. Those 3 (`ssh_invalid_host`,`ssh_not_configured`,`ssh_host_key_not_validated`) were never per-backend — both probes read one row ⇒ both come back "not usable" ⇒ "no firewall tools on this server", which is GH #82's misdiagnosis, the one `sudo_required` exists to prevent. `except SSHExecutionError` STAYS around `ssh_exec`: an unreachable host is still "not usable" ∵ a probe that raises ⊥ report on the other backend. (c) code = `no_firewall_backend` (V57), ⊥ source's `no_firewall_tools`. OPEN, ⊥ fixed: `BinaryCheck` carries ⊥ error code ⇒ host-down ∧ binary-absent collapse to one code; message worded "could not be run" so the SENTENCE stays true either way — the diagnosis ⊥ recovered, threading a code out of the probe = T25/T26's call. (d) `raw_output`/`raw_data` DROPPED — source shipped csf's whole `-g` dump ∧ imunify's whole JSON document beside the parsed verdict; DECISIONS §6.5 (old V75) says the `csf.deny`/`csf.allow` line ∧ ⊥ a raw iptables table, ∧ the result persists in LibreChat's MongoDB (V26). Bounded `matches` = the evidence. (e) partial answers STATED — source computed the combined verdict from whichever backend succeeded ∧ else fell through to `not_found` ⇒ a broken CSF beside a clean Imunify reported "⊥ blocked" on a box whose BLOCKING backend was silent. `unanswered_backends` names them; zero answers ⇒ `unknown`, ⊥ `not_found`; CSF's own `unknown` counts as ⊥ answered. **Generalised to V86 in the same change** ∵ T25's 2-part receipt is the 2nd instance. (f) V85's **2nd capper** ⇒ `CSFGrepParsed` gains `total_matches` (the cut is the parser's, so the count is too) ∧ the payload carries `total_matches`+`truncated`. ORDER clause deviates — see V85's amended instances line. (g) evidence ⊥ duplicated per backend — combined `matches` at top level, each backend entry = its verdict ∨ its failure code; the lines already name their source, ∧ a per-backend copy doubles the transcript to say it twice. (h) ⊥ named in this line: `tests/support/whm_firewall.py` CREATED (`FakeFirewall`, `firewall_context`, the 3-module `ssh_exec` install) ∵ T25/T26 = the next callers ∧ `support/whm.py`'s `__all__` is pinned to `FakeWHMServer` by `test_support_layout.py`; `install_fake_ssh_exec_in(monkeypatch, modules, handler)` added to `support/remote_exec.py` with the 1-module helper delegating (V66) ∵ a firewall call crosses 3 modules that each bound their own `ssh_exec`; `whm_server()` gains `ssh_password`/`ssh_private_key`/`ssh_host_key_fingerprint` overrides ∵ the defaults are ciphertext-SHAPED ∧ ⊥ decrypt. 12 mutations proven red: both guards, sequential-⊥-gather, query-an-unavailable-backend, `answered` ignoring failure ∧ CSF-`unknown`, `unknown`→`not_found` fallthrough, truncation unstated, capped-length-as-total, pin ⊥ required, sudo collapsed into no-backend, session held across SSH. ⊥ closes T68 — that row's subject = a silent no-op on an approved CHANGE (T25/T26), ⊥ this READ. Doc sweep same change (V84a): `docs/integrations/whm.md`, `core/remote_exec/sudo.py:37` (comment named `no_firewall_tools` as V57's code — false since T2), `whm/__init__.py`, `availability.py`, `csf_cli.py`, `imunify_cli.py`, `csf.py`, `mcp_tools/__init__.py`

## T25

Deviations from the row as written. **(a) Own module.** `noa_api/mcp_tools/whm_firewall_change.py`, ⊥ appended to `whm_firewall.py` where T24's docstring said T25/T26 would land: a CHANGE tool is 2 halves — the tool, which changes nothing, ∧ the runner, reachable only from `core.approvals.execution` — ∧ both plus T26 push one file past C14's 900. What is shared is CODE ⊥ a file: `gather_firewall_entries` is the in-process before-state (C9, V17, V66), ∧ `noa_firewall_comment`/`without_noa_comment_text` stay beside the READ that has to honour them. T24's registration docstring corrected in the same change (V84a). **(b) V96 closed by a MARKER, ⊥ by a surface-only rule.** Owner-decided 2026-08-15, choosing the T22 shape over the alternative: the reason IS written out (V43) ∧ withheld at T24's surface, rather than replaced on the box by a NOA-authored placeholder. That only works ∵ NOA AUTHORS the comment — `noa:<action_request_id> <reason>` — ∧ `whm_preflight_firewall_entries` cuts from the marker. Cut runs marker → END OF LINE ∵ csf gives a comment ⊥ closing boundary: an operator types prose, ∧ a reason carrying `)` ∨ `'` walks straight through any bracket rule, which is the same leak one length shorter (asserted with `ticket (NOC-4471) — customer 'acme' confirmed`). COST STATED ⊥ hidden: a token csf places AFTER the comment is dropped with it ⇒ `core/integrations/whm/imunify.py::format_imunify_matches` now renders `[expires: N]` BEFORE `(comment)` so the cut takes nothing else, ∧ an ORDERING test guards it ∵ swapping back shortens a model's evidence silently. Cut sits at the tool SURFACE, ⊥ in `parse_csf_grep_output`: the same lines reach the approval card ∧ the receipt through T25's evidence ∧ those are the operator's own (V27, V76 — `ActionResultView` has ⊥ evidence field ∧ `core/approvals/results.py` ⊥ joins `action_receipts`), so a cut in the parser takes the reason off the 2 surfaces that exist to SHOW it. Verdict computed from the FULL parse BEFORE the cut. Marker SURVIVES = the address of the row where the reason is readable behind the cookie, which is what a human on the box needs. A 3rd door found ∧ closed in the same pass: a backend failure message quotes the command it ⊥ run ⇒ `_backend_change_failure` cuts it too, ∵ that message becomes `result_summary` (V96b). Negative control ships beside the leak assertion — a line with ⊥ marker (LFD's own `smtpauth brute force`) kept INTACT — ∵ a rule that cut EVERY line passes the leak test ∧ destroys the evidence the tool exists for (V87). T26 inherits both ends; V96's reach is ⊥ discharged until it lands. **(c) ⊥ no-op branch, ∧ that is a DECISION.** T22/T23 answer instead of gating when the account already holds the state the change would produce; nothing here ever does — the allow entry carries a TTL ⇒ a repeat MOVES the expiry, which is exactly the case an operator re-runs this for when a window is closing. A clean address is ⊥ a no-op either: the release finds nothing ∧ the allow, with the stated TTL, is still the change that was asked for. 2 cases assert it (clean, already-allowlisted), ⊥ a docstring. **(d) Release tolerates a non-zero exit; the allow ⊥.** `csf -tr`/`-dr` ∧ Imunify `delete` exit non-zero for an entry that is ⊥ in that list — the COMMON shape, ∵ most releases target an address held by ONE backend ∨ by a temporary ban ⇒ a strict exit check refuses the ordinary release this tool exists for. Tolerated ∧ LOGGED (`whm_firewall_release_step_tolerated`), so "the entry was already gone" stays separable from "the command ⊥ run". `csf -ta` ∧ Imunify `add` = the entry the operator asked to EXIST ⇒ their failure is the backend's failure, code intact (`ssh_sudo_required` ≠ `csf_command_failed`). Neither is the authority on whether the change took: the POSTFLIGHT is, ∧ it is a fresh read through the same door (V57). **(e) V86 TIGHTENED on the CHANGE side.** `verified: true` requires EVERY usable backend to answer the confirming read — one silent ⇒ `status: changed`, `verified: false`, `verification: unavailable`, the backend NAMED, ∧ NEITHER `released` ∨ `allowlisted` present, ∵ an absent field beats a `false` nobody measured. Negative control: one backend INSTALLED ≠ one backend SILENT — a CSF-only box (most of them) verifies in full, ∧ without that case the unverified branch would fire for every such server. **(f) `change_target.py` hoist (V66).** The 2 refusals every post-approval runner makes from evidence it cannot use — `whm_server_unavailable` ∧ the new `change_evidence_unusable` — plus the 3 status words `changed`/`no_op`/`unavailable`, which land in ONE audit trail ∧ ⊥ have 2 spellings. `uuid_or_none` moved with them. `whm_account_change` re-exports ∀ of it ⇒ ⊥ existing caller ∨ test moved. **(g) Fan-out guard now scans a GLOB.** `test_whm_firewall_gate.py::FIREWALL_MODULES` held a LITERAL path to `whm_firewall.py`; splitting T25 into its own module would have dropped the CHANGE half out of the AST scan SILENTLY — the half where V57's harm actually lives, which is the guard's own stated reason for existing. Now `apps/api/src/noa_api/mcp_tools/whm_firewall*.py`, ∧ a glob matching nothing ASSERTS rather than scanning less than it claims (V87). Deliberately ⊥ widened to the whole `mcp_tools` package: a PMG tool's `asyncio.gather` is ⊥ this guard's business, ∧ a rule that fails for an unrelated module is a rule the next author routes around. **(h) V77's bound held in 3 places.** Schema `Field(ge=1, le=525600)` = what the model is TOLD ∧ what `tools/list` publishes (asserted there, incl. the ABSENCE of a `default` — a default is the permanent-ish entry V77 forbids, ∧ it would be invisible from the body ∵ an omitted argument never reaches it); the body guard = where a caller reaching the function directly meets it (C8's `assert_no_reason_argument` shape); ∧ the RUNNER re-checks after the JSONB round trip, with `bool` excluded EXPLICITLY ∵ `True` is an `int` in Python ∧ would otherwise pass as 1 minute. Resolved `expires_at` computed ONCE ⇒ csf's TTL-in-seconds ∧ Imunify's absolute epoch agree; deriving them apart is how the 2 lists expire by however long the first SSH hop took. Asserted by PROPERTY ⊥ by bytes — the value is clock-stamped (V87, B4). **(i) Tests split on V22's own boundary.** One file reached 1,235 lines vs C14's 900 ⇒ `test_whm_tools_firewall_release_and_allow.py` (tool + mount) ∧ `test_whm_firewall_release_runner.py` (runner), with the shared call helpers in `support/whm_firewall_change.py` ⊥ duplicated (V66). `support/whm_firewall.py` gained `FakeFirewallBox`, which dispatches on the SUB-command (`-g`/`-tr`/`-dr`/`-ta`, `list`/`delete`/`add`) ∧ answers reads from a QUEUE ∵ a CHANGE reads a backend twice ∧ the whole point of the 2nd read is that it differs — a 1-answer double passes against a postflight that never ran. KNOWN LIMIT recorded ⊥ unsaid: `test_whm_tools_firewall_preflight.py` now sits at EXACTLY 900 ⇒ T26 ! split it before adding to it.

## T26

Deviations from the row as written. **(a) Own module, ∧ a HOIST to make room.** `noa_api/mcp_tools/whm_firewall_allowlist.py`: T25 already sat at 775 of C14's 900 ∧ a CHANGE tool is 2 halves. What the 2 firewall CHANGE tools share moved to `whm_firewall_change_common.py` — the before-state shape (`firewall_state`), `BackendChange`, the tolerated commands, the 4 shared evidence keys, ∧ `resolve_firewall_change_target` — which is EXACTLY the move `change_target.py` was made by at T25(f): a 2nd caller is when shared code stops being one module's internals (V66). A LEAF on purpose: letting T26 import its machinery from T25's module would make the 2nd tool a dependent of the 1st ∧ the aggregate registrar a CYCLE, so instead `registry.py` ∧ `change_runners.py` each collect a 3rd contributor, the way every system's registrar is collected. `whm_firewall_change` re-exports all of it ⇒ ⊥ existing caller ∨ test moved, ∧ T25's 2 test files are UNTOUCHED ∧ green, which is the drift proof the hoist owes. T24's registration docstring corrected again in the same change (V84a). Glob `whm_firewall*.py` (T25(g)) picked up BOTH new modules with ⊥ edit — the guard that was widened at T25 paying for itself one task later. **(b) The COLLAPSED VERDICT — new §V.97, ∧ the whole correctness point of the task.** The row said "undo path"; what it ⊥ say is that NOA had ⊥ way to ask the question an undo asks. `parse_csf_grep_output` ∧ `parse_imunify_ip_list_response` both resolve block-over-allow (`csf.py`'s own "an IP in both is operationally still blocked") ⇒ an address in `csf.deny` ∧ `csf.allow` reads `blocked` ∧ the ALLOW entry — the only thing this tool touches — is INVISIBLE in the verdict. 2 harms, opposite directions, both silent: a removal that changed NOTHING reports `removed: true` ∵ the deny entry answered for it, ∧ the no-op (c) refuses a card for the address that most plainly HAS an entry to delete. ∴ `allow_entry: bool` now travels BESIDE the verdict on `CSFGrepParsed`, `ImunifyIPListResult` ∧ `BackendLookup`, computed from the MATCHES before the precedence drops them (∧ from the FULL match list, ⊥ the `max_matches` prefix — the cut shortens evidence, it ⊥ change what csf holds, V85's own line). Asked through `holds_allow_entry`, which SKIPS unanswered backends ∵ silence ⊥ "no allow entry" (V86). Negative controls ship on both sides: a block-only address reports ⊥ allow entry (else the flag reads `True` for any line at all), ∧ a blocked address with ⊥ allow entry is a CLEAN removal (else the failure branch fires for every re-blocked address, the ordinary state after a re-deny). **(c) A no-op branch — the OPPOSITE of T25(c), ∧ owner-decided 2026-08-15.** T25 has none ∵ its allow entry carries a TTL ⇒ a repeat MOVES the expiry ∧ is a real change. A removal has ⊥ such property: an address on ⊥ allow list is ALREADY in the state the change would produce, ∧ asking an operator to authorise that is worse than saying so (T22/T23's rule). GATED on a FULL answer (V86): "there is nothing to remove" is a claim about ABSENCE, ∧ a backend that stayed silent has ⊥ made it ⇒ 1 unanswered backend opens the CARD, ∵ answering would leave an entry standing on the silent backend with ⊥ card, ⊥ run ∧ ⊥ record. `not_found` is ⊥ the only no-op state — a DENIED address has ⊥ allow entry either, ∧ without that case an operator is asked to authorise a removal of nothing on every blocked address. Negative control: a backend ⊥ INSTALLED is ⊥ a backend SILENT — a CSF-only box (most of them) still no-ops, ∵ an absent backend is never in `lookups` at all. **(d) EVERY step tolerated, ∧ the sudo hole that opens.** `csf -tra` (temporary allow) ∧ `-ar` (`csf.allow`) are BOTH sent ∵ an entry lives in one list ∨ the other ∧ NOA ⊥ know which — T25 writes temporary, an operator's own hand-added ones are permanent — so the one that finds nothing exits non-zero, the ordinary case. Imunify `delete --purpose white` likewise. T25 could keep ONE strict command (`-ta` = the entry the operator asked to EXIST); a removal has ⊥ equivalent ⇒ **nothing here is required to succeed ∧ the postflight is the SOLE authority**. That is the hole: with every step tolerated, sudoers granting `csf -v` (all the availability probe runs) while REFUSING `csf -ar` would make a box look usable, tolerate every command, ∧ report an approved CHANGE as having found nothing to do. ∴ `tolerated_csf_step`/`tolerated_imunify_step` now RE-RAISE `ssh_sudo_required` ∧ swallow only the rest — **V55 amended**, held at the MECHANISM ⊥ per tool. Reaches BACK into T25 by construction (its `-tr`/`-dr` gain the same escalation) ∧ changes ⊥ observable there, ∵ its strict `-ta` raised the same code one step later; T25's tests confirm it unchanged. **(e) V96 DISCHARGED, ∧ the 3rd door is new.** A removal writes ⊥ comment ⇒ nothing new LEAVES ∧ V43's permission is unused on this side — the assert is that ⊥ command NOA sends carries the marker, ∵ "the reason ⊥ come back" is also true of a tool that had quietly started sending it. The entry DELETED is T25's ∧ carries `noa:<id> <reason>` ⇒ the bound applies on the way back all the same: the backend failure message quotes the entry it ⊥ remove (cut, `backend_change_failure`), the postflight's `csf -g` lines are read for a verdict ∧ ⊥ put in the payload (asserted on the FAILING branch, ∵ that is the branch where the lines exist), ∧ — the door T25 ⊥ have — **the NO-OP answer is a transcript surface**: a plain tool result, ⊥ a gate response (V26), decided FROM lines that carry the reason. Built from server name + address + 1 measured boolean (T23's rule one system over), ∧ asserted with a leaky allow line for a DIFFERENT address in the same read, ∵ a clean read proves nothing. **(f) ONE outcome boolean, ⊥ T25's two.** DECISIONS §6.5 forbids collapsing 2 outcomes into one "done"; a removal makes ONE claim, ∧ mirroring T25 with a 2nd field would be a field nobody measured. `removed` follows T25's own rule for absence: ⊥ PRESENT at all when a usable backend ⊥ answer the confirming read (`status: changed`, `verified: false`, `verification: unavailable`, backend NAMED), ∵ an absent field beats a `false` nobody measured. `firewall_allowlist_remove_failed` is its own code, ⊥ T25's `firewall_allow_failed`, which means the OPPOSITE thing (an entry that should exist ∧ does ⊥) — 1 audit trail, ∧ 2 codes that read alike are worse than 2 that ⊥. **(g) `docs/integrations/whm.md` gained T25's sections too.** T25 shipped without them (AGENTS.md: "update in same change as the feature") ⇒ the doc listed the preflight as the LAST firewall tool while 2 CHANGE tools existed — wrong, ⊥ merely incomplete, ∧ adding only T26 would leave a reader a removal tool with ⊥ release tool. Both tool rows, both per-tool sections, the command tables, the new error codes, ∧ one shared "the operator's reason, out ∧ back" section for V96's 3 sites. `whm_firewall_denylist_add_ttl` stays on the never-implement list untouched (C22). Verification: 2,117 pytest, ruff check + format clean, every source ∧ test file within C14; tests split on V22's own boundary (`test_whm_tools_firewall_allowlist_remove.py` tool + mount, `test_whm_firewall_allowlist_runner.py` runner) with call helpers in `support/whm_firewall_change.py` ⊥ duplicated (V66).

## T27

Deviations from the row as written. **(a) The row's whole flow existed on `noa-old` ∧ one BEHAVIOUR was corrected, ⊥ ported.** `MCP:proxmox/tools/cloudinit_tools.py` withheld `yopass_url` on EVERY failure past the set, including the verification branches. But the set had been ACCEPTED by then ⇒ the password may be LIVE ∧ the only copy of it is the link ⇒ withholding it is a lockout NOA created, ∧ V62's own sentence covers the OTHER direction ("set fail after yopass → URL holds unapplied password, old creds valid"). ∴ ONE rule, stated ∧ tested both ways: the link ships ⟺ NOA ⊥ RULE OUT that the password reached the VM. Ruled out = Proxmox REFUSED the write ∨ its task exited non-`OK` (⊥ link). ⊥ ruled out = regeneration failed, task poll timed out, verification unavailable, verification MISMATCHED (link ships). Carried on `StepFailure.password_may_be_live` — the step that failed is the only thing that knows — ⊥ recomputed by the caller. Both directions asserted against each other ∵ "always attach" ∧ "never attach" each pass half the suite (V87). **(b) 2 MODULES, ⊥ 1 — C14 forced it ∧ V22 placed the cut.** First draft measured 909 of C14's 900. `proxmox_password.py` = the tool (guards, preflight, gate); `proxmox_password_runner.py` = the runner. Dependency ONE-WAY (runner imports the tool's evidence keys + name; the tool imports nothing back) ⇒ `registry.py` reaches one, `change_runners.py` the other, ⊥ cycle — T26(a)'s shape, one system over. **(c) §T.69 built HERE as a 3-VALUE verdict, ⊥ a flag on a bool.** `noa-old`'s `cloudinit_dump_matches_password` answered `False` for 3 unrelated things: wrong password, ⊥ hash to read, ∧ libcrypt ⊥ load. ∴ `core/integrations/proxmox/cloudinit.py` NEW: `CryptVerdict` = `MATCH|MISMATCH|UNAVAILABLE`, ∧ ∀ unavailable carries its CAUSE (`crypt_library_unavailable`, `cloudinit_password_hash_absent`, `crypt_refused_stored_hash`) ∵ 3 causes = 3 remedies. `verified` ∧ `answered` DERIVED from the verdict, ⊥ stored beside it (B6's shape). Library probed ONCE, BEFORE the hash is read ⇒ a host that ⊥ compare says so rather than blaming a document it never read, ∧ ⊥ polls (asserted on the dump-read COUNT = 0). Loader INJECTED as a parameter, ⊥ a patched global: `_load_crypt_lib`'s cache is deliberately sticky ∧ a test that cleared it would leave the next one to discover that (V90's neighbourhood). At the runner the branch is `ok: TRUE` + `status: changed` + `verified: false` + `verification: unavailable` + `verification_cause` + the LINK — a FAILED run would read in the audit trail as "the password was ⊥ changed", which is the report V62 exists to stop. **(d) The `*0` bound is MEASURED, ⊥ reasoned.** libxcrypt refuses only an unknown `$scheme$`; 2 leading DES-alphabet chars are a VALID legacy salt ⇒ `not-a-crypt-hash` is COMPUTED ∧ answers `MISMATCH`, correctly. First draft asserted the opposite ∧ went red — recorded ∵ the module docstring had already claimed it. **(e) A `ciuser` MISMATCH is REFUSED, ⊥ gated — ⊥ in the row.** `cipassword` sets the password OF `ciuser`; asked for `ubuntu` on a VM whose `ciuser` is `debian`, the change works ∧ the delivered blob names an account the credential ⊥ belong to. `ciuser` ABSENT is ⊥ refused (Proxmox applies to the image default) ∧ the card shows `null`. Negative control ships: the MATCHING user is ⊥ refused, else a comparison written backwards blocks every real reset. **(f) The payload carries ⊥ reading of the cloud-init dump — TIGHTER than `noa-old`, ∧ structural.** That repo returned a sanitized `cloudinit_dump_user`. Under the gate that document becomes `tool_runs.result_summary` (`core/audit/summaries.py`) ∧ `noa_get_action_result` hands the summary to a MODEL ⇒ the crypt hash of the password NOA just set would reach the LLM through V45's row, ⊥ through any schema. What leaves is a VERDICT. Asserted on the SERIALIZED payload, the DERIVED summary ∧ the BUILT receipt, ⊥ on a key set (V87). Evidence built the same way — a NAMED field set, ⊥ a redaction pass over the config document — ∵ a filter has to keep being right as Proxmox adds keys ∧ a structure with nowhere to put one ⊥ leak by an omission nobody noticed (V93/V26's shape). **(g) V96 has ⊥ INSTANCE here, ∧ that is a property.** Cloud-init has ⊥ note field ⇒ the runner never reads `request.reason`, nothing C8 keeps from the LLM is written out, V43's permission unused — T23/T26's write-side shape. Asserted, ⊥ assumed: a reason on the request is absent from the payload, the summary ∧ every request path. **(h) `V86`'s shape on the PREFLIGHT.** Run state = the ONE tolerated read (it decides when the password takes effect, ⊥ what the change does) ⇒ its absence is NAMED in `unavailable_reads`, ⊥ a `null` an operator reads as "⊥ running". Config ∧ cloud-init are what the decision RESTS on ⇒ a failure there REFUSES (V35: a card that ⊥ describe its subject). **(i) ⊥ no-op branch, unlike T22/T23/T26** — a reset has ⊥ already-in-that-state reading ∵ the new password is new every time. **(j) New context fields ∧ a DELIVERY SEAM.** `McpToolContext` gained `secret_delivery` + `secret_password_length` + `proxmox_server_repository_factory` + `proxmox_client_factory`. `core/secrets/delivery.py` NEW: `_yopass_store` needs 3 settings ∧ the context holds ⊥ `Settings` (T15 deleted the singleton, C7) ⇒ configuration BOUND ONCE at `build_runtime`, ∧ a tool asks for DELIVERY, ⊥ for a URL. Protocol is KEYWORD-ONLY ∵ `username`/`password` are 2 strings of one type ∧ a swapped positional call encrypts a blob naming the password as the user, SILENTLY. Absent `YOPASS_BASE_URL` still BOOTS (C15: a tool error, ⊥ a crash) — the check stays inside `_yopass_store`, one answer. Verification: 2,221 pytest, ruff check + format clean, ∀ file within C14; 3 MUTATIONS proven red by their named tests — libcrypt-absent collapsed back to `MISMATCH` (5 red), delivery moved AFTER the write (V62's ordering, red on the empty-write-list assert, ⊥ on the return value), plaintext added to the payload (red on the serialized/summary/receipt assert, ∧ note `redact_sensitive_data` would ⊥ have caught it ∵ neither key was sensitive).

## T28

(a) **The enum collapse, and where it is actually done.** DECISIONS §9 adopted two merges and this is the first: one `proxmox_vm_nic` where `noa-old` had `proxmox_enable_vm_nic` and `proxmox_disable_vm_nic`. The merge is only real at the surface a model learns from, so `action` is `Literal["enable", "disable"]` and the assertion is on the *published* input schema off `server.list_tools()` — a free-string `action` that happened to work would leave the collapse undone where it is visible, and would send a fourth word to the body guard on every call. Bounded in three places, T25's discipline: the schema publishes the enum; the tool body re-checks it, because a caller reaching the function directly bypasses pydantic; and the runner re-checks it after the JSONB round trip, where a value that no longer parses is a request declined rather than guessed at. The recorded cost is not re-litigated: a role can no longer be granted one direction without the other (DECISIONS §9). (b) **The digest is the runner's, not the gate's — the whole of T28's design, and §V.98 was born here.** `noa-old` took the config digest as a *tool argument*: a preflight read it, the model handed it back on the change call, seconds apart. An approval gate turns those seconds into minutes, and the same arrangement then has two silent failures pointing in opposite directions. A gate-time *token* refuses an approved change on an edit with nothing to do with networking — memory, a disk, a description — and it refuses it **after** an operator typed a reason and pressed Approve, with "ask again" as the only remedy. A gate-time *value* is worse, because it succeeds: writing back the `netN` line the card described reverts a bridge, tag or model edit made while the request sat pending, silently, as a side effect of a change that was about the link state, and NOA is the author of it. So the runner re-reads the config, toggles `link_down` on the line **as it is now**, and writes under the digest from that same read — the compare-and-set window is its own read→write, which is what a CAS is for. §V.33 is bounded rather than contradicted: the evidence stays the source of the *target* (which server, which VM, which interface, which direction, none of which a model may re-supply) and is not the source of the *bytes*. (c) **What the approval window is checked against is the fact the operator approved**, which is the NIC's link state, compared at run time against the state on the evidence. Three outcomes and only one is a failure: still in the approved before-state → the change runs; already in the asked-for state → `no_op`, because somebody reached it first and the world is as the operator wanted it, so calling that a failure sends them to fix something that is not broken; the interface gone → `net_not_found` with its own sentence, because a line that no longer exists cannot be edited and a neighbouring one is not it. (d) **The codec is in `core/integrations/proxmox/nic.py`, beside `cloudinit.py`**, for two reasons pointing the same way: the `netN` grammar is a Proxmox fact rather than a workflow's, and both halves of the tool need it, which is the duplication §V.66 is about. A rewrite is a **whole-line write** — Proxmox has no "set the `link_down` field" call — so a segment the codec drops is a segment deleted from a live VM, and `tag=42` going missing puts a machine on a different VLAN while the tool answers `ok`. `set_link_down` keeps segment order, keeps every unrecognised segment, collapses a duplicated flag to one, and normalises a valueless `link_down` to the explicit form. Enabling **removes** the key rather than writing `link_down=0`: equivalent to Proxmox, and it makes a never-disabled NIC and a re-enabled one read identically afterwards. Truthiness is Proxmox's, not Python's — `link_down=0` is a non-empty string that means *up*, and reading it as set would report an enabled NIC as disabled on the card. One deliberate correction to the port: `noa-old` read the model/MAC segment **positionally** (`segments[0]`), so a hand-edited line leading with `bridge=` reports `model="bridge"`, `mac_address="vmbr0"` — a made-up model name in front of an operator. Here the segment is found by the *shape of its value*, a MAC address being recognisable where an option value is not, and a line with no MAC yields `model=None` rather than a guess. (e) **NIC selection** (C10, §V.18): a VM with exactly one interface has it inferred, because one candidate is not an ambiguity and refusing it would make the common call a two-turn conversation — the rule `docs/integrations/proxmox.md` already carried before the tool existed. The inference is recorded on the evidence as `auto_selected`, so an operator approving a change to a `netN` key they never typed can see that NOA chose it. Two or more interfaces with no `net` named returns the list as `choices` rather than a pick, and a `net` the VM does not have returns the same list under its own code, because the two remedies read differently. A VM with no interfaces at all gets `no_nics_found` rather than a `choices` refusal with nothing in it. (f) **The no-op exists at two moments and they are not the same claim.** At gate time it means "there is nothing for an operator to authorise" and no row is opened; at run time it means "somebody reached it first between Approve and execution". The gate-time payload is transcript (§V.26), so it is built from the server, node, vmid, net, action and one measured boolean — asserted on the payload's exact key set, not from the config it was decided from. (g) **The postflight asks the change's own question** (§V.97): it re-reads the config and recomputes the link state off the `netN` line, never off the task's exit status, which says only that Proxmox accepted a write. That distinction is a real case rather than a hypothetical — the test that carries it accepts the write, keeps the old line, and has the task exit `OK`, which is exactly the arrangement where a runner reading its success off the task reports a change as applied on a machine where it was not. A postflight read that cannot answer is `changed` + `verified: false` + `verification: unavailable` with its cause, never a bare `false`: Proxmox accepted the write, so calling it a failure would send an operator to repeat a change that has probably already happened (§V.62, §V.86 — unavailable is not verified, and it is not refuted either). (h) **§V.96 has no instance here, and that is asserted rather than assumed.** A `netN` line has no note field, NOA writes no `description`, and the only things on the wire are the interface line and a digest — so nothing C8 keeps from the LLM is written onto this VM, §V.43's permission is unused, and nothing NOA wrote comes back through a later READ. T27's and T23's situation, one system over. The assertion drives a sentinel reason through a real approval and hunts it in the serialized payload, the derived `result_summary`, the built receipt **and** the request bodies, with a negative control proving the helper really looks (§V.87); the runner's payload key set is asserted whole, and the interface's MAC and bridge are asserted absent from it, because that payload reaches a model through §V.45's audit row and §T.63. (i) **One thing moved for §V.66**: `ERROR_SERVER_UNAVAILABLE` / `MESSAGE_SERVER_UNAVAILABLE` were T27's, written into `proxmox_password_runner.py`. T28 made a second Proxmox runner, so they were hoisted to `proxmox_password.py` — the module both runners already import, and where `change_target.py`'s docstring had said the Proxmox pair lived all along, so the move made that sentence true. The runner re-exports both names, keeping one import path for anything already reaching for them there. (j) **Two standing tests were re-pointed, both by their own instructions.** `test_change_runner_registry.py`'s CHANGE set goes to six, and T28 is the first system to contribute a *second* CHANGE tool — the case where a runner map returning one entry per module rather than per tool name would silently drop one. `test_mcp_tool_rbac.py`'s `UNREGISTERED_TOOL` moves from `proxmox_vm_nic` to `pmg_whitelist`, which is the fourth time that constant has moved and leaves **one** catalogued-but-unbuilt name; its own comment records that when T29 lands, the assertions using it are to be deleted rather than re-pointed at a name outside the catalog, which `UNKNOWN_TOOL` already covers. (k) **Eight mutations were checked to go red** before the suite was called green, B5's lesson applied at build time: the postflight comparison deleted, the write not toggling `link_down`, the codec dropping unrecognised segments, `link_down=0` read as set, a write under a token that is not from this read (the fake enforces the digest the way Proxmox does, so "it re-read" is something the transport can fail), the no-op branch removed, the ambiguity turned into a pick, and the reason leaked into the payload. The lost-update assertion in particular is on the **bytes written**, because a runner writing the gate-time line back answers identically from the envelope.

## T29

(a) **The enum collapse, and the surface it closes.** DECISIONS §9's second merge: one `pmg_whitelist` where `noa-old` had `pmg_whitelist_add` and `pmg_whitelist_remove`. `action` is `Literal["add", "remove"]` and the assertion is on the *published* input schema off `server.list_tools()`, T28's argument one system over — a free-string parameter that happened to work leaves the collapse undone at the surface a model learns from. Bounded in three places (T25's discipline): the schema, the tool body (a caller reaching the function directly bypasses pydantic) and the runner after the JSONB round trip, where the normalised target is *re-parsed* as well, because it becomes an argv token in a `pmgsh` command. The recorded cost is not re-litigated: a role can no longer be granted "add" without "remove". This was the last unregistered catalogued name, so the registered set is now exactly `TOOL_CATALOG` — see (i). (b) **The preflight is `read_pmg_mynetworks` reused, not a second reader** (§V.66, C9, §V.17). Both PMG READ tools already run resolve → connect → `pmgsh ls` → parse with the database session closed before the SSH hop (T21's rule); a CHANGE gathering its own before-state would be a second answer to the question `pmg_whitelist_search` answers, free to drift from the one an operator got a minute earlier. (c) **§V.59 is worse on a CHANGE, and it was worse in a way the §V line had not said.** The written half was foreseen: `ipaddress.ip_network(strict=False)` masks host bits, so `203.0.113.10/24` is a request about `203.0.113.0/24`, and where the search tool only *answers* about the masked form this one **writes** it — so the add writes the normalised form (what membership was decided on and what the card showed) and both spellings travel on the card, in the no-op sentence and in the runner's payload, because a model told only `203.0.113.10/24` reports a host where a network changed. The unforeseen half is the **removal**: `mynetworks` can hold `1.2.3.4` and `1.2.3.4/32` at once — one entry to a reader, two lines in the file, which `core/integrations/pmg/mynetworks.py` already deliberately kept — and `noa-old` deduplicated while parsing and sent **one** `pmgsh delete` for the *normalised* form. That leaves the duplicate standing and aims the path segment at a spelling PMG may never have printed. The runner sends one `delete` per matching line, each naming `entry.cidr`, the token `pmgsh ls` emitted, which is the object identifier PMG itself gave. Asserted on the **bytes** — the composed `delete` path — because a runner sending the normalised form answers identically from the envelope; the negative control is a line PMG stores *as* `/32`, without which "strip the prefix length" would pass and would then fail to delete every line `mynetworks` genuinely holds as a network. (d) **The runner re-reads, and §V.98's two halves come apart here.** PMG offers no compare-and-set token, so V98's token half is vacuous — and only that half. What the approval window is checked against is the **fact** the operator approved, which is whether the address is an entry, re-measured at run time: still in the approved before-state → do the change; already in the asked-for state → `no_op`, because somebody reached it first and the world is as the operator wanted it; the list unreadable → refuse with the integration's own code, because a change decided against a read that did not answer is, here, an unauthorised write. A target with no CAS token is not exempt from re-reading; it just has less to re-read under. (e) **Two no-ops at two different moments** (T28's distinction): at gate time "there is nothing for an operator to authorise" and no row is opened; at run time "somebody reached it first between Approve and execution". The gate-time answer is transcript (§V.26), so it is built from the node's name, the two spellings and one measured boolean — asserted against the whitelist's other entries, the `200 OK` and the `id cidr` header all being absent from it, which is T26's rule one system over. (f) **The postflight asks the change's own question** (§V.97) and it is a sharper case than T28's. `pmgsh create`/`delete` print the HTTP status of the underlying API call, and `require_pmg_mutation_success` already **tolerates a non-zero exit when stdout carries `200 OK`** — so the command's own answer is exactly the wrong thing to verify against. The runner re-reads `mynetworks` and re-tests membership; the fixture's `ignore_writes` accepts the write and keeps the list unchanged, which is that case made real. Unreadable is `changed` + `verified: false` + `verification: unavailable` with its cause, never a bare `false` (§V.62, §V.86). (g) **§V.99 was born here: the apply step is separate from the write step.** `pmgsh` writes PMG's config and `pmgconfig sync --restart 1` is what Postfix picks up, so there is a third outcome that is neither success nor a plain failure — the entry is in the config and mail flow has not moved. Both ordinary shapes are wrong: `status: changed` is a lie, and a bare failure sends the operator back to add an entry that is already there, straight into (d)'s `no_op`. So it is `pmg_sync_failed` with `applied: false`. The bound rides with it and is stated rather than implied: this postflight verifies PMG's **config**, and the sync's own success is all NOA has on the live table, because NOA reads `mynetworks` through `pmgsh` and has no view of Postfix. The corollary is asserted too — a refused `delete` **stops**, leaving the remaining lines standing and never running `sync`, because applying a partial removal is the whole one reported wrongly. (h) **One source, so §V.86 has no partial case, and that is by construction rather than by choice.** PMG answers over one transport (§V.58), so there is no second backend to be silent while the first speaks: a failed `pmgsh ls` keeps its own code (`ssh_sudo_required` vs `pmgsh_command_failed` — different remedies, `noa-old` GH #82) and **no card is opened**. That is what makes this different from T26's dual-backend gate rather than a weaker version of it. `pmg_server_unavailable` is its own code for the same reason T27's is: an administrator sent to the WHM table is sent nowhere. (i) **T29 closed the catalog, and that cost a standing test its subject.** `test_mcp_tool_rbac.py` carried an `UNREGISTERED_TOOL` stand-in through T20, T27, T28 and T30, and T28's own note said that when T29 landed the assertions using it were to be **deleted** rather than re-pointed at a name outside the catalog. Deleting them outright would have left §V.83(c) — the gate carries the registered set and intersects with it — covered by nothing, so they moved instead onto a probe that builds `RbacToolMiddleware` with an empty registered set and asserts the refusal shape while `call_next` raises if the tool is ever dispatched: the shape `assert_change_runners_cover`'s no-runner probe already had, and the answer §V.87 gives to a predicate that stops separating. A new equality asserts the registered surface **is** `TOOL_CATALOG`, brittle on purpose in one direction so that a catalogued name added without a registrar re-opens the gap there rather than in front of an operator meeting fastmcp's `Unknown tool`. `test_change_runner_registry.py`'s CHANGE set goes to seven and is now the whole registered CHANGE surface. (j) **§V.96 has no instance, and it is asserted rather than assumed.** A `mynetworks` entry is a CIDR — no comment, no note, no description field — so nothing C8 keeps from the LLM is written onto a PMG node and nothing NOA wrote can come back through a later READ. The runner never reads `request.reason`, and a sentinel driven through a real approval is hunted in the serialized payload, the derived `result_summary`, the built receipt **and** every composed command, with a negative control proving the helper really looks (§V.87). It is also why a backend failure message travels whole here while T26 cuts its own through `backend_change_failure`: csf quotes an entry NOA wrote a comment onto, and `pmgsh` has no comment to quote. (k) **The fixture holds the whitelist as state, and four mutations were checked to go red** before the suite was called green (B5's lesson at build time). `FakePMGWhitelist` lets a `create` append and a `delete` remove, so the postflight reads back what the runner actually wrote — a canned "it is there now" would pass with the whole write-then-verify path deleted (§V.87). The mutations: deleting by the normalised form instead of the entry's spelling (four tests red, the bytes assertion among them); a postflight that assumes the write took (five red, including both §V.97 cases); a postflight that assumes it did not (a different five, which is why the first mutation alone would not have been enough); and the runner's re-read gate removed (the two pending-window no-ops red, and nothing else). §V.83(c)'s new probe was mutation-checked separately — dropping `granted & self._registered_tools` turns that one test red and no other in the suite.

## T31

AS BUILT ≠ this line, 4 deviations: (a) **PMG CLI layer re-signed**, ⊥ named in this line — `run_pmgsh_command` ∧ ∀ `run_pmg_*` took `(row, cipher)` ∧ resolved INSIDE ⇒ the tool ! hold its DB session across the SSH hop ∵ an ORM row ⊥ readable once the session closes. Now they take a resolved `SSHConnectionConfig`; the tool resolves ONCE inside its session ∧ closes before the hop (T21's rule; T24(a)'s change, one system over). Asserted as ORDER (`session-open`,`session-close`,`ssh`), ⊥ as a count. Command on the wire byte-identical. 2 consequences: row refusals (`ssh_invalid_host`,`ssh_invalid_port`,`ssh_not_configured`,`ssh_host_key_not_validated`) now raise AT the tool ⇒ they name the PMG SERVER, ⊥ arrive as `pmgsh_command_failed` (T24(b)'s shape, V82); ∧ `require_host_key_fingerprint` becomes the CALLER's — this module hard-coded `True`, which is exactly the knob T54's TOFU capture needs. **SUPERSEDES T18(d)'s row+cipher signature.** (b) `ERROR_UNKNOWN` hoisted `mcp_tools/whm_read.py` → `mcp_tools/results.py` ∵ `whm_firewall` already imported it ACROSS systems ∧ `pmg_read` = the 3rd caller ⇒ one import from a 2nd spelling (V66); ⊥ re-export left behind (T72's line). (c) result shape ≠ `noa-old`'s. DROPPED `id`/`ip`/`object_type`/`raw.line` (T24(d) — result persists in LibreChat's MongoDB, V26). ADDED `total_entries` ∵ `exists:false` @ 0 parsed entries ≠ `exists:false` @ 300, ∧ `pmgsh ls` output ⊥ separate an empty `mynetworks` from a format NOA ⊥ recognise ⇒ the count is the only thing showing WHICH one was got; ⊥ a refusal ∵ an empty whitelist is a real state ∧ refusing it would be the false alarm. `normalized_target` + the entry's RAW spelling both ship (V59 as amended) — the raw form is what an operator finds on the box ∧ what T29's removal ! name. (d) parser departs `noa-old` TWICE (T21(b): a port carries the code, ⊥ the defect) — the first parsable candidate ENDS the line (source skipped an already-`seen` candidate ∧ fell through to the ID column) ∧ duplicates KEPT, ⊥ deduped while parsing (2 spellings of one address = a fact about the whitelist, ∧ T29 ! know both lines exist). ⊥ named in this line: `core/servers/pmg_repository.py` ∧ `core/servers/pmg_ref.py` ∧ `core/integrations/pmg/mynetworks.py` — T18 deferred ∀ 3 to T29–T31; reads only, CRUD → T54. Ref resolution = a SIBLING of `whm_ref`, ⊥ a shared resolver: `core/servers/__init__` recorded at T19 that the THIRD system (Proxmox, T27/T28) is where a shared abstraction stops being a guess about what varies (V66) — policy identical (id → name → host, any tie = `choices`), row differs at both ends (WHM parses a host out of `base_url`, PMG stores it bare; `choices` fields differ). Also ⊥ named: `McpToolContext.pmg_server_repository_factory`; test-support inventory doubles for BOTH systems → `support/servers.py`, `support/pmg.py` keeping the integration-layer row + tool-path helpers (V66, the split `support/whm.py`/`support/whm_firewall.py` already has). V85 ⊥ apply (exact membership caps nothing — V85's own instances line says so), V86 ⊥ apply (1 source, ⊥ N). 10 mutations proven red: containment-for-exact, unnormalised target, a 2nd command on the read path, the SSH hop inside the session, the parser fall-through, pin ⊥ required, `total_entries` dropped, an ambiguity resolved by picking first, blank-target guard removed, raw spelling replaced by the normalised form. Doc sweep same change (V84a): `docs/integrations/pmg.md`, `core/integrations/pmg/__init__.py`, `core/servers/__init__.py`

## T32

AS BUILT = this line + 8 things it ⊥ name: (a) **the 3rd branch REFUSES** — `ChangeGateBranchUnavailableError` (`core/approvals/errors.py`), a `ChangeGateError` ⇒ the code survives the model boundary (V19), mapped 500 EXPLICITLY ∵ the branch is a module constant, ⊥ a tool arg ⇒ ⊥ caller can pick it; the existing subclass-tree test caught the missing map; (b) **⊥ `structured_content`** ∵ R29 measured a content-only result ∧ CHANGE skips the audit middleware (V46) ⇒ ⊥ reader owed one — an envelope = a 3rd place the URL lives; (c) **`NOA_EMBED_BASE_URL` got its 1st consumer** — `McpToolContext.embed_base_url`, REQUIRED (⊥ default) ∵ a default is a laptop address a forgetful deploy ships to every operator; resolved once in `build_runtime` beside the pending TTL (C7/T15's one-`get_settings()` rule). Config half still open ⇒ **V95 + T74**; (d) fixture base ≠ `Settings` default ⇒ a hardcoded literal ⊥ pass the wiring assertion (V87), the `PENDING_TTL_SECONDS` trick one field over; (e) **ONE text wording ∀ branch** ⇒ V25 = 1 claim about 1 string, ∧ it is an ADDRESS — `href`/`<a ` asserted ABSENT (V94); (f) **⊥ "reason", ⊥ "justif" in the text** — V71 read as covering what NOA EMITS, ⊥ only the agent prompt; (g) `approval_card_url()` split out — pure, exported, `rstrip`s the base though `core/config.py` already does ⇒ T56's `/tables/<token>` sibling has the join to reuse (V66); (h) lives in `change_gate.py` beside T33, ⊥ `results.py` — `results` → `change_gate` → `mcp_audit` → `results` = a CYCLE, named ∵ "tool result shapes" is what `results.py` calls itself ∧ the next reader will try it first. ⊥ caller yet (T22–T29 all `.`) ⇒ tests = sole caller, same shape T33 shipped in; `APPROVAL_CARD_PATH` ↔ `apps/web-embed/src/app/approvals/[id]` = 2 languages ⇒ ⊥ compiler checks that pair. Lane: `apps/api/tests/test_mcp_change_gate_response.py`, 15 cases

## T33

AS BUILT ≠ this line, 8 deviations: (a) gate = a FUNCTION the tool calls, ⊥ a middleware — every other cross-cutting rule on the tool path is one ∵ a tool ⊥ forget a middleware (V83b), ∧ this one ⊥ be: C9/V17 put the preflight IN-PROCESS inside the CHANGE call ⇒ the evidence the card shows is the tool's own ∧ a middleware sitting outside it has none. What replaces that property: `core/approvals/repository.py` = the ONLY writer of a PENDING row ∧ it writes ⊥ other status ⇒ a CHANGE tool that skipped the gate ∧ executed has ⊥ authorization row at all, ∧ V23 reads the row. (b) `approval_context` holds ONLY what ⊥ COLUMN holds — `arguments` (redacted), `requester` (`email`+`librechat_user_id`), `evidence`. ⊥ `tool_name`, ⊥ `conversation_ref`, ⊥ a `requested_at`: ∀ 3 are columns (T34) ∧ a copy = 2 records of one moment that can disagree — T34's own argument for dropping `args`,`risk`,`decided_by_user_id`,`updated_at`. Requester EMAIL persisted ∵ the FK is `SET NULL` ⇒ a join at render time loses the identity of a decision that was made. (c) **empty preflight evidence REFUSED** (`change_evidence_required`, ⊥ named in this line, owner-decided at plan time) — T34's model docstring already asserts "the gate always holds provenance + args + in-process preflight (V17,V35)" ⇒ this makes that sentence a MECHANISM ∧ T22–T29 inherit it by construction, ⊥ by each remembering (T68's rule). (d) C8 bound AT THE DOOR, ⊥ only in the schema: `assert_no_reason_argument` refuses `reason`,`proposed_reason`,`change_reason`,`approval_reason`,`justification`. EXPLICIT set, ⊥ substring — a future `reason_code` ⊥ what C8 is about ∧ a guessing rule refuses a change for the wrong cause (case asserted). TOP LEVEL only — a reason = a first-class param ∨ ⊥ a reason; `{"payload":{"reason":…}}` = an integration's own shape. + a registry SWEEP over `await server.list_tools()`: VACUOUS today (0 CHANGE tools), written anyway per V85's rule (the rule ! exist BEFORE the 2nd instance), with a PROBE tool proving the predicate separates ⇒ ⊥ a tautology (V87). (e) `pending_ttl_seconds` on `McpToolContext`, REQUIRED ⊥ defaulted (⊥ named in this line) — `expires_at` = NOT NULL by T34 ⇒ the gate ! compute it, ∧ the TTL is settings with ONE `get_settings()` caller (T5) ⇒ a default here = a 2nd answer that drifts silently. Test-support TTL = 900 ∵ 3600 = `Settings`' own ⇒ unseparable from a hardcoded literal; the mount-wiring test builds settings at 1234 for the same reason — `mounted_app`'s harness carries the production default ∧ the first version of that assertion passed against a hardcoded `create_app` (V87's shape, caught by MUTATION ⊥ by reading). (f) repository = PENDING ∧ nothing else: `status` ⊥ a parameter, ⊥ update method, ⊥ read method (T37/T63 add theirs when they need them). V28's transition = T37's, under a row lock, off a cookie POST (V22) ⇒ a writer that could set APPROVED = a 2nd door on the authorization, on the side an LLM reaches. Asserted on BOTH the Protocol ∧ the SQL class — a Protocol-only `decide` survived the first version of that test. (g) errors = `ChangeGateError` tree, `NoaError` subclasses ∵ `sanitize_tool_errors` passes a NoaError's own code through (V19) ∧ T37 raises from the same tree into HTTP (V73). `change_gate_unavailable` 503 (fail-closed on the INSERT, T73(c)'s argument one table over); the 2 programming-error codes 500, ⊥ 400 — a reason-shaped arg ∨ a skipped preflight is NOA's bug ∧ 400 would blame the caller for it. (h) **⊥ EXERCISED: a CHANGE `tools/call` over the real mount.** T22–T29 = `.` ⇒ nothing reaches this gate through `tools/call`; tests drive `open_change_request` inside the REAL identity ∧ header contextvars (`http_request_context` + `AuthenticatedUser`), ⊥ patched names — same shape as T73's CHANGE branch, ∧ T22 = where the gate first runs over the mount. ALSO ⊥ built here: `build_change_gate_response()` (T32) ⇒ the gate returns `PendingChangeRequest` ∧ what the MODEL sees is still unwritten. 14 mutations proven red: reason guard dropped, reason guard widened to substring, evidence guard dropped, requester read from arguments, TTL hardcoded (in the gate ∧ in `create_app`), redaction skipped, commit dropped, fail-open on write failure, `tool_name` duplicated into the context, `conversation_ref` dropped, repository writes APPROVED, `create_pending` accepts a `reason`, a 2nd writer can decide. Doc sweep same change (V84a): `mcp_tools/__init__.py` (approval gate stated entirely "still to come"), `mcp_tools/context.py` (stated T33 "will" make a CHANGE tool write `action_requests` ∧ share ONE session with its audit event — false twice: the gate writes ⊥ audit row at all, T73(a))

## T34

AS BUILT ≠ this line, 6 deviations: (a) **BOTH FKs `SET NULL` + nullable**, ⊥ named here — `noa-old` CASCADED `requested_by_user_id`. An approved CHANGE = an audit artifact (V46) ∧ T36's receipts FK into this row ⇒ CASCADE lets ONE user delete erase both what was authorised ∧ the receipt proving it ran (T35's argument for `tool_runs`, one table over). Fails CLOSED vs V27: NULL matches ⊥ caller ⇒ requester-match 404s. Asserted by a real `DELETE`, ⊥ by reading `ondelete` off metadata — that is a string until something deletes. (b) `approval_context` carries **⊥ server default**, deliberately ≠ `tool_runs.args` (`'{}'` there so "took ⊥ args" ≠ "⊥ recorded") — an empty context is ⊥ a legitimate state here ∵ the gate always holds provenance + args + in-process preflight (V17,V35) ⇒ an insert omitting it FAILS, ⊥ records a card with nothing on it (V33). (c) 4 `noa-old` columns ABSENT ∧ each bound by test, ⊥ by prose — this line names only `proposed_reason`: `args` (→ inside `approval_context`, V33 = one payload, one moment ⇒ 2 records of it can disagree), `risk` (→ `tool_runs`, which is what makes a failed READ writable; `noa-old` put it HERE instead ∧ that is precisely why its audit ⊥ say whether a run was a change), `decided_by_user_id` (V27 makes decider = requester ⇒ a 2nd identity column = 2 truths about one person), `updated_at` (V28 permits 1 mutation ∧ `decided_at` stamps it). ∧ the V43 guard scans **∀ of `Base.metadata`**, ⊥ this table ⇒ T36's receipts inherit the ban UNLISTED (`test_config.py`'s git-ls-files pattern, one axis over). (d) index set ⊥ named ∧ deliberately SMALLER than `tool_runs`': `(status, expires_at)` composite + `requested_by_user_id`. ⊥ `tool_name`, ⊥ `conversation_ref`, ⊥ `created_at` — nothing filters ∨ orders by them today, the discipline that left `risk` unindexed at T35. Composite ORDER asserted, ⊥ only membership: `status` LEADS ∵ T39's sweep = `status=PENDING AND expires_at < now()` ∧ a leading `status` serves status-only lookups too ⇒ ⊥ 2nd index; led by `expires_at` the sweep scans rows already decided. (e) `reason` = unbounded `Text`, ⊥ capped like `result_summary`(2000) ∵ ⊥ machine writes it ⇒ ⊥ result body to invite in, ∧ truncating the field that AUTHORISES a change is worse than storing a long one. T37 bounds the input at the endpoint. (f) **CLOSED 2026-08-08 by T37** (OPEN at T34): `ck_action_requests_decided_reason`, migration `0005_decided_reason_check` — `status NOT IN ('APPROVED','DENIED') OR (reason IS NOT NULL AND reason ~ '[^[:space:]]')`. T34 left the SHAPE to T37/T39 ∵ V15 named the approve ENDPOINT as the gate ∧ EXPIRED = decided-terminal with nobody to have typed one; T37 required a reason on DENY too (V15 as amended) ⇒ both decided states fit ONE predicate, ∧ PENDING/EXPIRED stay outside it so T39's sweep is still writable. 2 traps caught by TEST, ⊥ by reading: `btrim()` strips SPACES only ⇒ a one-tab reason passes it; ∧ `NULL ~ '…'` = NULL, which SATISFIES a CHECK ⇒ the `IS NOT NULL` arm is load-bearing, ⊥ redundant — without it the constraint catches every blank STRING ∧ waves through the NULL, the one case that matters most. T33 inherits 2 properties by CONSTRUCTION, ⊥ by discipline: it ! set `expires_at` ∧ ! write `approval_context` — ⊥ default on either ⇒ omitting one is an insert failure, ⊥ a silent NULL. 9 mutations proven red: `approval_context` given a default, `expires_at` made nullable, `EXPIRED` dropped, a `proposed_reason` column added, EITHER FK cascaded (2 runs), sweep index led by `expires_at`, PENDING default dropped, migration CHECK disabled. Doc sweep same change (V84a): `core/db/lifecycle.py` ∧ `core/db/models.py` both stated this table ⊥ exist yet, `test_schema_v1.py` held it in `NOT_YET_TABLES`, `test_tool_runs_schema.py::test_action_request_status_has_not_landed_early` asserted the enum ⊥ landed; `support/database.py` `MUTATED_TABLES` += it ∵ SET NULL on BOTH FKs ⇒ a leftover row outlives ∀ other row a test created

## T36

AS BUILT ≠ this line, 8 deviations: (a) **UNIQUE `action_request_id` — STATED, ⊥ inherited.** `noa-old` made `action_request_id` the PK ⇒ one-receipt-per-request came WITH the table; this line names a separate `id` ⇒ the property is stated here ∨ lost in the port (C13's copy-⊥-rewrite ⊥ extend to copying a shape that DROPS a guarantee). Load-bearing, ⊥ tidiness: T38's executor ∧ its reaper BOTH reach a finished run ⇒ a 2nd receipt turns "the receipt" into "some receipt" ∧ a reader picks one arbitrarily — T34's own argument for folding `args` into `approval_context`. ∧ it is the index an idempotent `ON CONFLICT (action_request_id) DO NOTHING` writer needs to work at all (the port's `create_action_receipt_if_missing`). (b) **`action_request_id` NOT NULL + `CASCADE` — the ONLY cascading FK added since T35** (owner-decided at plan time). Others are `SET NULL` ∵ their row still describes something without its subject: a `tool_runs` row minus its requester is still WHAT RAN. A receipt minus its request is a JSONB blob nothing is ABOUT — tool name, requester ∧ arguments ∀ live on `action_requests`. Audit-survival sits THERE, where T34 made both FKs `SET NULL` naming THIS table as the reason ⇒ asserted end-to-end here (`test_deleting_the_requester_keeps_the_receipt`) ∵ that sentence is a claim about 2 tables ∧ neither file can make it alone. (c) **`terminal_phase` ⊥ ported** (owner-decided) — carried the terminal state of a multi-phase workflow ∧ C16 drops workflows; that state lives on `tool_runs.status` ∧ `action_requests.status` ⇒ a 3rd column saying it again = a 3rd truth about one moment (T34(c)'s argument for the 4 columns it dropped). (d) **`schema_version` ⊥ ported** (owner-decided) — `approval_context` ∧ `tool_runs.args` are BOTH unversioned JSONB ⇒ versioning the 3rd makes their bareness read as deliberate when it is ⊥. (e) `receipt_data`, ⊥ the port's `payload` — this line's name; 2 names for 1 column is how a writer fills the wrong one. NO server default: `approval_context`'s argument verbatim, deliberately ≠ `tool_runs.args` (`'{}'` there so "took ⊥ args" ≠ "⊥ recorded") — an empty receipt is ⊥ a legitimate state, so an insert omitting it FAILS. (f) `tool_run_id` = nullable + `SET NULL`, mirroring `action_requests.tool_run_id` ⇒ ONE edge, ONE spelling at both ends; NULL describes life after the run row is deleted, ⊥ a receipt written without one. (g) index set = the UNIQUE ∧ nothing else — port indexed `tool_run_id`, `terminal_phase` ∧ `created_at`; nothing filters ∨ orders by them, the discipline that left `risk` unindexed at T35 ∧ kept T34's set smaller than `tool_runs`'. ∀ reader arrives holding an `action_request_id`, which the UNIQUE already indexes. (h) **⊥ EXERCISED at T36: a written receipt.** T38 = `.` then ⇒ ⊥ writer; T22–T29 = `.` ⇒ ⊥ CHANGE tool opens a request ⇒ correctness carried by TESTS, ⊥ by use (T37(a)'s ∧ T39(i)'s shape). **CLOSED 2026-08-09 by T38** — `core/audit/receipts.py` writes one per approved change, ∧ the UNIQUE this row stated is now exercised end-to-end by the executor ∧ the reaper both reaching one finished run. Still ⊥ reached through a real CHANGE tool (T22–T29 `.`), so the last leg of "by use" waits on T22. **8 mutations proven red**: unique dropped; unique moved onto `receipt_data`; `receipt_data` given a server default; `action_request_id` made nullable; `CASCADE` → `SET NULL`; run FK → `CASCADE`; a `terminal_phase` column added; migration ∧ model column sets disagreeing. The one-receipt assertion ships a NEGATIVE CONTROL (2 requests, 2 receipts) ∵ "the DB refuses a 2nd receipt" is a claim about nothing if the table refuses EVERY 2nd receipt (V87) — ∧ that control was PROVEN to separate by the wrong-column mutation, ⊥ reasoned. Doc sweep same change (V84a): `core/approvals/results.py` ∧ `core/audit/__init__.py` (both said the table ⊥ exist), `apps/web-embed/README.md` ×3 (receipt blocked on T36 **∧** T38 — T38 alone now), `test_action_requests_schema.py` (its `Base.metadata` V43 sweep called the receipt "the half that has not been built yet" — it now genuinely covers it), `test_tool_runs_schema.py`; `test_schema_v1.py` moves it `NOT_YET_TABLES` → `LATER_TABLES` ∧ `support/database.py` names it in `MUTATED_TABLES` — reached by CASCADE today, named anyway ∵ that is a fact about the FK, ⊥ about the list

## T37

AS BUILT ≠ this line, 11 deviations: (a) **executor SEAM, ⊥ the executor** (T38 `.` then) — `ApprovedChangeExecutor` Protocol (`runtime_checkable` so `deps._from_state` type-guards it) on `AppRuntime`; the only impl was `DeferredApprovedChangeExecutor`, which LOGGED `approved_change_execution_deferred` ∧ started nothing. ⊥ a silent no-op, deliberately: every call = a run that sits STARTED until T38. INERT ∵ T22–T29 `.` ⇒ ⊥ CHANGE tool exists to open a request ⇒ ⊥ operator can reach these routes in production — which is what made the placeholder safe ∧ why correctness here is carried by TESTS, ⊥ by use. **FILLED 2026-08-09 by T38** — `AsyncioApprovedChangeExecutor` (`core/approvals/execution_host.py`); the placeholder ∧ its log event are DELETED, ∵ a placeholder production ⊥ wire reads as a supported mode. The Protocol stays `start`-only: `AppRuntime` types the field to the HOST so the lifespan can `stop()` it, ∧ this service — handed one off `app.state` — ⊥ reach a shutdown method (T38(k)). V31's cap joined this file at the same time, which is why it now holds 2 locks (T38(d)). (b) **CSRF built here, ⊥ at T46** (V39 as amended) — owner decision at plan time; a `.` row is ⊥ a guard. (c) **expiry check-on-read at the decision door, ⊥ deferred to T39** (V32 as amended) — 3 lines, ∧ the alternative ships a window in which V32 is false. (d) **reason required on DENY** — T34(f)'s question, answered, + the DB CHECK that closes it (V15 as amended). (e) **decision writer = a SEPARATE class in a SEPARATE module** — `core/approvals/decisions.py`, ⊥ a method on T33's `SQLActionRequestRepository`, ∵ that one is reached from the MCP path ∧ a class that could write APPROVED there = a 2nd door on the authorization, on the side V22 closes. Nothing puts it on `McpToolContext`; asserted 2 ways — the tool context carries ⊥ decision type, ∧ T33's repository's public surface is pinned to `{create_pending, commit}`. (f) `start_change_run` lives ON the decision repository, ⊥ taken as a caller-supplied collaborator ⇒ "same session" is CONSTRUCTION, ⊥ an obligation (V29). `risk` fixed to CHANGE, ⊥ a parameter: V16 makes every row here a change, ∧ a parameter is somewhere for READ to be written into an approved change's audit row. (g) ORDER = CSRF → reason → lock. CSRF first ⇒ a forged cross-site POST never touches the DB ∧ every id answers 403 alike; blank reason before the lock ⇒ a submit that cannot succeed ⊥ queue behind someone else's transaction. Neither ordering leaks existence — a CSRF token is only obtainable from a card V27 already let the caller see. (h) errors = `ActionDecisionError`, a SIBLING tree to `ChangeGateError`, ⊥ a subclass — that tree's messages say "could not be submitted for approval", which is a sentence about a different moment, ∧ its base maps to 503 where a 404 ∧ a 409 live here. 5 members, each mapped by name (V73). (i) `lock_for_decision` → a frozen `LockedActionRequest`, ⊥ the ORM row: the service is what decides, ∧ a live ORM instance lets it write columns this design ⊥ permit it to write. (j) `reason` bounded at 2000 AT THE ENDPOINT (T34(e)'s "T37 bounds the input") → 422; blank ⊥ a pydantic constraint ∵ `min_length=1` answers 422 ∧ V15 names 409. (k) routes on the API (`noa_api/api/routes/action_requests.py`), ⊥ the embed app — §I.embed lists them ∵ the embed is their only caller. ⊥ built here: the GET detail (T41), a CSRF MINTING route (T41), the real executor + reaper + V31's cap (T38), the sweep ∧ the render-path check (T39). Gotcha, recorded ∵ it cost a full test-module cascade ∧ will recur: an alembic revision id ! be ≤ 32 chars (`alembic_version.version_num` = `VARCHAR(32)`) — a longer one fails AFTER the DDL, on the bookkeeping UPDATE. ⊥ an invariant: it fails loud at `alembic upgrade`, in-transaction, pre-merge. **20 mutations proven red**: csrf verify removed; csrf bound to session-⊥-request; csrf bound to request-⊥-session; `compare_digest` → `==`; key derivation loses its label; csrf age check dropped; `FOR UPDATE` dropped; status guard dropped; requester-match dropped; 404 → 403; blank-reason guard dropped; expiry check dropped; expiry refuses without writing; the run committed in its own transaction; executor handed the run before the commit; a denial links a run; the approved run classified READ; the CHECK disabled; the CHECK loses its `IS NOT NULL` arm; the CHECK uses bare `btrim`. **The `FOR UPDATE` one survived the first 2 versions of its test — B5, V89.** Doc sweep same change (V84a): `core/approvals/__init__.py` ("nothing here decides anything" — false now), `core/approvals/repository.py` (T37's transition now names the class ∧ the pin on this repository's surface), `core/audit/tool_runs.py` (named T38 as the 2nd `tool_runs` writer; T37 is), `core/db/models.py` (`ToolRun` ∧ `ActionRequest.tool_run_id` both said the executor opens the row), `noa_api/api/__init__.py`, `api/routes/__init__.py`, `main.py`, `mcp_tools/__init__.py` ("the endpoints (T37)" under still-to-come), `docs/spikes/librechat-embed-render-gate.md:124`

## T38

AS BUILT ≠ this line, 15 deviations: (a) `execute_approved_tool_run(action_request_id, tool_run_id)` — BOTH ids, ⊥ the one this line names. The seam already hands both, ∧ the receipt FKs the REQUEST while the arguments ∧ evidence live on `action_requests` ⇒ a lookup by run id alone = a 2nd way to reach the row V23 answers from. (b) **V23 re-read at the LAST door**, ⊥ named here — `status = APPROVED AND tool_run_id = :run` IN the statement, ⊥ checked after it (`core/approvals/execution.py`, the argument `core/approvals/reads.py` makes for V27's match). 2 identifiers ⊥ permission: the handoff crosses a task boundary ⇒ the row can move before the task is scheduled, ∧ a later caller (an operator tool, a retry) would otherwise be a 2nd door on "run this change" on the far side of V22. Parameterized over ∀ 3 non-APPROVED states ∵ the guard is one `==` ∧ a single case survives a mutation to `!=`. (c) **The reaper repairs ONE of its 2 populations.** Runs stuck STARTED past the deadline → FAILED + a summary naming the outcome **UNKNOWN**, ⊥ "failed": an interrupted change may have applied on the remote host ∧ `ToolRunStatus` offers ⊥ 3rd terminal state, so the distinction lives where an operator reads it. READ runs covered too — T73's middleware swallows a failed closing write ∧ names this reaper. But `APPROVED` + `tool_run_id IS NULL` = **DETECTED + logged at error, ⊥ repaired**: T37 writes run ∧ link in one transaction (V29) ⇒ the decision path ⊥ produce that pair, ∧ the way production CAN is a DELETE of the run row (`SET NULL`, T34) — in which case the change may well have COMPLETED, so a fabricated FAILED run = a claim nothing observed, ∧ this line reserves the row's creation ∧ the link to T37 anyway. This line's wording predates T37. Held by a test seeding the pair the ONLY way production reaches it (T39(e)'s shape), + a negative control proving the healthy shape ⊥ read as an anomaly. (d) **V31's cap needs a 2nd lock, ∧ that was MEASURED** — counting alone ⊥ hold it: 2 approvals of DIFFERENT requests by one operator ⊥ touch each other's rows ⇒ `FOR UPDATE` serializes nothing ∧ both read 0 under READ COMMITTED. `pg_advisory_xact_lock(hashtextextended(<key>, 0))` — key = `INFLIGHT_LOCK_NAMESPACE` (`noa:approvals:inflight-changes`) + the caller's id, namespaced so 2 features hashing raw user ids ⊥ serialize against each other — taken inside the decision's transaction, ∧ the repository takes the lock ∧ returns the count in ONE method ⇒ "counted under the lock" = CONSTRUCTION (`start_change_run`'s argument, one method over). Advisory ⊥ a row lock ∵ `users FOR UPDATE` would block `PATCH /admin/users/{id}` — an admin disabling an operator queueing behind that operator's approval; `hashtextextended` collisions cost throughput, ⊥ correctness. Deadlock-free: acquired always AFTER the row lock, ⊥ held while waiting for one. ORDER = reason → row lock → requester-match → status → deadline → **cap** → run → decision → commit → handoff. Cap LAST ∵ a 404 ∨ an expiry ⊥ cost an advisory lock ∧ an operator at their limit hears it about a request that was theirs; BEFORE `start_change_run` ∵ that insert IS the row being counted — after it, a cap of 1 admits every first approval ∧ refuses forever. `deny` ⊥ take it ∵ nothing starts. (e) code + class ⊥ named here: `ChangeExecutionLimitReachedError` / `change_execution_limit_reached`, **409** — ⊥ 429: nothing rate-limits the operator ∧ `Retry-After` = a number NOA ⊥ honestly produce (it depends on someone else's SSH round trip). (f) **A CHANGE tool has 2 halves ∧ this row built the 2nd's seam** — `noa_api/mcp_tools/change_runners.py::build_change_runners`, tool name → `ChangeRunner`, **EMPTY** (T22–T29 `.`). A runner answers the ordinary tool envelope ⇒ `status_for_payload` classifies ∧ `result_summary` bounds it, ⊥ a 2nd convention. Unknown name → FAILED + `change_runner_unavailable`, which is what makes shipping the executor before its 1st tool safe ∧ ⊥ a silent hole. + `register_mcp_tools` REFUSES at startup a registered CHANGE name with ⊥ runner, beside `assert_names_in_catalog` (V10's shape): a card an operator can approve in front of a dead execution path. VACUOUS today, written anyway per V85's rule, with a probe proving the predicate separates (V87). (g) **redacted arguments REFUSED, ⊥ executed** — `change_arguments_redacted`; `approval_context.arguments` is redacted at gate time ∧ redaction is by KEY NAME (`is_sensitive_key`), so the guard reads keys, ⊥ values: a legitimate argument whose value happens to be `[redacted]` is ⊥ a false refusal (case asserted, T33(d)'s explicit-set argument). **CORRECTED 2026-08-09 (B7)**: this line said WHAT the guard reads ∧ was silent on HOW DEEP — as built it read keys at DEPTH 1 (`for key in authorized.arguments`) while `redact_sensitive_data` RECURSES into maps ∧ sequences ⇒ `{"server": {"ssh_password": …}}` was stored redacted at gate time, passed the guard, ∧ the runner would have performed the change with the literal `[redacted]` where a credential belonged — the exact outcome this door exists to refuse. A fail-closed door that reads only the top level is ⊥ fail-closed. Now `core/secrets/redaction.py::sensitive_key_paths` — the redactor's OWN walk answering WHERE instead of rewriting (V66) ⇒ 1 traversal rule, 2 readers, ⊥ 2 spellings that can drift; dotted paths + `[i]` name the location without carrying the value. Refusal still by KEY NAME, ⊥ by value. Vacuous today ∵ C15/V49 generate secrets server-side; a fail-closed door so the tool that eventually declares one inherits it (T68's rule) — ∧ shipping that door EARLY is exactly why it shipped half-built. (h) **the receipt = 2-part ∧ idempotent**, ⊥ named here: `{ok, before, after}` — `before` = the gate's own preflight off `approval_context` (C9,V17,V33) ⇒ the authorisation ∧ the record of what happened describe ONE moment, ⊥ 2 readings minutes apart; `after` = what the runner answered. **CORRECTED 2026-08-09 (B8)**: `after` goes through `redact_sensitive_data` on the way in ∧ this line ⊥ say so — as built it stored the runner's answer RAW while the SAME payload reached `tool_runs.result_summary` through `core/audit/summaries.py`, which redacts ⇒ a runner answering `{ok, password}` kept the credential OUT of the summary ∧ PUT it in `action_receipts.receipt_data`, the row that OUTLIVES the call in front of T42's card, T63's read ∧ the admin audit surface (V8, V45). A per-writer exemption from one redaction rule is how ONE of them eventually writes the credential. `before` deliberately ⊥ re-redacted: a COPY of gate-time `approval_context` evidence, whose redaction is T33's — re-deciding it in this writer = a 2nd answer to one question ∧ a 2nd place to drift. Written for a FAILURE too ∵ V46 is literal ∧ a receipt with a before-state ∧ no working after-state IS the truthful record of a change that ⊥ complete. `core/audit/receipts.py` = 1 writer, `ON CONFLICT (action_request_id) DO NOTHING` over T36's UNIQUE — the constraint T36 stated naming THIS pair. Conflict target NAMED ⊥ inferred (a bare `DO NOTHING` swallows a future index's conflict too) ∧ the name pinned vs `pg_constraint`. (i) **terminal status + receipt = ONE commit** (V46) — proven by BREAKING the receipt insert ∧ asserting the run still reads STARTED, ⊥ by describing the boundary. (j) **2 V66 hoists same change**: `core/tasks/periodic.py::PeriodicTask` = T39's loop, now shared with the reaper ⇒ 4 properties proven ONCE (sleep-first V51, `except Exception` ⊥ `BaseException`, `cancel()` **∧** `await`, idempotent `start()`); `PendingExpirySweeper` delegates ∧ keeps its surface ⇒ T39's 14 loop tests = the regression proof, untouched. `core/audit/summaries.py` = `result_summary`+`status_for_payload` off `noa_api/mcp_audit.py` ∵ the executor records the same field from the same envelope; `mcp_audit` re-exports. ∧ `evidence_from_context` moved `card.py` → `context.py` when the executor became its 2nd reader. Extracted at the SECOND instance, unlike `core/servers`' per-system resolvers — those differ at both ends, these differ only in 3 constructor arguments. (k) **`DeferredApprovedChangeExecutor` DELETED**, ⊥ left beside the real one: production ⊥ wire it ∧ a placeholder nothing uses reads as a supported mode. `AppRuntime.approved_change_executor` typed to the HOST ⊥ the Protocol — the lifespan ! `stop()` it, ∧ the Protocol declares `start` alone so `ActionDecisionService`, handed one off `app.state`, ⊥ reach a shutdown method. (l) **lifespan stops 3 components, executor FIRST** — a change in flight holds a session; cancelling it leaves its run STARTED, which is the reaper's ⇒ nothing may still be STARTING work once the loops that resolve it stop. ∀ 3 finish before `engine.dispose()` (T39(h)'s rule, 3 components now). Cancelled runs left STARTED deliberately: a terminal status written at shutdown = an outcome nobody observed. (m) 2 settings, ⊥ 1 derived from the other: `APPROVAL_STRANDED_RUN_REAP_AFTER_SECONDS` (900, `ge=60`) = a LIFETIME, `APPROVAL_STRANDED_RUN_REAP_INTERVAL_SECONDS` (120, `ge=1`) = a resolution. Deadline < pending TTL asserted ∵ a stranded change spends its operator's V31 allowance until reaped ⇒ one crash ⊥ cost more than a whole approval window. (n) **`finish_run` needed a FIRST-WINS predicate ∧ this line ⊥ ask for one** — CORRECTED 2026-08-09 (B6, V91). The row above owes "the `finish_run` that moves the STARTED row"; as built it keyed its UPDATE on `ToolRun.id` ALONE ⇒ the executor ∧ the reaper ⊥ RACE for a finished run, they both simply take it. A change slower than the 900s deadline → reaper writes FAILED + an abandonment receipt → the executor finishes ∧ flips the run COMPLETED beside a receipt saying its outcome was never observed ((h)'s `DO NOTHING` keeps the STALE one) ⇒ status ∧ receipt contradict, ∧ (i)'s "one commit" ⊥ help ∵ the 2 commits are minutes apart. Reverse interleave worse: the reaper `SELECT`s the run STARTED, the executor commits COMPLETED + the real receipt, the reaper's UPDATE lands on top ⇒ a change that APPLIED on a remote host is reported abandoned to the operator who approved it. Same window frees a V31 slot while the task is still live in-process. ∴ `ToolRun.status == STARTED` in the WHERE for ∀ 4 callers — the UPDATE re-evaluates the predicate vs the newest COMMITTED row version ⇒ it closes the reaper's SELECT→UPDATE gap, ⊥ only the plain interleaving. (j)'s receipt idempotency read as if it covered the status too: the receipt was guarded, the status was ⊥. + `ReapOutcome.touched_anything` DELETED — 0 callers in src ∨ tests. The `LIMIT` this clause left OPEN — "a bounded batch changes what a pass MEANS, owner call" — is **CLOSED by (o)**. **20 mutations proven red** (+4 at the review — (g), (h), (n)): V23's status filter; V23's run-link match; the redaction guard; the receipt write; the receipt committed separately; a runner registered under another name; `start` awaiting inline; `stop()` ⊥ cancelling; reaper `<=`→`<`; reaper deadline dropped; reaper `status = STARTED` dropped; the detector's `IS NULL` dropped; a receipt for a READ run; the advisory lock; the cap counted AFTER the run starts; the cap guard dropped; the cap hardcoded in `deps`; the coverage guard's CALL SITE; the lifespan stopping the executor after `dispose()`; the reaper never started. **Review reds (19be7a2)**: a 2nd terminal write on a finished run; the same write fired from INSIDE a reaper pass, between `stranded_runs()` ∧ its UPDATE (V89 — `asyncio.gather` of the 2 passes with the predicate deleted); a NESTED redacted argument; a credential in the receipt's `after`. **2 survived the 1st pass ∧ both shapes recur**: a route harness that OVERRIDES a dependency ⊥ test that dependency (V31's read of settings — every other test stayed green), ∧ a probe on a GUARD ⊥ a probe on its CALL SITE (the runner-coverage check). **3 mutations HANG ⊥ red** (recorded ∵ it will recur, T39's 10th): `start` awaiting inline ∧ a dropped `task.cancel()` — both bounded with `asyncio.wait_for` ⇒ named reds; ∧ `except BaseException`, which is ⊥ boundable ∵ `wait_for` on `PeriodicTask.stop()` incidentally cancels the loop task ∧ MASKS the dropped-`cancel()` mutation ⇒ reached from the other side instead, a pass that RAISES `CancelledError` ! end the loop. Doc sweep same change (V84a): `core/approvals/__init__.py` (10 modules → 13, ∧ "Executing an approved change is T38"), `core/audit/__init__.py` ∧ `core/audit/tool_runs.py` (writer counts), `core/approvals/results.py` ∧ `reads.py` (a run ⊥ sit STARTED forever now), `core/db/models.py` ×2 ("Nothing writes this table yet"), `core/approvals/expiry.py`, `noa_api/mcp_tools/__init__.py`, `noa_api/mcp_audit.py`, `apps/web-embed/README.md` ×4 (receipt blocked on T36 ∧ T38 — on neither now; ∧ its poll-cap note said a STARTED run has ⊥ server-side terminator). (o) **the pass got a LIMIT, ∧ the LIMIT got a voice** — DECIDED ∧ BUILT 2026-08-09 (`52e5c28`), closing (n)'s OPEN clause; V92. The owner call was "what does one pass MEAN", 3 shapes on the table: a LIMIT with the pass still 1 transaction, a bounded batch committed PER BATCH, per-row commits. **CHOSE the LIMIT.** `reap`'s docstring PROMISED "a pass is one transaction" ∧ that promise is what makes a failure leave nothing half-reaped ∧ the next pass judge the set it would have judged — unbounded, the promise WAS the bug; bounded, its worst case = `batch_size` rows re-done. V91's first-wins predicate is what makes SMALLER transactions safe at all (a row the executor already finished = a no-op UPDATE), ∧ it is ⊥ a reason to split the commit: the drain the batch alone gives already beats arrival (V92d), so the promise is the better half of the trade. Per-batch ∨ per-row commits would have to REPLACE that docstring sentence, ⊥ silently contradict it — the fork this line records. 3rd setting beside (m)'s 2, ∧ ⊥ derived from either: `APPROVAL_STRANDED_RUN_REAP_BATCH_SIZE` 100 `ge=1` `le=1000` — (m) named a LIFETIME ∧ a RESOLUTION, this is a BOUND. `approved_without_run` bounded TOO though it writes nothing (V92f). `BoundedRows.truncated` DELETED at the mutation pass — 0 callers, `ReapOutcome` owns the 1 definition; same shape as (n)'s `touched_anything`, 2nd instance, ∧ found the same way (a mutation that SURVIVED). `FOR UPDATE OF tool_runs SKIP LOCKED` CONSIDERED ∧ REJECTED, V92(e) — the 2-replica duplication stays a KNOWN LIMIT. **17 mutations proven red**: each `LIMIT` dropped; each total faked from the page it bounds; `remaining` pinned 0; `truncated` pinned true ∧ pinned false; each log line's bound removed; the `id` tiebreaker dropped; the cut ⊥ oldest-first; the batch hardcoded in the loop ∧ in the wiring; the setting's floor, its ceiling, ∧ the DRAIN RATE the pair of settings makes (asserted as batch÷interval, ⊥ as the number alone — the number says nothing on its own). **2 survived the 1st pass, both shapes T38 already met**: a property with 0 callers, ∧ a tie-break test at N=2 that Postgres answered "correctly" for the wrong reason ⇒ rebuilt at N=6 with CHOSEN ids inserted in REVERSE, ∵ an assertion over random ids only SOMETIMES separates (V87's shape — a comparator that ⊥ separate). Doc sweep same change (V84a): `core/approvals/__init__.py` (reaper bullet), `core/config.py`, `.env.example`. ⊥ built here: the receipt's READ surfaces (T42(b)'s card payload, T63's join), ∀ CHANGE tool (T22–T29)

## T39

AS BUILT ≠ this line, 11 deviations: (a) **`<=`, ⊥ the `<` this line carried** — the door compares `expires_at <= decided_at` (T37), ∧ a row exactly ON its deadline ! be ONE thing: `<` here = refused at the door ∧ left PENDING by the sweep ⇒ a row that can ⊥ be answered ∧ ⊥ become terminal. Both comparisons pinned in ONE test (`test_a_request_exactly_on_its_deadline_expires_at_both_doors`) ⇒ an edit to either door has to break it to land; 2 mutations red, one per door. (b) **A THIRD writer class**, ⊥ a method on T37's — `core/approvals/expiry.py::SQLActionRequestExpiryRepository`, whose only reachable terminal status is EXPIRED (`status` ⊥ a parameter, predicate part of the statement). Same argument that split T33 from T37, one door further: a background loop has ⊥ operator behind it ∧ a GET is ⊥ a decision ⇒ neither ! hold a writer that can set APPROVED (V22). (c) **⊥ `SELECT … FOR UPDATE` on the sweep — deliberate, ⊥ an omission.** A bare `UPDATE` takes its own row locks ∧ under READ COMMITTED re-evaluates its WHERE vs the row version the transaction it waited for committed ⇒ a sweep colliding with an in-flight approval SKIPS the row, ⊥ expires a change already handed to an executor. An explicit lock = a 2nd mechanism to keep in step with the 1st for zero gain. Proven on the V89 rig: approval HELD OPEN between its locked read ∧ its commit, sweep issued INSIDE that window, assertion on ORDER (`sweep:returned` after `approve:committed`), + the negative control (a plain SELECT returns BEFORE that commit ⇒ the handshake genuinely overlapped). The 2 moments differ on purpose — the approval judges vs a `now` before the deadline, the sweep vs one after: that IS the case, a TTL passing while a human decides. (d) ONE predicate, one definition (`status = PENDING AND expires_at <= now`) — sweep ∧ check-on-read = the SAME `UPDATE` with an id added, ⊥ 2 queries with 2 spellings of "which rows are due". (e) Sweep writes `reason = NULL` EXPLICITLY. NULL on ∀ PENDING row by construction, but T34's CHECK EXEMPTS EXPIRED ⇒ ⊥ at the DB level would catch an EXPIRED row carrying one. Held by a test that seeds one by hand — else V69's shape, a property asserted by prose ∧ held by nothing. `tool_run_id` LEFT alone: ⊥ invariant says an expiry clears a link, ∧ clearing one = a destructive write with nothing behind it. (f) **Loop SLEEPS FIRST.** A pass at boot makes ∀ app start touch Postgres ∧ V51 says `/health` answers with Postgres down; one interval (60s) against an hour-long TTL buys nothing worth that. (g) Loop SWALLOWS per pass — `except Exception`, ⊥ `BaseException` ∵ `CancelledError` ∈ BaseException @ 3.11+ ⇒ `stop()` still stops it. A guarantee that ends the first time Postgres blinks ⊥ a guarantee. `run_once` RAISES: the LOOP is what swallows, so a caller driving one pass sees the failure. (h) Host = `PendingExpirySweeper` on `AppRuntime`, started by the lifespan ∧ stopped BEFORE `engine.dispose()` — a pass in flight would otherwise run vs a disposed pool, a shutdown-time error in the one component whose job is working when things go wrong. Ordering asserted, ⊥ described. (i) **Check-on-read ships as MECHANISM + request dependency** (`get_action_request_expiry_service`), ⊥ a live route: T41 ∧ T63 = `.` ⇒ ⊥ GET exists to call it. T37's executor-seam shape — correctness carried by TESTS, ⊥ by use. (j) `APPROVAL_EXPIRY_SWEEP_INTERVAL_SECONDS` (default 60, `ge=1`) = a RESOLUTION, ⊥ a lifetime — how late a request may still READ pending — ∧ deliberately ⊥ derived from the TTL. (k) 3 V66 moves same change: `_now`/`_as_utc` out of `decisions.py` → `core/approvals/clock.py` (2 doors compare a deadline ⇒ 1 definition of now); `SessionFactory` Protocol → `core/db/session.py` (`McpSessionFactory` states the same shape app-side ∵ `core/` ⊥ import an app); the live row helpers ∧ `ObservedDecisionRepository` → `support/action_decisions.py` (2 live files race the same window, ∧ 2 copies of that instrument = 2 things that can silently stop overlapping). **9 mutations proven red**: sweep predicate loses `status = PENDING`; sweep `<=`→`<`; door `<=`→`<`; check-on-read loses its id filter; sweep stops clearing `reason`; loop ⊥ survives a failing pass; loop sweeps before it sleeps; `stop()` drops the task ⊥ cancels it; lifespan stops the sweeper AFTER disposing the engine. A 10th ⊥ red ∧ recorded ∵ it will recur: dropping `task.cancel()` while KEEPING `await task` makes `stop()` await an infinite task ⇒ the suite HANGS, ⊥ fails — a timeout is a worse signal than a red, ∧ a mutation that hangs proves nothing. Doc sweep same change (V84a): `core/approvals/__init__.py` ("Four modules" — six now, ∧ what the 3rd writer is for), `core/db/models.py` (index comment still said `expires_at < now()`), `core/db/lifecycle.py` (`ActionRequestStatus` named T39 as still to come), `.env.example`

## T40

AS BUILT ≠ this line, 9 deviations: (a) **⊥ BIGSU, ⊥ Tailwind, ⊥ `.npmrc`** — owner-decided at plan time, ∧ the asymmetry was already in the spec: T47 names `@gio/bigsu-ui`, `@gio/bigsu-icons` ∧ the registry `.npmrc`; this line names ⊥ one of them. The card = a form ∧ 2 buttons in a small iframe ⇒ a design system + an internal-registry dependency on the embed's build is cost without return. Held by an eslint `no-restricted-imports` rule on `@gio/*`, ⊥ by prose (V84c) ⇒ adopting BIGSU here ! DELETE the rule on purpose. Dropping Next.js ENTIRELY was raised ∧ refused: it collides with C2 ∧ C12 ∧ T56 (§8.8 puts the table surface HERE, ⊥ `apps/api`), ∧ a static bundle ⊥ set `frame-ancestors` (T45,V41), ⊥ proxy the cookie server-side (T44 + AGENTS.md "browser ⊥ call FastAPI direct"), ⊥ hand the card a server-minted CSRF token off-URL (T41,V26,V39), ⊥ render the 401 state (V38) — 4 properties that ! have a server, ∧ the only other server is the one §8.8 rules out. (b) **`/healthz` BUILT here**, ⊥ named in this line — the one §I.embed route ⊥ other §T row owns, ∧ a scaffold with zero routes has nothing for lint/build/vitest/playwright to be green ABOUT. Touches nothing else, deliberately: a liveness probe that calls a dependency reports the DEPENDENCY's outage as this app's, ∧ a restart is then the wrong remedy. (c) **2nd eslint firewall rule** — `apps/admin-web` unimportable from here; C12's "⊥ shared source, ⊥ shared deps, ⊥ aliases" lived in AGENTS.md prose ∧ nothing held it. (d) **3 pins deliberate, ⊥ latest**: `typescript 6.0.3` ⊥ 7.0.2 ∵ `typescript-eslint@8.66.0` declares `typescript: >=4.8.4 <6.1.0` ⇒ TS7 puts lint ∧ typecheck on DIFFERENT compilers; `eslint 9.39.5` ⊥ 10.8.1 ∵ 9.x = the line this plugin set is proven against (C13); `@types/node 24.13.3` ⊥ 26.2.0 ∵ types track the RUNTIME major — a types package a major AHEAD describes APIs that are ⊥ there ∧ `tsc` then accepts code the deploy ⊥ run. Recorded ∵ it will recur: pnpm 11's `minimumReleaseAge` policy REFUSED `@types/node@26.2.0` (published <24h) ∧ auto-wrote a `pnpm-workspace.yaml` carrying an exclude for it — the exclude was DELETED ∧ the pin moved instead, ∵ a committed policy bypass for a types package is a worse artifact than a version choice. ⊥ `pnpm-workspace.yaml` in tree ⇒ ⊥ a workspace; own `pnpm-lock.yaml` committed (C12). (e) **`@testing-library/*` ⊥ installed** — nothing renders yet ∧ a dependency with ⊥ consumer is dead weight; lands with the card (T41). `vitest` still configured `environment: 'jsdom'` ∵ ∀ test this package grows renders DOM, ∧ route handlers keep working under it (Node's own `Request`/`Response` survive names jsdom ⊥ define). (f) **Playwright `webServer` waits on the SOCKET (`port: 3001`), ⊥ on `/healthz`** — MEASURED, ⊥ reasoned: the first version waited on the route, ∧ the `/healthz`-answers-503 mutation came back as a 120s startup TIMEOUT instead of `Expected: 200 Received: 503`. **Generalised to V90 in the same change** ∵ T41–T46 ∧ T56 each write their own e2e. (g) **hygiene scan = `git ls-files --cached --others --exclude-standard`**, ⊥ the bare `ls-files` `test_config.py` uses — tracked-only went red on its own first run at 0 files scanned, ∧ a brand-new oversized file stays invisible until `git add` ⇒ the cap would first speak at commit time, after the author stopped looking. The `scanned > 0` assertion is LOAD-BEARING, ⊥ decoration: an empty file list satisfies EVERY limit ⇒ a broken `ls-files` call reads as compliance (V87's shape). (h) **`AGENTS.md` ∧ `CLAUDE.md` inside `apps/web-embed` = written by `next dev` ITSELF**, committed per the note they carry — removing them from a diff only re-creates the uncommitted change. ⊥ NOA-authored guidance; T47 inherits the same pair. (i) `next.config.ts` pins `turbopack.root` ∧ `outputFileTracingRoot` to this package + `output: 'standalone'` (T60's input) ∵ ⊥ workspace ⇒ a lockfile higher up the monorepo otherwise drags unrelated files into the watch set ∧ into the standalone bundle. **4 mutations proven red**: caret on `next`; a 301-line `.ts` under `src/`; `/healthz` answering 503 (unit ∧ e2e — 2 lanes, 1 mutation); the `webServer` wiring deleted. ⊥ built here: CSP `frame-ancestors` (T45), the same-origin `/api/[...path]` proxy ∧ root-`.env` loading (T44), the card (T41), the table surface (T56), `Dockerfile` (T60), CI (T61). Doc sweep same change (V84a): `apps/web-embed/README.md` said "Not scaffolded yet" ∧ "Blocked on §T.59" — both stale since the gate cleared 2026-08-08; root `README.md`'s per-web-app check list gained the e2e lane

## T41

AS BUILT ≠ this line, 12 deviations: (a) **2 halves, ⊥ 1** — the API route `GET /action-requests/{id}` landed here too, ⊥ named in this line ∧ owed by §I.embed's own footnote ("GET = T41, ⊥ built"); a page ⊥ render a card no route answers. Added to T37's router ∵ same prefix, same cookie, same requester-match family — ∧ the read/write asymmetry is WIRED, ⊥ promised: `ApprovalCardService` over a repository with ⊥ `commit` ∧ ⊥ non-`SELECT` statement, vs `ActionDecisionService` next to it. (b) **⊥ CSRF-mint ROUTE, ever** — T46's answer taken: the token rides in the card's own GET, `null` once terminal. ⇒ T44's allowlist stayed at **4** (that row's as-built predicted T41 would widen it — corrected there), ∧ V39's "⊥ minting route here" now has a REASON, ⊥ a "yet": a token that arrives separately from the thing it authorises is one a page could hold without ever passing V27's match, ∧ that read is where the match happens. (c) **SERVER component, ⊥ a client fetch** — the page reads the incoming `Cookie` ∧ fetches server-side (`src/lib/approvals/detail.ts`) ⇒ the HTML that reaches the frame IS the authenticated card: ⊥ empty-box moment while a client call decides who you are, ∧ the token is part of the render. ⇒ the proxy's `GET action-requests/{id}` entry is now T42's polling seam, ⊥ this card's. `Authorization` ⊥ added on that server-side hop either (C5, T44(d)) — a 2nd code path, so re-proven, ⊥ cited. (d) **the decision POST landed HERE** ∵ this line says the buttons ! be JS-`fetch` handlers ⇒ the fetch IS the row. **T42 shrinks to polling + receipt**, same shape T46 shrank at T37. (e) **`core/approvals/reads.py` CREATED**, ⊥ named — the requester-matched `SELECT` (V27) ∧ V32's read-then-expire ordering extracted from T63's `results.py` ∧ SHARED. 2 readers ! guard one row identically while rendering different things, ∧ 2 spellings of an access control is how it holds at one surface ∧ ⊥ the other (V66). `ActionRunView` + `run_view` moved with it; `results.py` re-exports ⇒ T63's imports ∧ tests untouched, ∧ those tests are the regression proof. (f) **the card view is WIDER than `ActionResultView`, ∧ structurally so** — `evidence`, `requester`, `conversation_ref` exist here ∧ have ⊥ field there (V76). One row, 2 projections, ⊥ one filtered: the model's view can ⊥ leak what it cannot hold. ∧ ⊥ `reason` on either, bound at BOTH layers (response model ∧ view) ∵ pydantic IGNORES an extra key ⇒ a view-only addition ⊥ reach the body, so one mutation would have proven nothing. (g) **V32's "? call it first" DECLINED** — read first, expire second, same as T63. ∵ `expire_if_due` takes an id ∧ ⊥ requester, ∧ this id reaches an operator through a tool result that persists in LibreChat's MongoDB (V26) ⇒ "the caller supplied it" ≠ "the caller may see it". Cost = 1 poll of freshness; the card still ⊥ show a live PENDING past its deadline ∵ the view reports the write that just happened. (h) **V15's blank gate stays at the ENDPOINT** — the card submits what was typed ∧ renders the 409. A client-side refusal = a 2nd definition of blank, free to disagree with the endpoint's `.strip()` ∧ the DB predicate; V15 already says 2 spellings is 1 too many. `required` ⊥ used either — constraint validation belongs to a form submit, ∧ there is none (V80). (i) **401 ∧ 404 states BUILT here**, ⊥ named — V38's "⊥ blank card, ⊥ live Approve button" half binds the moment a card exists; T43 keeps the "Sign in to NOA" new-tab link-out. 404 = one sentence ∀ 3 causes (V27). A 200 whose body is ⊥ a card renders "could not load", ⊥ blank fields under a live button. (j) **`@testing-library/react` 16.3.2 + `@testing-library/dom` 10.4.1** — T40(e) parked them for this row; exact-pinned per `tests/pins.test.ts`. (k) **the e2e frames the card under R29's MEASURED sandbox string** (`allow-scripts allow-same-origin`, `allow-forms` ABSENT), clicks Approve, ∧ reads the stub's hit counter — ∧ ships a NEGATIVE CONTROL document in the SAME sandbox where a `fetch` lands ∧ a native form submit ⊥ (V87: without it "the fetch worked" is a claim about nothing; proven by widening the sandbox ∧ watching the control go red). Parent = T45's real server EXTENDED with `?src`/`?sandbox`, ⊥ copied (V66). V90 held by reusing T40's socket gates — ⊥ new readiness wait, ∧ the sandbox ∧ parent are SETUP, ⊥ subject. (l) **each counting test owns its own card id** ∵ the stub is 1 process shared by every spec file ∧ Playwright runs files in parallel ⇒ `/__hits` is cumulative ∧ a reset would race another file; a per-test id makes each count EXACT, ⊥ "at least". **13 mutations proven red**: requester-match dropped from the shared `WHERE`; `csrf` minted for a terminal card; CSRF signed without the request id; expiry moved AHEAD of the read (a foreign id then makes NOA write); the expiry call deleted; `reason` on the response model; `reason` on the view; buttons wrapped in a `<form>` with `type="submit"` (jsdom ∧ browser — the browser red landing on the HIT COUNTER, which is the mechanism); the sandbox widened to grant `allow-forms` (the control notices); a live Approve button on the 401 state; the loader dropping `cookie`; the loader relaying `authorization`. ⊥ built here: polling to terminal ∧ the receipt (T42), the link-out (T43), the table surface (T56). Doc sweep same change (V84a): both READMEs; `src/lib/proxy/routes.ts` (the entry's stated purpose — T42's polling now) ∧ `routes.test.ts` (its pinned-4 comment named a T41 mint route that will never exist); `core/approvals/csrf.py` (⊥ minting route, now with the reason); `core/approvals/expiry.py` (BOTH render paths read first); `core/approvals/__init__.py` (10 modules, 2 readers over 1 shared guard). ∧ `test_action_request_decision_routes.py`'s negative control MOVED, ⊥ deleted: it asserted the GET detail was ⊥ mounted ("the GET detail is T41's") ⇒ now asserts there is ⊥ COLLECTION route, ∵ V34 says one URL per request, reached by an id an operator was given, ⊥ by listing what exists

## T42

**AS BUILT ≠ this line, 6 deviations:** (a) **the split is a FLAG on the shared statement, ⊥ a 2nd read** — `select_requester_matched(..., include_receipt=)`; the card passes it, `core/approvals/results.py` ⊥ ⇒ V76's separation is a statement never ISSUED, ⊥ a field a projection drops. MEASURED: adding that join to the model path left ∀ payload test GREEN ∵ `ActionResultView` has nowhere to put one ⇒ the row would ride into the process that answers a model ∧ be dropped at render. That gap = V93, ∧ `apps/api/tests/test_requester_matched_read.py` compiles BOTH statements so the absence is asserted, ⊥ narrated (V69). (b) **before-state renders off the RECEIPT once one exists**, ⊥ beside `evidence` — T38's writer COPIES `approval_context`'s evidence onto the row ⇒ 2 headings = 1 fact twice in a frame §I.embed calls compact. (c) **⊥ receipt timestamp on the card** — `action_receipts.created_at` exists ∧ is ⊥ carried; `tool_runs` answers WHEN (V47) ∧ a 3rd stamp for 1 moment = T34's 3rd truth. (d) **⊥ receipt ⇒ ⊥ section**, ⊥ an empty one: an empty outcome block is a claim nothing recorded. (e) **⊥ re-derived, ⊥ re-redacted** — `after` was redacted by the writer (V8, V45), `before` = the gate's own evidence; a 2nd redaction policy here = 2 policies, ∧ the day they disagree one is wrong. Halves permissive on SHAPE (non-object ⇒ "nothing recorded", ∵ a `KeyError` in front of an operator is a blank iframe, V38), `ok` fail-closed on anything ⊥ literally `true` — a truthy string read as success tells an operator a change LANDED. (f) **8 mutations proven red**: join dropped; halves collapsed; `ok` made truthy; receipt joined on the model path; parser dropping the field; after-state unrendered; before-state rendered from BOTH sources; outcome section removed (browser lane). The browser lane watches the section APPEAR — absent while the run is in flight ⇒ both halves are something the frame FETCHED, ⊥ pre-rendered bytes. Doc sweep same change (V84a): `apps/web-embed/README.md` carried the receipt under "Still to come" ×3; `poll.ts` justified its 150-read cap by `DeferredApprovedChangeExecutor`, gone since T38 — cap STANDS, reason = the reaper's interval

## T43

AS BUILT = this line + 3 things it ⊥ name: (a) the address ALSO as TEXT ∵ the click-door is withholdable — `allow-popups` absent at `ToolCallInfo` (R13) ⇒ `target="_blank"` refused SILENTLY, ∧ where popups ARE granted the opened tab inherits the sandbox ⇒ native `<form>` submit inert in it (R32, V94, bounds C17/V25/V37/V80); (b) `NOA_SIGN_IN_URL` — new env var, WHOLE URL ∵ T50 owns the login route, per-REQUEST server read, `http`/`https` only, unusable → ⊥ link (⊥ a default); (c) retry = the poll's own reader, ⊥ a reload ⇒ the framed URL asserted UNCHANGED, which is the ⊥-redirect half bound as an absence. State itself pre-existed T43 (T41 rendered it) ⇒ what landed = the way OUT. Lanes: `src/lib/sign-in.test.ts`, `src/app/approvals/[id]/sign-in-notice.test.tsx`, `e2e/sign-in.browser.e2e.ts`. V65 went RED mid-build at 378 lines ⇒ browser lane split out ∧ frame helpers hoisted to `e2e/support/frame.ts` (the guard did its job, ⊥ a reviewer)

## T44

AS BUILT ≠ this line, 8 deviations: (a) **"or direct API origin" ⊥ a choice** — `AGENTS.md` settles it ("Browser ⊥ call FastAPI direct. Same-origin proxy route per web app") ∧ §I.embed's own footnote said the same ⇒ the disjunction was dead when this line was written. Recorded so it ⊥ read as still-open. (b) **ALLOWLIST, ⊥ pass-through** — owner-decided at plan time, ∧ the argument is 2 §V lines this row ⊥ cite: V42 says this app has ⊥ login page, ⊥ LDAP form, ⊥ credential handling, ∧ a generic proxy puts `POST /auth/login` ON the origin; V41 makes the embed the ONLY frameable NOA origin (admin answers `frame-ancestors 'none'`) ⇒ a pass-through also puts `/admin/*` (T51–T55) inside the LibreChat frame with the operator's cookie, which is the reachability that header exists to deny. `core` of it = `src/lib/proxy/routes.ts`, 4 entries, matched on METHOD ∧ shape TOGETHER (right path + wrong method = refused), ∧ the count PINNED by test ⇒ T42 ∧ T56 each widen it on purpose. **CORRECTED 2026-08-09 by T41**: this line read "T41's CSRF-mint route, T42 ∧ T56" — T41 needed ⊥ widening at all. T46's answer put the token in the card's own GET response ⇒ ⊥ mint route exists to allowlist, ∧ the count is still 4; the `GET action-requests/{id}` entry planted here as "the card's detail read" now serves T42's POLLING instead, ∵ T41's page reads that detail SERVER-side (T41(c)). **CORRECTED AGAIN 2026-08-09 by T56**: T56 ⊥ widen it either ⇒ the count is STILL 4 ∧ **0 of the 3 predicted wideners happened** — its page reads SERVER-side ∧ ⊥ poll ∵ a parked table ⊥ change once written, so the browser needs ⊥ route to it at all. The PIN's value is unchanged, ∧ that is the point: a surface that genuinely needs a browser route ! move that number to get one. Same shape as V83b ∧ V57-after-T68: guard at the MECHANISM, ∵ a guard the next surface has to remember is one it forgets. (c) **Refusal = 404 `route_not_proxied`, ⊥ 403, ∧ upstream ⊥ called** — the ORDER is the property: "this origin ⊥ carry the login route" is a fact about a request never SENT, ⊥ about a response that came back, ∧ a 404 an upstream produced looks identical from the browser ⇒ held by the `fetch` spy unit-side ∧ by the stub's `/__hits` in the browser lane, ⊥ by the status. Body = `{error_code, message}`, ⊥ `request_id`: V73 binds the API's 5 writers ∧ ⊥ request reached one ⇒ an id here names ⊥ log line. 404 ⊥ 403 ∵ a code that varied by cause makes this origin an existence oracle for the API's surface (V27's shape, one origin over). (d) **`authorization` STRIPPED outbound** — the 1 departure from the ported source. ∀ surface this app reaches is cookie-authenticated (V40, V22); a bearer token is LibreChat's to send (C5), ⊥ a browser's ⇒ forwarding the header makes the embed origin a relay for one. (e) **`{id}` ⊥ shape-validated** — the rule matches ONE non-empty segment ∧ hands it on. V27 owns what absent ∨ malformed ∨ foreign answers ∧ answers ∀ 3 alike; a UUID check here = a 2nd, more talkative judge in front of it. (f) **root `.env` loading landed here** (T40 parked it) ∧ as `config/root-env.ts`, ⊥ inline in `next.config.ts`: the PRECEDENCE rule is the part that can be got wrong — the real environment always wins, which is how the Playwright run points `NOA_API_URL` at its own stub — ∧ a rule inside the config ⊥ testable without importing the config's side effects. `key in env`, ⊥ truthiness: an explicitly-empty value = an operator's deliberate unset, ∧ refilling it re-points the proxy at an API they turned off. ⊥ per-app `.env.local` (C11) — 2 env files for 1 deployment = 2 places for `NOA_API_URL` to disagree, ∧ a proxy quietly talking to the wrong API is ⊥ a failure anyone sees. (g) **Methods bounded by what the module EXPORTS** — `GET` ∧ `POST` only ⇒ Next answers 405 for the rest ∧ the restriction costs ⊥ code to hold. `runtime = 'nodejs'` (a streaming body needs `duplex: 'half'`) ∧ `dynamic = 'force-dynamic'` (a cached proxy answer serves one operator's session state to the next request), both asserted on the module's own exports ∵ that is the knob Next reads — a response-header assertion tests the harness (T40(b)'s rule). (h) **e2e boots a STUB upstream ∧ ⊥ reuse the dev server** — V90's family, one step past T40(f). Stub ∵ a real API drags Postgres ∧ LDAP into a Playwright run ∧ turns either one's outage into a red PROXY test; `reuseExistingServer: false` ∵ a reused server was given a DIFFERENT `NOA_API_URL` ∧ would decide the outcome silently. Both servers still gate on the open SOCKET. `eslint.config.mjs` gained a Node-globals block SCOPED to `e2e/support/**/*.mjs` — a `process` reference inside `src/` stays an error. **Ported, ⊥ rewritten** (C13): `noa-old` `MCP:apps/web-bigsu/src/lib/proxy/http.ts` → `src/lib/proxy/http.ts`; V69 is why its tests came WITH it — traversal (raw, `%2e%2e`, backslash), off-origin `Location`, host scrub, status-0 remap ∧ the undici decompression rule each re-proven vs THIS copy, ⊥ cited from the source. **19 mutations proven red**: allowlist removed; method dropped from the match; empty path segment accepted; shape-length compare loosened; `authorization` forwarded; `cookie` dropped; `Set-Cookie` ⊥ copied back; post-normalization prefix-escape check dropped; `../` segments ⊥ stripped; off-origin `Location` returned raw (open redirect); internal host left in `Refresh`/`Link`; opaque-redirect status 0 ⊥ remapped; pass-through encoding relabelled identity; root `.env` overwrites the environment; truthiness instead of `in`; quotes left in the value; route moved off the Node runtime; answers made cacheable; refusal forwards upstream FIRST. The allowlist mutation was re-run on the BROWSER lane ∧ went red there too (login ∧ admin) ⇒ that lane SEPARATES, ⊥ merely passes. ⊥ built here: the card ∧ its CSRF-MINT route (T41), the decision POST ∧ polling (T42), the 401 state (T43), CSP `frame-ancestors` (T45), the CSRF carry (T46), the table surface (T56). Doc sweep same change (V84a): `apps/web-embed/README.md` listed this proxy under "Still to come" ∧ named ⊥ allowlist; root `README.md`'s e2e note described ONE server

## T45

AS BUILT ≠ this line, 8 deviations: (a) **origin CONFIGURED, ⊥ the literal** — `NOA_LIBRECHAT_ORIGIN` (`config/framing.ts`), default = this line's string. The seam PRE-EXISTED with zero readers (`.env.example:82`, `core/config.py:167`, both commented "(V41)") ⇒ it was planted for this row. 2 reasons it ! be a knob, ∧ the 2nd is the harder one: C21's re-verify rig runs LibreChat at `http://chat.noa.internal:3080` (`spikes/librechat-embed-render-gate/harness.env`) ⇒ a hardcoded `https://` makes the gate unrunnable against a CSP-carrying build; ∧ an `https` parent framing an `http` child dies as MIXED CONTENT before CSP is ever read ⇒ a refusal test would pass for a reason that ⊥ the header. Fail-closed either way: absent ∨ blank → the pinned default (header ⊥ omitted ∵ a variable nobody set ⊥ mean "frameable by anything"), ∧ a WIDENING value — wildcard, 2nd origin, path, bare hostname — THROWS ⇒ build ∨ boot failure (T8's rule), ⊥ a silent default that ships a header nobody meant. (b) **baked at BUILD, ⊥ runtime** — `output: 'standalone'` ⊥ execute `next.config.ts` at runtime ⇒ a runtime ConfigMap ⊥ widen the allowlist ∧ ⊥ change it either; moving LibreChat = rebuild, ∧ T60 ! carry it as a build arg. Recorded ∵ a ConfigMap that silently does nothing is the failure nobody sees. (c) **`headers()` in `next.config.ts`, ⊥ middleware** — read in the installed bytes, ⊥ assumed: `next/dist/server/lib/router-utils/resolve-routes.js:543-565` accumulates config headers into `resHeaders` ∧ ⊥ terminate the match ⇒ ONE entry on `/(.*)` covers pages, `/api/*` (T44), `/healthz` ∧ the 404 alike. Guard at the MECHANISM, ⊥ per-route (V83b's shape) — a route added later inherits it instead of remembering it. Same mechanism `noa-old` named for this invariant (`MCP:SPEC.md` T165) ⇒ port, ⊥ invention (C13). (d) **`X-Frame-Options` absence ASSERTED**, ⊥ merely stated — `frame-ancestors` supersedes XFO only where both are read, ∧ a `SAMEORIGIN` added later kills framing in a client that honours XFO over CSP ⇒ bound by test, ⊥ by this sentence (V84c). (e) scope = `frame-ancestors` ONLY — ⊥ `default-src`/`script-src`; a wider policy is a decision about a card that ⊥ exist yet (T41) ∧ would land untested. (f) **parent = a real server under 2 hostnames, ⊥ an intercepted response** — MEASURED: `page.route(...).fulfill` for the parent made BOTH frames die `net::ERR_BLOCKED_BY_LOCAL_NETWORK_ACCESS_CHECKS` before any response ⇒ the refusal spec passes with ⊥ CSP header in existence. V90's family — the SETUP decided the outcome. ∴ `e2e/support/framing-parent.mjs` + a `--host-resolver-rules` map of both names to loopback ⇒ the parent ORIGIN is the only variable between the frame that loads ∧ the frame Chromium refuses. (g) **V37's positive control ships BESIDE the refusal** — R29's render path is exactly what a framing header kills silently ⇒ the allowed parent frames `/healthz` ∧ the body reads `{"status":"ok"}`; without it the refusal passes just as well against a header that blocks everyone (V87). (h) 7 mutations proven red: default constant changed; header key renamed; source narrowed off `/(.*)` (unit ∧ browser); origin validation removed so `*` passes; blank env emitting an empty value instead of the default; `headers()` dropped from the config; the CSP entry deleted whole. The rename mutation also caught a SELF-REFERENTIAL assertion — the no-XFO check compared the key to the constant under test ⇒ stayed green when that constant became `X-Frame-Options`; now a literal. ⊥ built here: `frame-ancestors 'none'` on admin (T49), the card (T41), the decision POST (T42), the 401 state (T43), the CSRF carry (T46), the table surface (T56), the build arg (T60). Doc sweep same change (V84a): `apps/web-embed/README.md` carried this header under "Still to come"; root `README.md`'s e2e note named 2 servers; `.env.example`'s comment ⊥ say the value is read at BUILD time

## T51

AS BUILT ≠ this row's word "create", 6 deviations: (a) **⊥ `POST /admin/users` — OWNER DECISION 2026-08-18**, ∧ it is the row's own wording that is now wrong, ⊥ the code. A NOA operator is BORN AT LOGIN: `AuthService._provision` writes the row `is_active=False` on the first successful LDAP bind ∧ an admin enables it through `PATCH` (V7, C4) ⇒ "create" already has a home, ∧ LDAP — ⊥ this API — is source of truth for employment. `noa-old` `MCP` shipped ⊥ create route either (`api/admin/user_routes.py` = `get /users`, `patch`, `delete`, `get /tools`, `put /users/{id}/tools`, `post` migrate-grants, `put /users/{id}/roles`) ∧ the ported panel carries ⊥ create control (`apps/admin-web/src/lib/admin/users/users-api.ts` = 4 calls). ⇒ §I.admin-api's `/admin/users | GET/POST` row ∧ this §T row both need a `/ck:spec` amend; build ⊥ edit either (AGENTS.md). PROVEN live, ⊥ argued: an unknown operator's first login answered 403 `user_pending_approval` ∧ the row appeared in `GET /admin/users` as `is_active:false`, `last_login_at:null`. (b) **T9's engine shipped with NO transaction boundary** ∧ a route calling it would have answered 200 over a rollback: `SQLAuthorizationRepository` flushes ∀ method ∧ `noa_api.api.deps.get_db_session` commits ⊥ ever, so V14's "permission updates take effect immediately" was UNTRUE of every write the service could make. `commit()` added to the `AuthorizationRepository` Protocol + the SQL class, ∧ called as the LAST statement of ∀ 6 mutations (`create_role`, `delete_role`, `set_role_tools`, `set_user_roles`, `set_user_active`, `delete_user`) — ⊥ the 3 T51 uses, ∵ half a service that commits is the trap T52 walks into. Boundary in the SERVICE, ⊥ the routes: same rule `AuthService.authenticate` ∧ `ActionDecisionService.approve` follow, ∧ ∀ guard raises BEFORE the commit ⇒ a refused disable revokes nothing ∧ persists nothing. Idempotent `create_role` commits ⊥ (nothing changed, mirroring its no-event rule). Witness = a SECOND session, ⊥ the writer's own: `test_rbac_repository.py::test_commit_makes_a_disable_outlive_the_request` ships a NEGATIVE CONTROL (the same write flushed-only, observed as unchanged) ∵ inside one session flushed ∧ committed read identically ⇒ a green result could come from an observer that sees nothing (V89's shape, one mechanism over). Live proof at the API: the operator enabled by `PATCH` then LOGGED IN — a different request reading the committed row. (c) **internal `user:` roles STRIPPED from the response**, ∧ that is a wire decision T9's `AuthorizedUser` ⊥ make — `get_role_names` returns them ∧ the panel renders `roles` as the ASSIGNABLE set ∧ PUTs that set back ⇒ shipping one would make the UI ask for exactly what `set_user_roles` answers 400 `internal_role_forbidden` to. Preserved server-side regardless (the repository's `~LIKE 'user:%'` subquery) ⇒ hidden ≠ cleared, ∧ a test asserts BOTH halves. (d) **⊥ `direct_tools` key**, where `noa-old`'s `_to_user_response` carried one: V75 disables direct per-user grants (410, T65) ⇒ a field nothing can fill. Absent, ⊥ empty — the panel's drawer already renders that section only when non-empty. (e) **`last_active_admin` is UNREACHABLE through these 4 routes ∧ is recorded as such rather than tested into existence**: ∀ caller here holds `admin` ∧ is active ⇒ `count_active_admin_users() ≥ 2` whenever the target is a DIFFERENT active admin, ∧ when the target is the caller the self-guards (`self_deactivate_admin`, `self_delete_admin`, `self_remove_admin_role`) raise first. It fires only for `actor_user_id=None` — a script ∨ a future CLI. Covered where reachable (`test_rbac_engine.py::test_cannot_disable_last_active_admin` + 2 siblings) ∧ in the status map (`test_rbac_routes.py`); a route-level assertion would have required BUILDING A CALLER PRODUCTION ⊥ PRODUCE. (f) the 41 route tests build their OWN app ⇒ a missing `include_router` in `create_app` would leave them ALL green while the deployed panel 404s: `test_app_lifespan.py::test_admin_user_routes_are_mounted_on_the_real_app` reads the production OpenAPI schema ∧ pins the 3 paths ∧ their verb sets. ⊥ built here: `/admin/roles` (T52), tokens (T53), servers (T54), audit (T55), the direct-grants 410 (T65). `apps/admin-web` untouched — its 4 calls pre-existed (T48) ∧ its proxy is pass-through (T50)

## T52

AS BUILT = 6 routes ∧ ⊥ policy, 7 notes: (a) **⊥ rule moved out of T9's engine** — reserved-`admin` guard, name validator, catalog check on a grant, audit event ∧ `commit()` all pre-existed (T51(b) added the last) ⇒ T51's warning "half a service that commits is the trap T52 walks into" was ⊥ sprung: ∀ 3 mutations reached here (`create_role`, `delete_role`, `set_role_tools`) already committed. Handler = resolve actor, call 1 service method, shape answer. (b) **`GET /admin/tools` = the 6th route ∧ §I.admin-api carried ⊥ row for it** at build time ⇒ owed amend, landed by `/ck:spec` 2026-08-18; until then `test_app_lifespan.py::test_admin_role_routes_are_mounted_on_the_real_app` (production OpenAPI, 5 paths + verb sets) was the ONLY record NOA serves it. Placed on the ROLES router ∵ it is the vocabulary of `PUT /roles/{name}/tools` — `noa-old` kept it on its user router (`api/admin/user_routes.py`), nothing about it belongs there. (c) `core/auth/authorization_service.py` gained `list_tools()` — read, ⊥ `commit` — ∧ the route reads THAT, ⊥ a re-imported `TOOL_CATALOG`, so offered names ⊥ drift from accepted names (V66). (d) `admin` visible in `GET /roles` ∧ refused on the 3 writes, ∧ `GET /roles/admin/tools` answers the whole catalog ⊥ its empty grant rows (V10) ∵ displayed state = enforced state. (e) ⊥ `HTTPException` in the file: refusals = `AuthorizationError` subclasses, `noa_api.api.errors` owns status + body + `request_id` (V8, V73) — `noa-old` spelled the same 5 refusals out per handler (~35 lines each) ∧ hand-kept 4 `error_code` strings in step. (f) MUTATION-CHECKED 4× (B5's rule), each restored from snapshot ∧ files verified identical: dropped `set_role_tools`' `commit` → live 2nd-session witness + commit-count red; disabled `_reject_reserved_role` → 4 red; `list_tools` → `TOOL_CATALOG` → 2 narrowed-catalog red; dropped `AdminUserDep` from the tools route → 3 red. V100 held by a SECOND session (`test_commit_makes_a_role_grant_outlive_the_request`) + a flush-only negative control, ⊥ the writer's own session. (g) Gate coverage: `test_every_role_route_refuses_a_non_admin` / `_a_missing_cookie` (6 routes each), `test_the_admin_role_refuses_every_write` (3 verbs), `test_a_grant_change_is_visible_on_the_very_next_user_list` (V14), `test_an_unknown_tool_refusal_leaves_the_existing_grants_alone` (V100), `test_an_unknown_tools_body_does_not_echo_the_submitted_names` (V8). Suite 2501 passed, `ruff check` ∧ `ruff format --check` clean; live tests ran vs Postgres on :5432 with 0 skipped ∵ repo has ⊥ `docker-compose.yml` yet (T60's)

## T56

AS BUILT = this line + 11 things it ⊥ name: (a) **the STORE**, ⊥ named here at all (this line names a route ∧ a result) — `tool_result_tables` + migration `0007_tool_result_tables`: `token` UNIQUE = the SOLE index ∵ ∀ reader arrives holding one (T36's discipline, T34's set smaller still); `requested_by_user_id` nullable + **SET NULL** ∵ the READER matches on that column ⇒ NULL ! mean NOBODY ⊥ anybody (V27 fail-closed), asserted by a real `DELETE` ⊥ by reading `ondelete`; `column_labels`+`rows` JSONB with **⊥ server default** — `approval_context`/`receipt_data`'s rule, deliberately ≠ `tool_runs.args` (`'{}'` there so "took ⊥ args" ≠ "⊥ recorded") ∵ an empty parked page is ⊥ a legitimate state; `total_rows`+`truncated` NOT NULL = V85 STORED ⊥ derived (`len(rows)` is precisely the number a capped table ! ⊥ report as its total); `expires_at` NOT NULL + aware (T34's rule: a row without a deadline ⊥ expire). 5 columns REFUSED by test, each with its reason as the test id: `reason` (C8,V43 — a READ authorised nothing), `requested_by_email` (V27 = one requester column ∧ it is the FK), `status` ∧ `updated_at` (written once; the deadline IS the whole lifecycle), `tool_run_id` (T73 owns the audit row ⇒ a link = a 2nd record of one call). (b) **2 classes split by CAPABILITY** (`core/results/tables.py`), `core/approvals`' discipline one table over: the writer inserts ∧ commits ∧ has ⊥ read method; the reader has ⊥ `commit` ∧ ⊥ statement that is ⊥ a `SELECT`. Writer sits on the MCP tool path, reader behind the cookie ⇒ neither reachable from the other's side. (c) **3 guards, ONE `WHERE`** — `select_table_for_requester(token, requester, now)`: the token names the row, the requester decides whether it may be seen, the deadline decides whether it still exists. A function so a test COMPILES it (V93 — a guard applied AFTER the read leaves the row loaded ∧ ∀ payload test green, MEASURED at T42(b)). (d) **cap ∧ redaction at the WRITE** (V8, V85) — `cap_rows` = a PREFIX, ⊥ a re-sort ∵ only the producer knows which order is reproducible for its source (V85's amended clause, T24's `csf -g` deviation); `redact_sensitive_data` RECURSIVE, the audit path's own function ⇒ the parked table is ⊥ the one writer exempt (B7's depth lesson ∧ B8's per-writer-exemption one, both one table over). (e) **API `GET /tables/{token}`** — cookie `SessionUserDep` ∧ V6's row re-read ⇒ a disabled operator loses the table on their next request; ONE 404 `result_table_not_found` ∀ **4** causes (unknown \| foreign \| requester deleted \| expired), one body, only `request_id` differs (V27 as T37 amended it, V73). `token` = plain `str`, ⊥ shape-validated — V27 answers ∀ 4 alike ∧ a 422 for a malformed one would say which tokens are worth guessing (T44(e)/T63(e)'s rule). ⊥ expiry WRITE, unlike the card's (V32): ⊥ status column anybody reads ⇒ the row simply stops matching, ∧ there is nothing to correct. `core/results/errors.py`: `ResultTableNotFoundError` 404, `ResultTableUnavailableError` 503, bare `ResultTableError` 503 BY DECISION ⊥ by fallthrough (the subclass-tree test covers this tree too). (f) **MCP `mcp_tools/table_surface.py`** = T32/T33's 2-call split: `park_table_result` touches the DB — own session opened ∧ closed there ∵ the tool holds none by then (T21's rule), requester from `current_mcp_identity()` ⊥ a param ∵ a tool that could pass one could park under someone else's name, ∀ write failure → `ResultTableUnavailableError` ⊥ a result whose address leads nowhere — ∧ `build_table_result` is PURE. Text block **FIRST**, then the resource; count ∧ ORDER both asserted ∵ "a resource exists" stays true when the TEXT is the half that got dropped (V25). Bound stated IN that text, unconditional in SHAPE (both numbers even when they agree) ∵ a sentence that appears only on truncation is one a model learns to read as noise (V85). Address as PLAIN TEXT, ⊥ markup (V94, R32). ⊥ `structured_content` (T32(b)'s measured reason) — **WRONG for a READ, CORRECTED 2026-08-10 by T20(a)** (`178044e`): that reason's load-bearing clause is CHANGE-only (a CHANGE call skips the audit middleware, V46) ∧ `ToolRunAuditMiddleware` reads status + summary off `structured_content` where `None` = FAILED ⇒ content-only filed ∀ successful large listing in the audit trail as a failure with an empty summary (V20, V45). Now a counts-only envelope (`ok`, `total_rows`, `stored_rows`, `truncated`) — ⊥ rows, ⊥ token, ⊥ URL, content array byte-identical ⇒ T32(b)'s surviving clause ∧ R29's measured render path both hold. ⊥ sample row, ⊥ preview, ⊥ column value ∵ the result persists in LibreChat's MongoDB (V26). `ui://noa/table/<token>` prefix ≠ the card's so the 2 surfaces stay distinguishable in a transcript. (g) **2 hoists (V66)** — `mcp_tools/ui_resource.py` takes the iframe shape out of `change_gate.py`: `ui://` scheme + `text/uri-list` + URL-as-body stated ONCE ⇒ V64's "same mechanism as V37, ⊥ a 2nd" is a shared FUNCTION ∧ ⊥ a promise (R12, R29, R31c); `core/clock.py` takes `now_utc`/`as_utc` out of `core/approvals` (which now re-exports) ∵ a surface comparing a deadline has ⊥ business importing the approval gate to do it. (h) **2 settings REQUIRED on `McpToolContext`, ⊥ defaulted** (T32(c)/T33(e)'s rule): `RESULT_TABLE_TTL_SECONDS`=86400 `ge=60` — a LIFETIME, ∵ the URL persists in a transcript ⇒ an operator opens it hours later — ∧ `RESULT_TABLE_MAX_ROWS`=5000 `ge=1` **`le=50000`** ∵ a cap with no ceiling = the unbounded write spelled in an env var (T38/V92's rule). (i) **Embed `/tables/[token]` = a SERVER component** — `dynamic='force-dynamic'`, incoming `Cookie` forwarded, `Authorization` NEVER (T44(d)/T41(c)'s departure: a bearer is LibreChat's to send, C5). Read-only asserted BY NAME ⊥ by a control count (T43's rule): ⊥ approve, ⊥ deny, ⊥ reason box, ⊥ `<form>`, ⊥ `input`, ⊥ CSRF. 4 load outcomes ∵ 4 render differently; a 200 whose body is ⊥ a table → `unavailable`, ⊥ headings over nothing (V38's family). `SignInNotice`+`Notice` hoisted `app/approvals/[id]/` → `src/components/`, shared with the card ∵ 2 copies of "cannot authenticate here" = 2 places for the printed ADDRESS to go missing from one of them (V66, V94). (j) **PROXY ALLOWLIST STAYED 4 — T44's prediction was WRONG**, corrected in that row ∧ in its own test comment: this page reads SERVER-side ∧ ⊥ poll ∵ a parked table ⊥ change once written ⇒ ⊥ browser-reachable route to allowlist. T44's guard is unharmed — a surface that DOES need one still ! move that number. (k) **JSON `null` ≠ SQL NULL** — an ORM attribute set to `None` on a JSONB column stores `'null'::jsonb`, a VALUE that SATISFIES `NOT NULL` ⇒ "an insert omitting this column fails" expressed that way is ⊥ about an omitted column at all, ∧ the first version of that schema test was green against a column carrying a server default it never had. Rewritten as a Core `INSERT` that OMITS the column from the SQL — the only form of the question Postgres answers. → **B9**, ∧ V87 extended. **⊥ MEASURABLE in this harness: the cookie riding into the FRAME.** Embed = `localhost:3001`, parent = `chat.noa.internal:8110` ⇒ CROSS-SITE ⇒ Chromium withholds a `SameSite=Lax` cookie from the framed document itself. ⇒ the cookie spec runs TOP-LEVEL ∧ records why; what this lane proves = the LOADER forwards the browser's cookie, ∧ the in-frame ride stays R29's LIVE measurement (V40: production puts both under `noa.internal`). Found by the NEGATIVE CONTROL — a token the stub serves ONLY to a request carrying one — ⊥ by reading. Rest of the browser lane still frames the table under `MEASURED_SANDBOX` ∧ reads the stub's COUNTER to prove ⊥ POST (V80's rule: a request never made throws nothing). **1st PRODUCER LANDED 2026-08-10 — T20** (`whm_list_accounts`, `178044e`), which is where the copied reason above got caught — a surface with ⊥ caller had ⊥ way to be wrong about the reader it owes; T30 still `.` ⇒ 1 caller of 2 ∧ tests still the only other; `TABLE_SURFACE_PATH` ↔ `apps/web-embed/src/app/tables/[token]` = 2 languages ⇒ ⊥ compiler checks that pair (T32(g)'s note). Verification: 1884 pytest + ruff clean; 286 vitest + eslint + tsc clean; `next build` ships the route; 32 Playwright specs, 8 new. Doc sweep same change (V84a): `apps/web-embed/README.md`, `docs/integrations/whm.md`, `docs/integrations/pmg.md`, `core/db/models.py` (header + `tool_runs.result_summary`'s comment naming a surface that ⊥ exist yet), `test_schema_v1.py` (`LATER_TABLES` += it), `support/database.py` (`MUTATED_TABLES` += it ∵ SET NULL ⇒ a parked table outlives its requester)

## T59

AS BUILT ≠ this line, 3 deviations in the RIG, each named ∵ each is something the run ⊥ exercise: (a) probe tools sat on a SECOND FastMCP server at `/mcp-embed-probe` behind the same `NoaTokenVerifier` ∵ `register_mcp_tools` refuses uncatalogued names (V10, V83a) ⇒ token auth ∧ TOFU (V3) exercised, `RbacToolMiddleware` ∧ `ToolRunAuditMiddleware` ⊥. (b) the in-frame POST hit a stand-in with ⊥ CSRF (V39), ⊥ row lock (V28), ⊥ reason (V15) — it answered "did the cookie arrive", nothing else; T33/T37 build the real one. (c) plain HTTP ∧ one browser (Chromium 141) ⇒ TLS cookie flags ∧ Firefox/WebKit unmeasured. Rig committed ∵ C21's re-verify: `spikes/librechat-embed-render-gate/`, verdict `docs/spikes/librechat-embed-render-gate.md`

## T63

AS BUILT ≠ this line, 6 deviations: (a) **"receipt summary" = `tool_runs`, ⊥ `action_receipts`** — owner-decided at plan time: T36 = `.` ∧ its writer T38 = `.` ⇒ a join there answers `null` while LOOKING like a feature. **CORRECTED 2026-08-09 by T36** — table exists now ∧ the CALL stands: T38 still `.` ⇒ ⊥ row to join; the reason narrows from 2 missing pieces to 1. `action_requests.tool_run_id` is written inside the decision's own transaction (T37, V29) ⇒ the run row IS today's execution record (status, redacted summary, timing — V47); T36 joins BESIDE it when it lands. (b) **a 4th class in `core/approvals/`, ∧ the only one that writes NOTHING** — `results.py`: ⊥ `commit`, ⊥ status parameter, ⊥ statement that is ⊥ a `SELECT`. Same split that separated the first 3 writers, one door further: the one write this path needs (a stale PENDING made terminal before it is served) is T39's, whose reachable status is EXPIRED ∧ nothing else. (c) **requester-match in the `WHERE`** (`id = :id AND requested_by_user_id = :caller`) ⇒ a foreign row is ⊥ fetched ∧ a NULL requester (T34 `SET NULL`) matches nobody. **MEASURED non-difference, recorded ∵ V69**: moving that compare into a Python `!=` after the read leaves EVERY test green, live ones included — `None != caller` is True ⇒ even the orphan case still refuses. Statement kept for defence in depth (the row ⊥ exist in-process to be logged ∨ half-dropped by a later edit) ∧ said in the docstring, ⊥ implied by a test name. (d) **ORDER = requester-match, THEN expiry** — reverse of `expiry.py`'s own docstring, deliberately: `expire_if_due` takes an id ∧ ⊥ requester ⇒ expiry-first lets a prompt-injected id make NOA WRITE to a request its caller ⊥ see (V27, V76). Read-first ⇒ a foreign id = pure no-op. Cost = 1 poll of freshness if a decision commits in the window — inherent to ∀ unlocked read, ∧ V23 keeps the authority in the row. Closes T39(i)'s "⊥ a live route": this is the check-on-read's 1st live caller. (e) **id = `str`, parsed IN-TOOL, ⊥ a schema UUID** ⇒ malformed answers the SAME code ∧ SAME message as unknown ∧ foreign: ONE refusal for the whole family (V83d's shape), ⊥ a 2nd pydantic-shaped envelope for "there is ⊥ such request". `ERROR_ACTION_REQUEST_NOT_FOUND = ActionRequestNotFoundError.error_code` — imported, ⊥ retyped ⇒ this tool ∧ T37's endpoint answer 1 code for 1 fact; the MESSAGE is this surface's own (⊥ "to decide" — nothing is decided here). (f) **3 exclusions STRUCTURAL, ⊥ filtered** — `ActionResultView` has ⊥ field for `reason` (C8, V15, V43: LLM ⊥ author, ⊥ relay, ⊥ SEE), ⊥ for `evidence` (V17: born in-process, ⊥ in transcript), ⊥ for the requester block (V26) ⇒ the tool ⊥ emit what it never loads. ⊥ named in this line: `core/approvals/context.py` (V66 — `"arguments"` was spelled in the gate ∧ in `decisions`, T63 = the 3rd, ∧ a misspelt JSONB key reads as an ABSENT one ⇒ 1 constant, 1 extraction rule); `noa_api/mcp_tools/noa_read.py` (own module ∵ its subject = the approval gate, ⊥ a hosting system); `authenticated_caller` → `support/mcp_identity.py` (V66 — T33's gate tests ∧ this one both need a caller whose id they then assert a row against). **7 mutations proven red**: requester filter dropped from the WHERE; `reason` carried into the view ∧ payload; whole `approval_context` used as the arguments (evidence leaks); expiry check-on-read dropped; expiry run BEFORE the matched read; malformed id given its own code; tool registered CHANGE. An 8th ran GREEN ∧ is recorded as (c) — measured, ⊥ assumed. Doc sweep same change (V84a): `core/approvals/__init__.py` ("Six modules"), `core/approvals/expiry.py` (named T63 as a FUTURE caller ∧ put the check BEFORE the read), `noa_api/api/deps.py`, `noa_api/mcp_tools/__init__.py` (still listed T39 as still-to-come), `test_mcp_tool_rbac.py` ∧ `test_action_request_expiry.py` (both said T31/T63 unbuilt)

## T64

AS BUILT ≠ this line, 5 deviations: (a) "shared handler" = 4 handlers + 1 middleware, ⊥ one — `NoaError`-only left 404/405, 422 ∧ 500 in Starlette's default shape; ∀ registered in `install_error_handling(app)` (renamed from `install_error_handler`), the seam `main` ∧ both test harnesses already called (V66). (b) `noa_api/api/request_context.py` NEW, ⊥ named in this line — `RequestContextMiddleware` mints/adopts the id ∧ publishes it 3 ways ∵ 3 readers ⊥ reach each other: `scope["state"]` (raw-ASGI `McpAuthErrorMiddleware` + the fresh `Request` `ServerErrorMiddleware` builds), a `ContextVar`, ∧ structlog contextvars — without the 3rd the body id names nothing. (c) inbound `x-request-id` SANITIZED, ⊥ echoed verbatim (V73). (d) pydantic errors ⊥ forwarded to body ∨ log (V8). (e) `error_response()` sets the header itself ∵ the 500 answers from outside the middleware stack. Body key set now `{error_code, message, request_id}` ⇒ 2 existing V8 shape tests widened (`test_auth_routes.py`, `test_mcp_request_auth.py`), ⊥ new behavior. Verification: `apps/api/tests/test_error_envelope.py`, 1 group per writer + sanitizer + shape

## T67

AS BUILT ≠ this line, 4 deviations: (a) ⊥ a rename — `NOA_DB_SECRET_KEY` NEVER existed here: `git log -S` finds it in 2 places only, this row ∧ the test asserting its absence ⇒ name born correct at T5; `core/config.py` (field `noa_secret_encryption_key`, `_resolve_encryption_key` prod-required + dev-gen), `.env.example:14-17` (+ scope comment), `README.md:106` (+ scope sentence) ∀ already carried it. Recorded so "⊥ rename performed" reads as measurement, ⊥ as skipped work. (b) Dockerfiles ∧ `docker-compose.yml` named as write targets ∧ ⊥ touched ∵ ⊥ exist — T60 writes them, AFTER this row closes `x` ⇒ `NOA_DB_SECRET_KEY` there would meet nothing red (same "named ∧ ⊥ touched" shape as T70(a)). (c) ⇒ delivered work = BINDING, ⊥ renaming: `test_config.py::test_legacy_encryption_key_name_absent_from_every_tracked_file` scans ∀ git-tracked file (`git ls-files`, ⊥ FS walk ∵ it drops the dev's own `.env` ∧ `.venv` ∧ `node_modules` with ⊥ 2nd ignore list to drift) ⇒ T60's unwritten files inherit the rule unlisted; allowlist = `SPEC.md`,`DECISIONS.md`,`test_config.py` ∵ their subject IS the prohibition. + `::test_operator_docs_name_the_env_var_the_settings_field_defines` (parametrized `.env.example`,`README.md`) binds doc name → field ⇒ a code-only rename leaves README's scope sentence RED, ⊥ stale (V84a/c; pattern T70(d),T72(a)). BOTH red-first by mutation (legacy key appended to `.env.example`; field constant renamed), then reverted. (d) ⊥ named in this line: `REPO_ROOT` + the 3 name constants hoisted in `test_config.py`, existing `::test_encryption_key_name_reflects_scope` ∧ `::test_env_example_documents_every_required_var` re-read them ⇒ 1 place to edit on rename. FLAGGED, ⊥ fixed (T60's): `README.md:74` ∧ `AGENTS.md` instruct `docker compose up -d postgres` ∧ ⊥ compose file in repo

## T68

AS BUILT ≠ this line, 5 deviations: (a) the guard moved OFF the tools onto the MECHANISM — `core/integrations/whm/firewall_gate.py` NEW (⊥ named in this line): `require_usable_backends` raises, `run_on_usable_backends` = the only place a backend set becomes concurrent work ⇒ it ⊥ be entered with nothing to do. T24's version was a per-tool `_availability_refusal`, ∧ a per-tool check = a check the NEXT tool forgets, which is precisely this row's subject (T24: "⊥ closes T68 — that row's subject = a silent no-op on an approved CHANGE"). ⇒ T25/T26 inherit by construction, ⊥ by discipline. (b) V57's literal "each tool checks `available[…]`" now reads THROUGH one door — the property (zero ⇒ error, ⊥ empty-gather success) unchanged ∧ strengthened; ⊥ a §V re-word here (that = `/ck:spec`'s call). `AGENTS.md`'s line stays true as written. (c) proof the guard is ⊥ per-tool any more = the 2 zero-backend cases in `test_whm_tools_firewall_preflight.py` pass with NOTHING about availability written in that tool ∧ go red when the gate call is dropped from the fan-out — i.e. the tool tests now certify the shared door, ⊥ a copy. (d) recurrence bound by AST, ⊥ by prose — `test_whm_firewall_gate.py::test_the_firewall_fan_out_lives_in_one_place` pins the firewall path to exactly 2 `asyncio.gather` sites (`availability.check_firewall_binaries` = computes availability ⇒ ⊥ gated by it; `firewall_gate.run_on_usable_backends` = the door) ⇒ a hand-rolled fan-out in T25/T26 is RED, ⊥ silent (V84c, pattern `test_support_layout.py`/`test_pins.py`). ⊥ new exception class: `WHMFirewallCLIError` already covers "⊥ be run, ∨ ⊥ be trusted to have run", already lists `ssh_sudo_required`, already mapped once to 502 (V73); class-per-code = the rewrite C13/V69 forbid. `BACKEND_CSF`/`BACKEND_IMUNIFY` moved to `availability.py` (the module whose `as_tools_dict` spells them) ⇒ graph one-directional, ⊥ an alias hub in the tool (T72). 4 mutations proven red: gate call dropped (gate tests **∧** both tool tests), `sudo_required` ignored, unusable backend fanned out, hand-rolled gather in the tool. (e) **⊥ exercised: a live CHANGE tool.** T25/T26 = `.`, blocked behind the approval gate (T32–T38, ∀ `.`) ⇒ the door is proven on the READ path ∧ by unit tests, ⊥ on an approved CHANGE. What T25/T26 still owe = routing their mutation fan-out through this door (the AST guard makes ⊥ doing so red) ∧ V86's 2-part receipt. Doc sweep same change (V84a): `docs/integrations/whm.md:133-135,315`, `core/integrations/whm/availability.py` docstring, `whm/__init__.py` module list, `test_whm_firewall_availability.py` (module docstring ∧ `:222` — both named the TOOL as the enforcement point)

## T69

Built INSIDE T27 ∧ ⊥ as a separate pass, ∵ the guard has ⊥ caller without the reset ∧ a control with ⊥ caller is a control ⊥ test exercises (T15's own reason for parking `redaction.py` until T73, V69). Full detail in `docs/AS-BUILT.md` §T27(c)(d). What this row owes ∧ got: `_load_crypt_lib()` ported VERBATIM from `MCP:proxmox/tools/_cloudinit_passwords.py` (ctypes, ⊥ the stdlib `crypt` module — deprecated ∧ REMOVED in 3.13, ∧ C1 already pins `<3.13` for pgpy; binding to a 2nd removal = 2 reasons to be stuck on one interpreter). `None` ⇒ `CryptVerdict.UNAVAILABLE` + cause `crypt_library_unavailable`, ⊥ `False`, ⊥ a silent pass, **∧ ⊥ a `MISMATCH`** — the half the row's words ⊥ say ∧ the one that matters: `noa-old` reported a change that SUCCEEDED as one to repeat. Test with libcrypt unavailable = `test_proxmox_cloudinit_crypt.py::test_a_host_without_libcrypt_reports_unavailable` (dump carries the RIGHT password ⇒ a fallback compare answers `MATCH` ∧ goes red) + `::test_a_host_without_libcrypt_does_not_report_a_mismatch` (the verdict, ⊥ only `verified`) + at the runner `::test_a_host_without_libcrypt_reports_changed_but_unverified`. NEGATIVE CONTROL ships ∧ is the assertion this file would be worthless without (V87): `::test_verification_still_separates_a_wrong_password` — every OTHER test here passes against a function answering `UNAVAILABLE` unconditionally. The lock around `crypt(3)` kept ∧ its reason STATED: POSIX `crypt` returns a pointer into a STATIC buffer ∧ `CDLL` releases the GIL ⇒ 2 concurrent approved changes (T38) can read each other's result.

## T70

AS BUILT ≠ this line, 5 deviations: (a) `pyproject.toml` UNTOUCHED — named as a write target, zero edits landed; T71(b) already corrected the digit there ∧ ⊥ else about the pin belongs in a dependency file. Recorded so "named ∧ ⊥ touched" reads as decision, ⊥ omission. (b) README touched ANYWAY — the parenthetical's "README already rewritten" was half true: T71 fixed the DIGITS, ⊥ the POINTER, which still read "Full rationale: C23, T70" ⇒ sent the reader to a §T row instead of a document, ∧ that document ⊥ exist yet. Rewritten to point at `ARCHITECTURE.md` ∧ carry the silent-1.x-bump trigger where the reader lands first. (c) `docs/README.md` touched, ⊥ named in this line — it forward-referenced `ARCHITECTURE.md` (§T.62); file now exists at T70 carrying pins only ⇒ pointer SPLIT (pins §T.70, rest §T.62), else the docs index credits a task that ⊥ create it. (d) doc-binding TESTS ⊥ named in this line — `test_pins.py::test_pin_docs_track_the_servable_set` (parametrized over `ARCHITECTURE.md` ∧ `README.md`, BOTH directions: era added to `SUPPORTED_PROTOCOL_VERSIONS` ⇒ docs incomplete, era removed ⇒ docs wrong) + `::test_architecture_doc_records_the_pin_and_its_re_open_trigger`. ∵ "pin reads as decision" ⊥ hold on prose alone — T71 found the false digit standing in 4 files precisely ∵ nothing compared prose to source (V84c). Convention the test rests on, in those 2 files ONLY: backticked date = protocol era, bare date = verification date. (e) `ARCHITECTURE.md` CREATED here, scope = MCP pins section ONLY ⇒ T62 still owes topology + module boundaries + the rest, INTO this file

## T71

AS BUILT: premise **CORRECTED, ⊥ confirmed** — `LATEST_PROTOCOL_VERSION = '2025-11-25'` @ tag `v1.29.0` ⇒ every "era = `2025-06-18`" sentence shipped was false (R9). 3 deviations from "read-only": (a) the read spilled past the 2 named constants — a digit ⊥ actionable without knowing WHO picks it ⇒ `Client.connect` + LibreChat's lockfile + `connection.ts` read too, landing R26; live era = `2025-11-25`. (b) prose in 4 files asserted the false digit as fact (`README.md:44,47`, `noa_api/mcp_server.py:3`, `apps/api/pyproject.toml:12`, `tests/test_pins.py:29`) ⇒ corrected here, ⊥ deferred to T70 — this line's own bar ("⊥ ship C23 rationale text as fact until done") makes leaving them a knowing false claim. (c) a source-read alone ⊥ hold: old tests "proved" `2025-06-18` only ∵ the harness ASKED for it — a self-fulfilling assertion, same shape as B2's dead control (V69) ⇒ `tests/support/mcp_mount.py` now speaks as LibreChat does (`CLIENT_LATEST_PROTOCOL_VERSION`), + `test_the_mount_answers_the_era_the_client_asks_for` (3 cases: client-latest, older client, unsupported→fallback) ∧ `test_pins.py::test_server_serves_every_era_a_v1_client_can_ask_for` (set covers both, `2026-07-28` still out)

## T72

AS BUILT ≠ this line, 3 deviations: (a) guard test `tests/test_support_layout.py` CREATED, ⊥ named in this line ∵ "mechanical" describes the EDIT, ⊥ the property — ∧ the property survives only until the next port re-adds a shim, which is invisible at the call site (`from support.whm import command_result` reads WHM-owned). Machine-readable ⇒ bind by test, ⊥ discipline (V84c, the pattern `test_pins.py` set). 2 asserts: `support.whm.__all__ == ["FakeWHMServer"]` ∧ an AST scan of ∀ test module for `support.whm` imports naming anything else ⇒ a new test file inherits the rule unlisted. BOTH red pre-change. (b) `support/whm.py` KEEPS its `PINNED_FINGERPRINT` ∧ `SSH_PASSWORD` imports — ⊥ surviving shims: `FakeWHMServer` takes both as field defaults ⇒ they stay module ATTRIBUTES ⇒ an attribute-reachability assert ⊥ available; guard binds `__all__` + import sites instead. (c) 3 docstrings ⊥ named in this line asserted the re-export as fact ⇒ fixed same change per V84a (restatement ⊥ outlive what it restates — code comments included, ⊥ only `README`/`AGENTS`/`docs`): `test_whm_firewall_cli.py:17` named `support.whm` as the patched `ssh_exec` namespace (wrong module even before this row — T18 moved it), `support/remote_exec.py:3-7` ∧ `support/secrets.py:3-5` both said `support/whm.py` re-exports them

## T73

AS BUILT ≠ this line, 7 deviations: (a) `risk` ⊥ a constant — registrars now return `Mapping[str, ToolRisk]` (`register_whm_read_tools`, `register_mcp_tools`), RBAC middleware takes the KEYS, audit middleware takes the MAP ∵ a hardcoded `READ` makes T22's first CHANGE tool record itself as a READ **∧** write a row for a change that ⊥ happened — silently, in the audit trail. Risk declared where the tool is defined ⇒ a registrar ⊥ omit it (`test_every_registered_tool_declares_a_risk`). CHANGE = skipped here: its `tools/call` opens the gate (T33) ∧ executes nothing; V46's row = T38's. (b) **middleware ORDER load-bearing ∧ measured** — `FastMCP._run_middleware` composes over `reversed(self.middleware)` (`server.py:513`) ⇒ first added = OUTERMOST. RBAC first, audit second ⇒ audit wraps EXECUTION, ⊥ refusals. First red-first attempt used an UNREGISTERED name ∧ **survived the order swap** ∵ such a name ∉ risk map either way; the case that tells = a REGISTERED tool with grant revoked (`test_a_call_refused_by_rbac_writes_no_row`). `mcp_server.py` prose corrected to that, ⊥ the wrong claim the weak test licensed. (c) **fail-closed on the opening write** (owner-decided): STARTED row ⊥ commit ⇒ tool ⊥ run, caller gets `tool_audit_unavailable` in the tool envelope + `is_error` ∵ V45 says ∀ READ writes a row — fail-open leaves the invariant asserted by prose ∧ held by nothing = B2's shape (V69). Terminal write ⊥ symmetric: tool already ran ⇒ log `mcp_tool_audit_finish_failed`, leave row STARTED (T38 reaper's state), ⊥ misreport a call that happened. (d) `conversation_ref` source RESEARCHED, ⊥ assumed — LibreChat @ pin `45cc53c4` sends `params:{name,arguments}`, **⊥ `_meta`** (`packages/api/src/mcp/MCPManager.ts:512-518`) ⇒ ⊥ conversation id in the call at all. Its header templating DOES resolve `{{LIBRECHAT_BODY_CONVERSATIONID}}` (`packages/api/src/utils/env.ts:130`, `ALLOWED_BODY_FIELDS=['conversationId','parentMessageId','messageId']`) ⇒ NOA reads header `X-Noa-Conversation-Ref`; **T57 owes the YAML side** (named ∧ ⊥ touched, T67(b)/T70(a) shape). Absent ∨ unusable → NULL ∧ call proceeds (label, ⊥ scope — old V165 dissolved). Sanitized ∵ it lands in Postgres AND structlog = V73's log-forging surface ⇒ `sanitize_header_label(value,*,max_length)` extracted in `request_context.py`, `sanitize_request_id` delegates (V66). (e) 2 statements, 2 TRANSACTIONS — `SQLToolRunRepository` owns its session ∧ commits (like `SQLMcpIdentityRepository`, ⊥ like the flush-only `core/` repositories) ∵ one transaction spanning the call means the row appears only once the call ENDS ⇒ a process dying mid-call leaves nothing ∧ STARTED has no purpose. Asserted as commit ORDER (`["STARTED","COMPLETED"]`), ⊥ as a count. (f) `core/secrets/redaction.py` ported (T35 deferred it here) with ONE departure from `noa-old`: that repo **encrypted** sensitive audit args (`_encrypt_sensitive_args`) so admin could decrypt back; §V45/V47 say **redacted** ⇒ one-way here, ∵ a recoverable audit table turns one Fernet key into every credential ever passed to a tool. Applied to `result_summary` too, ⊥ only `args`. (g) ⊥ named in this line: `tests/support/tool_runs.py` double wired into `build_tool_context` for **∀** mount test, ⊥ only the audit ones — the middleware refuses what it ⊥ record ⇒ the T19 RBAC tests would fail for an unrelated reason, ∧ a fixture that disabled the audit path would let the suite pass with V45 unheld. 11 mutations proven red (missing middleware, order swap, fail-open, always-COMPLETED, unredacted args, unredacted summary, verbatim conv-ref, no truncation, single commit, risk=CHANGE, CHANGE audited, finish raises). Doc sweep same change (V84a): `mcp_tools/__init__.py`, `core/db/models.py`, `core/auth/tool_catalog.py`, `test_tool_runs_schema.py`, `docs/integrations/whm.md:252` all asserted "⊥ built"/"table ⊥ exist" — the whm.md line was false twice over (T35 built the table) ⇒ ⊥ citation-grep, CLAIM-grep found it

---

# §R ARCHIVE

Findings consumed by completed tasks ∨ superseded. Same `id|topic|finding|source` columns.

R1|fastmcp auth base|`class TokenVerifier(AuthProvider)` in `fastmcp.server.auth`; abstract `async def verify_token(self, token: str) -> AccessToken \| None`, `None` = reject. Ctor `(base_url=None, required_scopes=None, resource_base_url=None)` — `resource_server_url` ⊥ exist|`fastmcp/server/auth/auth.py:366,408,373-378` (fastmcp==3.4.5)

R2|AccessToken shape|Return fastmcp `AccessToken`, ⊥ SDK's. Required `token: str`, `client_id: str`, `scopes: list[str]`; optional `expires_at: int \| None`, `resource`, `subject`, `claims: dict` (fastmcp default `{}`). SDK middleware rejects when `expires_at < int(time.time())` ⇒ expiry enforced upstream|`fastmcp/server/auth/auth.py:54-57`; `mcp/server/auth/provider.py:39-46`; `mcp/server/auth/middleware/bearer_auth.py:70-71`

R3|verifier wiring|`FastMCP(..., auth=<TokenVerifier instance>)` — keyword-only, instance passed direct, ⊥ wrapper (TokenVerifier ⊂ AuthProvider). `RemoteAuthProvider` only if RFC 9728 metadata routes wanted|`fastmcp/server/server.py:329,424`

R4|headers inside `verify_token`|`verify_token` receives token string only, BUT `RequestContextMiddleware` inserted outermost (`middleware.insert(0, …)`) ahead of `AuthenticationMiddleware` ⇒ `get_http_request()`/`get_http_headers()` DO work inside `verify_token`. LIVE-VERIFIED 2026-08-06: `X-Noa-LibreChat-User` read there, 200 ⇒ TOFU binding may live in verifier|`fastmcp/server/http.py:404,361-383`; `mcp/server/auth/middleware/bearer_auth.py:54-65`

R5|header helper caveat|`get_http_headers()` default excludes `authorization` ∧ `mcp-session-id` — need `include={"authorization"}`. Custom `x-noa-librechat-user` returned, key lowercased. Identity in tools: `get_access_token()`, DI `CurrentAccessToken()`, `TokenClaim(name)` from `fastmcp.server.dependencies`|`fastmcp/server/dependencies.py:430-448,467,1299,1356`

R6|ASGI mount|`http_app(path=None, middleware=None, json_response=None, stateless_http=None, transport="http", event_store=None, retry_interval=None, host_origin_protection=None, allowed_hosts=None, allowed_origins=None) -> StarletteWithLifespan`. `combine_lifespans(*lifespans)` EXISTS at `fastmcp.utilities.lifespan` (⊥ in `fastmcp.__all__`); documented pattern `FastAPI(lifespan=combine_lifespans(app_lifespan, mcp_app.lifespan))` + `app.mount("/mcp", mcp_app)`|`fastmcp/server/mixins/transport.py:370-382`; `fastmcp/utilities/lifespan.py:12-14,26-37`

R7|v3 removed ctor kwargs|`FastMCP(stateless_http=…)`, `json_response`, `streamable_http_path`, `sse_path`, `host`, `port`, `debug`, `log_level`, `tool_serializer`, `include_tags`/`exclude_tags` raise `TypeError` — pass to `http_app()`/`run_http_async()` or `FASTMCP_*` env. `JWTVerifier`/`StaticTokenVerifier` behind lazy `__getattr__` ⇒ import from `fastmcp.server.auth.providers.jwt` for static analysis|`fastmcp/server/server.py:148-174`; `fastmcp/server/auth/__init__.py:34-55`

R17|T59 evidence verdict, source-read stage (SUPERSEDED by R29)|PASS-candidate at pin `45cc53c4` + lockfile `@mcp-ui/client@5.7.0`: `text/uri-list` ⇒ iframe `src` at NOA origin, `allow-same-origin` present, zero proxy ⇒ first-party cookie rides. ⊥ closed T59 by itself — the owed live run ran 2026-08-08 ∧ PASSED ⇒ **SUPERSEDED by R29**. Caveats = R13 (⊥ `allow-forms`) + R15 (draft kills uri-list)|mcp-ui `v5.7.0/.../HTMLResourceRenderer.tsx#L147-153`

---

# §B BUG DETAIL

Full cause/fix narrative for `SPEC.md` §B rows. Same `id|date|cause|fix` columns.

B2|2026-08-07|**FIXED 2026-08-07** (`known_hosts=([], [], [])`, V82). Host-key pin WAS INERT — `ssh_exec` accepts any host key. `core/remote_exec/ssh.py:99` passes `known_hosts=None` ⇒ asyncssh sets `_trusted_host_keys = None` (`asyncssh/connection.py:3509-3511`) ∧ the whole comparison, incl. `self._owner.validate_host_public_key(...)`, sits inside `if self._trusted_host_keys is not None:` (`connection.py:1359-1367`) ⇒ falls through to `return key`. asyncssh's own doc (`connection.py:7576-7577`): "If this is explicitly set to `None`, server host key validation will be disabled." ⇒ `_PinnedHostKeySSHClient.validate_host_public_key` (`ssh.py:55`) never called, `ssh_host_key_mismatch` (`ssh.py:213-220`) unreachable from a real handshake, `require_host_key_fingerprint=True` = DB-column-non-empty check ∧ nothing more. Proven live vs real `asyncssh.create_server` with a wrong pin → connected, exit 0, command ran. **⊥ port drift** — `noa-old` `MCP` has the identical `known_hosts=None` + callback pairing ⇒ **§V69's premise FALSE**: "host-key pinning, TOFU refresh already hardened there" names a control that never worked upstream; C13/V69 copy-⊥-rewrite was obeyed correctly ∧ inherited a dead control. Port made the claim worse: `ssh.py:9-11` docstring (⊥ in source) states the inverse of the truth. Suite CERTIFIES the dead control: `apps/api/tests/test_remote_exec_ssh.py:125-131` has the double call the callback itself + synthesize `HostKeyNotVerifiable`, docstring `:3-5` claims "the way the real handshake does", `:268` asserts `known_hosts is None` AS a safety property. Blast radius = ∀ SSH-carried path: WHM `csf_cli.py:94`, `imunify_cli.py:141`, `availability.py:101,105`; PMG `pmgsh` writes to `/config/mynetworks`. Decrypted SSH password/key presented to whatever answers `host:port` (KEX+auth complete before any check) ⇒ MITM/DNS-hijack harvests the credential; spoofed host returns forged `csf -g` text → `parse_csf_grep_output` renders a clean verdict → T25 receipt before-state. Proxmox unaffected (HTTP-only, I.ext). False docs now: `docs/integrations/whm.md:189`. FIX OWED, ⊥ applied: `ssh_get_host_fingerprint` ! also → base64 pubkey, T54 stores beside fingerprint, pass `known_hosts=([key], [], [])` ⇒ reject in-handshake ∧ pre-auth, dead callback class deletable. Post-connect fingerprint compare = stopgap ONLY ∵ credential already sent. **AS FIXED ≠ that plan** — no pubkey column, no T54 dependency, one kwarg: `known_hosts=([], [], [])`. Empty trusted set ⇒ `_trusted_host_keys` non-`None` ⇒ asyncssh keeps the block live ∧ falls through to `validate_host_public_key` in KEX, so the existing fingerprint-only storage suffices ∧ `_PinnedHostKeySSHClient` goes from dead code to the live check. Same kwarg on the TOFU path ⇒ `ssh_get_host_fingerprint` now reports the key the handshake validated (`get_server_host_key()` fallback retained). Closes the TOFU-refresh gap too. Verified live vs `asyncssh.create_server`: wrong pin → `ssh_host_key_mismatch` ∧ server recorded ZERO auth attempts (credential ⊥ sent); right pin → command ran. Fake-handshake pin tests REPLACED by loopback-server tests; `known_hosts is None` assertion inverted to `== ([], [], [])`. `docs/integrations/whm.md:189` true as of the fix. §V69 re-worded|V82,V69

B3|2026-08-07|B2's fix re-worded §V69 ∧ ⊥ swept its restatements ⇒ `README.md:113-114` ("SSH banner stripping, `sudo -n` escalation, host-key pinning, and TOFU refresh are already hardened in them") ∧ `AGENTS.md:96-97` (same sentence, caveman) kept teaching the premise B2 disproved. `AGENTS.md` = the file ∀ agent loads as project instruction ⇒ retracted premise shipped as a standing order to trust an inert control ∧ to copy it forward on the next port. Neither file cited V69 ⇒ a citation grep found nothing; only a CLAIM grep ("hardened") found them. Separately `docs/integrations/whm.md:21` ∧ `docs/integrations/pmg.md:24` cited §V.69 for "host-key pinning with TOFU refresh": that claim is TRUE of NOA post-7cbc11c, but V69 ⊥ carry it any more — V82 does ⇒ citation pointed at the line that retracts the claim it supports. Docs-only, ⊥ code defect. FIX: README ∧ AGENTS now state pinning ∧ TOFU were ⊥ hardened upstream + name B2/V82; integration docs re-cite §V.82; V69 widened to "§V ∨ any doc"|V84,V69,V82

B4|2026-08-07|`test_auth_routes.py::test_logout_is_idempotent` ∧ `test_jwt_service.py::test_clear_session_cookie_is_idempotent_and_needs_no_auth` compared 2 `Set-Cookie` headers VERBATIM. Starlette `delete_cookie` → `set_cookie(..., max_age=0, expires=0)`, ∧ `http.cookies.Morsel.OutputString` renders an int `expires` via `_getdate(0)` = **now** as HTTP-date ⇒ 2 clears straddling a second boundary emit `expires=Fri, 07 Aug 2026 16:11:11 GMT` vs `...16:11:12 GMT`, byte-identical elsewhere ⇒ red ~1-in-N, green on rerun ∧ on 3 repeats. Test defect, ⊥ code defect — idempotence HOLDS: what differed is a stamp a browser reads as "already expired" either way, ∧ `Max-Age=0` (the attribute that does the clearing) never moved. Load ⊥ the cause; a 1s gap between the 2 asserts is the whole mechanism. Reproduced deterministically: 2 `clear_session_cookie` calls + `sleep(1.05)` ⇒ raw compare False, `cookie_shape` compare True. Pre-existing since T8. FIX: `tests/support/cookies.py::cookie_shape(header)` → `(name, value, attributes − expires)`, both tests compare through it (⊥ 2 copies of the drop rule — the 2 files already own separate `set_cookie_header` ∵ httpx `get_list` vs Starlette `getlist`, so the SHARED part is the clock rule, V66) + `test_jwt_service.py::test_cookie_shape_still_separates_cookies_that_differ` (a clear aimed at another `Domain` ! stay distinguishable) so the drop ⊥ degrade into a tautology (V87)|V87,V66,V67,V6,T8

B5|2026-08-08|`test_action_request_decisions_live.py::test_concurrent_approves_produce_one_decision_and_one_run` (T37, V28) asserted "one winner, one `tool_runs` row" over an `asyncio.gather` of 2 `service.approve` calls ∧ **passed with `.with_for_update()` DELETED** — measured twice: the plain gather, ∧ a 2nd version that added an event handshake but released the first transaction BEFORE the second's SELECT was issued. Cause: gather ⊥ a race — the first coroutine finishes INSERT+UPDATE+COMMIT before the second's read reaches the server ⇒ the second reads APPROVED ∧ 409s for the RIGHT reason under the WRONG mechanism. ⇒ V28's row lock was asserted by prose ∧ held by nothing — B2's shape ∧ V69's, one layer up. Test defect, ⊥ code defect: `FOR UPDATE` was present ∧ correct throughout. ⊥ caught by reading ∧ ⊥ by the suite — caught by a MUTATION battery run at build time, which is the only reason it is in this table ∧ ⊥ in production. FIX: `ObservedDecisionRepository` holds the first transaction open between its locked read ∧ its commit; the second ANNOUNCES before its own read; the assertion moved to ORDER (`second:read` after `first:committed`) with the win count kept as a second check; + `test_an_unlocked_read_of_the_same_row_does_not_wait` proving the forbidden order is REACHABLE, else the new assertion is itself a tautology. 20/20 mutations red after|V89,V28,V87

B6|2026-08-09|`core/audit/tool_runs.py::finish_run` (T38, `a1fb7a9`) keyed its UPDATE on `ToolRun.id` ALONE ⇒ the 2 writers that can reach ONE finished run — the executor finishing a change, ∧ the reaper calling it abandoned past `APPROVAL_STRANDED_RUN_REAP_AFTER_SECONDS` (900) — ⊥ raced for it, both simply TOOK it. Interleave A: change runs > 900s → reaper writes FAILED + an abandonment receipt → executor finishes → run flips COMPLETED beside a receipt saying the outcome was never observed (`create_if_missing`'s `ON CONFLICT DO NOTHING` keeps the STALE receipt) ⇒ status ∧ receipt contradict, ∧ V46's "one commit" ⊥ help ∵ the 2 commits are minutes apart. Interleave B (worse): reaper `SELECT`s the run STARTED, executor commits COMPLETED + the real receipt, reaper's UPDATE lands ON TOP ⇒ a change that APPLIED on a remote host is reported abandoned to the operator who approved it. Same window frees a V31 in-flight slot while the execution task is still live in-process ⇒ the cap admits a 2nd change. Code defect, ⊥ test defect: the suite exercised each writer ALONE, ∧ V46's receipt idempotency (T36's UNIQUE) read as if it covered the STATUS too — the receipt was guarded, the status was ⊥. V69's shape again, one field over: a control asserted for one column ∧ assumed for its neighbour. Found by review of `a1fb7a9`, ⊥ by production. FIX (`19be7a2`): `ToolRun.status == ToolRunStatus.STARTED` in the UPDATE's WHERE for ∀ 4 callers ⇒ first terminal write wins; the predicate is re-evaluated by the UPDATE vs the newest COMMITTED row version, so the reaper's SELECT→UPDATE gap closes with the plain interleaving. + `ReapOutcome.touched_anything` deleted (0 callers). 2 reds: a plain 2nd terminal write, ∧ a live test firing the competing commit from INSIDE the pass between `stranded_runs()` ∧ the write, asserting the run keeps COMPLETED ∧ the executor's receipt (V89 — `asyncio.gather` of the 2 passes with the predicate deleted). OPEN, ⊥ fixed: `stranded_runs` carries ⊥ `LIMIT`|V91,V46,V31,V89,T38

B7|2026-08-09|`core/approvals/execution.py` (T38(g), `a1fb7a9`) refused a change whose authorized arguments came back redacted, but scanned DEPTH 1 — `for key in authorized.arguments` — while `redact_sensitive_data` RECURSES into maps ∧ sequences ⇒ `{"server": {"ssh_password": …}}` was stored redacted at gate time, PASSED the guard, ∧ the runner would have performed the change with the literal `[redacted]` where a credential belonged: the exact outcome the door exists to refuse. Guard ∧ redactor agreed about KEYS ∧ disagreed about SHAPE ⇒ a fail-closed door that reads only the top level is ⊥ fail-closed. Vacuous in production today (C15/V49 ⇒ ∀ secret generated server-side, ⊥ passed as an argument; ∀ CHANGE tool T22–T29 still `.`) — the defect is that a door T68's rule says to ship EARLY shipped HALF-BUILT, ∧ a vacuous guard is exactly the one nothing exercises. Found by review of `a1fb7a9`. FIX (`19be7a2`): `core/secrets/redaction.py::sensitive_key_paths` — the redactor's OWN walk answering WHERE instead of rewriting (V66) ⇒ 1 traversal rule, 2 readers, ⊥ 2 spellings that can drift; dotted paths + `[i]` name the location without carrying the value. Refusal still by KEY NAME ⇒ T33(d)'s legitimate-`[redacted]`-value case still ⊥ a false refusal. 2 reds: a nested redacted argument at the executor, ∧ the locator's own path set + a negative control so it ⊥ degrade into a tautology (V87)|V8,V66,V49,T38,T33

B8|2026-08-09|`core/approvals/execution.py::build_receipt` (T38(h), `a1fb7a9`) stored the runner's answer RAW (`RECEIPT_AFTER_KEY: dict(payload)`). The SAME payload reaches `tool_runs.result_summary` through `core/audit/summaries.py`, which redacts ⇒ a runner answering `{ok: true, password: …}` kept the credential OUT of the summary ∧ PUT it in `action_receipts.receipt_data` — the row that OUTLIVES the call, in front of T42's card, T63's read ∧ the admin audit surface (V8, V45). A per-writer exemption from one redaction rule is precisely what `summaries.py`'s own docstring warns about, ∧ the receipt is the LONGER-lived of the 2 readers. Vacuous today ∵ ⊥ CHANGE runner exists (T22–T29 `.`) ∧ C15/V49 keep generated secrets on the yopass path — the defect is a writer built BEFORE its 1st caller ∧ built without the rule its sibling already had. Found by review of `a1fb7a9`. FIX (`19be7a2`): `after` goes through `redact_sensitive_data` on the way in. `before` deliberately UNCHANGED — a copy of gate-time `approval_context` evidence, whose redaction is T33's; re-deciding it in this writer = a 2nd answer to one question ∧ a 2nd place to drift. Red: the existing `test_a_credential_in_a_result_is_redacted_before_it_is_stored` asserted the SUMMARY only while its docstring already CLAIMED the receipt — strengthened to assert what it said (V87's shape, a compare that ⊥ separate)|V8,V45,V46,T38,T42,T63

B9|2026-08-09|`test_tool_result_tables_schema.py::test_an_insert_that_omits_a_payload_column_fails` (T56, `0f120a7`) asserted "⊥ server default on `column_labels`/`rows`" through an ORM instance with the attribute set to `None`. SQLAlchemy's JSON/JSONB types serialise Python `None` to **JSON `null`** (`none_as_null` defaults False) ⇒ the INSERT carries a VALUE, that value SATISFIES `NOT NULL`, ∧ the statement OMITS nothing — so the test said nothing about a default ∧ was green against a column carrying `server_default=text("'{}'::jsonb")`, the exact state it exists to refuse. Test defect, ⊥ code defect: both columns have ⊥ default ∧ never did. B4/B5's family — a compare that ⊥ SEPARATE (V87) — one type-coercion layer down: the sentinel the test meant to send was rewritten by the TYPE before Postgres ever saw it, so ⊥ amount of reading the model ∨ the migration finds it. ⊥ caught by reading; caught by asking what SQL the statement actually emitted. FIX: `sa.insert(ToolResultTable).values(**values)` with the column `del`eted from the dict ⇒ absent from the SQL, which is the only form of the question Postgres answers; the ORM-instance path kept for the round-trip case, where a value IS the subject. V87 extended so the next absence test ⊥ inherit the rule from this row alone|V87,V93,T56

---

# §V FULL TEXT

Pre-trim text of the `SPEC.md` §V rows whose rationale was moved here 2026-08-15.
`SPEC.md` keeps every normative clause, instance list ∧ binding-test path; this file keeps
the reasoning, the measurement narrative ∧ the decision history behind each.

## V6

V6. Login sets httpOnly `noa_session` cookie (SameSite=Lax, Domain=`.noa.internal`, Path=/). Logout clears with max-age=0. Logout idempotent without auth. Session JWT ⊥ revocable pre-`exp` — ⊥ `jti`, ⊥ denylist ⇒ logout kills the *cookie*, ⊥ the token: a held copy verifies until `exp`. V4 cascade-revoke-on-disable = `mcp_tokens` only (T11), ⊥ session equivalent exists. ⇒ T8/T9 ! re-read `users.is_active` ∀ authenticated request — that re-read is the ONLY thing bounding a disabled admin's session, ⊥ an optimization. Unbounded window otherwise = `AUTH_JWT_ACCESS_TOKEN_TTL_SECONDS` (default 3600). Session-kill-on-disable (denylist \| `jti` \| shorter TTL) = OPEN owner decision, ⊥ agent call. Research feeding that decision: R18 (server-side invalidation on logout = mandatory per OWASP + ASVS V3.3.1 ⇒ cookie-clear-only fails it), R19 (⊥ normative mandate to kill *live* sessions on account disable; per-request `is_active` re-read = common practice, ⊥ substitute for R18), R20 (numbers: NIST AAL2 reauth SHOULD ≤ 24h ∧ inactivity SHOULD ≤ 1h; OWASP absolute 4–8h ⇒ 3600s ∈ range), R21 (denylist = CONFLICTING OWASP guidance, key `(jti, iss)`, ⊥ raw-JWT key), R22 (short TTL + refresh rotation = only mechanism with MUST-level text). ⇒ logout-invalidation gap is a spec-acknowledged deviation, ⊥ an oversight.

## V24

V24. `build_change_gate_response()` shapes every CHANGE tool result; 3 branches: link-out text \| UI resource \| elicitation (future). Active branch = **UI resource** (path A), selected by T59's live run 2026-08-08 (R29), ⊥ assumed. 2 branches ship TOGETHER, ⊥ either/or: the iframe is the surface ∧ the plain URL rides in the text block ∀ result (V25) — the frame can fail to load, ∧ then the URL is the only door. Elicitation stays future. Upgrade path = swap branch, backend unchanged. **BUILT 2026-08-09 (T32)** ∧ the 3rd branch REFUSES, ⊥ falls through: `ChangeGateBranchUnavailableError`, a `ChangeGateError` ⇒ the code survives V19's boundary, mapped 500 EXPLICITLY (⊥ the 503 fallback) ∵ the branch is a module constant (`ACTIVE_CHANGE_GATE_BRANCH`), ⊥ a tool arg ⇒ ⊥ caller can select it — ∧ the missing map was found by the existing subclass-tree test, ⊥ by a reviewer. ∧ ⊥ `structured_content`: R29 measured a content-only result, so an envelope beside it = a 3rd place the URL lives ∧ a shape nothing has rendered; CHANGE skips the audit middleware (V46) ⇒ ⊥ reader is owed one. "2 branches ship TOGETHER" asserted on the COUNT — 2 blocks, text FIRST — ⊥ on "a resource exists", which stays green when the text block is the thing that got dropped.

## V32

V32. Pending requests expire on TTL → terminal `expired`; approve/deny on expired → 409 `action_request_expired`. **Check-on-read at the DECISION door LANDED 2026-08-08 (T37)**: `expires_at <= now` under the lock already held → WRITE EXPIRED + `decided_at` → commit → THEN raise. It writes ∵ refusing without the write leaves a row still reading PENDING ⇒ the next reader repeats the discovery. `reason` stays NULL — V15's CHECK exempts EXPIRED ∵ an expiry is the ABSENCE of an answer, ⊥ one. **Sweep LANDED 2026-08-08 (T39)**: `core/approvals/expiry.py`, set-based `UPDATE … WHERE status = PENDING AND expires_at <= now RETURNING id`, hosted by an in-process asyncio task the lifespan starts ∧ stops. Deadline compare = `<=` at BOTH doors (T39(a)) — a row exactly ON its deadline is one thing, ⊥ two. ⊥ `FOR UPDATE` there ∧ that ⊥ weaken V28: a bare `UPDATE` re-checks its WHERE vs the version the transaction it waited for committed ⇒ it skips a row an approval just won, ⊥ clobbers it (proven on V89's rig, T39(c)). The writer is a THIRD class that can write EXPIRED ∧ nothing else — a loop with ⊥ operator behind it ∧ a GET sit on the far side of V22 too. Check-on-read for the RENDER path landed as mechanism + request dependency; **1st live caller = T63's `noa_get_action_result` (2026-08-08)**, ∧ it runs the check AFTER its requester-matched read ∵ `expire_if_due` takes an id ∧ ⊥ requester ⇒ expiry-first lets a prompt-injected id make NOA WRITE to a row its caller ⊥ see (V27). A surface that resolves the row itself (T41's GET) ? call it first — **2nd live caller = T41's card (2026-08-09), ∧ it DECLINED that option**: the id in the card's URL reaches an operator through a tool result that persists in LibreChat's MongoDB (V26) ⇒ "the caller supplied it" ≠ "the caller may see it", so the same read-then-expire order holds at BOTH doors. ⇒ this clause is now a permission ⊥ exercised, ∧ the ordering lives in ONE place for both readers (`core/approvals/reads.py::apply_due_expiry`, V66) — a 3rd reader inherits it instead of choosing. Cost recorded: 1 poll of freshness (a decision committing between read ∧ UPDATE makes the UPDATE skip the row, which is T39(c)'s property, ∧ the reader answers the PENDING it read; V23 keeps authority in the row).

## V39

V39. CSRF token server-minted, signed, session-bound. Double-submit alone insufficient — any `*.noa.internal` sibling can plant cookie. **BUILT 2026-08-08 by T37, ⊥ by T46** — T46 was `.` ∧ a `.` row is ⊥ a guard; a live decision endpoint with ⊥ CSRF check IS this line's threat. `core/approvals/csrf.py`: HMAC-SHA256 over `(version, user_id, action_request_id, issued_at)`, wire = `v1.<issued_at>.<sig>`. **Bound to the REQUEST as well as the session** — more than this line asked ∧ free: a token minted for one card ⊥ authorise another, so a second open tab is ⊥ a spare key. Key = `HMAC(AUTH_JWT_SECRET, b"noa/approvals/csrf/v1")` — DERIVED, ⊥ a 2nd required prod secret ⇒ ⊥ 2nd key to rotate out of step, ∧ the label makes a session JWT unusable here (ASSERTED by test, ⊥ reasoned). TTL = `APPROVAL_PENDING_TTL_SECONDS`, ⊥ a number of its own ∵ the request's `expires_at` is checked under the lock anyway ⇒ a 2nd timeout = a 2nd thing to get wrong. `hmac.compare_digest`. Clock = V79's rule: zero leeway, future `issued_at` invalid. Age checked AFTER the signature ⇒ a forged stamp ⊥ buy a longer window. Refusal = 403 `csrf_token_invalid`, ONE code ∀ cause (absent, malformed, foreign operator, foreign card, stale) — naming which is telling a caller how close they got. ⊥ minting ROUTE here: T41's card is the surface. **SETTLED 2026-08-09 by T41 — ⊥ minting route EVER**: the token is minted inside the card's own `GET /action-requests/{id}` ∧ returned in that body, `csrf: null` once the request is terminal. 2 properties that buys, ∧ the 1st is the point: a token can ⊥ be obtained without passing V27's requester-match, ∵ the match IS that read — a separate mint route would be a 2nd door to a key, in front of the door the key opens. ∧ ⊥ live token on a card nobody may decide ⇒ the renderer reads `csrf === null` to draw ⊥ buttons at all (V38's family), so an operator ⊥ shown an action that could only ever 409. The token ⊥ rendered as text either — it rides in the `fetch` body (V26, asserted in the browser lane).

## V80

V80. In-iframe approve/deny ! POST via JS `fetch` — ⊥ native `<form>` submit, ⊥ `<form action>` fallback. ∵ sandbox at pin `45cc53c4` = `allow-scripts allow-same-origin` (+`allow-popups` on one render site) with **`allow-forms` ABSENT ∀ render paths** (R13) ⇒ a form submit dies silently in-frame. Cookie + CSRF discipline unchanged (V22,V39). Link-out path (V25) = top-level document ⇒ unaffected, forms fine there. **Re-verified LIVE 2026-08-08** (R29) at BOTH render sites: `allow-scripts allow-same-origin` (`ToolCallInfo`, the tool-call panel) ∧ `allow-popups allow-scripts allow-same-origin` (`MCPUIResource`, the `\ui{}` marker in assistant text) — `allow-forms` absent in BOTH, ∧ the in-frame `fetch` POST returned 200. Bound by test, ⊥ by discipline (V84c): `spikes/librechat-embed-render-gate/verify_librechat_pin.sh` FAILS if the shipped `@mcp-ui/client` bytes ever mention `allow-forms` ⇒ a bump that grants it turns this line red instead of leaving it stale. Re-check sandbox string ∀ T59 re-verify. **2nd binding landed 2026-08-09 (T41)** ∧ it bounds the OTHER side — the pin script watches what LibreChat SHIPS, `apps/web-embed/e2e/approvals.browser.e2e.ts` watches what NOA's card DOES under that string: the card is framed with `sandbox="allow-scripts allow-same-origin"` (the constant lives in `playwright.config.ts::MEASURED_SANDBOX`, pinned so a spec ⊥ quietly widen it), Approve is clicked in-frame, ∧ the stub's hit counter says the POST arrived. + a NEGATIVE CONTROL document in the SAME sandbox where a `fetch` lands ∧ a native `<form>` submit records ZERO hits — asserted on the COUNTER, ⊥ on an exception, ∵ the sandbox blocks a submit SILENTLY, which is the whole reason this line exists. Control proven to separate by widening the sandbox to grant `allow-forms` ∧ watching the form hit appear (V87). ∧ the card carries ⊥ `<form>` at all, both buttons `type="button"` — one rule stated twice, so a `<form>` introduced by a future layout above them still submits nothing. **3rd binding 2026-08-09 (T43, R32) ∧ it RETRACTS half of this line's own sentence**: "Link-out path (V25) = top-level document ⇒ unaffected, forms fine there" holds ONLY for a tab with ⊥ opener. MEASURED at both pinned sandbox strings: 0 tabs under `MEASURED_SANDBOX` (silently — asserted on the page count), a tab under `POPUP_SANDBOX` in which a `fetch` arrives ∧ a native `<form>` submit ⊥ does ∧ the tab ⊥ navigates. ⇒ this line's rule reaches PAST the frame, into what the frame OPENS: ∀ document NOA sends an operator to from in here ⊥ assume a form submits. The permissive run doubles as the negative control for the 0-tab claim (V87). Lane = `apps/web-embed/e2e/sign-in.browser.e2e.ts` (V94).

## V92

V92. A background pass that WRITES ! carry a LIMIT ∧ ! report what the LIMIT hid. (a) **BOUND**: a repair loop reading a population it then writes ! cap the read IN the statement, ⊥ slice after loading. Unbounded, 1 pass after a long outage loads ∀ row (T38's carried each request's `approval_context` evidence), issues N UPDATEs + N receipt INSERTs in ONE transaction, holds ∀ those row locks ∧ its own xmin till the last write lands (WAL growth, autovacuum blocked), ∧ a failure near the end rolls the whole pass back for the next to repeat. (b) **REPORT**: a pass that hit its bound ! ship `truncated` + `remaining` beside `count` — V85 one writer over. `count` alone answers "how many did this pass RESOLVE", which an operator reads as "how many WERE there" ⇒ a capped pass logging like a complete one reads as "everything is resolved". Total counted in the SAME statement as the page — `count(*) OVER ()`, ∵ Postgres evaluates a window over ∀ `WHERE` admitted, BEFORE `ORDER BY`/`LIMIT` ⇒ page ∧ total = 1 snapshot, ⊥ 2 reads that nearly agree. ONE definition of "was this pass capped" (`ReapOutcome`), ⊥ a 2nd on the page object that ⊥ callers ∧ ⊥ drift-checkable. (c) The cut ordered by a UNIQUENESS-COMPLETING key BEFORE it: `(created_at, id)`, ⊥ `created_at` alone (⊥ unique ⇒ a tied group splits differently per call). Oldest FIRST ∵ the oldest stranded run has spent the most of its operator's V31 allowance. V85's ordering clause, at the place it can actually fail. (d) BATCH ÷ INTERVAL = the DRAIN RATE ∧ it ! be stated, ⊥ implied: `APPROVAL_STRANDED_RUN_REAP_BATCH_SIZE` 100 (`ge=1`, `le=1000` — a batch with ⊥ ceiling = the unbounded pass spelled in an env var) ÷ 120s = 50/min. Beats arrival ∵ a stranded row COSTS a process death mid-call ∨ a failed closing audit write (T73), ∧ the population that can strand at 1 instant = the runs in flight at that instant (V31 caps the CHANGE half per operator); a SUSTAINED 50/min = NOA failing 50 calls a minute, ⊥ a backlog a reaper is the remedy for. The 2 knobs COMPOSE — a faster drain = lower the interval, which is what a resolution is for. (e) **⊥ `FOR UPDATE ... SKIP LOCKED` on the rows.** It would hold each run from the READ until the batch commits ⇒ the executor's terminal write for a change that DID complete queues behind the repair loop ∧ then finds its own `status = STARTED` false (V91) ⇒ "finished a little late" → "reported abandoned", DETERMINISTICALLY, in the repair loop's favour. A repair loop ⊥ outrank the worker ⇒ the 2 writers keep racing on COMMIT ORDER, which is the race V91 already answers. ⇒ 2 replicas both reap the same rows: OUTCOME right (V91's predicate + V46's `DO NOTHING`), COST = duplicated work ∧ the 2nd blocking on the 1st's row locks for one batch. KNOWN LIMIT, recorded ⊥ unsaid; remedy if it bites = 1 advisory lock around the PASS (T38(d)'s instrument), ⊥ a lock on the rows. (f) Read-only ⊥ an exemption: T38's detector writes nothing ∧ still names ∀ id it found in ONE log line ⇒ an unbounded detector = an unbounded log entry. Instances: T38 both populations (`core/approvals/reaper.py`).

## V95

V95. A setting whose value REACHES an operator ∨ a browser ! refuse its development default outside development — `NOA_EMBED_BASE_URL` (the approval address, V26) ∧ `NOA_API_URL` (the proxy target). Same file ∧ same block as V52/V53 (`core/config.py::_apply_environment_rules`), ∵ the failure mode is the same one env rules exist for: a deploy that ⊥ set the var BOOTS CLEAN ∧ then hands every operator `http://localhost:3001/approvals/<id>` — an address that resolves on ONE laptop ∧ silently nowhere else. ⊥ a startup WARNING: a warning in a container log is a thing nobody reads until an operator says "the button does nothing", which is V80's silent-refusal shape one layer down. DISTINCT from V52/V53 ∵ those guard SECRETS ∧ a missing secret is LOUD at first use; an address is WRONG at first use ∧ looks fine — ⊥ exception, ⊥ 500, just a dead link in a chat transcript. Test ! carry the negative control: development boots ON the default, else the rule is unprovable from "it raised" (V87). FOUND 2026-08-09 by T32, ⊥ shipped — T32 made `McpToolContext.embed_base_url` REQUIRED (⊥ default) so the WIRING ⊥ be forgotten, which closes the CODE half ∧ leaves the CONFIG half open; this line is that boundary, ∧ T74 is the half still owed.

## V57

V57. Dual-backend CSF+Imunify: ∀ firewall operation reaches the backends through ONE door — `core/integrations/whm/firewall_gate.py::run_on_usable_backends` — which reads `available["csf"]` ∧ `available["imunify"]` ∧ acts on the usable ones in parallel via `asyncio.gather`. **Zero backends available → error `no_firewall_backend`, ⊥ success.** `noa-old` bug: empty task dict → `gather()` → `[]` → reports ok having changed nothing. Silent no-op on approved CHANGE = \⊥ acceptable. The check sits at the MECHANISM, **⊥ per tool** (AMENDED 2026-08-07 by T68 — this line read "each tool checks" ∵ T24 shipped it inside the preflight, ∧ a per-tool guard = a guard the NEXT tool forgets; the tool that forgets it is a CHANGE reporting an approved release it never made, which is the harm, ⊥ the READ's `unknown`). ⇒ 2 properties the door owns ∧ a tool ⊥ opt out of: it ⊥ be entered with nothing to do, ∧ a hand-rolled `asyncio.gather` anywhere on the firewall path is RED, ⊥ silent (AST guard, V84c — `test_whm_firewall_gate.py`). Binaries present ∧ `sudo -n` denied → `ssh_sudo_required`, ⊥ `no_firewall_backend`: 2 causes, 2 remedies, ∧ "no firewall tools here" sends an operator to install what is already installed (`noa-old` GH #82, V55). **Bounds the ZERO case only** — one backend usable ∧ the other silent is a PARTIAL answer, ⊥ covered here, → V86 (T24).

## V64

V64. Large READ results (`whm_list_accounts`, `pmg_whitelist_list`) → tool result carries short summary + URL to NOA-origin table page, ⊥ full table in context. Zero token cost for table body. Render mechanism = path A (iframe UI resource), chosen by T59 2026-08-08 (R29) — same mechanism as V37, ⊥ a 2nd. "⊥ mix" = ⊥ 2 render mechanisms; it is ⊥ a ban on pairing the 2 fields, which mcp-ui REQUIRES together: `ui://` = LibreChat's classifier (`parsers.ts:183`, R31c) ∧ `text/uri-list` = the `src` render mode (R12). Measured, ∴ said plainly here. Shared capability, ⊥ per-tool special case. **BUILT 2026-08-09 by T56** — store `tool_result_tables`, read `GET /tables/{token}` (requester-match + deadline in ONE `WHERE`, V27/V93), page `/tables/[token]` (server component, cookie forwarded, read-only), resource `ui://noa/table/<token>`. "Same mechanism as V37, ⊥ a 2nd" is now CODE ∧ ⊥ prose: the 3 iframe fields come from ONE function (`mcp_tools/ui_resource.py`) that T32's gate response ∧ this surface both call — only the `ui://` name ∧ the route differ. **1st PRODUCER LANDED 2026-08-10 — T20** `whm_list_accounts` (`178044e`): rows sorted by `user`, parked, answer = summary + the plain `/tables/{token}` address + the `ui://noa/table/<token>` resource, ⊥ row ∧ ⊥ column value in the transcript (V26). The 1st real producer also CORRECTED the offload's result SHAPE: a parked READ carries a counts-only `structured_content` envelope (`ok`, `total_rows`, `stored_rows`, `truncated`) ∵ V45's audit row reads status ∧ summary off it ∧ a missing envelope = FAILED — T56(f) had copied T32(b)'s "⊥ `structured_content`", a CHANGE's reason, verbatim (T20(a)). ⊥ token ∧ ⊥ URL in it ⇒ the surviving clause holds ∧ the content array is byte-identical to what R29 measured. T30 `pmg_whitelist_list` still `.` ⇒ 1 caller of 2, ∧ the 2nd is where "shared capability, ⊥ per-tool special case" gets tested for real.

## V85

V85. A READ that CAPS rows ! carry its own bound: total-match count ∧ a truncation flag beside the rows. ∵ the model reports what it was handed — silent first-N ⇒ "there are twenty accounts" @ 200, a fabrication the TOOL authored, ⊥ the model (V71 says the agent config forbids fabrication; this says the tool ⊥ manufacture one). ∧ the capped set ! be ordered by a stable key BEFORE the cut — an unsorted truncation = arbitrary subset that changes between identical calls, ∧ upstream order (`listaccts`, `pmgsh`) = the remote system's, undocumented. Distinct from V64, ⊥ overlapping: V64 = large listing OFFLOADED whole to a table surface (nothing dropped, T59-gated); V85 = rows DROPPED in-process ∵ an operator-supplied limit. ⊥ a table surface here — a bounded search's answer fits the transcript. Instances: T21 `whm_search_accounts` (T20/T30 route through V64; T31 = exact membership, T63 = one record ⇒ neither caps). Written at 1 instance on purpose, same as V83 at T19: the rule ! exist BEFORE the 2nd capper, ∵ the 2nd is where "the first N" starts reading as "all of them". **2nd capper LANDED 2026-08-07 — T24**, `csf -g` evidence, cut at 20 inside `parse_csf_grep_output` ⇒ `CSFGrepParsed.total_matches` reports the pre-cut count ∵ the cut is the PARSER's, ⊥ the tool's, so the count ! be too. The ORDER clause deviates there, deliberately: kept order = **csf's own**, ⊥ a sort NOA applies — `csf -g` renders the current tables ∧ files ⇒ identical state gives an identical prefix, which IS the property this clause protects, while `listaccts` (where the clause was written) guarantees nothing; ∧ an alphabetical sort scrambles the deny/allow grouping the evidence is read by. ⇒ "ordered by a stable key" = **reproducible across identical calls**, ⊥ necessarily a sort in NOA's code. A source whose order is undocumented ∨ server-chosen still owes the sort. **EXTENDED to a WRITER 2026-08-09 — V92**: same reporting rule where the capped thing is a background repair PASS rather than a tool result, ∧ the ordering clause there is load-bearing at a place it can fail (`created_at` ⊥ unique). **3rd capper LANDED 2026-08-09 — T56**, where the capped thing is a PARKED table: the cut ∧ the PRE-cut total are computed ∧ stored at WRITE time (`cap_rows` → `tool_result_tables.total_rows`/`truncated`) ∵ a reader computing `len(rows)` reports the CAP as the total, ∧ that reader is a browser page nobody audits. Bound stated TWICE by design — in the tool's text block the MODEL reads ∧ on the page the OPERATOR reads — ∵ V64 puts those 2 readers on opposite sides of the offload ∧ neither sees the other's copy. Ordering clause DELEGATED, ⊥ dropped: `cap_rows` keeps a PREFIX ∧ never re-sorts, so the reproducible order is owed by the producing tool (T20, T30) — T24's deviation generalised into a signature.

## V86

V86. A verdict read from a SUBSET ! name the subset. A READ that asks N sources one question ships the sources that produced ⊥ usable answer beside the combined result, ∧ zero answers ⇒ `unknown`, ⊥ the benign value. ∵ V57 bounds only the ZERO case (⊥ backend usable ⇒ `no_firewall_backend`); the PARTIAL case is where the fabrication lives — `noa-old` computed the verdict from whichever backend succeeded ∧ else fell through to `not_found`, so a broken CSF beside a clean Imunify reported "this address is ⊥ blocked" on a box whose BLOCKING backend was silent. Same shape as V85 one axis over: V85 = ROWS dropped in-process, V86 = a whole SOURCE dropped; both say the tool ⊥ manufacture a claim its data ⊥ carry. A source's own "I ⊥ recognise this output" counts as ⊥ answered, ⊥ as clean — silence ≠ evidence of absence (CSF's `unknown` verdict; `core.integrations.whm.csf` holds the same line one layer down). The verdict still ships: one dead source is ⊥ a reason to withhold the others' answer, only to bound what it means. Instances: T24 (`csf`,`imunify`) = the only one today. T25/T26 inherit — a 2-part receipt (before-state, after-state) built off a half-answering pair tells the same lie about a CHANGE, where it is worse.

## V87

V87. A test comparing 2 outputs ! compare what the CODE decides, ⊥ what the CLOCK stamps. Time-derived bytes (`Set-Cookie`'s `Expires`, `Date`, `iat`/`exp`, receipt timestamps) ! leave an equality compare ∧ be asserted by property instead (presence, bound, order). ∵ such a compare is a coin flip: green ∀ run inside one second, red ~1-in-N across a boundary ⇒ the suite teaches "flaky, rerun it", which is the habit that lets a REAL intermittent failure ride. ∧ the drop ! ship a case proving the compare STILL separates 2 outputs that genuinely differ — a field-dropping comparator degrades into a tautology silently, ∧ a tautology passes ∀ run, incl. the ones it was written to catch (V69's shape: a control asserted by prose ∧ held by nothing). B4 = the `delete_cookie` case: `expires=0` renders through `_getdate(0)` = **now**, so 2 clears differ in that byte range alone. **EXTENDED 2026-08-09 by T56 (B9)** — an ABSENCE asserted through an ORM ATTRIBUTE is the same non-separating compare, one type-coercion layer down: a JSON/JSONB column set to Python `None` serialises to `'null'::jsonb`, a VALUE that SATISFIES `NOT NULL` ⇒ "an insert omitting this column fails" written that way omits NOTHING, ∧ its outcome is the same whether the column carries a server default or ⊥. ∴ a no-default ∨ NOT-NULL claim ! be issued as a STATEMENT that leaves the column out of the SQL (Core `INSERT`), ⊥ as an instance with the attribute cleared. Rule generalises past JSONB: ∀ compare where a type COERCES the sentinel the test meant to send.

## V89

V89. A test asserting a CONCURRENCY control ! prove the 2 parties OVERLAPPED. `asyncio.gather` of 2 callers is ⊥ a race — they finish in whatever order the driver returns results, ∧ the first commonly completes before the second's read reaches the server ⇒ the test passes with the LOCK DELETED (B5, measured twice). ⇒ 3 obligations: (a) the window is HELD OPEN on purpose — a hook between the locked read ∧ the commit, ⊥ a hope; (b) the assertion is on ORDER (an event journal — `second:read` after `first:committed`), ⊥ on the win count, which comes out right for the wrong reason; (c) a NEGATIVE CONTROL shows the forbidden order is REACHABLE (an unlocked read of the same row does ⊥ wait), else the ordering assertion is a tautology. Same family as V87 one axis over: V87 = a comparator that ⊥ separate, V89 = a race that ⊥ race; both = a test that ⊥ fail, which is V69's shape — a control asserted by prose ∧ held by nothing. Instances: T37 (`test_action_request_decisions_live.py`) ∧ T39 (`test_action_request_expiry_live.py` — the sweep vs an approval in flight). The rig MOVED to `support/action_decisions.py` at T39 rather than being copied (V66): 2 copies of the thing that holds the window open = 2 things that can silently stop overlapping, ∧ obligation (a) is the one nothing else would catch.

## V90

V90. A test's SETUP gate ⊥ be the thing under test. A readiness ∨ preflight wait pointed at the subject converts the subject's FAILURE into a TIMEOUT — the suite reports "startup took too long" where the truth is `Expected: 200 Received: 503`, ∧ a timeout names nothing to fix ∵ every cause produces the same stall. MEASURED at T40, ⊥ reasoned: Playwright's `webServer` waiting on `/healthz` answered the 503 mutation with a 120s stall; waiting on the open SOCKET (`port`) answered the same mutation in 7s with the assertion. ⇒ the gate ! sit one layer BELOW the subject — a socket under a route, a process under a socket. Same family as V87 ∧ V89 one axis over: V87 = a comparator that ⊥ separate, V89 = a race that ⊥ race, V90 = a red that carries ⊥ information; ∀ 3 = V69's shape, a control asserted by prose ∧ held by nothing. V89 already records the sibling case from the OTHER side (a mutation that HANGS proves nothing — a timeout is a worse signal than a red); this is the same fact about the harness rather than about the mutation. Instances: T40 (`playwright.config.ts`). T41–T46 ∧ T56 inherit ∵ each writes its own e2e — the card's readiness gate ! be something that is ⊥ the card. **1st inheritor: T41 (2026-08-09), ∧ it added ⊥ gate** — the card's e2e reuses T40's socket waits ∧ waits on the FRAME's own content (`#card` attached, then a locator inside it), never on a route the card serves. The 2 servers it leans on (the stub upstream, the framing parent) are SETUP, ⊥ subject, ∧ both were already gated on their open sockets. Recorded as the pattern the rest inherit: satisfying this line at T41 meant reusing the gate, ⊥ writing one — a new readiness wait is the thing to justify, ⊥ the thing to add.

## V73

V73. ∀ error response carries `request_id` in body + `x-request-id` header, same value. (`noa-old` V8.) "∀ error response" = **5 writers, ⊥ 1**: `NoaError` handler ∧ `StarletteHTTPException` (404/405) ∧ `RequestValidationError` (422) ∧ bare `Exception` (500) ∧ `McpAuthErrorMiddleware._send_error` (raw ASGI — mounted MCP app has ⊥ exception-handler graph). A `NoaError`-only handler leaves 4 of them in Starlette's default shape ⇒ ⊥ `error_code`, ⊥ `request_id`. ONE seam installs ∀ of it — `install_error_handling(app)`, called by `noa_api.main` ∧ both test harnesses ⇒ ⊥ surface carries a different envelope (V66). Header set by `error_response()` ITSELF, ⊥ left to `RequestContextMiddleware`'s `send` wrapper ∵ Starlette's `ServerErrorMiddleware` sits OUTSIDE the user middleware stack ⇒ the 500 never passes through that wrapper. Both may set it; `MutableHeaders.__setitem__` replaces ⇒ ⊥ duplicate. Inbound `x-request-id` adopted ⊥ verbatim: ≤200 chars ∧ `^[A-Za-z0-9._:-]+$` else mint `uuid4()` — the value re-emits as a response header ∧ into structlog output ⇒ unbounded/newline value = log-forging surface (`noa-old` echoed verbatim). Id also bound to structlog contextvars ⇒ the id in the body names the log line; structlog default processor chain already carries `merge_contextvars` ⇒ ⊥ logging config. **BOUND**: header rides ∀ response (superset of this line); body key covers ∀ error NOA shapes, ⊥ fastmcp's JSON-RPC `error` payloads — those = a `result`/`error` object, ⊥ an HTTP error response ⇒ reaching them = patch fastmcp.

